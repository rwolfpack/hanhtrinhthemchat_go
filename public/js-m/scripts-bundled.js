/*
 * ATTENTION: The "eval" devtool has been used (maybe by default in mode: "development").
 * This devtool is neither made for production nor for readable output files.
 * It uses "eval()" calls to create a separate source file in the browser devtools.
 * If you are trying to read the output file, select a different devtool (https://webpack.js.org/configuration/devtool/)
 * or disable the default devtool with "devtool: false".
 * If you are looking for production-ready output files, see mode: "production" (https://webpack.js.org/configuration/mode/).
 */
/******/ (() => { // webpackBootstrap
/******/ 	var __webpack_modules__ = ({

/***/ "./app/static/js-m/modules/Main.js":
/*!*****************************************!*\
  !*** ./app/static/js-m/modules/Main.js ***!
  \*****************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

  "use strict";
  eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"default\": () => (/* binding */ Main)\n/* harmony export */ });\n/* harmony import */ var scroll_out__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! scroll-out */ \"./node_modules/scroll-out/lib/index.js\");\n/* harmony import */ var scroll_out__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(scroll_out__WEBPACK_IMPORTED_MODULE_0__);\n/* harmony import */ var _utils__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./utils */ \"./app/static/js-m/modules/utils.js\");\n/* harmony import */ var _Common__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./_Common */ \"./app/static/js-m/modules/_Common.js\");\n/* harmony import */ var _Stage1__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./_Stage1 */ \"./app/static/js-m/modules/_Stage1.js\");\n/* harmony import */ var _Stage2__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ./_Stage2 */ \"./app/static/js-m/modules/_Stage2.js\");\n/* harmony import */ var _MobileModalControl__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ./_MobileModalControl */ \"./app/static/js-m/modules/_MobileModalControl.js\");\n/* harmony import */ var _ProgressBarAnimation__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ./_ProgressBarAnimation */ \"./app/static/js-m/modules/_ProgressBarAnimation.js\");\nfunction _typeof(o) { \"@babel/helpers - typeof\"; return _typeof = \"function\" == typeof Symbol && \"symbol\" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && \"function\" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? \"symbol\" : typeof o; }, _typeof(o); }\nfunction _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError(\"Cannot call a class as a function\"); }\nfunction _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, \"value\" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }\nfunction _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, \"prototype\", { writable: !1 }), e; }\nfunction _toPropertyKey(t) { var i = _toPrimitive(t, \"string\"); return \"symbol\" == _typeof(i) ? i : i + \"\"; }\nfunction _toPrimitive(t, r) { if (\"object\" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || \"default\"); if (\"object\" != _typeof(i)) return i; throw new TypeError(\"@@toPrimitive must return a primitive value.\"); } return (\"string\" === r ? String : Number)(t); }\n// Libraries\n\n\n// Ultils\n\n\n\n\n\n\nvar Main = /*#__PURE__*/function () {\n  /* ===================================\r\n   *  CONSTRUCTOR\r\n   * =================================== */\n  function Main() {\n    _classCallCheck(this, Main);\n    // Page Listener\n    window.PageListener = new _utils__WEBPACK_IMPORTED_MODULE_1__.pageListener();\n\n    // Ipad devices and below\n    window.IS_MOBILE = window.innerWidth <= 1080;\n    $(window).on(\"resize\", function () {\n      window.IS_MOBILE = window.innerWidth <= 1080;\n    });\n\n    // ScrollSmoother.create({\n    //   smooth: 1, // how long (in seconds) it takes to \"catch up\" to the native scroll position\n    //   effects: true, // looks for data-speed and data-lag attributes on elements\n    //   smoothTouch: 0.1, // much shorter smoothing time on touch devices (default is NO smoothing on touch devices)\n    // });\n\n    // Common Behavior\n    var common = new _Common__WEBPACK_IMPORTED_MODULE_2__[\"default\"]();\n    window.motulAnimation = gsap.timeline({\n      scrollTrigger: {\n        trigger: \".scrollContainer\",\n        pin: true,\n        // pinSpacing: false,\n        scrub: true,\n        horizontal: true,\n        inertia: false,\n        end: \"+=20000\",\n        start: \"top top\" // when the top of the trigger hits the top of the viewport\n        // markers: true,\n        // snap: {\n        //   snapTo: \"labels\",\n        //   duration: { min: 0.2, max: 1 },\n        //   delay: 0.3,\n        //   ease: \"power1.easeOut\",\n        // },\n      }\n    });\n    var stage1 = new _Stage1__WEBPACK_IMPORTED_MODULE_3__[\"default\"]();\n    var stage2 = new _Stage2__WEBPACK_IMPORTED_MODULE_4__[\"default\"]();\n    var mobileModalControl = new _MobileModalControl__WEBPACK_IMPORTED_MODULE_5__[\"default\"]();\n    var progressBarAnimation = new _ProgressBarAnimation__WEBPACK_IMPORTED_MODULE_6__[\"default\"]();\n\n    // let mobileSectionBaseAnimation = new MobileSectionBaseAnimation();\n\n    // let round3and4Animation = new Round3Round4Animation();\n\n    // Bind Event\n    this.bindEvents();\n  }\n\n  /* ===================================\r\n   *  EVENTS\r\n   * =================================== */\n  return _createClass(Main, [{\n    key: \"bindEvents\",\n    value: function bindEvents() {\n      this.ReloadOnResize();\n      this.ScrollOutSetup();\n\n      // $(document).ready(function() {\n      var videosLoaded = 0;\n      var totalVideosToLoad = 2;\n      var countPercent = 0;\n      var countFirstProcess = 60;\n      var countSecondProcess = 100;\n      while (countPercent < countFirstProcess) {\n        $(\".percent-count span\").html(countPercent);\n        countPercent++;\n      }\n      $(\"video\").on(\"loadeddata\", function () {\n        videosLoaded++;\n\n        // console.log(videosLoaded);\n\n        if (videosLoaded == 1) {\n          $(\".percent-count span\").html(90);\n        }\n        if (videosLoaded >= totalVideosToLoad) {\n          // console.log(\"true\");\n          $(\".percent-count span\").html(countSecondProcess);\n          setTimeout(function () {\n            $(\"#loading\").addClass(\"deactive\");\n          }, 500);\n        }\n      });\n      // });\n\n      // for web cache \n      $(window).on(\"load\", function () {\n        // setTimeout(() => {\n        $(\"#loading\").addClass(\"deactive\");\n        // }, 500);\n      });\n    }\n\n    /* ===================================\r\n     *  METHODS\r\n     * =================================== */\n  }, {\n    key: \"updateProcess\",\n    value: function updateProcess(paramCount) {\n      $(\".percent-count span\").text(paramCount);\n    }\n  }, {\n    key: \"ScrollOutSetup\",\n    value: function ScrollOutSetup() {\n      scroll_out__WEBPACK_IMPORTED_MODULE_0___default()({\n        onShown: function onShown(el) {\n          // use the web animation API\n          // console.log(\"in: \",$(el).attr(\"id\"));\n          var elementID = $(el).attr(\"id\");\n          PageListener.emit(\"\".concat(elementID, \"-anim\"));\n        },\n        threshold: 0.275,\n        onHidden: function onHidden(el) {\n          var elementID = $(el).attr(\"id\");\n          PageListener.emit(\"\".concat(elementID, \"-hide\"));\n        }\n      });\n    }\n  }, {\n    key: \"ReloadOnResize\",\n    value: function ReloadOnResize() {\n      $(window).on('resize', function () {\n        location.reload();\n      });\n      window.onload = function () {\n        // Scroll to the top-left position of the page\n        window.scrollTo(0, 0);\n      };\n    }\n  }]);\n}();\n\n\n//# sourceURL=webpack://frontend-boilerplate/./app/static/js-m/modules/Main.js?");
  
  /***/ }),
  
  /***/ "./app/static/js-m/modules/_Common.js":
  /*!********************************************!*\
    !*** ./app/static/js-m/modules/_Common.js ***!
    \********************************************/
  /***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {
  
  "use strict";
  eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"default\": () => (/* binding */ Common)\n/* harmony export */ });\n/* harmony import */ var youtube_player__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! youtube-player */ \"./node_modules/youtube-player/dist/index.js\");\n/* harmony import */ var youtube_player__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(youtube_player__WEBPACK_IMPORTED_MODULE_0__);\nfunction _typeof(o) { \"@babel/helpers - typeof\"; return _typeof = \"function\" == typeof Symbol && \"symbol\" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && \"function\" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? \"symbol\" : typeof o; }, _typeof(o); }\nfunction _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError(\"Cannot call a class as a function\"); }\nfunction _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, \"value\" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }\nfunction _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, \"prototype\", { writable: !1 }), e; }\nfunction _toPropertyKey(t) { var i = _toPrimitive(t, \"string\"); return \"symbol\" == _typeof(i) ? i : i + \"\"; }\nfunction _toPrimitive(t, r) { if (\"object\" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || \"default\"); if (\"object\" != _typeof(i)) return i; throw new TypeError(\"@@toPrimitive must return a primitive value.\"); } return (\"string\" === r ? String : Number)(t); }\n\nvar Common = /*#__PURE__*/function () {\n  /* ===================================\n   *  CONSTRUCTOR\n   * =================================== */\n  function Common() {\n    _classCallCheck(this, Common);\n    this.bindEvents();\n  }\n\n  /* ===================================\n   *  EVENTS\n   * =================================== */\n  return _createClass(Common, [{\n    key: \"bindEvents\",\n    value: function bindEvents() {\n      /* Video Modal Setup */\n      if ($(\"#video-modal\").length > 0) {\n        this.SetupVideoPlayer();\n      }\n    }\n\n    /* ===================================\n     *  METHODS\n     * =================================== */\n    /* Modal Video Control */\n  }, {\n    key: \"SetupVideoPlayer\",\n    value: function SetupVideoPlayer() {\n      var _this = this;\n      this.playerYT = youtube_player__WEBPACK_IMPORTED_MODULE_0___default()(\"video-modal-video\", {\n        videoId: \"qQzmp1XahxY\",\n        // Default Clip\n        playerVars: {\n          disablekb: 1,\n          fs: 0,\n          modestbranding: 1,\n          rel: 0\n        }\n      });\n      this.playerYT.addEventListener(\"onStateChange\", function (e) {\n        if (e.data == 0) {\n          _this.CloseModalClip();\n        }\n      });\n\n      // console.log(this.playerYT);\n\n      $(document).on(\"click\", \".play-modal-video\", function (e) {\n        _this.videoCode = \"qQzmp1XahxY\"; // Default Youtube Video ID\n\n        if ($(e.target).parents(\".play-modal-video\").length > 0) {\n          _this.videoCode = $(e.target).parents(\".play-modal-video\").data(\"video-id\");\n        } else {\n          _this.videoCode = $(e.target).data(\"video-id\");\n        }\n        _this.PlayModalClip(_this.videoCode);\n      });\n      $(document).on(\"click\", \".close-video-modal\", function (e) {\n        _this.CloseModalClip();\n      });\n      $(document).on(\"keyup\", function (e) {\n        if (e.key == \"Escape\") {\n          _this.CloseModalClip();\n        }\n      });\n    }\n  }, {\n    key: \"PlayModalClip\",\n    value: function PlayModalClip() {\n      var clipID = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : \"\";\n      $(\"body\").addClass(\"show-modal\");\n      $(\".video-modal\").addClass(\"active\");\n      this.playerYT.unMute();\n      this.playerYT.loadVideoById(clipID);\n      // console.log(clipID);\n    }\n  }, {\n    key: \"CloseModalClip\",\n    value: function CloseModalClip() {\n      var _this2 = this;\n      this.playerYT.mute();\n      $(\"body\").removeClass(\"show-modal\");\n      $(\".video-modal\").removeClass(\"active\");\n      setTimeout(function () {\n        _this2.playerYT.stopVideo();\n      }, 200);\n    }\n  }]);\n}();\n\n\n//# sourceURL=webpack://frontend-boilerplate/./app/static/js-m/modules/_Common.js?");
  
  /***/ }),
  
  /***/ "./app/static/js-m/modules/_MobileModalControl.js":
  /*!********************************************************!*\
    !*** ./app/static/js-m/modules/_MobileModalControl.js ***!
    \********************************************************/
  /***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {
  
  "use strict";
  eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"default\": () => (/* binding */ MobileModalControl)\n/* harmony export */ });\nfunction _typeof(o) { \"@babel/helpers - typeof\"; return _typeof = \"function\" == typeof Symbol && \"symbol\" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && \"function\" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? \"symbol\" : typeof o; }, _typeof(o); }\nfunction _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError(\"Cannot call a class as a function\"); }\nfunction _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, \"value\" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }\nfunction _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, \"prototype\", { writable: !1 }), e; }\nfunction _toPropertyKey(t) { var i = _toPrimitive(t, \"string\"); return \"symbol\" == _typeof(i) ? i : i + \"\"; }\nfunction _toPrimitive(t, r) { if (\"object\" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || \"default\"); if (\"object\" != _typeof(i)) return i; throw new TypeError(\"@@toPrimitive must return a primitive value.\"); } return (\"string\" === r ? String : Number)(t); }\nvar MobileModalControl = /*#__PURE__*/function () {\n  /* ===================================\n   *  CONSTRUCTOR\n   * =================================== */\n  function MobileModalControl() {\n    _classCallCheck(this, MobileModalControl);\n    this.bindEvents();\n  }\n\n  /* ===================================\n   *  EVENTS\n   * =================================== */\n  return _createClass(MobileModalControl, [{\n    key: \"bindEvents\",\n    value: function bindEvents() {\n      if (IS_MOBILE) {\n        this.DetailModalSetup();\n        this.RaceModalSetup();\n        this.IosMenubarDevice();\n        this.ProductSliderMbSetup();\n      }\n    }\n\n    /* ===================================\n     *  METHODS\n     * =================================== */\n  }, {\n    key: \"IosMenubarDevice\",\n    value: function IosMenubarDevice() {\n      var windowHeight = function windowHeight() {\n        var doc = document.documentElement;\n        doc.style.setProperty('--window-height', \"\".concat(window.innerHeight, \"px\"));\n      };\n      window.addEventListener('resize', windowHeight);\n      windowHeight();\n    }\n  }, {\n    key: \"DetailModalSetup\",\n    value: function DetailModalSetup() {\n      this.$closeModalBtn = $(\".btn-close\");\n      this.$detailBtn = $(\".btn-detail\");\n      this.$detailBtn.on(\"click\", function (e) {\n        var $target = $(e.target);\n        $(\".modal .layout-item\").removeClass(\"active\");\n        if ($target.parents(\".btn-detail\").length > 0) {\n          $target = $target.parents(\".btn-detail\");\n        }\n        console.log($target.data(\"id\"));\n\n        // if(!$(\".modal\").hasClass(\"active\")) {\n        $(\".modal\").addClass(\"active\");\n        $(\".modal .layout-item[data-prod='\".concat($target.prop(\"id\"), \"']\")).addClass(\"active\");\n        $(\"html\").css(\"overflow\");\n        // }\n\n        if ($target.data(\"id\") == \"open-product-list\") {\n          $(\"#close-modal-btn\").css(\"display\", \"none\");\n          $(\"#close-modal-btn-style-2\").css(\"display\", \"flex\");\n          $(\".modal .layout-item[data-prod='\".concat($target.data(\"id\"), \"']\")).addClass(\"active\");\n        } else {\n          $(\"#close-modal-btn\").css(\"display\", \"block\");\n          $(\"#close-modal-btn-style-2\").css(\"display\", \"none\");\n        }\n        ;\n      });\n      this.$closeModalBtn.on(\"click\", function () {\n        $(\".modal\").removeClass(\"active\");\n        $(\".modal .layout-item\").removeClass(\"active\");\n      });\n    }\n  }, {\n    key: \"RaceModalSetup\",\n    value: function RaceModalSetup() {\n      this.$openRaceBtn = $(\"#race-line-modal .arrow-control\");\n      this.$openRaceBtn.on(\"click\", function () {\n        $(\"#race-line-modal\").toggleClass(\"active\");\n      });\n    }\n  }, {\n    key: \"ProductSliderMbSetup\",\n    value: function ProductSliderMbSetup() {\n      // VARIABLES\n      this.$ProductSlider = $(\".products-modal-slider\");\n\n      // Slider Setup\n      this.$ProductSlider.slick({\n        centerPadding: '24vw',\n        centerMode: true,\n        slidesToShow: 1,\n        nextArrow: \"<div class=\\\"slide-control next-slide\\\"><figure class=\\\"img-holder arrow-ico\\\"><svg\\n      xmlns=\\\"http://www.w3.org/2000/svg\\\"\\n      width=\\\"12\\\"\\n      height=\\\"16\\\"\\n      viewBox=\\\"0 0 12 16\\\"\\n      fill=\\\"none\\\"\\n    >\\n      <path\\n        d=\\\"M1 7C0.447715 7 -7.24234e-08 7.44772 0 8C7.24234e-08 8.55228 0.447715 9 1 9L1 7ZM11.7071 8.70711C12.0976 8.31658 12.0976 7.68342 11.7071 7.29289L5.34314 0.928932C4.95262 0.538407 4.31946 0.538407 3.92893 0.928932C3.53841 1.31946 3.53841 1.95262 3.92893 2.34315L9.58579 8L3.92893 13.6569C3.53841 14.0474 3.53841 14.6805 3.92893 15.0711C4.31946 15.4616 4.95262 15.4616 5.34315 15.0711L11.7071 8.70711ZM1 9L11 9L11 7L1 7L1 9Z\\\"\\n        fill=\\\"white\\\"\\n      />\\n    </svg></figure></div>\",\n        prevArrow: \"<div class=\\\"slide-control prev-slide\\\"><figure class=\\\"img-holder arrow-ico\\\"><svg\\n      xmlns=\\\"http://www.w3.org/2000/svg\\\"\\n      width=\\\"12\\\"\\n      height=\\\"16\\\"\\n      viewBox=\\\"0 0 12 16\\\"\\n      fill=\\\"none\\\"\\n    >\\n      <path\\n        d=\\\"M1 7C0.447715 7 -7.24234e-08 7.44772 0 8C7.24234e-08 8.55228 0.447715 9 1 9L1 7ZM11.7071 8.70711C12.0976 8.31658 12.0976 7.68342 11.7071 7.29289L5.34314 0.928932C4.95262 0.538407 4.31946 0.538407 3.92893 0.928932C3.53841 1.31946 3.53841 1.95262 3.92893 2.34315L9.58579 8L3.92893 13.6569C3.53841 14.0474 3.53841 14.6805 3.92893 15.0711C4.31946 15.4616 4.95262 15.4616 5.34315 15.0711L11.7071 8.70711ZM1 9L11 9L11 7L1 7L1 9Z\\\"\\n        fill=\\\"white\\\"\\n      />\\n    </svg></figure></div>\"\n      });\n    }\n  }]);\n}();\n\n\n//# sourceURL=webpack://frontend-boilerplate/./app/static/js-m/modules/_MobileModalControl.js?");
  
  /***/ }),
  
  /***/ "./app/static/js-m/modules/_ProgressBarAnimation.js":
  /*!**********************************************************!*\
    !*** ./app/static/js-m/modules/_ProgressBarAnimation.js ***!
    \**********************************************************/
  /***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {
  
  "use strict";
  eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"default\": () => (/* binding */ ProgressBarAnimation)\n/* harmony export */ });\nfunction _typeof(o) { \"@babel/helpers - typeof\"; return _typeof = \"function\" == typeof Symbol && \"symbol\" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && \"function\" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? \"symbol\" : typeof o; }, _typeof(o); }\nfunction _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError(\"Cannot call a class as a function\"); }\nfunction _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, \"value\" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }\nfunction _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, \"prototype\", { writable: !1 }), e; }\nfunction _toPropertyKey(t) { var i = _toPrimitive(t, \"string\"); return \"symbol\" == _typeof(i) ? i : i + \"\"; }\nfunction _toPrimitive(t, r) { if (\"object\" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || \"default\"); if (\"object\" != _typeof(i)) return i; throw new TypeError(\"@@toPrimitive must return a primitive value.\"); } return (\"string\" === r ? String : Number)(t); }\nvar $windowWidth = $(window).width();\nconsole.log($windowWidth);\nvar ProgressBarAnimation = /*#__PURE__*/function () {\n  /* ===================================\r\n   *  CONSTRUCTOR\r\n   * =================================== */\n  function ProgressBarAnimation() {\n    _classCallCheck(this, ProgressBarAnimation);\n    this.bindEvents();\n  }\n\n  /* ===================================\r\n   *  EVENTS\r\n   * =================================== */\n  return _createClass(ProgressBarAnimation, [{\n    key: \"bindEvents\",\n    value: function bindEvents() {\n      if (IS_MOBILE) {\n        this.TimelineProgressBarAnimationSetup();\n      }\n    }\n\n    /* ===================================\r\n     *  METHODS\r\n     * =================================== */\n\n    // EXTRA - Progress Bar Animation Setup\n  }, {\n    key: \"TimelineProgressBarAnimationSetup\",\n    value: function TimelineProgressBarAnimationSetup() {\n      // VARIABLES\n      var isPhase2 = $(\".is-phase-2\");\n      this.$progressWheel = $(\"#progress-wheel\");\n      this.$progressCoverLine = $(\"#progress-cover-line\");\n      var COVERLINE_WIDTH = this.$progressCoverLine.innerWidth();\n      motulAnimation.to(this.$progressWheel, {\n        x: isPhase2.length > 0 ? 180 / 360 * $windowWidth : 170 / 360 * $windowWidth,\n        duration: (80 + 110) * 6,\n        ease: \"none\"\n      }, \"anim-start\");\n      motulAnimation.to(this.$progressCoverLine, {\n        width: COVERLINE_WIDTH - (isPhase2.length > 0 ? 120 / 360 * $windowWidth : 170 / 360 * $windowWidth),\n        duration: (80 + 110) * 6,\n        ease: \"none\"\n      }, \"anim-start\");\n\n      // motulAnimation.to(\n      //   this.$progressWheel,\n      //   {\n      //     x: isPhase2.length > 0 ? (180/360)*$windowWidth : (140/360)*$windowWidth,\n      //     duration: (80 + 110)*2,\n      //     ease: \"none\",\n      //   },\n      //   \"start-northernmost-ha-giang-section-finish\"\n      // );\n\n      // motulAnimation.to(\n      //   this.$progressCoverLine,\n      //   {\n      //     width: COVERLINE_WIDTH - (isPhase2.length > 0 ? (180/360)*$windowWidth : (140/360)*$windowWidth),\n      //     duration: (80 + 110)*2,\n      //     ease: \"none\",\n      //   },\n      //   \"start-northernmost-ha-giang-section-finish\"\n      // );\n\n      motulAnimation.to(this.$progressWheel, {\n        x: isPhase2.length > 0 ? 265 / 360 * $windowWidth : 200 / 360 * $windowWidth,\n        duration: isPhase2.length > 0 ? (80 + 110) * 2.5 : (80 + 110) * 1.8,\n        ease: \"none\"\n      }, \"round-four-hide\");\n      motulAnimation.to(this.$progressCoverLine, {\n        width: COVERLINE_WIDTH - (isPhase2.length > 0 ? 265 / 360 * $windowWidth : 200 / 360 * $windowWidth),\n        duration: isPhase2.length > 0 ? (80 + 110) * 2.5 : (80 + 110) * 1.8,\n        ease: \"none\"\n      }, \"round-four-hide\");\n    }\n  }]);\n}();\n\n\n//# sourceURL=webpack://frontend-boilerplate/./app/static/js-m/modules/_ProgressBarAnimation.js?");
  
  /***/ }),
  
  /***/ "./app/static/js-m/modules/_Stage1.js":
  /*!********************************************!*\
    !*** ./app/static/js-m/modules/_Stage1.js ***!
    \********************************************/
  /***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {
  
  "use strict";
  eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"default\": () => (/* binding */ Stage1)\n/* harmony export */ });\n/* harmony import */ var _rounds_components_hero_banner_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./rounds/components/hero-banner.js */ \"./app/static/js-m/modules/rounds/components/hero-banner.js\");\n/* harmony import */ var _rounds_north_index_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./rounds/north/index.js */ \"./app/static/js-m/modules/rounds/north/index.js\");\nfunction _typeof(o) { \"@babel/helpers - typeof\"; return _typeof = \"function\" == typeof Symbol && \"symbol\" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && \"function\" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? \"symbol\" : typeof o; }, _typeof(o); }\nfunction _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError(\"Cannot call a class as a function\"); }\nfunction _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, \"value\" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }\nfunction _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, \"prototype\", { writable: !1 }), e; }\nfunction _toPropertyKey(t) { var i = _toPrimitive(t, \"string\"); return \"symbol\" == _typeof(i) ? i : i + \"\"; }\nfunction _toPrimitive(t, r) { if (\"object\" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || \"default\"); if (\"object\" != _typeof(i)) return i; throw new TypeError(\"@@toPrimitive must return a primitive value.\"); } return (\"string\" === r ? String : Number)(t); }\nvar SCREEN_BREAK = 1;\nvar DURATION_SCREEN_SPEED = 15;\n\n\n// import { voyagerAnimationSetup } from \"./rounds/components/voyager.js\";\nvar Stage1 = /*#__PURE__*/function () {\n  /* ===================================\r\n   *  CONSTRUCTOR\r\n   * =================================== */\n  function Stage1() {\n    _classCallCheck(this, Stage1);\n    this.bindEvents();\n  }\n\n  /* ===================================\r\n   *  EVENTS\r\n   * =================================== */\n  return _createClass(Stage1, [{\n    key: \"bindEvents\",\n    value: function bindEvents() {\n      if (IS_MOBILE) {\n        this.HorizontalScrollMobileSetup();\n      }\n    }\n\n    /* ===================================\r\n     *  METHODS\r\n     * =================================== */\n  }, {\n    key: \"HorizontalScrollMobileSetup\",\n    value: function HorizontalScrollMobileSetup() {\n      this.$sections = $(\".section\");\n      this.$scrollContainer = document.querySelector(\".section-holder\");\n\n      // this.heroBannerAnimationSetup();\n      (0,_rounds_components_hero_banner_js__WEBPACK_IMPORTED_MODULE_0__.heroBannerAnimationSetup)(DURATION_SCREEN_SPEED, this.sections, this.$scrollContainer);\n\n      // Animation for voyager section\n      this.voyagerAnimationSetup();\n\n      // Animation for northernmost Ha Giang section\n      this.northernmostHaGiangSectionSetup();\n\n      // North\n      (0,_rounds_north_index_js__WEBPACK_IMPORTED_MODULE_1__[\"default\"])();\n      this.roundOneSection();\n      this.roundTwoSection();\n      this.roundThreeSection();\n      this.roundFourSection();\n\n      // Animation for vertical split section\n      // this.verticalSplitAnimationSetup();\n\n      // Animation for start journey section\n      // this.startJourneySection();\n\n      // Animation for product section\n      // this.productAnimationSetup();\n\n      // this.preparationAnimationSetup();\n\n      // SETUP SCROLLING\n      this.handleSetupScroll();\n    }\n\n    /**\r\n     * Setup - Scrolling Controller\r\n     */\n  }, {\n    key: \"handleSetupScroll\",\n    value: function handleSetupScroll() {\n      var sections = gsap.utils.toArray(\"[snap]\");\n\n      // SET UP SCROLL\n      // let horizontalScroll = ScrollTrigger.create({\n      //   animation: motulAnimation,\n      //   trigger: \".scrollContainer\",\n      //   pin: true,\n      //   pinSpacing: false,\n      //   scrub: 1,\n      //   horizontal: true,\n      //   markers: false,\n      //   start: () => \"top top\",\n      //   end: () => `+=18000`,\n      // });\n    }\n\n    // FIRST SCREEN / HERO SCREEN\n    // heroBannerAnimationSetup() {\n    //   this.$heroSection = $(\"#hero-banner-section\");\n\n    //   this.$raceLineModal = $(\"#race-line-modal\");\n    //   this.$raceModalControl = this.$raceLineModal.find(\".arrow-control\");\n    //   this.$scrollAnnouce = this.$raceLineModal.find(\".annouce-label\");\n\n    //   motulAnimation.addLabel(\"anim-start\");\n    //   motulAnimation.to(\n    //     this.$scrollContainer,\n    //     {\n    //       x: -this.DistanceCalc(this.$heroSection),\n    //       duration: DURATION_SCREEN_SPEED,\n    //       ease: \"none\",\n    //     },\n    //     \"anim-start\"\n    //   );\n\n    //   motulAnimation.addLabel(\"hero-banner-hide\");\n\n    //   motulAnimation.fromTo(\n    //     this.$raceModalControl,\n    //     {\n    //       autoAlpha: 0,\n    //       duration: DURATION_SCREEN_SPEED,\n    //       ease: \"none\",\n    //     },\n    //     {\n    //       autoAlpha: 1,\n    //       duration: DURATION_SCREEN_SPEED,\n    //       ease: \"none\",\n    //     },\n    //     \"hero-banner-hide-=1\"\n    //   );\n\n    //   motulAnimation.to(\n    //     this.$raceLineModal,\n    //     {\n    //       y: \"100%\",\n    //       duration: DURATION_SCREEN_SPEED,\n    //       ease: \"none\",\n    //     },\n    //     `hero-banner-hide-=${DURATION_SCREEN_SPEED}`\n    //   );\n\n    //   motulAnimation.to(\n    //     this.$scrollAnnouce,\n    //     {\n    //       autoAlpha: 0,\n    //       duration: DURATION_SCREEN_SPEED,\n    //       ease: \"none\",\n    //     },\n    //     `hero-banner-hide-=${DURATION_SCREEN_SPEED}`\n    //   );\n    // }\n\n    // VOYAGER SECTION\n  }, {\n    key: \"voyagerAnimationSetup\",\n    value: function voyagerAnimationSetup() {\n      var tmpDurationLabel = DURATION_SCREEN_SPEED / 2;\n      this.$voyagerSection = $(\".voyagers-section\");\n\n      // TITLE & CONTENT\n      this.$voyagerTitle = this.$voyagerSection.find(\".section-title\");\n      this.$voyagerContent = this.$voyagerSection.find(\".section-desc\");\n\n      // KOL DEFINE\n      //// KOL - ONE\n      this.$KolOne = this.$voyagerSection.find(\".kol-wrapper .kol-1\");\n      this.$KolOneName = this.$KolOne.find(\".kol-name\");\n      this.$KolOneDesc = this.$KolOne.find(\".kol-desc\");\n      this.$KolOneImgWhite = this.$KolOne.find(\".img-white\");\n      this.$KolOneImgRed = this.$KolOne.find(\".img-red\");\n      this.$KolTwo = this.$voyagerSection.find(\".kol-wrapper .kol-2\");\n      this.$KolTwoName = this.$KolTwo.find(\".kol-name\");\n      this.$KolTwoDesc = this.$KolTwo.find(\".kol-desc\");\n      this.$KolTwoImgWhite = this.$KolTwo.find(\".img-white\");\n      this.$KolTwoImgRed = this.$KolTwo.find(\".img-red\");\n      this.$KolThree = this.$voyagerSection.find(\".kol-wrapper .kol-3\");\n      this.$KolThreeName = this.$KolThree.find(\".kol-name\");\n      this.$KolThreeDesc = this.$KolThree.find(\".kol-desc\");\n      this.$KolThreeImgWhite = this.$KolThree.find(\".img-white\");\n      this.$KolThreeImgRed = this.$KolThree.find(\".img-red\");\n      this.$KolFour = this.$voyagerSection.find(\".kol-wrapper .kol-4\");\n      this.$KolFourName = this.$KolFour.find(\".kol-name\");\n      this.$KolFourDesc = this.$KolFour.find(\".kol-desc\");\n      this.$KolFourImgWhite = this.$KolFour.find(\".img-white\");\n      this.$KolFourImgRed = this.$KolFour.find(\".img-red\");\n      this.$KolBreak = this.$voyagerSection.find(\".kol-wrapper .kol-break\");\n      this.$KolBreakTitle = this.$KolBreak.find(\".kol-wrapper .kol-info\");\n      this.$KolBreakImage = this.$KolBreak.find(\".kol-img\");\n\n      // Add Label Start Voyager\n      motulAnimation.add(\"voyager-animation-start\");\n\n      // Voyager Introduction\n      motulAnimation.fromTo([this.$voyagerTitle], {\n        ease: \"none\",\n        autoAlpha: 0,\n        scale: 1.5\n      }, {\n        ease: \"none\",\n        autoAlpha: 1,\n        scale: 1,\n        duration: DURATION_SCREEN_SPEED\n      }, \"voyager-animation-start-=\".concat(DURATION_SCREEN_SPEED));\n      motulAnimation.fromTo([this.$voyagerContent], {\n        ease: \"none\",\n        autoAlpha: 0,\n        scale: 1.5\n      }, {\n        autoAlpha: 1,\n        scale: 1,\n        ease: \"power1.easeIn\"\n      }, \"voyager-animation-start-=\".concat(tmpDurationLabel));\n\n      // Voyager - KOL ONE\n      // 2.25 is the duration when run end of each screen\n      var durationKol = DURATION_SCREEN_SPEED;\n      this.repeatKOLAnimation(\"$KolOne\", tmpDurationLabel = tmpDurationLabel / 5, \"voyager-animation-start\", durationKol); // + 2.25\n\n      this.repeatKOLAnimation(\"$KolTwo\", tmpDurationLabel += durationKol * 1.5, \"voyager-animation-start\", durationKol);\n      this.repeatKOLAnimation(\"$KolThree\", tmpDurationLabel += durationKol * 1.5, \"voyager-animation-start\", durationKol);\n      this.repeatKOLAnimation(\"$KolFour\", tmpDurationLabel += durationKol * 1.5, \"voyager-animation-start\", durationKol);\n\n      // KOL BREAK - HÀNH TRÌNH NÀO ĐỦ THỬ THÁCH\n      motulAnimation.to([this.$KolBreak], {\n        x: \"-100%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"voyager-animation-start+=\".concat(tmpDurationLabel += DURATION_SCREEN_SPEED));\n      motulAnimation.to([this.$KolBreakTitle], {\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\",\n        scale: 1.1\n      }, \"voyager-animation-start+=\".concat(tmpDurationLabel));\n      motulAnimation.to([this.$KolBreakImage], {\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\",\n        scale: 1.2\n      }, \"voyager-animation-start+=\".concat(tmpDurationLabel));\n      motulAnimation.addLabel(\"voyager-section-animation-end\");\n\n      // SCROLL CONTROLLER\n      motulAnimation.to(this.$scrollContainer, {\n        x: -this.DistanceCalc(this.$voyagerSection),\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"voyager-section-animation-end\");\n      motulAnimation.addLabel(\"voyager-section-end\");\n    }\n    // add\n    // NORTHERNMOST HA GIANG SECTION\n  }, {\n    key: \"northernmostHaGiangSectionSetup\",\n    value: function northernmostHaGiangSectionSetup() {\n      motulAnimation.addLabel(\"start-northernmost-ha-giang-section\");\n      this.$startNorthernmostHaGiangSection = $(\".start-northernmost-ha-giang-section\");\n      this.$startNorthernmostHaGiangSectionMainContent = this.$startNorthernmostHaGiangSection.find(\".main-content\");\n      this.$startNorthernmostHaGiangSectionOneMedia = this.$startNorthernmostHaGiangSection.find(\".one-media\");\n      this.$startNorthernmostHaGiangSectionTwoMedia = this.$startNorthernmostHaGiangSection.find(\".two-medias\");\n      motulAnimation.to([this.$startNorthernmostHaGiangSectionMainContent, this.$startNorthernmostHaGiangSectionOneMedia], {\n        x: \"-100%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"start-northernmost-ha-giang-section\");\n      motulAnimation.to(this.$startNorthernmostHaGiangSectionOneMedia, {\n        x: \"-100%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"start-northernmost-ha-giang-section+=\".concat(DURATION_SCREEN_SPEED));\n      motulAnimation.to(this.$startNorthernmostHaGiangSectionTwoMedia, {\n        x: \"-100%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"start-northernmost-ha-giang-section+=\".concat(DURATION_SCREEN_SPEED));\n      motulAnimation.addLabel(\"start-northernmost-ha-giang-section-finish\");\n      motulAnimation.to(this.$scrollContainer, {\n        x: -this.DistanceCalc(this.$startNorthernmostHaGiangSection),\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"start-northernmost-ha-giang-section-finish\");\n\n      // End northernmost Ha Giang section\n      // this.$endNorthernmostHaGiangSection = $(\n      //   \".end-northernmost-ha-giang-section\"\n      // );\n      // const $endNorthernmostHaGiangSectionEndingScene =\n      //   this.$endNorthernmostHaGiangSection.find(\".ending-scene\");\n      // const $endNorthernmostHaGiangSectionEndingGradientBg =\n      //   $endNorthernmostHaGiangSectionEndingScene.find(\".gradient-bg-overlay\");\n      // const $endNorthernmostHaGiangSectionEndingTxt1 =\n      //   $endNorthernmostHaGiangSectionEndingScene.find(\n      //     \".title-group.group-1 > *\"\n      //   );\n\n      // motulAnimation.addLabel(\"end-northernmost-ha-giang-section-start\");\n      // motulAnimation.to(\n      //   $endNorthernmostHaGiangSectionEndingScene,\n      //   { x: 0, duration: DURATION_SCREEN_SPEED, ease: \"none\" },\n      //   \"end-northernmost-ha-giang-section-start\"\n      // );\n\n      // motulAnimation.to(\n      //   $endNorthernmostHaGiangSectionEndingGradientBg,\n      //   { x: \"-66.66%\", duration: DURATION_SCREEN_SPEED * 2, ease: \"none\" },\n      //   \"end-northernmost-ha-giang-section-start\"\n      // );\n\n      // motulAnimation.fromTo(\n      //   $endNorthernmostHaGiangSectionEndingTxt1,\n      //   { y: 30, autoAlpha: 0 },\n      //   { y: 0, autoAlpha: 1, duration: DURATION_SCREEN_SPEED / 2, ease: \"none\" },\n      //   \"end-northernmost-ha-giang-section-start\"\n      // );\n\n      // motulAnimation.to(\n      //   $endNorthernmostHaGiangSectionEndingTxt1,\n      //   {\n      //     y: -30,\n      //     autoAlpha: 0,\n      //     duration: DURATION_SCREEN_SPEED / 4,\n      //     ease: \"none\",\n      //   },\n      //   `end-northernmost-ha-giang-section-start+=${DURATION_SCREEN_SPEED}`\n      // );\n    }\n  }, {\n    key: \"showproductAnimationSetup\",\n    value: function showproductAnimationSetup() {\n      var tmpDuration = DURATION_SCREEN_SPEED;\n      this.$showproductSection = $(\".show-products-section\");\n      this.$showproductSectionPartOne = this.$showproductSection.find(\".show-section-part.part-1\");\n      this.$showproductSectionPartTwo = this.$showproductSection.find(\".show-section-part.part-2\");\n      this.$showproductSectionPartThree = this.$showproductSection.find(\".show-section-part.part-3\");\n      this.$showproductSectionPartFour = this.$showproductSection.find(\".show-section-part.part-4\");\n\n      // PRODUCT\n      this.$primaryShowProduct = this.$showproductSection.find(\".part-2 .img-holder.product-1\");\n      this.$ExtraShowProduct = this.$showproductSection.find(\".img-holder.product-2\");\n      motulAnimation.addLabel(\"show-products-section-start\");\n      motulAnimation.to([this.$showproductSectionPartOne, this.$showproductSectionPartTwo], {\n        x: 0,\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"show-products-section-start\");\n      motulAnimation.to([this.$showproductSectionPartTwo], {\n        x: \"-100%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"show-products-section-start+=\".concat(DURATION_SCREEN_SPEED));\n\n      // motulAnimation.to(\n      //   [this.$showproductSectionPartThree],\n      //   {\n      //     x: 0,\n      //     duration: DURATION_SCREEN_SPEED,\n      //     ease: \"none\",\n      //   },\n      //   `show-products-section-start+=${DURATION_SCREEN_SPEED}`\n      // );\n\n      // Product - Motul H-Tech 100 Plus\n      motulAnimation.fromTo([this.$primaryShowProduct], {\n        x: \"100%\"\n      }, {\n        x: 0,\n        duration: DURATION_SCREEN_SPEED + 2,\n        ease: \"none\"\n      }, \"show-products-section-start-=\".concat(3));\n      motulAnimation.to([this.$showproductSectionPartThree], {\n        x: \"-100%\",\n        duration: DURATION_SCREEN_SPEED + 3,\n        ease: \"none\"\n      }, \"show-products-section-start+=\".concat(DURATION_SCREEN_SPEED * 2));\n      motulAnimation.to([this.$showproductSectionPartFour], {\n        x: \"-100%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"show-products-section-start+=\".concat(DURATION_SCREEN_SPEED * 3));\n\n      // Product - Motul Engine Clean\n      motulAnimation.fromTo([this.$ExtraShowProduct], {\n        y: -70\n      }, {\n        y: 10,\n        duration: DURATION_SCREEN_SPEED + 3,\n        ease: \"none\"\n      }, \"show-products-section-start+=\".concat(DURATION_SCREEN_SPEED * 3));\n\n      // go to next section\n      motulAnimation.addLabel(\"show-products-section-product-end\");\n      motulAnimation.to(this.$scrollContainer, {\n        x: -this.DistanceCalc(this.$showproductSection),\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"show-products-section-product-end\");\n      // console.log(\"------------------>\", this.DistanceCalc(this.$showproductSection));\n\n      // motulAnimation.addLabel(\"product-section-hide\");\n    }\n\n    // NORTH - Round 1\n  }, {\n    key: \"NorthRound1AnimationSetup\",\n    value: function NorthRound1AnimationSetup() {\n      // VARIABLES\n      this.$NorthRound1Section = $(\"#north-round-1-1-section\");\n      this.$NorthRound1SectionTitle = this.$NorthRound1Section.find(\".main-content .context-w-decor\");\n      this.$preparationSubTitle = this.$NorthRound1Section.find(\".main-content .content-label\");\n      this.$firstround1preMainContent = this.$NorthRound1Section.find(\".main-content\");\n      this.$firstround1preExtraContent = this.$NorthRound1Section.find(\".extra-content\");\n      motulAnimation.addLabel(\"north-round-1-1-section-start\");\n      motulAnimation.to([this.$firstround1preMainContent, this.$firstround1preExtraContent], {\n        x: \"-100%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"north-round-1-1-section-start\");\n      motulAnimation.fromTo([this.$NorthRound1SectionTitle, this.$preparationSubTitle], {\n        x: -20\n      }, {\n        x: 30,\n        duration: DURATION_SCREEN_SPEED * 2\n      }, \"north-round-1-1-section-start-=\".concat(DURATION_SCREEN_SPEED / 2));\n\n      // motulAnimation.to(\n      //   this.$NorthRound1Section,\n      //   {\n      //     x: \"-50%\",\n      //     duration: DURATION_SCREEN_SPEED * 2,\n      //     ease: \"none\",\n      //   },\n      //   \"north-round-1-1-section-start\"\n      // );\n\n      // FINISH & GO TO NEXT SECTION\n      motulAnimation.addLabel(\"north-round-1-1-section-finish\");\n      motulAnimation.to(this.$scrollContainer, {\n        x: -this.DistanceCalc(this.$NorthRound1Section),\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"north-round-1-1-section-finish\");\n      motulAnimation.addLabel(\"north-round-1-1-section-hide\");\n    }\n  }, {\n    key: \"NorthRound2AnimationSetup\",\n    value: function NorthRound2AnimationSetup() {\n      // VARIABLES\n      this.$NorthRound2Section = $(\"#north-round-1-2-section\");\n      this.$NorthRound2Title = this.$NorthRound2Section.find(\".main-content .round-label\");\n      this.$NorthRound2SubTitle = this.$NorthRound2Section.find(\".main-content .round-name\");\n      this.$NorthRound2Button = this.$NorthRound2Section.find(\".main-content .img-holder img\");\n      this.$NorthRound2BackgroundDecor = this.$NorthRound2Section.find(\".extra-content .bg-decor\");\n      this.$NorthRound2Map = this.$NorthRound2Section.find(\".round-map .img-holder\");\n      this.$NorthRound2MainContent = this.$NorthRound2Section.find(\".main-content\");\n      this.$NorthRound2ExtraContent = this.$NorthRound2Section.find(\".extra-content\");\n      this.$NorthRound2Gallery = this.$NorthRound2Section.find(\".round-1-gallery-scene\");\n      this.$NorthRound2ExtraMainLayout = this.$NorthRound2ExtraContent.find(\".main-layout\");\n      var $carView = this.$NorthRound2Section.find(\".vehicle-view\");\n      var $extraLayout = this.$NorthRound2Section.find(\".extra-layout\");\n      motulAnimation.addLabel(\"first-round-2-section-start\");\n      motulAnimation.to([this.$NorthRound2MainContent, this.$NorthRound2ExtraContent], {\n        x: \"-100%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"first-round-2-section-start\");\n      motulAnimation.to([this.$NorthRound2ExtraContent], {\n        x: \"-200%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"first-round-2-section-start+=\".concat(DURATION_SCREEN_SPEED));\n      motulAnimation.to([this.$NorthRound2Gallery], {\n        x: \"-100%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"first-round-2-section-start+=\".concat(DURATION_SCREEN_SPEED));\n      motulAnimation.to([this.$NorthRound2Gallery], {\n        x: \"-200%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"first-round-2-section-start+=\".concat(DURATION_SCREEN_SPEED * 2));\n      motulAnimation.to([$extraLayout],\n      // [$carView],\n      {\n        x: \"-100%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"first-round-2-section-start+=\".concat(DURATION_SCREEN_SPEED * 2));\n      motulAnimation.to([$extraLayout],\n      // [$carView],\n      {\n        x: \"-140%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"first-round-2-section-start+=\".concat(DURATION_SCREEN_SPEED * 4.5));\n      motulAnimation.to([$carView],\n      // [$extraLayout],\n      {\n        x: \"-100%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"first-round-2-section-start+=\".concat(DURATION_SCREEN_SPEED * 4.5));\n\n      // motulAnimation.to(\n      //     [$carView1],\n      //     {\n      //       x: \"-300%\",\n      //       duration: DURATION_SCREEN_SPEED,\n      //       ease: \"none\",\n      //     },\n      //     `first-round-2-section-start+=${DURATION_SCREEN_SPEED*2}`\n      //   );\n      // motulAnimation.to(\n      //     [$carView2],\n      //     {\n      //       x: \"-150%\",\n      //       duration: DURATION_SCREEN_SPEED,\n      //       ease: \"none\",\n      //     },\n      //     `first-round-2-section-start+=${DURATION_SCREEN_SPEED*2}`\n      //   );\n\n      motulAnimation.to([this.$NorthRound2ExtraMainLayout], {\n        y: 40,\n        autoAlpha: 0,\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"first-round-2-section-start+=\".concat(DURATION_SCREEN_SPEED));\n      motulAnimation.addLabel(\"first-round-2-section-finish\");\n      motulAnimation.to(this.$scrollContainer, {\n        x: -this.DistanceCalc(this.$NorthRound2Section),\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"first-round-2-section-finish\");\n      motulAnimation.to([$extraLayout.find(\".center-col .content-group\")], {\n        y: 40,\n        autoAlpha: 0,\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"first-round-2-section-finish\");\n\n      // motulAnimation.addLabel(\"round-one-hide\");\n    }\n\n    // START JOURNEY NORTH SECTION\n  }, {\n    key: \"startJourneyNorthSectionSetup\",\n    value: function startJourneyNorthSectionSetup() {\n      this.$startJourneyNorthSection = $(\".start-journey-north-section\");\n      var $startJourneyNorthSectionEndingScene = this.$startJourneyNorthSection.find(\".ending-scene\");\n      var $startJourneyNorthSectionEndingGradientBg = $startJourneyNorthSectionEndingScene.find(\".gradient-bg-overlay\");\n      var $startJourneyNorthSectionEndingTxt1 = $startJourneyNorthSectionEndingScene.find(\".title-group.group-1 > *\");\n      var $startJourneyNorthSectionEndingTxt2 = $startJourneyNorthSectionEndingScene.find(\".title-group.group-2 > *\");\n      motulAnimation.addLabel(\"start-journey-north-section\");\n      motulAnimation.to(this.$scrollContainer, {\n        x: -this.DistanceCalc(this.$endNorthernmostHaGiangSection),\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"start-journey-north-section\");\n      motulAnimation.to($startJourneyNorthSectionEndingScene, {\n        x: 0,\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"start-journey-north-section\");\n      motulAnimation.to($startJourneyNorthSectionEndingGradientBg, {\n        x: \"-66.66%\",\n        duration: DURATION_SCREEN_SPEED * 2,\n        ease: \"none\"\n      }, \"start-journey-north-section\");\n      motulAnimation.fromTo($startJourneyNorthSectionEndingTxt1, {\n        y: 30,\n        autoAlpha: 0\n      }, {\n        y: 0,\n        autoAlpha: 1,\n        duration: DURATION_SCREEN_SPEED / 2,\n        ease: \"none\"\n      }, \"start-journey-north-section\");\n\n      // motulAnimation.addLabel('show-text-start-journey-north-section');\n      motulAnimation.to($startJourneyNorthSectionEndingTxt1, {\n        y: -30,\n        autoAlpha: 0,\n        duration: DURATION_SCREEN_SPEED / 4,\n        ease: \"none\"\n      }, \"start-journey-north-section+=\".concat(DURATION_SCREEN_SPEED));\n      // motulAnimation.fromTo(\n      //   $startJourneyNorthSectionEndingTxt2,\n      //   { y: 30, autoAlpha: 0 },\n      //   { y: 0, autoAlpha: 1, duration: DURATION_SCREEN_SPEED / 2, ease: 'none' },\n      //   'start-journey-north-section'\n      // );\n      // motulAnimation.to(\n      //   $startJourneyNorthSectionEndingTxt2,\n      //   { y: -30, autoAlpha: 0, duration: DURATION_SCREEN_SPEED / 4, ease: 'none' },\n      //   `start-journey-north-section+=${DURATION_SCREEN_SPEED}`\n      // );\n      motulAnimation.fromTo($startJourneyNorthSectionEndingTxt2, {\n        y: 30,\n        autoAlpha: 0\n      }, {\n        y: 0,\n        autoAlpha: 1,\n        duration: DURATION_SCREEN_SPEED / 2,\n        ease: \"none\"\n      }, \"start-journey-north-section+=\".concat(DURATION_SCREEN_SPEED));\n\n      // motulAnimation.to(\n      //   this.$scrollContainer,\n      //   {\n      //     x: -this.DistanceCalc(this.$startJourneyNorthSection),\n      //     duration: DURATION_SCREEN_SPEED,\n      //     ease: \"none\",\n      //   },\n      //   `start-journey-north-section`\n      // );\n\n      //\n      // motulAnimation.fromTo(\n      //   $endingTxt1,\n      //   {\n      //     y: 30,\n      //     autoAlpha: 0,\n      //   },\n      //   {\n      //     y: 0,\n      //     autoAlpha: 1,\n      //     duration: DURATION_SCREEN_SPEED / 2,\n      //     ease: \"none\",\n      //   },\n      //   \"round-4-ending-show\"\n      // );\n      //\n      // motulAnimation.addLabel(\"round-4-end-first-txt-show\");\n      // motulAnimation.to(\n      //   $endingTxt1,\n      //   {\n      //     y: -30,\n      //     autoAlpha: 0,\n      //     duration: DURATION_SCREEN_SPEED / 4,\n      //     ease: \"none\",\n      //   },\n      //   `round-4-ending-show+=${DURATION_SCREEN_SPEED}`\n      // );\n      // motulAnimation.fromTo(\n      //   $endingTxt2,\n      //   {\n      //     y: 30,\n      //     autoAlpha: 0,\n      //   },\n      //   {\n      //     y: 0,\n      //     autoAlpha: 1,\n      //     duration: DURATION_SCREEN_SPEED / 2,\n      //     ease: \"none\",\n      //   },\n      //   `round-4-ending-show+=${DURATION_SCREEN_SPEED}`\n      // );\n      //\n    }\n\n    // INTRODUCE 2 STAGE\n  }, {\n    key: \"verticalSplitAnimationSetup\",\n    value: function verticalSplitAnimationSetup() {\n      var tmpDuration = DURATION_SCREEN_SPEED / 2;\n      this.$verticalSplitSection = $(\".vertical-split-section\");\n\n      // CA MAU - TOP\n      this.$camauSection = this.$verticalSplitSection.find(\".top-col\");\n      this.$camauTitle = this.$verticalSplitSection.find(\".slogan\");\n      this.$camauSubTitle = this.$verticalSplitSection.find(\".area\");\n      this.$camauImage = this.$verticalSplitSection.find(\".text-decor .img-holder\");\n      this.$camauSectionExtraLayer = this.$verticalSplitSection.find(\".layer-content\");\n\n      // PHU YEN - BOTTOM\n      this.$phuyenSection = this.$verticalSplitSection.find(\".bottom-col\");\n      this.$phuyenLabel = this.$phuyenSection.find(\".label\");\n      this.$phuyenDesctination = this.$phuyenSection.find(\".destination\");\n      this.$phuyenTimeline = this.$phuyenSection.find(\".timeline\");\n      motulAnimation.addLabel(\"vertical-split-section-start\");\n\n      // TOP - CÀ MAU\n      // motulAnimation.fromTo(\n      //   [this.$camauTitle, this.$camauSubTitle],\n      //   {\n      //     x: 0,\n      //   },\n      //   {\n      //     x: 20,\n      //     duration: DURATION_SCREEN_SPEED,\n      //     ease: \"none\",\n      //   },\n      //   `vertical-split-section-start-=${DURATION_SCREEN_SPEED / 2}`\n      // );\n\n      // motulAnimation.fromTo(\n      //   [this.$camauImage],\n      //   {\n      //     x: 0,\n      //   },\n      //   {\n      //     x: 50,\n      //     ease: \"none\",\n      //     duration: DURATION_SCREEN_SPEED,\n      //   },\n      //   `vertical-split-section-start-=${DURATION_SCREEN_SPEED / 5}`\n      // );\n\n      // BOTTOM - PHÚ YÊN\n      motulAnimation.fromTo([this.$phuyenLabel, this.$phuyenDesctination, this.$phuyenTimeline], {\n        x: 0\n      }, {\n        x: -30,\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"vertical-split-section-start-=\".concat(DURATION_SCREEN_SPEED / 2));\n\n      // section 2 animation\n      motulAnimation.to(this.$scrollContainer, {\n        x: -this.DistanceCalc(this.$verticalSplitSection),\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"linear.none\"\n      }, \"vertical-split-section-start\");\n\n      // motulAnimation.fromTo(\n      //   [this.$camauSectionExtraLayer],\n      //   {\n      //     autoAlpha: 0,\n      //   },\n      //   {\n      //     autoAlpha: 1,\n      //     bottom: 0,\n      //     ease: \"none\",\n      //     duration: DURATION_SCREEN_SPEED - 1,\n      //   },\n      //   `vertical-split-section-start`\n      // );\n\n      motulAnimation.addLabel(\"vertical-split-section-end\");\n    }\n  }, {\n    key: \"startJourneySection\",\n    value: function startJourneySection() {\n      this.$startJourneySection = $(\".start-journey-section\");\n      this.$startJourneyLogo = this.$startJourneySection.find(\".logo-holder\");\n      this.$startJourneyTitle = this.$startJourneySection.find(\".section-title\");\n      this.$startJourneySubTitle = this.$startJourneySection.find(\".section-sub-title\");\n      motulAnimation.addLabel(\"start-journey-start\");\n      motulAnimation.fromTo([this.$startJourneyLogo, this.$startJourneyTitle, this.$startJourneySubTitle], {\n        x: -30\n      }, {\n        x: 5,\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"start-journey-start-=\".concat(DURATION_SCREEN_SPEED));\n\n      // section 3 animation\n      motulAnimation.to(this.$scrollContainer, {\n        x: -this.DistanceCalc(this.$startJourneySection),\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"start-journey-start\");\n      motulAnimation.addLabel(\"start-journey-hide\");\n    }\n  }, {\n    key: \"productAnimationSetup\",\n    value: function productAnimationSetup() {\n      var tmpDuration = DURATION_SCREEN_SPEED;\n      this.$productSection = $(\".products-section\");\n      this.$productSectionPartOne = this.$productSection.find(\".section-part.part-1\");\n      this.$productSectionPartTwo = this.$productSection.find(\".section-part.part-2\");\n      this.$productSectionPartThree = this.$productSection.find(\".section-part.part-3\");\n      this.$productSectionPartFour = this.$productSection.find(\".section-part.part-4\");\n\n      // PRODUCT\n      this.$primaryProduct = this.$productSection.find(\".part-2 .img-holder.product-1\");\n      this.$ExtraProduct = this.$productSection.find(\".img-holder.product-2\");\n      motulAnimation.addLabel(\"products-section-start\");\n      motulAnimation.to([this.$productSectionPartOne, this.$productSectionPartTwo], {\n        x: \"-100%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"products-section-start\");\n      motulAnimation.to([this.$productSectionPartTwo], {\n        x: \"-200%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"products-section-start+=\".concat(DURATION_SCREEN_SPEED));\n      motulAnimation.to([this.$productSectionPartThree], {\n        x: \"-100%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"products-section-start+=\".concat(DURATION_SCREEN_SPEED));\n\n      // Product - Motul H-Tech 100 Plus\n      motulAnimation.fromTo([this.$primaryProduct], {\n        x: 0\n      }, {\n        x: 100,\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"products-section-start+=\".concat(DURATION_SCREEN_SPEED));\n      motulAnimation.to([this.$productSectionPartThree], {\n        x: \"-200%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"products-section-start+=\".concat(DURATION_SCREEN_SPEED * 2));\n\n      // Product - Motul Engine Clean\n      motulAnimation.fromTo([this.$ExtraProduct], {\n        y: -50\n      }, {\n        y: 10,\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"products-section-start+=\".concat(DURATION_SCREEN_SPEED * 2));\n      motulAnimation.to([this.$productSectionPartFour], {\n        x: \"-100%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"products-section-start+=\".concat(DURATION_SCREEN_SPEED * 2));\n\n      // motulAnimation.to(\n      //   [this.$productSectionPartFour],\n      //   {\n      //     x: \"-200%\",\n      //     duration: DURATION_SCREEN_SPEED,\n      //     ease: \"none\",\n      //   },\n      //   `products-section-start+=${DURATION_SCREEN_SPEED * 3}`\n      // );\n\n      // Fake video frame by frame scrolling\n      var frames = {\n        val: 1\n      };\n      var totalFrame = 100;\n      motulAnimation.to(frames, {\n        val: totalFrame,\n        roundProps: \"val\",\n        duration: DURATION_SCREEN_SPEED * 2,\n        onUpdate: function onUpdate() {\n          $(\"#video-footage-scroll-1\").attr(\"src\", \"static/img-m/frames/\".concat(frames.val - 25 > 0 ? frames.val - 25 : 1, \".jpg\"));\n          var frameCarValue = frames.val;\n          $(\"#video-footage-scroll-2\").attr(\"src\", \"static/img-m/frames/car/\".concat(frames.val - 35 > 0 ? frames.val - 35 : 1, \".jpg\"));\n        }\n      }, \"products-section-start+=\".concat(DURATION_SCREEN_SPEED * 2));\n\n      // // go to next section\n      motulAnimation.addLabel(\"products-section-product-end\");\n      motulAnimation.to(this.$scrollContainer, {\n        x: -this.DistanceCalc(this.$productSection),\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"products-section-start+=\".concat(DURATION_SCREEN_SPEED * 3));\n\n      // motulAnimation.addLabel(\"product-section-hide\");\n    }\n  }, {\n    key: \"preparationAnimationSetup\",\n    value: function preparationAnimationSetup() {\n      // VARIABLES\n      this.$preparationSection = $(\"#preparation-day-section\");\n      this.$preparationSectionTitle = this.$preparationSection.find(\".main-content .context-w-decor\");\n      this.$preparationSubTitle = this.$preparationSection.find(\".main-content .content-label\");\n      this.$preMainContent = this.$preparationSection.find(\".main-content\");\n      this.$preExtraContent = this.$preparationSection.find(\".extra-content\");\n      motulAnimation.addLabel(\"preparation-day-section-start\");\n      motulAnimation.to([this.$preMainContent, this.$preExtraContent], {\n        x: \"-100%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"preparation-day-section-start\");\n      motulAnimation.fromTo([this.$preparationSectionTitle, this.$preparationSubTitle], {\n        x: -20\n      }, {\n        x: 30,\n        duration: DURATION_SCREEN_SPEED * 2\n      }, \"preparation-day-section-start-=\".concat(DURATION_SCREEN_SPEED / 2));\n\n      // motulAnimation.to(\n      //   this.$preparationSection,\n      //   {\n      //     x: \"-50%\",\n      //     duration: DURATION_SCREEN_SPEED * 2,\n      //     ease: \"none\",\n      //   },\n      //   \"preparation-day-section-start\"\n      // );\n\n      // FINISH & GO TO NEXT SECTION\n      motulAnimation.addLabel(\"preparation-day-section-finish\");\n      motulAnimation.to(this.$scrollContainer, {\n        x: -this.DistanceCalc(this.$preparationSection),\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"preparation-day-section-finish\");\n      motulAnimation.addLabel(\"preparation-section-hide\");\n    }\n  }, {\n    key: \"roundOneSection\",\n    value: function roundOneSection() {\n      // VARIABLES\n      this.$roundOneSection = $(\"#round-1-section\");\n      this.$roundOneTitle = this.$roundOneSection.find(\".main-content .round-label\");\n      this.$roundOneSubTitle = this.$roundOneSection.find(\".main-content .round-name\");\n      this.$roundOneButton = this.$roundOneSection.find(\".main-content .img-holder img\");\n      this.$roundOneBackgroundDecor = this.$roundOneSection.find(\".extra-content .bg-decor\");\n      this.$roundOneMap = this.$roundOneSection.find(\".round-map .img-holder\");\n      this.$roundOneMainContent = this.$roundOneSection.find(\".main-content\");\n      this.$roundOneExtraContent = this.$roundOneSection.find(\".extra-content\");\n      this.$roundOneExtraMainLayout = this.$roundOneExtraContent.find(\".main-layout\");\n      this.$roundOneMediaOne = this.$roundOneSection.find(\".round-media.round-media-1\");\n      this.$roundOneMediaTwo = this.$roundOneSection.find(\".round-media.round-media-2\");\n      this.$roundOneMapLayout = this.$roundOneSection.find(\".round-map\");\n      motulAnimation.addLabel(\"round-1-section-start\");\n      motulAnimation.to([this.$roundOneMainContent, this.$roundOneExtraContent\n      // this.$roundOneMediaOne,\n      // this.$roundOneMediaTwo,\n      // this.$roundOneMapLayout,\n      ], {\n        x: \"-100%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"round-1-section-start\");\n      motulAnimation.to([this.$roundOneExtraContent], {\n        x: \"-200%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"round-1-section-start+=\".concat(DURATION_SCREEN_SPEED));\n      motulAnimation.to([this.$roundOneExtraMainLayout], {\n        y: 40,\n        autoAlpha: 0,\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"round-1-section-start+=\".concat(DURATION_SCREEN_SPEED));\n      motulAnimation.to([this.$roundOneMediaOne], {\n        x: \"-100%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"round-1-section-start+=\".concat(DURATION_SCREEN_SPEED));\n      motulAnimation.to([this.$roundOneMediaOne], {\n        x: \"-200%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"round-1-section-start+=\".concat(DURATION_SCREEN_SPEED * 2));\n      motulAnimation.to([this.$roundOneMediaTwo], {\n        x: \"-100%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"round-1-section-start+=\".concat(DURATION_SCREEN_SPEED * 2));\n\n      // motulAnimation.to(\n      //   [this.$roundOneMediaTwo],\n      //   {\n      //     x: \"-200%\",\n      //     duration: DURATION_SCREEN_SPEED,\n      //     ease: \"none\",\n      //   },\n      //   `round-1-section-start+=${DURATION_SCREEN_SPEED * 3}`\n      // );\n\n      // motulAnimation.to(\n      //   [this.$roundOneMapLayout],\n      //   {\n      //     x: \"-100%\",\n      //     duration: DURATION_SCREEN_SPEED,\n      //     ease: \"none\",\n      //   },\n      //   `round-1-section-start+=${DURATION_SCREEN_SPEED * 3}`\n      // );\n\n      motulAnimation.addLabel(\"round-1-section-finish\");\n      motulAnimation.to(this.$scrollContainer, {\n        x: -this.DistanceCalc(this.$roundOneSection),\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"round-1-section-finish\");\n      motulAnimation.addLabel(\"round-one-hide\");\n    }\n  }, {\n    key: \"roundTwoSection\",\n    value: function roundTwoSection() {\n      this.$roundTwoSection = $(\"#round-2-section\");\n      this.$roundTwoTitle = this.$roundTwoSection.find(\".main-content .round-label\");\n      this.$roundTwoSubTitle = this.$roundTwoSection.find(\".main-content .round-name\");\n      this.$roundTwoButton = this.$roundTwoSection.find(\".main-content .img-holder img\");\n      this.$roundTwoMainContent = this.$roundTwoSection.find(\".main-content\");\n      this.$roundTwoExtraContent = this.$roundTwoSection.find(\".extra-content\");\n      this.$roundTwoExtraMainLayout = this.$roundTwoExtraContent.find(\".main-layout\");\n      this.$roundTwoMedia = this.$roundTwoSection.find(\".round-media\");\n      this.$roundTwoMapLayout = this.$roundTwoSection.find(\".round-map\");\n      motulAnimation.addLabel(\"round-2-section-start\");\n      motulAnimation.to([this.$roundTwoMainContent, this.$roundTwoExtraContent\n      // this.$roundTwoMedia,\n      // this.$roundTwoMapLayout,\n      ], {\n        x: \"-100%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"round-2-section-start\");\n      motulAnimation.to([this.$roundTwoTitle, this.$roundTwoSubTitle, this.$roundTwoButton], {\n        scale: 1.1,\n        duration: DURATION_SCREEN_SPEED\n      }, \"round-2-section-start-=\".concat(DURATION_SCREEN_SPEED / 2));\n      motulAnimation.to([this.$roundTwoExtraContent\n      // this.$roundTwoMedia,\n      // this.$roundTwoMapLayout,\n      ], {\n        x: \"-200%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"round-2-section-start+=\".concat(DURATION_SCREEN_SPEED));\n      motulAnimation.to([this.$roundTwoExtraMainLayout], {\n        y: 40,\n        autoAlpha: 0,\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"round-2-section-start+=\".concat(DURATION_SCREEN_SPEED));\n      motulAnimation.to([this.$roundTwoMedia\n      // this.$roundTwoMapLayout,\n      ], {\n        x: \"-100%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"round-2-section-start+=\".concat(DURATION_SCREEN_SPEED));\n\n      // motulAnimation.to(\n      //   [\n      //     this.$roundTwoMedia,\n      //     // this.$roundTwoMapLayout,\n      //   ],\n      //   {\n      //     x: \"-200%\",\n      //     duration: DURATION_SCREEN_SPEED,\n      //     ease: \"none\",\n      //   },\n      //   `round-2-section-start+=${DURATION_SCREEN_SPEED * 2}`\n      // );\n\n      // motulAnimation.to(\n      //   [this.$roundTwoMapLayout],\n      //   {\n      //     x: \"-100%\",\n      //     duration: DURATION_SCREEN_SPEED,\n      //     ease: \"none\",\n      //   },\n      //   `round-2-section-start+=${DURATION_SCREEN_SPEED * 2}`\n      // );\n\n      // motulAnimation.to(\n      //   this.$roundTwoSection,\n      //   {\n      //     x: \"-50%\",\n      //     duration: DURATION_SCREEN_SPEED * 2,\n      //     ease: \"none\",\n      //   },\n      //   `round-2-section-start+=${DURATION_SCREEN_SPEED / 2}`\n      // );\n\n      motulAnimation.addLabel(\"round-2-section-finish\");\n      motulAnimation.to(this.$scrollContainer, {\n        x: -this.DistanceCalc(this.$roundTwoSection),\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"round-2-section-finish\");\n      motulAnimation.addLabel(\"round-two-hide\");\n    }\n  }, {\n    key: \"roundThreeSection\",\n    value: function roundThreeSection() {\n      this.$roundThreeSection = $(\"#round-3-section\");\n      var $round3Intro = this.$roundThreeSection.find(\".intro-scene\");\n\n      // Round 3 Detail Screen\n      var $round3Detail = this.$roundThreeSection.find(\".round-3-detail-scene\");\n      var $round3Detail_Block1 = $round3Detail.find(\"#round-3-detail-slide-1\");\n      var $round3Detail_Block2 = $round3Detail.find(\"#round-3-detail-slide-2\");\n      var $round3Detail_Block3 = $round3Detail.find(\"#round-3-detail-slide-3\");\n      var $round3Detail_Block4 = $round3Detail.find(\"#round-3-detail-slide-4\");\n      var $round3Wheel = $round3Detail.find(\".wheel-img img\");\n      var $round3Detail_Block1_content = $round3Detail_Block1.find(\".block-1 *\");\n      var $round3Detail_Block2_content = $round3Detail_Block2.find(\".block-2 .block-context *\");\n\n      // Round 3 Map Screen\n      var $round3Map = this.$roundThreeSection.find(\".round-3-map-scene\");\n      var $round3Map_Col1 = $round3Map.find(\"#round-3-map-slide-1\");\n      var $round3Map_Col2 = $round3Map.find(\"#round-3-map-slide-2\");\n      var $round3Map_Col3 = $round3Map.find(\"#round-3-map-slide-3\");\n\n      // Path and length for the map\n      var mapPath = document.getElementById(\"primary-path-to-destiny\");\n      var length = mapPath.getTotalLength();\n      gsap.set(mapPath, {\n        strokeDasharray: length,\n        strokeDashoffset: length\n      });\n\n      // Round 3 Post Scene\n      var $round3PostScene = this.$roundThreeSection.find(\".round-3-post-scene\");\n      var $round3PostScene_Car = $round3PostScene.find(\".car-img\");\n      motulAnimation.addLabel(\"round-3-section-start\");\n      motulAnimation.to([$round3Intro, $round3Detail], {\n        x: \"-100%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"round-3-section-start\");\n      motulAnimation.to([$round3Detail_Block2, $round3Detail_Block1], {\n        x: \"-100%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"round-3-section-start+=\".concat(DURATION_SCREEN_SPEED));\n      motulAnimation.to([$round3Detail_Block1_content], {\n        y: 40,\n        autoAlpha: 0,\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"round-3-section-start+=\".concat(DURATION_SCREEN_SPEED));\n      motulAnimation.to($round3Detail_Block2, {\n        x: \"-200%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"round-3-section-start+=\".concat(DURATION_SCREEN_SPEED * 2));\n      motulAnimation.to([$round3Detail_Block2_content], {\n        y: 40,\n        autoAlpha: 0,\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"round-3-section-start+=\".concat(DURATION_SCREEN_SPEED * 2));\n\n      // motulAnimation.to(\n      //   $round3Wheel,\n      //   {\n      //     rotation: 180,\n      //     duration: DURATION_SCREEN_SPEED,\n      //     ease: \"none\",\n      //   },\n      //   `round-3-section-start+=${DURATION_SCREEN_SPEED * 2}`\n      // );\n\n      motulAnimation.to($round3Detail_Block3, {\n        x: \"-100%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"round-3-section-start+=\".concat(DURATION_SCREEN_SPEED * 2));\n\n      // motulAnimation.to(\n      //   $round3Detail_Block3,\n      //   {\n      //     x: \"-200%\",\n      //     duration: DURATION_SCREEN_SPEED,\n      //     ease: \"none\",\n      //   },\n      //   `round-3-section-start+=${DURATION_SCREEN_SPEED * 3}`\n      // );\n      // motulAnimation.to(\n      //   $round3Wheel,\n      //   {\n      //     rotation: 360,\n      //     duration: DURATION_SCREEN_SPEED,\n      //     ease: \"none\",\n      //     x: 100,\n      //   },\n      //   `round-3-section-start+=${DURATION_SCREEN_SPEED * 3}`\n      // );\n      // motulAnimation.to(\n      //   $round3Detail_Block4,\n      //   {\n      //     x: \"-100%\",\n      //     duration: DURATION_SCREEN_SPEED,\n      //     ease: \"none\",\n      //   },\n      //   `round-3-section-start+=${DURATION_SCREEN_SPEED * 3}`\n      // );\n\n      // End Of Round 3 Detail Animation\n      motulAnimation.addLabel(\"round-3-detail-animation-end\");\n      motulAnimation.to($round3Map, {\n        x: \"-100%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"round-3-detail-animation-end\");\n      motulAnimation.to([$round3Map_Col2, $round3Map_Col1], {\n        x: \"-100%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      });\n      motulAnimation.addLabel(\"round-3-map-effect-show\");\n      motulAnimation.fromTo($(\"#primary-path-to-destiny\"), {\n        strokeDashoffset: length\n      }, {\n        strokeDashoffset: 0,\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"round-3-map-effect-show\");\n      motulAnimation.addLabel(\"round-3-map-effect-end\");\n      motulAnimation.to($round3Map_Col2, {\n        x: \"-200%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"round-3-map-effect-end\");\n      motulAnimation.to($round3Map_Col3, {\n        x: \"-100%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"round-3-map-effect-end\");\n      motulAnimation.addLabel(\"round-3-map-animation-end\");\n\n      // Post Scene Animation\n      motulAnimation.to($round3Map, {\n        x: \"-200%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"round-3-map-animation-end\");\n      motulAnimation.to($round3PostScene, {\n        x: \"-100%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"round-3-map-animation-end\");\n      motulAnimation.add(\"round-3-animation-end\");\n\n      // Car run from left to right\n      motulAnimation.fromTo($round3PostScene_Car, {\n        x: \"-40%\"\n      }, {\n        x: \"180%\",\n        duration: DURATION_SCREEN_SPEED * 1.5,\n        ease: \"none\"\n      }, \"round-3-map-animation-end\");\n      motulAnimation.fromTo($round3PostScene, {\n        backgroundColor: \"#d7920d\"\n      }, {\n        backgroundColor: \"#000000\",\n        duration: 5,\n        ease: \"none\"\n      }, \"round-3-map-animation-end+=\".concat(DURATION_SCREEN_SPEED - 5));\n      motulAnimation.fromTo($round3PostScene.find(\".top-block\"), {\n        webkitFilter: \"brightness(1)\",\n        filter: \"brightness(1)\"\n      }, {\n        webkitFilter: \"brightness(0.1)\",\n        filter: \"brightness(0.1)\",\n        duration: 5,\n        ease: \"none\"\n      }, \"round-3-map-animation-end+=\".concat(DURATION_SCREEN_SPEED - 5));\n\n      // Round 3 Hide Animation\n      console.log(\"🚀 ~ S-------->n):\", -this.DistanceCalc(this.$roundThreeSection));\n      motulAnimation.to(this.$scrollContainer, {\n        x: -this.DistanceCalc(this.$roundThreeSection),\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"round-3-animation-end\");\n      motulAnimation.addLabel(\"round-three-hide\");\n    }\n  }, {\n    key: \"roundFourSection\",\n    value: function roundFourSection() {\n      // VARIABLES\n      this.$roundFourSection = $(\"#round-4-section\");\n\n      // Intro Scene\n      var $introScene = this.$roundFourSection.find(\".intro-scene\");\n\n      // Gallery Scene\n      var $galleryScene = this.$roundFourSection.find(\".round-4-gallery-scene\");\n\n      // Detail Scene\n      var $DetailScene = this.$roundFourSection.find(\".detail-scene\");\n\n      // Detail Scene Context\n      var $DetailSceneContext = $DetailScene.find(\".detail-context\");\n\n      // Ending Scene\n      var $Endingscene = this.$roundFourSection.find(\".ending-scene\");\n      var $gradientBg = $Endingscene.find(\".gradient-bg-overlay\");\n      var $endingTxt1 = $Endingscene.find(\".title-group.group-1 > *\");\n      var $endingTxt2 = $Endingscene.find(\".title-group.group-2 > *\");\n      var $imgTag = this.$roundFourSection.find(\"#mui-ca-mau-tag\");\n      motulAnimation.addLabel(\"round-4-section-start\");\n      motulAnimation.to([$galleryScene, $introScene], {\n        x: \"-100%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"round-4-section-start\");\n      motulAnimation.fromTo([$imgTag], {\n        autoAlpha: 0,\n        scale: 3\n      }, {\n        autoAlpha: 1,\n        scale: 1,\n        duration: 3,\n        ease: \"power1.easeIn\"\n      }, \"round-4-section-start+=\".concat(DURATION_SCREEN_SPEED));\n      motulAnimation.addLabel(\"round-4-gallery-show\");\n      motulAnimation.to($galleryScene, {\n        x: \"-200%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"round-4-gallery-show\");\n      motulAnimation.to($DetailScene, {\n        x: \"-100%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"round-4-gallery-show\");\n      motulAnimation.addLabel(\"round-4-detail-show\");\n      motulAnimation.to($DetailSceneContext, {\n        y: 40,\n        autoAlpha: 0,\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"round-4-detail-show\");\n      motulAnimation.to($Endingscene, {\n        x: \"-100%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"round-4-detail-show\");\n      motulAnimation.addLabel(\"round-4-ending-show\");\n      motulAnimation.to($gradientBg, {\n        x: \"-66.66%\",\n        duration: DURATION_SCREEN_SPEED * 2,\n        ease: \"none\"\n      }, \"round-4-ending-show\");\n      motulAnimation.fromTo($endingTxt1, {\n        y: 30,\n        autoAlpha: 0\n      }, {\n        y: 0,\n        autoAlpha: 1,\n        duration: DURATION_SCREEN_SPEED / 2,\n        ease: \"none\"\n      }, \"round-4-ending-show\");\n      motulAnimation.addLabel(\"round-4-end-first-txt-show\");\n      motulAnimation.to($endingTxt1, {\n        y: -30,\n        autoAlpha: 0,\n        duration: DURATION_SCREEN_SPEED / 4,\n        ease: \"none\"\n      }, \"round-4-ending-show+=\".concat(DURATION_SCREEN_SPEED));\n      motulAnimation.fromTo($endingTxt2, {\n        y: 30,\n        autoAlpha: 0\n      }, {\n        y: 0,\n        autoAlpha: 1,\n        duration: DURATION_SCREEN_SPEED / 2,\n        ease: \"none\"\n      }, \"round-4-ending-show+=\".concat(DURATION_SCREEN_SPEED));\n\n      // Do nothing animation\n      // motulAnimation.to(this.$roundFourSection, {\n      //   x: 0,\n      //   duration: DURATION_SCREEN_SPEED * 12,\n      //   ease: \"none\",\n      // });\n      motulAnimation.addLabel(\"round-four-hide\");\n    }\n\n    // Repeat KOL ANIMATION\n    /**\r\n     *\r\n     * @param $positionKOL\r\n     * @param $startFrom\r\n     * @param $labelName\r\n     * @param durationKol\r\n     */\n  }, {\n    key: \"repeatKOLAnimation\",\n    value: function repeatKOLAnimation($positionKOL) {\n      var $startFrom = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 0.5;\n      var $labelName = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : \"voyager-animation-start\";\n      var durationKol = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : 4;\n      var startFromSet = $startFrom;\n      motulAnimation.to(this[\"\".concat($positionKOL)], {\n        x: \"-100%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"\".concat($labelName, \"+=\").concat(startFromSet));\n      motulAnimation.to(this[\"\".concat($positionKOL, \"ImgRed\")], {\n        ease: \"none\",\n        autoAlpha: 0,\n        duration: DURATION_SCREEN_SPEED\n      }, \"\".concat($labelName, \"+=\").concat(startFromSet + durationKol / 8));\n      motulAnimation.to(this[\"\".concat($positionKOL, \"ImgWhite\")], {\n        ease: \"none\",\n        autoAlpha: 1,\n        duration: DURATION_SCREEN_SPEED\n      }, \"\".concat($labelName, \"+=\").concat(startFromSet + durationKol / 6));\n      motulAnimation.fromTo([this[\"\".concat($positionKOL, \"Desc\")]], {\n        ease: \"none\",\n        autoAlpha: 0\n      }, {\n        ease: \"none\",\n        autoAlpha: 1,\n        duration: DURATION_SCREEN_SPEED\n      }, \"\".concat($labelName, \"+=\").concat(startFromSet + durationKol / 4));\n      motulAnimation.fromTo([this[\"\".concat($positionKOL, \"Name\")]], {\n        ease: \"none\",\n        autoAlpha: 0,\n        scale: 1.2\n      }, {\n        ease: \"none\",\n        autoAlpha: 1,\n        scale: 1,\n        duration: DURATION_SCREEN_SPEED - 2\n      }, \"\".concat($labelName, \"+=\").concat(startFromSet + durationKol / 3));\n    }\n  }, {\n    key: \"DistanceCalc\",\n    value: function DistanceCalc() {\n      var $section = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : null;\n      if ($section !== null) {\n        var previousSibling = $section.prevAll(\".section\");\n        var distance = $section.outerWidth();\n        if (previousSibling.length > 0) {\n          previousSibling.each(function (i, $sibEl) {\n            distance += $($sibEl).outerWidth();\n          });\n        }\n        return distance;\n      }\n      return 0;\n    }\n  }]);\n}();\n\n\n//# sourceURL=webpack://frontend-boilerplate/./app/static/js-m/modules/_Stage1.js?");
  
  /***/ }),
  
  /***/ "./app/static/js-m/modules/_Stage2.js":
  /*!********************************************!*\
    !*** ./app/static/js-m/modules/_Stage2.js ***!
    \********************************************/
  /***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {
  
  "use strict";
  eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"default\": () => (/* binding */ Stage2)\n/* harmony export */ });\nfunction _typeof(o) { \"@babel/helpers - typeof\"; return _typeof = \"function\" == typeof Symbol && \"symbol\" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && \"function\" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? \"symbol\" : typeof o; }, _typeof(o); }\nfunction _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError(\"Cannot call a class as a function\"); }\nfunction _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, \"value\" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }\nfunction _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, \"prototype\", { writable: !1 }), e; }\nfunction _toPropertyKey(t) { var i = _toPrimitive(t, \"string\"); return \"symbol\" == _typeof(i) ? i : i + \"\"; }\nfunction _toPrimitive(t, r) { if (\"object\" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || \"default\"); if (\"object\" != _typeof(i)) return i; throw new TypeError(\"@@toPrimitive must return a primitive value.\"); } return (\"string\" === r ? String : Number)(t); }\nvar DURATION_SCREEN_SPEED = 15;\nvar Stage2 = /*#__PURE__*/function () {\n  /* ===================================\r\n   *  CONSTRUCTOR\r\n   * =================================== */\n  function Stage2() {\n    _classCallCheck(this, Stage2);\n    this.bindEvents();\n  }\n\n  /* ===================================\r\n   *  EVENTS\r\n   * =================================== */\n  return _createClass(Stage2, [{\n    key: \"bindEvents\",\n    value: function bindEvents() {\n      if (IS_MOBILE) {\n        this.Stage2HorizontalScrollMobileSetup();\n      }\n    }\n  }, {\n    key: \"Stage2HorizontalScrollMobileSetup\",\n    value: function Stage2HorizontalScrollMobileSetup() {\n      this.$sections = $(\".section\");\n      this.$scrollContainer = document.querySelector(\".section-holder\");\n\n      // Transition Section\n      this.TransitionSectionAnim();\n      this.Stage2StartJourneySection();\n      this.Stage2PreparationAnimationSetup();\n      this.Stage2ProductAnimationSetup();\n      this.Stage2RoundOneSection();\n      this.Stage2RoundTwoSection();\n      this.Stage2RoundThreeSection();\n      if ($(\".is-phase-2\").length > 0) {\n        this.Stage2RoundFourSection();\n      }\n    }\n  }, {\n    key: \"TransitionSectionAnim\",\n    value: function TransitionSectionAnim() {\n      this.$roundFourSection = $(\"#round-4-section\");\n      this.$transitionSection = $(\"#transition-section\");\n      var $transitionScene = this.$transitionSection.find(\".transition-scene\");\n      motulAnimation.addLabel(\"stage-2-anim-start\");\n      motulAnimation.to(this.$scrollContainer, {\n        x: -this.DistanceCalc(this.$roundFourSection),\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"stage-2-anim-start\");\n      motulAnimation.to([$transitionScene], {\n        x: \"-50%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"stage-2-anim-start+=\".concat(DURATION_SCREEN_SPEED));\n\n      // Fake video frame by frame scrolling\n      var frames = {\n        val: 1\n      };\n      var totalFrame = 125;\n      motulAnimation.to(frames, {\n        val: totalFrame,\n        roundProps: \"val\",\n        duration: DURATION_SCREEN_SPEED * 2,\n        onUpdate: function onUpdate() {\n          $(\"#transition-banner\").attr(\"src\", \"static/img-m/frames/dawn/\".concat(\n          // (frames.val - 35) > 0 ? (frames.val - 35) : 1\n          1000 + frames.val * 2 - 70 > 1000 ? 1000 + frames.val * 2 - 70 : 1000, \".jpg\"));\n        }\n      }, \"stage-2-anim-start+=\".concat(DURATION_SCREEN_SPEED));\n      motulAnimation.to(this.$scrollContainer, {\n        x: -this.DistanceCalc(this.$transitionSection),\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"stage-2-anim-start+=\".concat(DURATION_SCREEN_SPEED * 2));\n      motulAnimation.addLabel(\"stage-2-anim-hide\");\n    }\n  }, {\n    key: \"Stage2StartJourneySection\",\n    value: function Stage2StartJourneySection() {\n      this.$stage2StartJourneySection = $(\".stage-2-start-journey-section\");\n      motulAnimation.addLabel(\"stage-2-start-journey-start\");\n      motulAnimation.to(this.$scrollContainer, {\n        x: -this.DistanceCalc(this.$stage2StartJourneySection),\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"stage-2-start-journey-start\");\n      motulAnimation.addLabel(\"stage-2-start-journey-hide\");\n    }\n  }, {\n    key: \"Stage2PreparationAnimationSetup\",\n    value: function Stage2PreparationAnimationSetup() {\n      // VARIABLES\n      this.$preparationSection = $(\"#stage-2-preparation-day-section\");\n      this.$preparationSectionTitle = this.$preparationSection.find(\".main-content .context-w-decor\");\n      this.$preparationSubTitle = this.$preparationSection.find(\".main-content .content-label\");\n      this.$preSectionPart = this.$preparationSection.find(\".section-part.part-1\");\n      this.$preMainContent = this.$preparationSection.find(\".main-content\");\n      this.$preExtraContent = this.$preparationSection.find(\".extra-content\");\n      motulAnimation.addLabel(\"preparation-day-section-start\");\n      motulAnimation.to([this.$preSectionPart], {\n        x: \"-100%\",\n        duration: DURATION_SCREEN_SPEED * 2,\n        ease: \"none\"\n      }, \"preparation-day-section-start\");\n      motulAnimation.to([this.$preSectionPart.find(\".section-title\")], {\n        left: \"75%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"preparation-day-section-start\");\n      motulAnimation.to([this.$preMainContent], {\n        x: \"-100%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"preparation-day-section-start+=\".concat(DURATION_SCREEN_SPEED));\n      motulAnimation.to([this.$preMainContent], {\n        x: \"-200%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"preparation-day-section-start+=\".concat(DURATION_SCREEN_SPEED * 2));\n      motulAnimation.to([this.$preExtraContent], {\n        x: \"-100%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"preparation-day-section-start+=\".concat(DURATION_SCREEN_SPEED * 2));\n      motulAnimation.fromTo([this.$preparationSectionTitle, this.$preparationSubTitle], {\n        x: -20\n      }, {\n        x: 0,\n        duration: DURATION_SCREEN_SPEED\n      }, \"preparation-day-section-start-=\".concat(DURATION_SCREEN_SPEED / 2));\n\n      // FINISH & GO TO NEXT SECTION\n      motulAnimation.addLabel(\"preparation-day-section-finish\");\n      motulAnimation.to(this.$scrollContainer, {\n        x: -this.DistanceCalc(this.$preparationSection),\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"preparation-day-section-finish\");\n      motulAnimation.addLabel(\"preparation-section-hide\");\n    }\n  }, {\n    key: \"Stage2ProductAnimationSetup\",\n    value: function Stage2ProductAnimationSetup() {\n      this.$productSection = $(\"#stage-2-products-section\");\n      this.$productSectionPartOne = this.$productSection.find(\".section-part.part-1\");\n      this.$productSectionPartTwo = this.$productSection.find(\".section-part.part-2\");\n      this.$productSectionPartThree = this.$productSection.find(\".section-part.part-3\");\n\n      // PRODUCT\n      this.$primaryProduct = this.$productSection.find(\".part-2 .img-holder.product-1\");\n      this.$ExtraProduct = this.$productSection.find(\".img-holder.product-2\");\n      motulAnimation.addLabel(\"products-section-start\");\n      motulAnimation.to([this.$productSectionPartTwo, this.$productSectionPartThree], {\n        x: \"-100%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"products-section-start\");\n\n      // motulAnimation.to(\n      //   [this.$productSectionPartThree],\n      //   {\n      //     x: \"-200%\",\n      //     duration: DURATION_SCREEN_SPEED,\n      //     ease: \"none\",\n      //   },\n      //   `products-section-start+=${DURATION_SCREEN_SPEED}`\n      // );\n\n      // motulAnimation.to(\n      //   [this.$productSectionPartOne],\n      //   {\n      //     x: \"-100%\",\n      //     duration: DURATION_SCREEN_SPEED*2,\n      //     ease: \"none\",\n      //   },\n      //   `products-section-start+=${DURATION_SCREEN_SPEED}`\n      // );\n\n      // motulAnimation.fromTo(\n      //   [this.$productSectionPartOne.find(\".section-title\")],\n      //   {\n      //     left: \"25%\",\n      //   },\n      //   {\n      //     left: \"75%\",\n      //     duration: DURATION_SCREEN_SPEED,\n      //     ease: \"none\",\n      //   },\n      //   `products-section-start+=${DURATION_SCREEN_SPEED*2}`\n      // );\n\n      // Product - Motul H-Tech 100 Plus\n      motulAnimation.fromTo([this.$primaryProduct], {\n        x: 0\n      }, {\n        x: 100,\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"products-section-start\");\n\n      // Product - Motul Engine Clean\n      motulAnimation.fromTo([this.$ExtraProduct], {\n        y: -50\n      }, {\n        y: 10,\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"products-section-start+=\".concat(DURATION_SCREEN_SPEED));\n\n      // // go to next section\n      motulAnimation.addLabel(\"products-section-product-end\");\n      motulAnimation.to(this.$scrollContainer, {\n        x: -this.DistanceCalc(this.$productSection),\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"products-section-start+=\".concat(DURATION_SCREEN_SPEED));\n    }\n  }, {\n    key: \"Stage2RoundOneSection\",\n    value: function Stage2RoundOneSection() {\n      // VARIABLES\n      this.$roundOneSection = $(\"#stage-2-round-1-section\");\n      this.$roundOneTitle = this.$roundOneSection.find(\".main-content .round-label\");\n      this.$roundOneSubTitle = this.$roundOneSection.find(\".main-content .round-name\");\n      this.$roundOneButton = this.$roundOneSection.find(\".main-content .img-holder img\");\n      this.$roundOneBackgroundDecor = this.$roundOneSection.find(\".extra-content .bg-decor\");\n      this.$roundOneMap = this.$roundOneSection.find(\".round-map .img-holder\");\n      this.$roundOneMainContent = this.$roundOneSection.find(\".main-content\");\n      this.$roundOneExtraContent = this.$roundOneSection.find(\".extra-content\");\n      this.$roundOneGallery = this.$roundOneSection.find(\".round-1-gallery-scene\");\n      this.$roundOneExtraMainLayout = this.$roundOneExtraContent.find(\".main-layout\");\n      var $carView = this.$roundOneSection.find(\".vehicle-view\");\n      var $extraLayout = this.$roundOneSection.find(\".extra-layout\");\n      motulAnimation.addLabel(\"round-1-section-start\");\n      motulAnimation.to([this.$roundOneMainContent, this.$roundOneExtraContent], {\n        x: \"-100%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"round-1-section-start\");\n      motulAnimation.to([this.$roundOneExtraContent], {\n        x: \"-200%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"round-1-section-start+=\".concat(DURATION_SCREEN_SPEED));\n      motulAnimation.to([this.$roundOneGallery], {\n        x: \"-100%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"round-1-section-start+=\".concat(DURATION_SCREEN_SPEED));\n      motulAnimation.to([this.$roundOneGallery], {\n        x: \"-200%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"round-1-section-start+=\".concat(DURATION_SCREEN_SPEED * 2));\n      motulAnimation.to([$carView], {\n        x: \"-100%\",\n        duration: DURATION_SCREEN_SPEED * 2.5,\n        ease: \"none\"\n      }, \"round-1-section-start+=\".concat(DURATION_SCREEN_SPEED * 2));\n      motulAnimation.to([$carView], {\n        x: \"-140%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"round-1-section-start+=\".concat(DURATION_SCREEN_SPEED * 4.5));\n      motulAnimation.to([$extraLayout], {\n        x: \"-100%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"round-1-section-start+=\".concat(DURATION_SCREEN_SPEED * 4.5));\n\n      // motulAnimation.to(\n      //     [$carView1],\n      //     {\n      //       x: \"-300%\",\n      //       duration: DURATION_SCREEN_SPEED,\n      //       ease: \"none\",\n      //     },\n      //     `round-1-section-start+=${DURATION_SCREEN_SPEED*2}`\n      //   );\n      // motulAnimation.to(\n      //     [$carView2],\n      //     {\n      //       x: \"-150%\",\n      //       duration: DURATION_SCREEN_SPEED,\n      //       ease: \"none\",\n      //     },\n      //     `round-1-section-start+=${DURATION_SCREEN_SPEED*2}`\n      //   );\n\n      motulAnimation.to([this.$roundOneExtraMainLayout], {\n        y: 40,\n        autoAlpha: 0,\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"round-1-section-start+=\".concat(DURATION_SCREEN_SPEED));\n      motulAnimation.addLabel(\"round-1-section-finish\");\n      motulAnimation.to(this.$scrollContainer, {\n        x: -this.DistanceCalc(this.$roundOneSection),\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"round-1-section-finish\");\n      motulAnimation.to([$extraLayout.find(\".center-col .content-group\")], {\n        y: 40,\n        autoAlpha: 0,\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"round-1-section-finish\");\n\n      // motulAnimation.addLabel(\"round-one-hide\");\n    }\n  }, {\n    key: \"Stage2RoundTwoSection\",\n    value: function Stage2RoundTwoSection() {\n      // VARIABLES\n      this.$stage2RoundTwoSection = $(\"#stage-2-round-2-section\");\n\n      // Intro Scene\n      var $introScene = this.$stage2RoundTwoSection.find(\".intro-scene\");\n\n      // Gallery Scene\n      var $galleryScene = this.$stage2RoundTwoSection.find(\".round-2-gallery-scene\");\n\n      // Detail Scene\n      var $DetailScene = this.$stage2RoundTwoSection.find(\".detail-scene\");\n\n      // Detail Scene Context\n      var $DetailSceneContext = $DetailScene.find(\".detail-context\");\n\n      // Ending Scene\n\n      motulAnimation.addLabel(\"stage-2-round-2-section-start\");\n      motulAnimation.to([$galleryScene, $introScene], {\n        x: \"-100%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"stage-2-round-2-section-start\");\n      motulAnimation.addLabel(\"stage-2-round-2-gallery-show\");\n      motulAnimation.to($galleryScene, {\n        x: \"-200%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"stage-2-round-2-gallery-show\");\n      motulAnimation.to($DetailScene, {\n        x: \"-100%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"stage-2-round-2-gallery-show\");\n      motulAnimation.addLabel(\"stage-2-round-2-detail-show\");\n      motulAnimation.to($DetailSceneContext, {\n        y: 40,\n        autoAlpha: 0,\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"stage-2-round-2-detail-show\");\n      // Round 2 Hide Animation\n      motulAnimation.to(this.$scrollContainer, {\n        x: -this.DistanceCalc(this.$stage2RoundTwoSection),\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"stage-2-round-2-detail-show\");\n      motulAnimation.addLabel(\"stage-2-round-two-hide\");\n    }\n  }, {\n    key: \"Stage2RoundThreeSection\",\n    value: function Stage2RoundThreeSection() {\n      this.$stage2RoundThreeSection = $(\"#stage-2-round-3-section\");\n      var $round3Intro = this.$stage2RoundThreeSection.find(\".intro-scene\");\n\n      // Round 3 Detail Screen\n      var $Stage2Round3Detail = this.$stage2RoundThreeSection.find(\".round-3-detail-scene\");\n      var $Stage2Round3Detail_Block1 = $Stage2Round3Detail.find(\"#stage-2-round-3-detail-slide-1\");\n      var $Stage2Round3Detail_Block2 = $Stage2Round3Detail.find(\"#stage-2-round-3-detail-slide-2\");\n      var $Stage2Round3Detail_Block3 = $Stage2Round3Detail.find(\"#stage-2-round-3-detail-slide-3\");\n      var $Stage2Round3Detail_Block1_content = $Stage2Round3Detail_Block1.find(\".block-1 *\");\n      var $Stage2Round3Detail_Block2_content = $Stage2Round3Detail_Block2.find(\".block-2 .block-context *\");\n      motulAnimation.addLabel(\"stage-2-round-3-section-start\");\n      motulAnimation.to([$round3Intro, $Stage2Round3Detail], {\n        x: \"-100%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"stage-2-round-3-section-start\");\n      motulAnimation.to([$Stage2Round3Detail_Block2, $Stage2Round3Detail_Block1], {\n        x: \"-100%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"stage-2-round-3-section-start+=\".concat(DURATION_SCREEN_SPEED));\n      motulAnimation.to([$Stage2Round3Detail_Block1_content], {\n        y: 40,\n        autoAlpha: 0,\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"stage-2-round-3-section-start+=\".concat(DURATION_SCREEN_SPEED));\n      motulAnimation.to($Stage2Round3Detail_Block2, {\n        x: \"-200%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"stage-2-round-3-section-start+=\".concat(DURATION_SCREEN_SPEED * 2));\n      motulAnimation.to([$Stage2Round3Detail_Block2_content], {\n        y: 40,\n        autoAlpha: 0,\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"stage-2-round-3-section-start+=\".concat(DURATION_SCREEN_SPEED * 2));\n      motulAnimation.to($Stage2Round3Detail_Block3, {\n        x: \"-100%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"stage-2-round-3-section-start+=\".concat(DURATION_SCREEN_SPEED * 2));\n\n      // End Of Round 3 Detail Animation\n      motulAnimation.addLabel(\"stage-2-round-3-detail-animation-end\");\n      if ($(\".is-phase-2\").length > 0) {\n        // Round 3 Hide Animation\n        motulAnimation.to(this.$scrollContainer, {\n          x: -this.DistanceCalc(this.$stage2RoundThreeSection),\n          duration: DURATION_SCREEN_SPEED,\n          ease: \"none\"\n        }, \"stage-2-round-3-animation-end\");\n        motulAnimation.to($Stage2Round3Detail_Block3.find(\".light-yellow-txt\"), {\n          y: 40,\n          autoAlpha: 0,\n          duration: DURATION_SCREEN_SPEED,\n          ease: \"none\"\n        }, \"stage-2-round-3-animation-end\");\n        motulAnimation.addLabel(\"round-three-hide\");\n      }\n    }\n  }, {\n    key: \"Stage2RoundFourSection\",\n    value: function Stage2RoundFourSection() {\n      // VARIABLES\n      this.$stage2RoundFourSection = $(\"#stage-2-round-4-section\");\n      this.$stage2RoundFourTitle = this.$stage2RoundFourSection.find(\".main-content .round-label\");\n      this.$stage2RoundFourSubTitle = this.$stage2RoundFourSection.find(\".main-content .round-name\");\n      this.$stage2RoundFourRoundContent = this.$stage2RoundFourSection.find(\".main-content .round-content\");\n      this.$stage2RoundFourButton = this.$stage2RoundFourSection.find(\".main-content .img-holder img\");\n      this.$stage2RoundFourMap = this.$stage2RoundFourSection.find(\".round-map .img-holder\");\n      this.$stage2RoundFourMainContent = this.$stage2RoundFourSection.find(\".main-content\");\n      this.$stage2RoundFourMediaOne = this.$stage2RoundFourSection.find(\".round-media.round-media-1\");\n      this.$stage2RoundFourMediaTwo = this.$stage2RoundFourSection.find(\".round-media.round-media-2\");\n      this.$stage2RoundFourMediaThree = this.$stage2RoundFourSection.find(\".round-media.round-media-3\");\n      this.$stage2RoundFourMediaFour = this.$stage2RoundFourSection.find(\".round-media.round-media-4\");\n      this.$stage2RoundFourMediaFour = this.$stage2RoundFourSection.find(\".round-media.round-media-4\");\n      this.$stage2RoundFourMediaFive = this.$stage2RoundFourSection.find(\".round-media.round-media-5\");\n      this.$stage2RoundFourMediaSix = this.$stage2RoundFourSection.find(\".round-media.round-media-6\");\n      this.$stage2RoundFourExtraContent = this.$stage2RoundFourSection.find(\".extra-content\");\n      this.$stage2RoundFourEndScene = this.$stage2RoundFourSection.find(\".end-stage-scene\");\n      motulAnimation.addLabel(\"stage-2-round-4-section-start\");\n      motulAnimation.to([this.$stage2RoundFourMainContent], {\n        x: \"-100%\",\n        duration: DURATION_SCREEN_SPEED * 2,\n        ease: \"none\"\n      }, \"stage-2-round-4-section-start\");\n      motulAnimation.to([this.$stage2RoundFourRoundContent], {\n        left: \"75%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"stage-2-round-4-section-start\");\n\n      // Fake video frame by frame scrolling\n      var frames = {\n        val: 1\n      };\n      var totalFrame = 60;\n      motulAnimation.to(frames, {\n        val: totalFrame,\n        roundProps: \"val\",\n        duration: DURATION_SCREEN_SPEED * 3,\n        onUpdate: function onUpdate() {\n          $(\"#video-footage-scroll-3\").attr(\"src\", \"static/img-m/frames/camp/\".concat(\n          // (frames.val - 35) > 0 ? (frames.val - 35) : 1\n          1000 + frames.val * 3 > 1000 ? 1000 + frames.val * 3 : 1000, \".jpg\"));\n        }\n      }, \"stage-2-round-4-section-start\");\n      motulAnimation.to([this.$stage2RoundFourMediaOne], {\n        x: \"-100%\",\n        duration: DURATION_SCREEN_SPEED * 2.5,\n        ease: \"none\"\n      }, \"stage-2-round-4-section-start+=\".concat(DURATION_SCREEN_SPEED));\n      motulAnimation.to([this.$stage2RoundFourMediaOne], {\n        x: \"-140%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"stage-2-round-4-section-start+=\".concat(DURATION_SCREEN_SPEED * 3.5));\n      motulAnimation.to([this.$stage2RoundFourMediaThree], {\n        x: \"-100%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"stage-2-round-4-section-start+=\".concat(DURATION_SCREEN_SPEED * 3.5));\n      motulAnimation.to([this.$stage2RoundFourMediaThree], {\n        x: \"-200%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"stage-2-round-4-section-start+=\".concat(DURATION_SCREEN_SPEED * 4.5));\n      motulAnimation.to([this.$stage2RoundFourMediaThree.find(\".round-desc\")], {\n        y: 40,\n        autoAlpha: 0,\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"stage-2-round-4-section-start+=\".concat(DURATION_SCREEN_SPEED * 4.5));\n      motulAnimation.to([this.$stage2RoundFourMediaFour], {\n        x: \"-100%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"stage-2-round-4-section-start+=\".concat(DURATION_SCREEN_SPEED * 4.5));\n      motulAnimation.addLabel(\"stage-2-round-4-phrase-1-finish\");\n      motulAnimation.addLabel(\"stage-2-round-4-phrase-1-start\");\n      motulAnimation.to([this.$stage2RoundFourMediaFour], {\n        x: \"-200%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"stage-2-round-4-phrase-1-start\");\n      motulAnimation.to([this.$stage2RoundFourExtraContent], {\n        x: \"-100%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"stage-2-round-4-phrase-1-start\");\n      motulAnimation.to([this.$stage2RoundFourExtraContent], {\n        x: \"-200%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"stage-2-round-4-phrase-1-start+=\".concat(DURATION_SCREEN_SPEED));\n      motulAnimation.to([this.$stage2RoundFourExtraContent.find(\".main-layout\")], {\n        y: 40,\n        autoAlpha: 0,\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"stage-2-round-4-phrase-1-start+=\".concat(DURATION_SCREEN_SPEED));\n      motulAnimation.to([this.$stage2RoundFourMediaFive], {\n        x: \"-100%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"stage-2-round-4-phrase-1-start+=\".concat(DURATION_SCREEN_SPEED));\n      motulAnimation.to([this.$stage2RoundFourMediaFive], {\n        x: \"-200%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"stage-2-round-4-phrase-1-start+=\".concat(DURATION_SCREEN_SPEED * 2));\n      motulAnimation.to([this.$stage2RoundFourMediaSix], {\n        x: \"-100%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"stage-2-round-4-phrase-1-start+=\".concat(DURATION_SCREEN_SPEED * 2));\n      motulAnimation.fromTo([this.$stage2RoundFourMediaSix.find(\".img-tag\")], {\n        autoAlpha: 0,\n        scale: 3\n      }, {\n        autoAlpha: 1,\n        scale: 1,\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"stage-2-round-4-phrase-1-start+=\".concat(DURATION_SCREEN_SPEED * 3));\n      motulAnimation.to([this.$stage2RoundFourMediaSix], {\n        x: \"-200%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"stage-2-round-4-phrase-1-start+=\".concat(DURATION_SCREEN_SPEED * 4));\n      motulAnimation.to([this.$stage2RoundFourMediaSix.find(\".round-desc\")], {\n        y: 40,\n        autoAlpha: 0,\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"stage-2-round-4-phrase-1-start+=\".concat(DURATION_SCREEN_SPEED * 4));\n      motulAnimation.to([this.$stage2RoundFourEndScene], {\n        x: \"-100%\",\n        duration: DURATION_SCREEN_SPEED,\n        ease: \"none\"\n      }, \"stage-2-round-4-phrase-1-start+=\".concat(DURATION_SCREEN_SPEED * 4));\n      // motulAnimation.to(\n      //   this.$scrollContainer,\n      //   {\n      //     x: -this.DistanceCalc(this.$stage2RoundFourSection),\n      //     duration: DURATION_SCREEN_SPEED,\n      //     ease: \"none\",\n      //   },\n      //   `stage-2-round-4-section-finish`\n      // );\n\n      motulAnimation.addLabel(\"stage-2-round-four-hide\");\n    }\n  }, {\n    key: \"DistanceCalc\",\n    value: function DistanceCalc() {\n      var $section = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : null;\n      if ($section !== null) {\n        var previousSibling = $section.prevAll(\".section\");\n        var distance = $section.outerWidth();\n        if (previousSibling.length > 0) {\n          previousSibling.each(function (i, $sibEl) {\n            distance += $($sibEl).outerWidth();\n          });\n        }\n        return distance;\n      }\n      return 0;\n    }\n  }]);\n}();\n\n\n//# sourceURL=webpack://frontend-boilerplate/./app/static/js-m/modules/_Stage2.js?");
  
  /***/ }),
  
  /***/ "./app/static/js-m/modules/lazy-load.js":
  /*!**********************************************!*\
    !*** ./app/static/js-m/modules/lazy-load.js ***!
    \**********************************************/
  /***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {
  
  "use strict";
  eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"default\": () => (__WEBPACK_DEFAULT_EXPORT__)\n/* harmony export */ });\nvar lazyLoad = function lazyLoad() {\n  document.addEventListener(\"DOMContentLoaded\", function () {\n    var lazyloadImages;\n\n    // Using Intersection Observer API\n    if (\"IntersectionObserver\" in window) {\n      lazyloadImages = document.querySelectorAll(\".lazy\");\n      var imageObserver = new IntersectionObserver(function (entries, observer) {\n        entries.forEach(function (entry) {\n          if (entry.isIntersecting) {\n            var image = entry.target;\n            image.src = image.dataset.src;\n            image.classList.remove(\"lazy\");\n            imageObserver.unobserve(image);\n          }\n        });\n      });\n      lazyloadImages.forEach(function (image) {\n        imageObserver.observe(image);\n      });\n    } else {\n      var _lazyload = function lazyload() {\n        if (lazyloadThrottleTimeout) {\n          clearTimeout(lazyloadThrottleTimeout);\n        }\n        lazyloadThrottleTimeout = setTimeout(function () {\n          var scrollTop = window.pageYOffset;\n          lazyloadImages.forEach(function (img) {\n            if (img.offsetTop < window.innerHeight + scrollTop) {\n              img.src = img.dataset.src;\n              img.classList.remove(\"lazy\");\n            }\n          });\n          if (lazyloadImages.length == 0) {\n            document.removeEventListener(\"scroll\", _lazyload);\n            window.removeEventListener(\"resize\", _lazyload);\n            window.removeEventListener(\"orientationChange\", _lazyload);\n          }\n        }, 20);\n      };\n      // Backup\n      var lazyloadThrottleTimeout;\n      lazyloadImages = document.querySelectorAll(\".lazy\");\n      document.addEventListener(\"scroll\", _lazyload);\n      window.addEventListener(\"resize\", _lazyload);\n      window.addEventListener(\"orientationChange\", _lazyload);\n    }\n  });\n  document.addEventListener(\"DOMContentLoaded\", function () {\n    var lazyVideos = [].slice.call(document.querySelectorAll(\"video.lazy\"));\n    if (\"IntersectionObserver\" in window) {\n      var lazyVideoObserver = new IntersectionObserver(function (entries, observer) {\n        entries.forEach(function (video) {\n          if (video.isIntersecting) {\n            for (var source in video.target.children) {\n              var videoSource = video.target.children[source];\n              if (typeof videoSource.tagName === \"string\" && videoSource.tagName === \"SOURCE\") {\n                videoSource.src = videoSource.dataset.src;\n              }\n            }\n            video.target.load();\n            video.target.classList.remove(\"lazy\");\n            lazyVideoObserver.unobserve(video.target);\n          }\n        });\n      });\n      lazyVideos.forEach(function (lazyVideo) {\n        lazyVideoObserver.observe(lazyVideo);\n      });\n    }\n  });\n};\n/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (lazyLoad);\n\n//# sourceURL=webpack://frontend-boilerplate/./app/static/js-m/modules/lazy-load.js?");
  
  /***/ }),
  
  /***/ "./app/static/js-m/modules/rounds/components/hero-banner.js":
  /*!******************************************************************!*\
    !*** ./app/static/js-m/modules/rounds/components/hero-banner.js ***!
    \******************************************************************/
  /***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {
  
  "use strict";
  eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   heroBannerAnimationSetup: () => (/* binding */ heroBannerAnimationSetup)\n/* harmony export */ });\n/* harmony import */ var _utils_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../../utils.js */ \"./app/static/js-m/modules/utils.js\");\n\nfunction heroBannerAnimationSetup(DURATION_SCREEN_SPEED, sections, scrollContainer) {\n  var heroSection = $(\"#hero-banner-section\");\n  var raceLineModal = $(\"#race-line-modal\");\n  var raceModalControl = raceLineModal.find(\".arrow-control\");\n  var scrollAnnouce = raceLineModal.find(\".annouce-label\");\n  motulAnimation.addLabel(\"anim-start\");\n  motulAnimation.to(scrollContainer, {\n    x: -(0,_utils_js__WEBPACK_IMPORTED_MODULE_0__.DistanceCalc)(heroSection),\n    duration: DURATION_SCREEN_SPEED,\n    ease: \"none\"\n  }, \"anim-start\");\n  motulAnimation.addLabel(\"hero-banner-hide\");\n  motulAnimation.fromTo(raceModalControl, {\n    autoAlpha: 0,\n    duration: DURATION_SCREEN_SPEED,\n    ease: \"none\"\n  }, {\n    autoAlpha: 1,\n    duration: DURATION_SCREEN_SPEED,\n    ease: \"none\"\n  }, \"hero-banner-hide-=1\");\n  motulAnimation.to(raceLineModal, {\n    y: \"100%\",\n    duration: DURATION_SCREEN_SPEED,\n    ease: \"none\"\n  }, \"hero-banner-hide-=\".concat(DURATION_SCREEN_SPEED));\n  motulAnimation.to(scrollAnnouce, {\n    autoAlpha: 0,\n    duration: DURATION_SCREEN_SPEED,\n    ease: \"none\"\n  }, \"hero-banner-hide-=\".concat(DURATION_SCREEN_SPEED));\n}\n\n//# sourceURL=webpack://frontend-boilerplate/./app/static/js-m/modules/rounds/components/hero-banner.js?");
  
  /***/ }),
  
  /***/ "./app/static/js-m/modules/rounds/north/index.js":
  /*!*******************************************************!*\
    !*** ./app/static/js-m/modules/rounds/north/index.js ***!
    \*******************************************************/
  /***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {
  
  "use strict";
  eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"default\": () => (/* binding */ NorthAniSetup)\n/* harmony export */ });\n/* harmony import */ var _intro__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./intro */ \"./app/static/js-m/modules/rounds/north/intro.js\");\n/* harmony import */ var _product__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./product */ \"./app/static/js-m/modules/rounds/north/product.js\");\n/* harmony import */ var _r1__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./r1 */ \"./app/static/js-m/modules/rounds/north/r1.js\");\n/* harmony import */ var _r2__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./r2 */ \"./app/static/js-m/modules/rounds/north/r2.js\");\n/* harmony import */ var _r3__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ./r3 */ \"./app/static/js-m/modules/rounds/north/r3.js\");\n\n\n\n\n\nfunction NorthAniSetup() {\n  var DURATION_SCREEN_SPEED = 15;\n  var scrollContainer = document.querySelector(\".section-holder\");\n  (0,_intro__WEBPACK_IMPORTED_MODULE_0__.northIntroAni)(DURATION_SCREEN_SPEED, scrollContainer);\n  (0,_product__WEBPACK_IMPORTED_MODULE_1__.northProductAni)(DURATION_SCREEN_SPEED, scrollContainer);\n  (0,_r1__WEBPACK_IMPORTED_MODULE_2__.northR1Ani)(DURATION_SCREEN_SPEED, scrollContainer);\n  (0,_r2__WEBPACK_IMPORTED_MODULE_3__.northR2Ani)(DURATION_SCREEN_SPEED, scrollContainer);\n  (0,_r3__WEBPACK_IMPORTED_MODULE_4__.northR3Ani)(DURATION_SCREEN_SPEED, scrollContainer);\n}\n\n//# sourceURL=webpack://frontend-boilerplate/./app/static/js-m/modules/rounds/north/index.js?");
  
  /***/ }),
  
  /***/ "./app/static/js-m/modules/rounds/north/intro.js":
  /*!*******************************************************!*\
    !*** ./app/static/js-m/modules/rounds/north/intro.js ***!
    \*******************************************************/
  /***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {
  
  "use strict";
  eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   northIntroAni: () => (/* binding */ northIntroAni)\n/* harmony export */ });\n/* harmony import */ var _utils_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../../utils.js */ \"./app/static/js-m/modules/utils.js\");\n\nfunction northIntroAni(DURATION_SCREEN_SPEED, scrollContainer) {\n  var startNorthernmostHaGiangSection = $(\".start-northernmost-ha-giang-section\");\n  var startNorthernmostHaGiangSectionMainContent = startNorthernmostHaGiangSection.find(\".main-content\");\n  var startNorthernmostHaGiangSectionOneMedia = startNorthernmostHaGiangSection.find(\".one-media\");\n  var startNorthernmostHaGiangSectionTwoMedia = startNorthernmostHaGiangSection.find(\".two-medias\");\n  motulAnimation.addLabel(\"start-northernmost-ha-giang-section\");\n  motulAnimation.to([startNorthernmostHaGiangSectionMainContent, startNorthernmostHaGiangSectionOneMedia], {\n    x: \"-100%\",\n    duration: DURATION_SCREEN_SPEED,\n    ease: \"none\"\n  }, \"start-northernmost-ha-giang-section\");\n  motulAnimation.to(startNorthernmostHaGiangSectionOneMedia, {\n    x: \"-100%\",\n    duration: DURATION_SCREEN_SPEED,\n    ease: \"none\"\n  }, \"start-northernmost-ha-giang-section+=\".concat(DURATION_SCREEN_SPEED));\n  motulAnimation.to(startNorthernmostHaGiangSectionTwoMedia, {\n    x: \"-100%\",\n    duration: DURATION_SCREEN_SPEED,\n    ease: \"none\"\n  }, \"start-northernmost-ha-giang-section+=\".concat(DURATION_SCREEN_SPEED));\n  motulAnimation.addLabel(\"start-northernmost-ha-giang-section-finish\");\n  motulAnimation.to(scrollContainer, {\n    x: -(0,_utils_js__WEBPACK_IMPORTED_MODULE_0__.DistanceCalc)(startNorthernmostHaGiangSection),\n    duration: DURATION_SCREEN_SPEED,\n    ease: \"none\"\n  }, \"start-northernmost-ha-giang-section-finish\");\n\n  // End northernmost Ha Giang section\n  // const endNorthernmostHaGiangSection = $(\".end-northernmost-ha-giang-section\");\n  // const endNorthernmostHaGiangSectionEndingScene =\n  //   endNorthernmostHaGiangSection.find(\".ending-scene\");\n  // const $endNorthernmostHaGiangSectionEndingGradientBg =\n  //   endNorthernmostHaGiangSectionEndingScene.find(\".gradient-bg-overlay\");\n  // const endNorthernmostHaGiangSectionEndingTxt1 =\n  //   endNorthernmostHaGiangSectionEndingScene.find(\".title-group.group-1 > *\");\n\n  // motulAnimation.addLabel(\"end-northernmost-ha-giang-section-start\");\n  // motulAnimation.to(\n  //   endNorthernmostHaGiangSectionEndingScene,\n  //   { x: 0, duration: DURATION_SCREEN_SPEED, ease: \"none\" },\n  //   \"end-northernmost-ha-giang-section-start\"\n  // );\n\n  // motulAnimation.to(\n  //   $endNorthernmostHaGiangSectionEndingGradientBg,\n  //   { x: \"-66.66%\", duration: DURATION_SCREEN_SPEED * 2, ease: \"none\" },\n  //   \"end-northernmost-ha-giang-section-start\"\n  // );\n\n  // motulAnimation.fromTo(\n  //   endNorthernmostHaGiangSectionEndingTxt1,\n  //   { y: 30, autoAlpha: 0 },\n  //   { y: 0, autoAlpha: 1, duration: DURATION_SCREEN_SPEED / 2, ease: \"none\" },\n  //   \"end-northernmost-ha-giang-section-start\"\n  // );\n\n  // motulAnimation.to(\n  //   endNorthernmostHaGiangSectionEndingTxt1,\n  //   {\n  //     y: -30,\n  //     autoAlpha: 0,\n  //     duration: DURATION_SCREEN_SPEED / 4,\n  //     ease: \"none\",\n  //   },\n  //   `end-northernmost-ha-giang-section-start+=${DURATION_SCREEN_SPEED}`\n  // );\n\n  // TODO remove this if add some screen before this\n  // motulAnimation.addLabel(\"clear-end-northernmost-ha-giang-section-start\");\n  // motulAnimation.to(\n  //   scrollContainer,\n  //   {\n  //     x: -DistanceCalc(endNorthernmostHaGiangSection),\n  //     duration: DURATION_SCREEN_SPEED,\n  //     ease: \"none\",\n  //   },\n  //   `clear-end-northernmost-ha-giang-section-start`\n  // );\n}\n\n//# sourceURL=webpack://frontend-boilerplate/./app/static/js-m/modules/rounds/north/intro.js?");
  
  /***/ }),
  
  /***/ "./app/static/js-m/modules/rounds/north/product.js":
  /*!*********************************************************!*\
    !*** ./app/static/js-m/modules/rounds/north/product.js ***!
    \*********************************************************/
  /***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {
  
  "use strict";
  eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   northProductAni: () => (/* binding */ northProductAni)\n/* harmony export */ });\n/* harmony import */ var _utils_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../../utils.js */ \"./app/static/js-m/modules/utils.js\");\n\nfunction northProductAni(DURATION_SCREEN_SPEED, scrollContainer) {\n  //   let tmpDuration = DURATION_SCREEN_SPEED;\n\n  var showProductSection = $(\".show-products-section\");\n  var showProductSectionPartOne = showProductSection.find(\".show-section-part.part-1\");\n  var showProductSectionPartTwo = showProductSection.find(\".show-section-part.part-2\");\n  var showProductSectionPartThree = showProductSection.find(\".show-section-part.part-3\");\n  var showProductSectionPartFour = showProductSection.find(\".show-section-part.part-4\");\n\n  // PRODUCT\n  var primaryShowProduct = showProductSection.find(\".part-2 .img-holder.product-1\");\n  var ExtraShowProduct = showProductSection.find(\".img-holder.product-2\");\n  motulAnimation.addLabel(\"show-products-section-start\");\n  motulAnimation.to([showProductSectionPartOne], {\n    x: 0,\n    duration: DURATION_SCREEN_SPEED,\n    ease: \"none\"\n  }, \"show-products-section-start\");\n  motulAnimation.to([showProductSectionPartTwo], {\n    x: \"-100%\",\n    duration: DURATION_SCREEN_SPEED + 5,\n    ease: \"none\"\n  }, \"show-products-section-start-=\".concat(DURATION_SCREEN_SPEED));\n\n  // motulAnimation.to(\n  //   [showProductSectionPartThree],\n  //   {\n  //     x: 0,\n  //     duration: DURATION_SCREEN_SPEED,\n  //     ease: \"none\",\n  //   },\n  //   `show-products-section-start+=${DURATION_SCREEN_SPEED}`\n  // );\n\n  // Product - Motul H-Tech 100 Plus\n  motulAnimation.fromTo([primaryShowProduct], {\n    x: \"100%\"\n  }, {\n    x: 0,\n    duration: DURATION_SCREEN_SPEED + 2,\n    ease: \"none\"\n  }, \"show-products-section-start-=\".concat(3));\n  motulAnimation.to([showProductSectionPartThree], {\n    x: \"-100%\",\n    duration: DURATION_SCREEN_SPEED + 3,\n    ease: \"none\"\n  }, \"show-products-section-start+=\".concat(DURATION_SCREEN_SPEED * 3));\n  motulAnimation.to([showProductSectionPartFour], {\n    x: \"-100%\",\n    duration: DURATION_SCREEN_SPEED + 3,\n    ease: \"none\"\n  }, \"show-products-section-start+=\".concat(DURATION_SCREEN_SPEED * 4));\n\n  // Product - Motul Engine Clean\n  motulAnimation.fromTo([ExtraShowProduct], {\n    y: -70\n  }, {\n    y: 10,\n    duration: DURATION_SCREEN_SPEED + 3,\n    ease: \"none\"\n  }, \"show-products-section-start+=\".concat(DURATION_SCREEN_SPEED * 3));\n\n  // go to next section\n  motulAnimation.addLabel(\"show-products-section-product-end\");\n  motulAnimation.to(scrollContainer, {\n    x: -(0,_utils_js__WEBPACK_IMPORTED_MODULE_0__.DistanceCalc)(showProductSection),\n    duration: DURATION_SCREEN_SPEED,\n    ease: \"none\"\n  }, \"show-products-section-product-end\");\n  // console.log(\"------------------>\", this.DistanceCalc(this.$showproductSection));\n\n  // motulAnimation.addLabel(\"product-section-hide\");\n}\n\n//# sourceURL=webpack://frontend-boilerplate/./app/static/js-m/modules/rounds/north/product.js?");
  
  /***/ }),
  
  /***/ "./app/static/js-m/modules/rounds/north/r1.js":
  /*!****************************************************!*\
    !*** ./app/static/js-m/modules/rounds/north/r1.js ***!
    \****************************************************/
  /***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {
  
  "use strict";
  eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   northR1Ani: () => (/* binding */ northR1Ani)\n/* harmony export */ });\n/* harmony import */ var _utils_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../../utils.js */ \"./app/static/js-m/modules/utils.js\");\n\nfunction northR1Ani(DURATION_SCREEN_SPEED, scrollContainer) {\n  // NorthRound1AnimationSetup() {\n  // VARIABLES\n  var $NorthRound1Section = $(\"#north-round-1-1-section\");\n  var $NorthRound1SectionTitle = $NorthRound1Section.find(\".main-content .context-w-decor\");\n  var $preparationSubTitle = $NorthRound1Section.find(\".main-content .content-label\");\n  var $firstround1preMainContent = $NorthRound1Section.find(\".main-content\");\n  var $firstround1preExtraContent = $NorthRound1Section.find(\".extra-content\");\n  motulAnimation.addLabel(\"north-round-1-1-section-start\");\n  motulAnimation.to([$firstround1preMainContent, $firstround1preExtraContent], {\n    x: \"-100%\",\n    duration: DURATION_SCREEN_SPEED,\n    ease: \"none\"\n  }, \"north-round-1-1-section-start\");\n  motulAnimation.fromTo([$NorthRound1SectionTitle, $preparationSubTitle], {\n    x: -20\n  }, {\n    x: 30,\n    duration: DURATION_SCREEN_SPEED * 2\n  }, \"north-round-1-1-section-start-=\".concat(DURATION_SCREEN_SPEED / 2));\n\n  // motulAnimation.to(\n  //   $NorthRound1Section,\n  //   {\n  //     x: \"-50%\",\n  //     duration: DURATION_SCREEN_SPEED * 2,\n  //     ease: \"none\",\n  //   },\n  //   \"north-round-1-1-section-start\"\n  // );\n\n  // FINISH & GO TO NEXT SECTION\n  motulAnimation.addLabel(\"north-round-1-1-section-finish\");\n  motulAnimation.to(scrollContainer, {\n    x: -(0,_utils_js__WEBPACK_IMPORTED_MODULE_0__.DistanceCalc)($NorthRound1Section),\n    duration: DURATION_SCREEN_SPEED,\n    ease: \"none\"\n  }, \"north-round-1-1-section-finish\");\n  motulAnimation.addLabel(\"north-round-1-1-section-hide\");\n  // }\n\n  // NorthRound2AnimationSetup() {\n  // VARIABLES\n  var $NorthRound2Section = $(\"#north-round-1-2-section\");\n  var $NorthRound2Title = $NorthRound2Section.find(\".main-content .round-label\");\n  var $NorthRound2SubTitle = $NorthRound2Section.find(\".main-content .round-name\");\n  var $NorthRound2Button = $NorthRound2Section.find(\".main-content .img-holder img\");\n  var $NorthRound2BackgroundDecor = $NorthRound2Section.find(\".extra-content .bg-decor\");\n  var $NorthRound2Map = $NorthRound2Section.find(\".round-map .img-holder\");\n  var $NorthRound2MainContent = $NorthRound2Section.find(\".main-content\");\n  var $NorthRound2ExtraContent = $NorthRound2Section.find(\".extra-content\");\n  var $NorthRound2Gallery = $NorthRound2Section.find(\".round-1-gallery-scene\");\n  var $NorthRound2ExtraMainLayout = $NorthRound2ExtraContent.find(\".main-layout\");\n  var $carView = $NorthRound2Section.find(\".vehicle-view\");\n  var $extraLayout = $NorthRound2Section.find(\".extra-layout\");\n  motulAnimation.addLabel(\"first-round-2-section-start\");\n  motulAnimation.to([$NorthRound2MainContent, $NorthRound2ExtraContent], {\n    x: \"-100%\",\n    duration: DURATION_SCREEN_SPEED,\n    ease: \"none\"\n  }, \"first-round-2-section-start\");\n  motulAnimation.to([$NorthRound2ExtraContent], {\n    x: \"-200%\",\n    duration: DURATION_SCREEN_SPEED,\n    ease: \"none\"\n  }, \"first-round-2-section-start+=\".concat(DURATION_SCREEN_SPEED));\n  motulAnimation.to([$NorthRound2Gallery], {\n    x: \"-100%\",\n    duration: DURATION_SCREEN_SPEED,\n    ease: \"none\"\n  }, \"first-round-2-section-start+=\".concat(DURATION_SCREEN_SPEED));\n  motulAnimation.to([$NorthRound2Gallery], {\n    x: \"-200%\",\n    duration: DURATION_SCREEN_SPEED,\n    ease: \"none\"\n  }, \"first-round-2-section-start+=\".concat(DURATION_SCREEN_SPEED * 2));\n  motulAnimation.to([$extraLayout],\n  // [$carView],\n  {\n    x: \"-100%\",\n    duration: DURATION_SCREEN_SPEED,\n    ease: \"none\"\n  }, \"first-round-2-section-start+=\".concat(DURATION_SCREEN_SPEED * 2));\n  motulAnimation.to([$extraLayout],\n  // [$carView],\n  {\n    x: \"-140%\",\n    duration: DURATION_SCREEN_SPEED,\n    ease: \"none\"\n  }, \"first-round-2-section-start+=\".concat(DURATION_SCREEN_SPEED * 4.5));\n  motulAnimation.to([$carView],\n  // [$extraLayout],\n  {\n    x: \"-100%\",\n    duration: DURATION_SCREEN_SPEED,\n    ease: \"none\"\n  }, \"first-round-2-section-start+=\".concat(DURATION_SCREEN_SPEED * 4.5));\n\n  // motulAnimation.to(\n  //     [$carView1],\n  //     {\n  //       x: \"-300%\",\n  //       duration: DURATION_SCREEN_SPEED,\n  //       ease: \"none\",\n  //     },\n  //     `first-round-2-section-start+=${DURATION_SCREEN_SPEED*2}`\n  //   );\n  // motulAnimation.to(\n  //     [$carView2],\n  //     {\n  //       x: \"-150%\",\n  //       duration: DURATION_SCREEN_SPEED,\n  //       ease: \"none\",\n  //     },\n  //     `first-round-2-section-start+=${DURATION_SCREEN_SPEED*2}`\n  //   );\n\n  motulAnimation.to([$NorthRound2ExtraMainLayout], {\n    y: 40,\n    autoAlpha: 0,\n    duration: DURATION_SCREEN_SPEED,\n    ease: \"none\"\n  }, \"first-round-2-section-start+=\".concat(DURATION_SCREEN_SPEED));\n  motulAnimation.addLabel(\"first-round-2-section-finish\");\n  motulAnimation.to(scrollContainer, {\n    x: -(0,_utils_js__WEBPACK_IMPORTED_MODULE_0__.DistanceCalc)($NorthRound2Section),\n    duration: DURATION_SCREEN_SPEED,\n    ease: \"none\"\n  }, \"first-round-2-section-finish\");\n  motulAnimation.to([$extraLayout.find(\".center-col .content-group\")], {\n    y: 40,\n    autoAlpha: 0,\n    duration: DURATION_SCREEN_SPEED,\n    ease: \"none\"\n  }, \"first-round-2-section-finish\");\n\n  // motulAnimation.addLabel(\"round-one-hide\");\n  // }\n}\n\n//# sourceURL=webpack://frontend-boilerplate/./app/static/js-m/modules/rounds/north/r1.js?");
  
  /***/ }),
  
  /***/ "./app/static/js-m/modules/rounds/north/r2.js":
  /*!****************************************************!*\
    !*** ./app/static/js-m/modules/rounds/north/r2.js ***!
    \****************************************************/
  /***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {
  
  "use strict";
  eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   northR2Ani: () => (/* binding */ northR2Ani)\n/* harmony export */ });\n/* harmony import */ var _utils_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../../utils.js */ \"./app/static/js-m/modules/utils.js\");\n\nfunction northR2Ani(DURATION_SCREEN_SPEED, scrollContainer) {\n  var roundThreeSection = $(\"#north-r2\");\n  var round3Intro = roundThreeSection.find(\".intro-scene\");\n  var round3Intro_Title = round3Intro.find(\".main-content\");\n\n  // Round 3 Detail Screen\n  var round3Detail = roundThreeSection.find(\".round-3-detail-scene\");\n  var round3Detail_Block1 = round3Detail.find(\"#round-3-detail-slide-1\");\n  var round3Detail_Block2 = round3Detail.find(\"#round-3-detail-slide-2\");\n  var round3Detail_Block3 = round3Detail.find(\"#round-3-detail-slide-3\");\n  // const round3Detail_Block4 = round3Detail.find(\"#round-3-detail-slide-4\");\n  // const round3Wheel = round3Detail.find(\".wheel-img img\");\n\n  var round3Detail_Block1_content = round3Detail_Block1.find(\".block-1 *\");\n  var round3Detail_Block2_content = round3Detail_Block2.find(\".block-2 .block-context *\");\n  var block3_content_1 = round3Detail_Block3.find(\".text-block-1\");\n  var block3_content_2 = round3Detail_Block3.find(\".text-block-2\");\n\n  // Round 3 Map Screen\n  var round3EngineIntro = roundThreeSection.find(\".round-3-intro-engine\");\n  var contentEngineIntro = round3EngineIntro.find(\".main-content\");\n\n  // Round 3 Post Scene\n  var round3PostScene = roundThreeSection.find(\".round-3-post-scene\");\n  var round3PostScene_Title = round3PostScene.find(\".block-context\");\n\n  // Round 3 section 5\n  var section5 = roundThreeSection.find(\".north-r2-section-5\");\n  var contentSection5 = section5.find(\".main-content\");\n  var slide1Section5 = section5.find(\"#north-r2-s5-slide-1\");\n  var slide2Section5 = section5.find(\"#north-r2-s5-slide-2\");\n  var slide3Section5 = section5.find(\"#north-r2-s5-slide-3\");\n\n  // Round 3 section 6\n  var section6 = roundThreeSection.find(\".north-r2-section-6\");\n  var slide1Section6 = section6.find(\"#north-r2-s6-slide-1\");\n  var iconSlide1Section6 = slide1Section6.find(\".icon\");\n  // const slide2Section6 = section6.find(\"#north-r2-s6-slide-2\");\n  var slide3Section6 = section6.find(\"#north-r2-s6-slide-3\");\n  var contentSlide3Section6 = slide3Section6.find(\".main-content\");\n\n  // Round 3 section 7\n  var section7 = roundThreeSection.find(\".north-r2-section-7\");\n  var slide1Section7 = section7.find(\"#north-r2-s7-slide-1\");\n  var contentSlide1Section7 = slide1Section7.find(\".main-content\");\n  var slide2Section7 = section7.find(\"#north-r2-s7-slide-2\");\n  var imgsSlide2Section7 = section7.find(\".imgs\");\n  var slide3Section7 = section7.find(\"#north-r2-s7-slide-3\");\n\n  // Path and length for the map\n  var mapPath = document.getElementById(\"primary-path-to-destiny\");\n  var length = mapPath.getTotalLength();\n\n  // gsap.set(mapPath, {\n  //   strokeDasharray: length,\n  //   strokeDashoffset: length,\n  // });\n\n  motulAnimation.addLabel(\"north-r2-start\");\n  motulAnimation.to([round3Intro, round3Detail], {\n    x: \"-100%\",\n    duration: DURATION_SCREEN_SPEED,\n    ease: \"none\"\n  }, \"north-r2-start\");\n  motulAnimation.fromTo(round3Intro_Title, {\n    x: \"100%\",\n    opacity: 0\n  }, {\n    x: \"0\",\n    opacity: 1,\n    duration: DURATION_SCREEN_SPEED / 2,\n    ease: \"none\"\n  }, \"north-r2-start-=\".concat(DURATION_SCREEN_SPEED / 2));\n  motulAnimation.to([round3Detail_Block2, round3Detail_Block1], {\n    x: \"-100%\",\n    duration: DURATION_SCREEN_SPEED,\n    ease: \"none\"\n  }, \"north-r2-start+=\".concat(DURATION_SCREEN_SPEED));\n  motulAnimation.to([round3Detail_Block1_content], {\n    y: 40,\n    autoAlpha: 0,\n    duration: DURATION_SCREEN_SPEED,\n    ease: \"none\"\n  }, \"north-r2-start+=\".concat(DURATION_SCREEN_SPEED));\n  motulAnimation.to(round3Detail_Block2, {\n    x: \"-200%\",\n    duration: DURATION_SCREEN_SPEED,\n    ease: \"none\"\n  }, \"north-r2-start+=\".concat(DURATION_SCREEN_SPEED * 2));\n  motulAnimation.to([round3Detail_Block2_content], {\n    y: 40,\n    autoAlpha: 0,\n    duration: DURATION_SCREEN_SPEED,\n    ease: \"none\"\n  }, \"north-r2-start+=\".concat(DURATION_SCREEN_SPEED * 2));\n  motulAnimation.to(round3Detail_Block3, {\n    x: \"-100%\",\n    duration: DURATION_SCREEN_SPEED,\n    ease: \"none\"\n  }, \"north-r2-start+=\".concat(DURATION_SCREEN_SPEED * 2));\n  motulAnimation.fromTo(block3_content_1, {\n    x: \"-100%\",\n    opacity: 0\n  }, {\n    x: 0,\n    opacity: 1,\n    duration: DURATION_SCREEN_SPEED / 2,\n    ease: \"none\"\n  }, \"north-r2-start+=\".concat(DURATION_SCREEN_SPEED * 2 + 3));\n  motulAnimation.fromTo(block3_content_2, {\n    x: \"100%\",\n    opacity: 0\n  }, {\n    x: 0,\n    opacity: 1,\n    duration: DURATION_SCREEN_SPEED / 2,\n    ease: \"none\"\n  }, \"north-r2-start+=\".concat(DURATION_SCREEN_SPEED * 2 + 10));\n\n  // End Of Round 3 Detail Animation\n  motulAnimation.addLabel(\"north-r2-detail-animation-end\");\n  motulAnimation.to(round3EngineIntro, {\n    x: \"-100%\",\n    duration: DURATION_SCREEN_SPEED,\n    ease: \"none\"\n  }, \"north-r2-detail-animation-end+=\".concat(0));\n  motulAnimation.fromTo(contentEngineIntro, {\n    x: \"-100%\",\n    opacity: 0\n  }, {\n    x: \"0\",\n    opacity: 1,\n    duration: DURATION_SCREEN_SPEED / 2,\n    ease: \"none\"\n  }, \"north-r2-detail-animation-end+=\".concat(DURATION_SCREEN_SPEED / 2));\n  motulAnimation.to(round3PostScene, {\n    x: \"-100%\",\n    duration: DURATION_SCREEN_SPEED,\n    ease: \"none\"\n  }, \"north-r2-map-animation-end\");\n  motulAnimation.add(\"north-r2-animation-end\");\n\n  // Title run from left to right\n  motulAnimation.fromTo(round3PostScene_Title, {\n    x: \"-100%\"\n  }, {\n    x: \"20%\",\n    duration: DURATION_SCREEN_SPEED,\n    ease: \"none\"\n  }, \"north-r2-map-animation-end-=\".concat(2));\n  motulAnimation.fromTo(round3PostScene, {\n    backgroundColor: \"transparent\"\n  }, {\n    backgroundColor: \"#000000\",\n    duration: 5,\n    ease: \"none\"\n  }, \"north-r2-map-animation-end+=\".concat(DURATION_SCREEN_SPEED - 5));\n  motulAnimation.fromTo(round3PostScene.find(\".top-block\"), {\n    webkitFilter: \"brightness(1)\",\n    filter: \"brightness(1)\"\n  }, {\n    webkitFilter: \"brightness(0.1)\",\n    filter: \"brightness(0.1)\",\n    duration: 5,\n    ease: \"none\"\n  }, \"north-r2-map-animation-end+=\".concat(DURATION_SCREEN_SPEED - 5));\n\n  //Section 5\n  motulAnimation.addLabel(\"north-r2-s5-start\");\n  motulAnimation.to([section5], {\n    x: \"-100%\",\n    duration: DURATION_SCREEN_SPEED,\n    ease: \"none\"\n  }, \"north-r2-s5-start\");\n  motulAnimation.fromTo(contentSection5, {\n    y: \"-150%\",\n    opacity: 0\n  }, {\n    y: \"0%\",\n    opacity: 1,\n    duration: DURATION_SCREEN_SPEED,\n    ease: \"none\"\n  }, \"north-r2-s5-start+=\".concat(DURATION_SCREEN_SPEED * 3));\n  motulAnimation.to(slide1Section5, {\n    x: \"-100%\",\n    duration: DURATION_SCREEN_SPEED,\n    ease: \"none\"\n  }, \"north-r2-s5-start+=\".concat(DURATION_SCREEN_SPEED * 2));\n  motulAnimation.to(slide2Section5, {\n    x: \"-100%\",\n    duration: DURATION_SCREEN_SPEED,\n    ease: \"none\"\n  }, \"north-r2-s5-start+=\".concat(DURATION_SCREEN_SPEED * 2));\n  motulAnimation.to(slide3Section5, {\n    x: \"-100%\",\n    duration: DURATION_SCREEN_SPEED,\n    ease: \"none\"\n  }, \"north-r2-s5-start+=\".concat(DURATION_SCREEN_SPEED * 3));\n\n  // Section 6\n  motulAnimation.addLabel(\"north-r2-s6-start\");\n  motulAnimation.to([section6], {\n    x: \"-100%\",\n    duration: DURATION_SCREEN_SPEED,\n    ease: \"none\"\n  }, \"north-r2-s6-start\");\n  motulAnimation.to(slide1Section6, {\n    x: \"-100%\",\n    duration: DURATION_SCREEN_SPEED,\n    ease: \"none\"\n  }, \"north-r2-s6-start+=\".concat(DURATION_SCREEN_SPEED * 1));\n  motulAnimation.fromTo(iconSlide1Section6, {\n    opacity: 0,\n    scale: 1.3\n  }, {\n    opacity: 1,\n    scale: 1,\n    duration: DURATION_SCREEN_SPEED / 2,\n    ease: \"none\"\n  }, \"north-r2-s6-start+=\".concat(DURATION_SCREEN_SPEED / 2));\n\n  // motulAnimation.fromTo(\n  //   slide2Section6,\n  //   {\n  //     x: \"0\",\n  //     scale: 0.9,\n  //     webkitFilter: \"brightness(0.2)\",\n  //     filter: \"brightness(0.2)\",\n  //   },\n  //   {\n  //     x: \"-100%\",\n  //     scale: 1.2,\n  //     webkitFilter: \"brightness(1)\",\n  //     filter: \"brightness(1)\",\n  //     duration: DURATION_SCREEN_SPEED,\n  //     ease: \"none\",\n  //   },\n  //   `north-r2-s6-start+=${DURATION_SCREEN_SPEED * 1}`\n  // );\n\n  motulAnimation.fromTo(slide1Section6, {\n    webkitFilter: \"brightness(1)\",\n    filter: \"brightness(1)\"\n  }, {\n    webkitFilter: \"brightness(0.2)\",\n    filter: \"brightness(0.2)\",\n    duration: DURATION_SCREEN_SPEED,\n    ease: \"none\"\n  }, \"north-r2-s6-start+=\".concat(DURATION_SCREEN_SPEED * 2));\n  motulAnimation.fromTo(contentSlide3Section6, {\n    opacity: \"0\",\n    scale: 0.3,\n    webkitFilter: \"blur(6px)\",\n    filter: \"blur(6px)\"\n  }, {\n    opacity: 1,\n    scale: 1,\n    webkitFilter: \"blur(0)\",\n    filter: \"blur(0)\",\n    duration: DURATION_SCREEN_SPEED,\n    ease: \"none\"\n  }, \"north-r2-s6-start+=\".concat(DURATION_SCREEN_SPEED * 1));\n  motulAnimation.to(slide3Section6, {\n    x: \"-100%\",\n    duration: DURATION_SCREEN_SPEED,\n    ease: \"none\"\n  }, \"north-r2-s6-start+=\".concat(DURATION_SCREEN_SPEED * 1));\n\n  // Section 7\n  motulAnimation.addLabel(\"north-r2-s7-start\");\n  motulAnimation.to([section7], {\n    x: \"-100%\",\n    duration: DURATION_SCREEN_SPEED,\n    ease: \"none\"\n  }, \"north-r2-s7-start\");\n  motulAnimation.fromTo(contentSlide1Section7, {\n    x: \"-100%\",\n    opacity: 0\n  }, {\n    x: \"0%\",\n    opacity: 1,\n    duration: DURATION_SCREEN_SPEED,\n    ease: \"none\"\n  }, \"north-r2-s7-start+=\".concat(DURATION_SCREEN_SPEED * 0));\n  motulAnimation.to(slide1Section7, {\n    x: \"-100%\",\n    duration: DURATION_SCREEN_SPEED,\n    ease: \"none\"\n  }, \"north-r2-s7-start+=\".concat(DURATION_SCREEN_SPEED * 2));\n  motulAnimation.to(slide2Section7, {\n    x: \"-100%\",\n    duration: DURATION_SCREEN_SPEED,\n    ease: \"none\"\n  }, \"north-r2-7-start-=\".concat(DURATION_SCREEN_SPEED));\n  motulAnimation.fromTo(imgsSlide2Section7, {\n    webkitFilter: \"brightness(0.2)\",\n    filter: \"brightness(0.2)\"\n  }, {\n    webkitFilter: \"brightness(1.8)\",\n    filter: \"brightness(1.8)\",\n    duration: DURATION_SCREEN_SPEED,\n    ease: \"none\"\n  }, \"north-r2-7-start-=\".concat(DURATION_SCREEN_SPEED));\n  motulAnimation.fromTo(imgsSlide2Section7, {\n    webkitFilter: \"brightness(1.8)\",\n    filter: \"brightness(1.8)\"\n  }, {\n    webkitFilter: \"brightness(0.2)\",\n    filter: \"brightness(0.2)\",\n    duration: DURATION_SCREEN_SPEED,\n    ease: \"none\"\n  }, \"north-r2-7-start+=\".concat(0));\n  motulAnimation.to(slide3Section7, {\n    x: \"-100%\",\n    duration: DURATION_SCREEN_SPEED,\n    ease: \"none\"\n  }, \"north-r2-s7-start+=\".concat(DURATION_SCREEN_SPEED * 3));\n  motulAnimation.addLabel(\"north-r2-animation-end\");\n  // Round 3 Hide Animation\n\n  motulAnimation.to(scrollContainer, {\n    x: -(0,_utils_js__WEBPACK_IMPORTED_MODULE_0__.DistanceCalc)(roundThreeSection),\n    duration: DURATION_SCREEN_SPEED,\n    ease: \"none\"\n  }, \"north-r2-animation-end\");\n  motulAnimation.addLabel(\"north-r2-hide\");\n}\n\n//# sourceURL=webpack://frontend-boilerplate/./app/static/js-m/modules/rounds/north/r2.js?");
  
  /***/ }),
  
  /***/ "./app/static/js-m/modules/rounds/north/r3.js":
  /*!****************************************************!*\
    !*** ./app/static/js-m/modules/rounds/north/r3.js ***!
    \****************************************************/
  /***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {
  
  "use strict";
  eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   northR3Ani: () => (/* binding */ northR3Ani)\n/* harmony export */ });\n/* harmony import */ var _utils_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../../utils.js */ \"./app/static/js-m/modules/utils.js\");\n\nfunction northR3Ani(DURATION_SCREEN_SPEED, scrollContainer) {\n  // START LUNG CU, THUONG PHUNG, MEO VAC STAGE 3 JOURNEY NORTH POLE SECTION\n  function lungCuThuongPhungMeoVacStage3JourneyNorthPoleSectionSetup() {\n    var $lungCuThuongPhungMeoVacStage3JourneyNorthPoleSection = $(\".lung-cu-thuong-phung-meo-vac-stage-3-journey-north-pole\");\n    var $roundTwoTitle = $lungCuThuongPhungMeoVacStage3JourneyNorthPoleSection.find(\".main-content .round-label\");\n    var $roundTwoSubTitle = $lungCuThuongPhungMeoVacStage3JourneyNorthPoleSection.find(\".main-content .round-name\");\n    var $roundTwoButton = $lungCuThuongPhungMeoVacStage3JourneyNorthPoleSection.find(\".main-content .img-holder img\");\n    var $roundTwoMainContent = $lungCuThuongPhungMeoVacStage3JourneyNorthPoleSection.find(\".main-content\");\n    var $roundTwoExtraContent = $lungCuThuongPhungMeoVacStage3JourneyNorthPoleSection.find(\".extra-content\");\n    var $roundTwoExtraMainLayout = $roundTwoExtraContent.find(\".main-layout\");\n    var $roundTwoMedia = $lungCuThuongPhungMeoVacStage3JourneyNorthPoleSection.find(\".round-media\");\n    motulAnimation.addLabel(\"lung-cu-thuong-phung-meo-vac-stage-3-journey-north-pole\");\n    motulAnimation.to([$roundTwoMainContent, $roundTwoExtraContent], {\n      x: \"-100%\",\n      duration: DURATION_SCREEN_SPEED,\n      ease: \"none\"\n    }, \"lung-cu-thuong-phung-meo-vac-stage-3-journey-north-pole\");\n    motulAnimation.to([$roundTwoTitle, $roundTwoSubTitle, $roundTwoButton], {\n      scale: 1.1,\n      duration: DURATION_SCREEN_SPEED\n    }, \"lung-cu-thuong-phung-meo-vac-stage-3-journey-north-pole-=\".concat(DURATION_SCREEN_SPEED / 2));\n    motulAnimation.to([$roundTwoExtraContent], {\n      x: \"-200%\",\n      duration: DURATION_SCREEN_SPEED,\n      ease: \"none\"\n    }, \"lung-cu-thuong-phung-meo-vac-stage-3-journey-north-pole+=\".concat(DURATION_SCREEN_SPEED));\n    motulAnimation.to([$roundTwoExtraMainLayout], {\n      y: 40,\n      autoAlpha: 0,\n      duration: DURATION_SCREEN_SPEED,\n      ease: \"none\"\n    }, \"lung-cu-thuong-phung-meo-vac-stage-3-journey-north-pole+=\".concat(DURATION_SCREEN_SPEED));\n    motulAnimation.to([$roundTwoMedia], {\n      x: \"-100%\",\n      duration: DURATION_SCREEN_SPEED,\n      ease: \"none\"\n    }, \"lung-cu-thuong-phung-meo-vac-stage-3-journey-north-pole+=\".concat(DURATION_SCREEN_SPEED));\n    motulAnimation.addLabel(\"close-lung-cu-thuong-phung-meo-vac-stage-3-journey-north-pole\");\n    motulAnimation.to(scrollContainer, {\n      x: -(0,_utils_js__WEBPACK_IMPORTED_MODULE_0__.DistanceCalc)($lungCuThuongPhungMeoVacStage3JourneyNorthPoleSection),\n      duration: DURATION_SCREEN_SPEED,\n      ease: \"none\"\n    }, \"close-lung-cu-thuong-phung-meo-vac-stage-3-journey-north-pole\");\n  }\n\n  // START MA LI PENG, NHO QUE STAGE 3 JOURNEY NORTH POLE SECTION\n  function maLiPengNhoQueStage3JourneyNorthPoleSectionSetup() {\n    var $maLiPengNhoQueStage3JourneyNorthPoleSection = $(\".ma-li-peng-nho-que-stage-3-journey-north-pole\");\n    var $logoHolder = $maLiPengNhoQueStage3JourneyNorthPoleSection.find(\".logo-holder\");\n    var $sectionTitle = $maLiPengNhoQueStage3JourneyNorthPoleSection.find(\".section-title\");\n    var $sectionSubTitle = $maLiPengNhoQueStage3JourneyNorthPoleSection.find(\".section-sub-title\");\n    motulAnimation.addLabel(\"ma-li-peng-nho-que-stage-3-journey-north-pole\");\n    motulAnimation.fromTo([$logoHolder, $sectionTitle, $sectionSubTitle], {\n      x: -30\n    }, {\n      x: 5,\n      duration: DURATION_SCREEN_SPEED,\n      ease: \"none\"\n    }, \"ma-li-peng-nho-que-stage-3-journey-north-pole-=\".concat(DURATION_SCREEN_SPEED));\n    motulAnimation.to(scrollContainer, {\n      x: -(0,_utils_js__WEBPACK_IMPORTED_MODULE_0__.DistanceCalc)($maLiPengNhoQueStage3JourneyNorthPoleSection),\n      duration: DURATION_SCREEN_SPEED,\n      ease: \"none\"\n    }, \"ma-li-peng-nho-que-stage-3-journey-north-pole\");\n  }\n\n  // START THUONG PHUNG STAGE 3 JOURNEY NORTH POLE SECTION\n  function thuongPhungStage3JourneyNorthPoleSectionSetup() {\n    var $thuongPhungStage3JourneyNorthPoleSection = $(\".thuong-phung-stage-3-journey-north-pole\");\n    var $logoHolder = $thuongPhungStage3JourneyNorthPoleSection.find(\".logo-holder\");\n    var $sectionTitle = $thuongPhungStage3JourneyNorthPoleSection.find(\".section-title\");\n    var $sectionSubTitle = $thuongPhungStage3JourneyNorthPoleSection.find(\".section-sub-title\");\n    motulAnimation.addLabel(\"thuong-phung-stage-3-journey-north-pole\");\n    motulAnimation.fromTo([$logoHolder, $sectionTitle, $sectionSubTitle], {\n      x: -30\n    }, {\n      x: 5,\n      duration: DURATION_SCREEN_SPEED,\n      ease: \"none\"\n    }, \"thuong-phung-stage-3-journey-north-pole-=\".concat(DURATION_SCREEN_SPEED));\n    motulAnimation.to(scrollContainer, {\n      x: -(0,_utils_js__WEBPACK_IMPORTED_MODULE_0__.DistanceCalc)($thuongPhungStage3JourneyNorthPoleSection),\n      duration: DURATION_SCREEN_SPEED,\n      ease: \"none\"\n    }, \"thuong-phung-stage-3-journey-north-pole\");\n  }\n\n  // START KNOWLEDGE CONNECTION STAGE 3 JOURNEY NORTH POLE SECTION\n  function knowledgeConnectionStage3JourneyNorthPoleSectionSetup() {\n    var $knowledgeConnectionStage3JourneyNorthPoleSection = $(\".knowledge-connection-stage-3-journey-north-pole\");\n    var $preMainContent = $knowledgeConnectionStage3JourneyNorthPoleSection.find(\".main-content\");\n    var $preExtraContent = $knowledgeConnectionStage3JourneyNorthPoleSection.find(\".extra-content\");\n    motulAnimation.addLabel(\"knowledge-connection-stage-3-journey-north-pole\");\n    motulAnimation.to([$preMainContent, $preExtraContent], {\n      x: \"-100%\",\n      duration: DURATION_SCREEN_SPEED,\n      ease: \"none\"\n    }, \"knowledge-connection-stage-3-journey-north-pole\");\n    motulAnimation.addLabel(\"close-knowledge-connection-stage-3-journey-north-pole\");\n    motulAnimation.to(scrollContainer, {\n      x: -(0,_utils_js__WEBPACK_IMPORTED_MODULE_0__.DistanceCalc)($knowledgeConnectionStage3JourneyNorthPoleSection),\n      duration: DURATION_SCREEN_SPEED,\n      ease: \"none\"\n    }, \"close-knowledge-connection-stage-3-journey-north-pole\");\n  }\n\n  // START COMPUTER ROOM STAGE 3 JOURNEY NORTH POLE SECTION\n  function computerRoomStage3JourneyNorthPoleSectionSetup() {\n    var $computerRoomStage3JourneyNorthPoleSection = $(\".computer-room-stage-3-journey-north-pole\");\n    var $roundTwoTitle = $computerRoomStage3JourneyNorthPoleSection.find(\".main-content .round-label\");\n    var $roundTwoSubTitle = $computerRoomStage3JourneyNorthPoleSection.find(\".main-content .round-name\");\n    var $roundTwoButton = $computerRoomStage3JourneyNorthPoleSection.find(\".main-content .img-holder img\");\n    var $roundTwoMainContent = $computerRoomStage3JourneyNorthPoleSection.find(\".main-content\");\n    var $roundTwoExtraContent = $computerRoomStage3JourneyNorthPoleSection.find(\".extra-content\");\n    var $roundTwoExtraMainLayout = $roundTwoExtraContent.find(\".main-layout\");\n    var $roundTwoMedia = $computerRoomStage3JourneyNorthPoleSection.find(\".round-media\");\n    motulAnimation.addLabel(\"computer-room-stage-3-journey-north-pole\");\n    motulAnimation.to([$roundTwoMainContent, $roundTwoExtraContent], {\n      x: \"-100%\",\n      duration: DURATION_SCREEN_SPEED,\n      ease: \"none\"\n    }, \"computer-room-stage-3-journey-north-pole\");\n    motulAnimation.to([$roundTwoTitle, $roundTwoSubTitle, $roundTwoButton], {\n      scale: 1.1,\n      duration: DURATION_SCREEN_SPEED\n    }, \"computer-room-stage-3-journey-north-pole-=\".concat(DURATION_SCREEN_SPEED / 2));\n    motulAnimation.to([$roundTwoExtraContent], {\n      x: \"-200%\",\n      duration: DURATION_SCREEN_SPEED,\n      ease: \"none\"\n    }, \"computer-room-stage-3-journey-north-pole+=\".concat(DURATION_SCREEN_SPEED));\n    motulAnimation.to([$roundTwoExtraMainLayout], {\n      y: 40,\n      autoAlpha: 0,\n      duration: DURATION_SCREEN_SPEED,\n      ease: \"none\"\n    }, \"computer-room-stage-3-journey-north-pole+=\".concat(DURATION_SCREEN_SPEED));\n    motulAnimation.to([$roundTwoMedia], {\n      x: \"-100%\",\n      duration: DURATION_SCREEN_SPEED,\n      ease: \"none\"\n    }, \"computer-room-stage-3-journey-north-pole+=\".concat(DURATION_SCREEN_SPEED));\n    motulAnimation.addLabel(\"close-computer-room-stage-3-journey-north-pole\");\n    motulAnimation.to(scrollContainer, {\n      x: -(0,_utils_js__WEBPACK_IMPORTED_MODULE_0__.DistanceCalc)($computerRoomStage3JourneyNorthPoleSection),\n      duration: DURATION_SCREEN_SPEED,\n      ease: \"none\"\n    }, \"close-computer-room-stage-3-journey-north-pole\");\n  }\n\n  // START PA VI STAGE 3 JOURNEY NORTH POLE SECTION\n  function paViStage3JourneyNorthPoleSectionSetup() {\n    var $paViStage3JourneyNorthPoleSection = $(\".pa-vi-stage-3-journey-north-pole\");\n    var $logoHolder = $paViStage3JourneyNorthPoleSection.find(\".logo-holder\");\n    var $sectionTitle = $paViStage3JourneyNorthPoleSection.find(\".section-title\");\n    var $sectionSubTitle = $paViStage3JourneyNorthPoleSection.find(\".section-sub-title\");\n    motulAnimation.addLabel(\"pa-vi-stage-3-journey-north-pole\");\n    motulAnimation.fromTo([$logoHolder, $sectionTitle, $sectionSubTitle], {\n      x: -30\n    }, {\n      x: 5,\n      duration: DURATION_SCREEN_SPEED,\n      ease: \"none\"\n    }, \"pa-vi-stage-3-journey-north-pole-=\".concat(DURATION_SCREEN_SPEED));\n    motulAnimation.to(scrollContainer, {\n      x: -(0,_utils_js__WEBPACK_IMPORTED_MODULE_0__.DistanceCalc)($paViStage3JourneyNorthPoleSection),\n      duration: DURATION_SCREEN_SPEED,\n      ease: \"none\"\n    }, \"pa-vi-stage-3-journey-north-pole\");\n  }\n\n  // START CAMPFIRE STAGE 3 JOURNEY NORTH POLE SECTION\n  function campfireStage3JourneyNorthPoleSectionSetup() {\n    var $campfireStage3JourneyNorthPoleSection = $(\".campfire-stage-3-journey-north-pole\");\n    var $logoHolder = $campfireStage3JourneyNorthPoleSection.find(\".logo-holder\");\n    var $sectionTitle = $campfireStage3JourneyNorthPoleSection.find(\".section-title\");\n    var $sectionSubTitle = $campfireStage3JourneyNorthPoleSection.find(\".section-sub-title\");\n    motulAnimation.addLabel(\"campfire-stage-3-journey-north-pole\");\n    motulAnimation.fromTo([$logoHolder, $sectionTitle, $sectionSubTitle], {\n      x: -30\n    }, {\n      x: 5,\n      duration: DURATION_SCREEN_SPEED,\n      ease: \"none\"\n    }, \"campfire-stage-3-journey-north-pole-=\".concat(DURATION_SCREEN_SPEED));\n    motulAnimation.to(scrollContainer, {\n      x: -(0,_utils_js__WEBPACK_IMPORTED_MODULE_0__.DistanceCalc)($campfireStage3JourneyNorthPoleSection),\n      duration: DURATION_SCREEN_SPEED,\n      ease: \"none\"\n    }, \"campfire-stage-3-journey-north-pole\");\n  }\n\n  // START SPACE TOGETHER STAGE 3 JOURNEY NORTH POLE SECTION\n  function spaceTogetherStage3JourneyNorthPoleSectionSetup() {\n    var $spaceTogetherStage3JourneyNorthPoleSection = $(\".space-together-stage-3-journey-north-pole\");\n    var $logoHolder = $spaceTogetherStage3JourneyNorthPoleSection.find(\".logo-holder\");\n    var $sectionTitle = $spaceTogetherStage3JourneyNorthPoleSection.find(\".section-title\");\n    var $sectionSubTitle = $spaceTogetherStage3JourneyNorthPoleSection.find(\".section-sub-title\");\n    motulAnimation.addLabel(\"space-together-stage-3-journey-north-pole\");\n    motulAnimation.fromTo([$logoHolder, $sectionTitle, $sectionSubTitle], {\n      x: -30\n    }, {\n      x: 5,\n      duration: DURATION_SCREEN_SPEED,\n      ease: \"none\"\n    }, \"space-together-stage-3-journey-north-pole-=\".concat(DURATION_SCREEN_SPEED));\n    motulAnimation.to(scrollContainer, {\n      x: -(0,_utils_js__WEBPACK_IMPORTED_MODULE_0__.DistanceCalc)($spaceTogetherStage3JourneyNorthPoleSection),\n      duration: DURATION_SCREEN_SPEED,\n      ease: \"none\"\n    }, \"space-together-stage-3-journey-north-pole\");\n  }\n\n  // START END THE JOURNEY NORTH 3 LUNG CU SECTION\n  function endTheJourneyNorth3LungCuSectionSetup() {\n    var $endTheJourneyNorth3LungCuSection = $(\".end-the-journey-north-3-lung-cu-section\");\n    var $endTheJourneyNorth3LungCuSectionEndingScene = $endTheJourneyNorth3LungCuSection.find(\".ending-scene\");\n    var $endTheJourneyNorth3LungCuSectionEndingGradientBg = $endTheJourneyNorth3LungCuSectionEndingScene.find(\".gradient-bg-overlay\");\n    var $endTheJourneyNorth3LungCuSectionEndingTxt1 = $endTheJourneyNorth3LungCuSectionEndingScene.find(\".title-group.group-1 > *\");\n    var $endTheJourneyNorth3LungCuSectionEndingTxt2 = $endTheJourneyNorth3LungCuSectionEndingScene.find(\".title-group.group-2 > *\");\n    motulAnimation.addLabel(\"end-the-journey-north-3-lung-cu-section\");\n    motulAnimation.to($endTheJourneyNorth3LungCuSectionEndingScene, {\n      x: 0,\n      duration: DURATION_SCREEN_SPEED,\n      ease: \"none\"\n    }, \"end-the-journey-north-3-lung-cu-section\");\n    motulAnimation.to($endTheJourneyNorth3LungCuSectionEndingGradientBg, {\n      x: \"-66.66%\",\n      duration: DURATION_SCREEN_SPEED * 2,\n      ease: \"none\"\n    }, \"end-the-journey-north-3-lung-cu-section\");\n    motulAnimation.fromTo($endTheJourneyNorth3LungCuSectionEndingTxt1, {\n      y: 30,\n      autoAlpha: 0\n    }, {\n      y: 0,\n      autoAlpha: 1,\n      duration: DURATION_SCREEN_SPEED / 2,\n      ease: \"none\"\n    }, \"end-the-journey-north-3-lung-cu-section\");\n\n    // motulAnimation.addLabel('show-text-end-the-journey-north-3-lung-cu-section');\n    motulAnimation.to($endTheJourneyNorth3LungCuSectionEndingTxt1, {\n      y: -30,\n      autoAlpha: 0,\n      duration: DURATION_SCREEN_SPEED / 4,\n      ease: \"none\"\n    }, \"end-the-journey-north-3-lung-cu-section+=\".concat(DURATION_SCREEN_SPEED));\n    motulAnimation.fromTo($endTheJourneyNorth3LungCuSectionEndingTxt2, {\n      y: 30,\n      autoAlpha: 0\n    }, {\n      y: 0,\n      autoAlpha: 1,\n      duration: DURATION_SCREEN_SPEED / 2,\n      ease: \"none\"\n    }, \"end-the-journey-north-3-lung-cu-section+=\".concat(DURATION_SCREEN_SPEED));\n    motulAnimation.addLabel(\"close-end-the-journey-north-3-lung-cu-section\");\n    motulAnimation.to(scrollContainer, {\n      x: -(0,_utils_js__WEBPACK_IMPORTED_MODULE_0__.DistanceCalc)($endTheJourneyNorth3LungCuSection),\n      duration: DURATION_SCREEN_SPEED,\n      ease: \"none\"\n    }, \"close-end-the-journey-north-3-lung-cu-section\");\n  }\n\n  // START THE JOURNEY SOUTH SECTION\n  function startJourneySouthSectionSetup() {\n    var $startJourneySouthSection = $(\".start-journey-south-section\");\n    motulAnimation.addLabel(\"start-journey-south-section\");\n    motulAnimation.to($startJourneySouthSection, {\n      x: 0,\n      duration: DURATION_SCREEN_SPEED,\n      ease: \"none\"\n    }, \"start-journey-south-section\");\n  }\n  function initial() {\n    // Animation for Lung Cu, Thuong Phung, Meo Vac stage 3 journey north pole section\n    lungCuThuongPhungMeoVacStage3JourneyNorthPoleSectionSetup();\n\n    // Animation for Ma Li Peng, Nho Que stage 3 journey north pole section\n    maLiPengNhoQueStage3JourneyNorthPoleSectionSetup();\n\n    // Animation for Thuong Phung stage 3 journey north pole section\n    thuongPhungStage3JourneyNorthPoleSectionSetup();\n\n    // Animation for knowledge connection stage 3 journey north pole section\n    knowledgeConnectionStage3JourneyNorthPoleSectionSetup();\n\n    // Animation for computer room stage 3 journey north pole section\n    computerRoomStage3JourneyNorthPoleSectionSetup();\n\n    // Animation for Pa Vi stage 3 journey north pole section\n    paViStage3JourneyNorthPoleSectionSetup();\n\n    // Animation for campfire stage 3 journey north pole section\n    campfireStage3JourneyNorthPoleSectionSetup();\n\n    // Animation for space together stage 3 journey north pole section\n    spaceTogetherStage3JourneyNorthPoleSectionSetup();\n\n    // Animation for end the journey north 3 Lung Cu section\n    endTheJourneyNorth3LungCuSectionSetup();\n\n    // Animation for start journey South section\n    startJourneySouthSectionSetup();\n  }\n  initial();\n}\n\n//# sourceURL=webpack://frontend-boilerplate/./app/static/js-m/modules/rounds/north/r3.js?");
  
  /***/ }),
  
  /***/ "./app/static/js-m/modules/utils.js":
  /*!******************************************!*\
    !*** ./app/static/js-m/modules/utils.js ***!
    \******************************************/
  /***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {
  
  "use strict";
  eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   ANIMATION_DURATION: () => (/* binding */ ANIMATION_DURATION),\n/* harmony export */   ANIMTION_STAGGER_TIME: () => (/* binding */ ANIMTION_STAGGER_TIME),\n/* harmony export */   CreateInfiniteAnimation: () => (/* binding */ _CreateInfiniteAnimation),\n/* harmony export */   DistanceCalc: () => (/* binding */ DistanceCalc),\n/* harmony export */   END_STATE_ANIMATION: () => (/* binding */ END_STATE_ANIMATION),\n/* harmony export */   START_STATE_ANIMATION: () => (/* binding */ START_STATE_ANIMATION),\n/* harmony export */   SectionIntroAnimationInit: () => (/* binding */ SectionIntroAnimationInit),\n/* harmony export */   pageListener: () => (/* binding */ pageListener)\n/* harmony export */ });\n/* harmony import */ var gsap__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! gsap */ \"./node_modules/gsap/index.js\");\n/* harmony import */ var gsap__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! gsap */ \"./node_modules/gsap/gsap-core.js\");\nfunction _typeof(o) { \"@babel/helpers - typeof\"; return _typeof = \"function\" == typeof Symbol && \"symbol\" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && \"function\" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? \"symbol\" : typeof o; }, _typeof(o); }\nfunction ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }\nfunction _objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys(Object(t), !0).forEach(function (r) { _defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }\nfunction _defineProperty(e, r, t) { return (r = _toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }\nfunction _toPropertyKey(t) { var i = _toPrimitive(t, \"string\"); return \"symbol\" == _typeof(i) ? i : i + \"\"; }\nfunction _toPrimitive(t, r) { if (\"object\" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || \"default\"); if (\"object\" != _typeof(i)) return i; throw new TypeError(\"@@toPrimitive must return a primitive value.\"); } return (\"string\" === r ? String : Number)(t); }\n\n\n// Constant for all stagger animation\nvar START_STATE_ANIMATION = {\n  y: 30,\n  autoAlpha: 0\n};\nvar END_STATE_ANIMATION = {\n  y: 0,\n  autoAlpha: 1\n};\nvar ANIMATION_DURATION = 0.6;\nvar ANIMTION_STAGGER_TIME = 0.15;\n\n// Custom Event Listener\nfunction pageListener() {\n  this.events = {};\n}\npageListener.prototype.on = function (eventType, listener) {\n  // If the eventType Property not exist yet, create an empty aray of that property\n  this.events[eventType] = this.events[eventType] || [];\n  this.events[eventType].push(listener);\n};\npageListener.prototype.emit = function (eventType, param) {\n  if (this.events[eventType] && this.events[eventType].length > 0) {\n    // Loop through the events[eventType] array of function and invoke each of them\n    this.events[eventType].forEach(function (item) {\n      item(param);\n    });\n  }\n};\n\n// Uniform animation of all intro block\nvar SectionIntroAnimationInit = function SectionIntroAnimationInit($introBlock) {\n  if ($introBlock.length > 0) {\n    // Variables\n    var $label = $introBlock.find(\".section-label\");\n    var $title = $introBlock.find(\".section-title\");\n    var $decor = $introBlock.find(\".section-intro-decor\");\n    var $descContent = $introBlock.find(\".desc-content\");\n    var $desc = $descContent.find(\".desc-link, .section-desc\");\n    var $cta = $descContent.find(\".cta-holder\");\n\n    // Condition Check\n    var arrayToProcess = [];\n    if ($label.length > 0) {\n      arrayToProcess.push($label);\n    }\n    if ($title.length > 0) {\n      arrayToProcess.push($title);\n    }\n    if ($desc.length > 0) {\n      arrayToProcess.push($desc);\n    }\n    if ($cta.length > 0) {\n      arrayToProcess.push($cta);\n    }\n\n    // if ($decor.length > 0) {\n    //   arrayToProcess.push($decor);\n    // }\n\n    // Init State\n\n    gsap__WEBPACK_IMPORTED_MODULE_0__.TweenMax.set(arrayToProcess, {\n      autoAlpha: 0\n    });\n\n    // Timeline Build\n    var AnimationTimeline = new gsap__WEBPACK_IMPORTED_MODULE_1__.TimelineMax({\n      paused: true\n    });\n\n    // Start\n    AnimationTimeline.add(\"anim-start\");\n    AnimationTimeline.staggerFromTo(arrayToProcess, ANIMATION_DURATION, _objectSpread({}, START_STATE_ANIMATION), _objectSpread({}, END_STATE_ANIMATION), ANIMTION_STAGGER_TIME, \"anim-start+=0.35\");\n    AnimationTimeline.add(\"intro-end\");\n    AnimationTimeline.fromTo($decor, ANIMATION_DURATION * 1.8, _objectSpread(_objectSpread({}, START_STATE_ANIMATION), {}, {\n      x: 30,\n      scale: 0.9\n    }), _objectSpread(_objectSpread({}, END_STATE_ANIMATION), {}, {\n      x: 0,\n      scale: 1,\n      ease: gsap__WEBPACK_IMPORTED_MODULE_1__.Power1.easeInOut\n    }), \"intro-end-=0.5\");\n    return AnimationTimeline;\n  }\n};\nvar _CreateInfiniteAnimation = function CreateInfiniteAnimation($el) {\n  var data = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {\n    TIME_MIN: 18,\n    TIME_MAX: 22,\n    X_RANGE: 240,\n    Y_RANGE: 240,\n    MIN_RANGE: 150\n    // DEG_RANGE: 90,\n  };\n  // Duration Random\n  var dur = data.TIME_MIN + Math.random() * (data.TIME_MAX - data.TIME_MIN);\n\n  // Animation Random X & Y on the element using above duration\n  gsap__WEBPACK_IMPORTED_MODULE_0__.TweenMax.to($el, dur, {\n    x: (data.MIN_RANGE + (data.X_RANGE - data.MIN_RANGE) * Math.random()) * (Math.random() < 0.5 ? -1 : 1),\n    y: (data.MIN_RANGE + (data.Y_RANGE - data.MIN_RANGE) * Math.random()) * (Math.random() < 0.5 ? -1 : 1),\n    // rotation: data.DEG_RANGE * Math.random() * (Math.random() < 0.5 ? -1 : 1),\n    ease: gsap__WEBPACK_IMPORTED_MODULE_1__.Linear.easeNone,\n    onComplete: function onComplete() {\n      return _CreateInfiniteAnimation($el);\n    }\n  });\n};\nfunction DistanceCalc() {\n  var $section = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : null;\n  if ($section !== null) {\n    var previousSibling = $section.prevAll(\".section\");\n    var distance = $section.outerWidth();\n    if (previousSibling.length > 0) {\n      previousSibling.each(function (i, $sibEl) {\n        distance += $($sibEl).outerWidth();\n      });\n    }\n    return distance;\n  }\n  return 0;\n}\n\n\n//# sourceURL=webpack://frontend-boilerplate/./app/static/js-m/modules/utils.js?");
  
  /***/ }),
  
  /***/ "./app/static/js-m/scripts.js":
  /*!************************************!*\
    !*** ./app/static/js-m/scripts.js ***!
    \************************************/
  /***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {
  
  "use strict";
  eval("__webpack_require__.r(__webpack_exports__);\n/* harmony import */ var _modules_Main__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./modules/Main */ \"./app/static/js-m/modules/Main.js\");\n/* harmony import */ var _modules_lazy_load__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./modules/lazy-load */ \"./app/static/js-m/modules/lazy-load.js\");\n\n\n\n// Run Lazy Load Image Function First\n(0,_modules_lazy_load__WEBPACK_IMPORTED_MODULE_1__[\"default\"])();\n$(document).ready(function () {\n  var main = new _modules_Main__WEBPACK_IMPORTED_MODULE_0__[\"default\"]();\n});\nvar windowHeight = function windowHeight() {\n  var doc = document.documentElement;\n  doc.style.setProperty(\"--window-height\", \"\".concat(window.innerHeight, \"px\"));\n};\nwindow.addEventListener(\"resize\", windowHeight);\nwindowHeight();\n\n//# sourceURL=webpack://frontend-boilerplate/./app/static/js-m/scripts.js?");
  
  /***/ }),
  
  /***/ "./node_modules/debug/src/browser.js":
  /*!*******************************************!*\
    !*** ./node_modules/debug/src/browser.js ***!
    \*******************************************/
  /***/ ((module, exports, __webpack_require__) => {
  
  eval("/* eslint-env browser */\n\n/**\n * This is the web browser implementation of `debug()`.\n */\n\nexports.formatArgs = formatArgs;\nexports.save = save;\nexports.load = load;\nexports.useColors = useColors;\nexports.storage = localstorage();\nexports.destroy = (() => {\n\tlet warned = false;\n\n\treturn () => {\n\t\tif (!warned) {\n\t\t\twarned = true;\n\t\t\tconsole.warn('Instance method `debug.destroy()` is deprecated and no longer does anything. It will be removed in the next major version of `debug`.');\n\t\t}\n\t};\n})();\n\n/**\n * Colors.\n */\n\nexports.colors = [\n\t'#0000CC',\n\t'#0000FF',\n\t'#0033CC',\n\t'#0033FF',\n\t'#0066CC',\n\t'#0066FF',\n\t'#0099CC',\n\t'#0099FF',\n\t'#00CC00',\n\t'#00CC33',\n\t'#00CC66',\n\t'#00CC99',\n\t'#00CCCC',\n\t'#00CCFF',\n\t'#3300CC',\n\t'#3300FF',\n\t'#3333CC',\n\t'#3333FF',\n\t'#3366CC',\n\t'#3366FF',\n\t'#3399CC',\n\t'#3399FF',\n\t'#33CC00',\n\t'#33CC33',\n\t'#33CC66',\n\t'#33CC99',\n\t'#33CCCC',\n\t'#33CCFF',\n\t'#6600CC',\n\t'#6600FF',\n\t'#6633CC',\n\t'#6633FF',\n\t'#66CC00',\n\t'#66CC33',\n\t'#9900CC',\n\t'#9900FF',\n\t'#9933CC',\n\t'#9933FF',\n\t'#99CC00',\n\t'#99CC33',\n\t'#CC0000',\n\t'#CC0033',\n\t'#CC0066',\n\t'#CC0099',\n\t'#CC00CC',\n\t'#CC00FF',\n\t'#CC3300',\n\t'#CC3333',\n\t'#CC3366',\n\t'#CC3399',\n\t'#CC33CC',\n\t'#CC33FF',\n\t'#CC6600',\n\t'#CC6633',\n\t'#CC9900',\n\t'#CC9933',\n\t'#CCCC00',\n\t'#CCCC33',\n\t'#FF0000',\n\t'#FF0033',\n\t'#FF0066',\n\t'#FF0099',\n\t'#FF00CC',\n\t'#FF00FF',\n\t'#FF3300',\n\t'#FF3333',\n\t'#FF3366',\n\t'#FF3399',\n\t'#FF33CC',\n\t'#FF33FF',\n\t'#FF6600',\n\t'#FF6633',\n\t'#FF9900',\n\t'#FF9933',\n\t'#FFCC00',\n\t'#FFCC33'\n];\n\n/**\n * Currently only WebKit-based Web Inspectors, Firefox >= v31,\n * and the Firebug extension (any Firefox version) are known\n * to support \"%c\" CSS customizations.\n *\n * TODO: add a `localStorage` variable to explicitly enable/disable colors\n */\n\n// eslint-disable-next-line complexity\nfunction useColors() {\n\t// NB: In an Electron preload script, document will be defined but not fully\n\t// initialized. Since we know we're in Chrome, we'll just detect this case\n\t// explicitly\n\tif (typeof window !== 'undefined' && window.process && (window.process.type === 'renderer' || window.process.__nwjs)) {\n\t\treturn true;\n\t}\n\n\t// Internet Explorer and Edge do not support colors.\n\tif (typeof navigator !== 'undefined' && navigator.userAgent && navigator.userAgent.toLowerCase().match(/(edge|trident)\\/(\\d+)/)) {\n\t\treturn false;\n\t}\n\n\tlet m;\n\n\t// Is webkit? http://stackoverflow.com/a/16459606/376773\n\t// document is undefined in react-native: https://github.com/facebook/react-native/pull/1632\n\treturn (typeof document !== 'undefined' && document.documentElement && document.documentElement.style && document.documentElement.style.WebkitAppearance) ||\n\t\t// Is firebug? http://stackoverflow.com/a/398120/376773\n\t\t(typeof window !== 'undefined' && window.console && (window.console.firebug || (window.console.exception && window.console.table))) ||\n\t\t// Is firefox >= v31?\n\t\t// https://developer.mozilla.org/en-US/docs/Tools/Web_Console#Styling_messages\n\t\t(typeof navigator !== 'undefined' && navigator.userAgent && (m = navigator.userAgent.toLowerCase().match(/firefox\\/(\\d+)/)) && parseInt(m[1], 10) >= 31) ||\n\t\t// Double check webkit in userAgent just in case we are in a worker\n\t\t(typeof navigator !== 'undefined' && navigator.userAgent && navigator.userAgent.toLowerCase().match(/applewebkit\\/(\\d+)/));\n}\n\n/**\n * Colorize log arguments if enabled.\n *\n * @api public\n */\n\nfunction formatArgs(args) {\n\targs[0] = (this.useColors ? '%c' : '') +\n\t\tthis.namespace +\n\t\t(this.useColors ? ' %c' : ' ') +\n\t\targs[0] +\n\t\t(this.useColors ? '%c ' : ' ') +\n\t\t'+' + module.exports.humanize(this.diff);\n\n\tif (!this.useColors) {\n\t\treturn;\n\t}\n\n\tconst c = 'color: ' + this.color;\n\targs.splice(1, 0, c, 'color: inherit');\n\n\t// The final \"%c\" is somewhat tricky, because there could be other\n\t// arguments passed either before or after the %c, so we need to\n\t// figure out the correct index to insert the CSS into\n\tlet index = 0;\n\tlet lastC = 0;\n\targs[0].replace(/%[a-zA-Z%]/g, match => {\n\t\tif (match === '%%') {\n\t\t\treturn;\n\t\t}\n\t\tindex++;\n\t\tif (match === '%c') {\n\t\t\t// We only are interested in the *last* %c\n\t\t\t// (the user may have provided their own)\n\t\t\tlastC = index;\n\t\t}\n\t});\n\n\targs.splice(lastC, 0, c);\n}\n\n/**\n * Invokes `console.debug()` when available.\n * No-op when `console.debug` is not a \"function\".\n * If `console.debug` is not available, falls back\n * to `console.log`.\n *\n * @api public\n */\nexports.log = console.debug || console.log || (() => {});\n\n/**\n * Save `namespaces`.\n *\n * @param {String} namespaces\n * @api private\n */\nfunction save(namespaces) {\n\ttry {\n\t\tif (namespaces) {\n\t\t\texports.storage.setItem('debug', namespaces);\n\t\t} else {\n\t\t\texports.storage.removeItem('debug');\n\t\t}\n\t} catch (error) {\n\t\t// Swallow\n\t\t// XXX (@Qix-) should we be logging these?\n\t}\n}\n\n/**\n * Load `namespaces`.\n *\n * @return {String} returns the previously persisted debug modes\n * @api private\n */\nfunction load() {\n\tlet r;\n\ttry {\n\t\tr = exports.storage.getItem('debug');\n\t} catch (error) {\n\t\t// Swallow\n\t\t// XXX (@Qix-) should we be logging these?\n\t}\n\n\t// If debug isn't set in LS, and we're in Electron, try to load $DEBUG\n\tif (!r && typeof process !== 'undefined' && 'env' in process) {\n\t\tr = process.env.DEBUG;\n\t}\n\n\treturn r;\n}\n\n/**\n * Localstorage attempts to return the localstorage.\n *\n * This is necessary because safari throws\n * when a user disables cookies/localstorage\n * and you attempt to access it.\n *\n * @return {LocalStorage}\n * @api private\n */\n\nfunction localstorage() {\n\ttry {\n\t\t// TVMLKit (Apple TV JS Runtime) does not have a window object, just localStorage in the global context\n\t\t// The Browser also has localStorage in the global context.\n\t\treturn localStorage;\n\t} catch (error) {\n\t\t// Swallow\n\t\t// XXX (@Qix-) should we be logging these?\n\t}\n}\n\nmodule.exports = __webpack_require__(/*! ./common */ \"./node_modules/debug/src/common.js\")(exports);\n\nconst {formatters} = module.exports;\n\n/**\n * Map %j to `JSON.stringify()`, since no Web Inspectors do that by default.\n */\n\nformatters.j = function (v) {\n\ttry {\n\t\treturn JSON.stringify(v);\n\t} catch (error) {\n\t\treturn '[UnexpectedJSONParseError]: ' + error.message;\n\t}\n};\n\n\n//# sourceURL=webpack://frontend-boilerplate/./node_modules/debug/src/browser.js?");
  
  /***/ }),
  
  /***/ "./node_modules/debug/src/common.js":
  /*!******************************************!*\
    !*** ./node_modules/debug/src/common.js ***!
    \******************************************/
  /***/ ((module, __unused_webpack_exports, __webpack_require__) => {
  
  eval("\n/**\n * This is the common logic for both the Node.js and web browser\n * implementations of `debug()`.\n */\n\nfunction setup(env) {\n\tcreateDebug.debug = createDebug;\n\tcreateDebug.default = createDebug;\n\tcreateDebug.coerce = coerce;\n\tcreateDebug.disable = disable;\n\tcreateDebug.enable = enable;\n\tcreateDebug.enabled = enabled;\n\tcreateDebug.humanize = __webpack_require__(/*! ms */ \"./node_modules/ms/index.js\");\n\tcreateDebug.destroy = destroy;\n\n\tObject.keys(env).forEach(key => {\n\t\tcreateDebug[key] = env[key];\n\t});\n\n\t/**\n\t* The currently active debug mode names, and names to skip.\n\t*/\n\n\tcreateDebug.names = [];\n\tcreateDebug.skips = [];\n\n\t/**\n\t* Map of special \"%n\" handling functions, for the debug \"format\" argument.\n\t*\n\t* Valid key names are a single, lower or upper-case letter, i.e. \"n\" and \"N\".\n\t*/\n\tcreateDebug.formatters = {};\n\n\t/**\n\t* Selects a color for a debug namespace\n\t* @param {String} namespace The namespace string for the debug instance to be colored\n\t* @return {Number|String} An ANSI color code for the given namespace\n\t* @api private\n\t*/\n\tfunction selectColor(namespace) {\n\t\tlet hash = 0;\n\n\t\tfor (let i = 0; i < namespace.length; i++) {\n\t\t\thash = ((hash << 5) - hash) + namespace.charCodeAt(i);\n\t\t\thash |= 0; // Convert to 32bit integer\n\t\t}\n\n\t\treturn createDebug.colors[Math.abs(hash) % createDebug.colors.length];\n\t}\n\tcreateDebug.selectColor = selectColor;\n\n\t/**\n\t* Create a debugger with the given `namespace`.\n\t*\n\t* @param {String} namespace\n\t* @return {Function}\n\t* @api public\n\t*/\n\tfunction createDebug(namespace) {\n\t\tlet prevTime;\n\t\tlet enableOverride = null;\n\t\tlet namespacesCache;\n\t\tlet enabledCache;\n\n\t\tfunction debug(...args) {\n\t\t\t// Disabled?\n\t\t\tif (!debug.enabled) {\n\t\t\t\treturn;\n\t\t\t}\n\n\t\t\tconst self = debug;\n\n\t\t\t// Set `diff` timestamp\n\t\t\tconst curr = Number(new Date());\n\t\t\tconst ms = curr - (prevTime || curr);\n\t\t\tself.diff = ms;\n\t\t\tself.prev = prevTime;\n\t\t\tself.curr = curr;\n\t\t\tprevTime = curr;\n\n\t\t\targs[0] = createDebug.coerce(args[0]);\n\n\t\t\tif (typeof args[0] !== 'string') {\n\t\t\t\t// Anything else let's inspect with %O\n\t\t\t\targs.unshift('%O');\n\t\t\t}\n\n\t\t\t// Apply any `formatters` transformations\n\t\t\tlet index = 0;\n\t\t\targs[0] = args[0].replace(/%([a-zA-Z%])/g, (match, format) => {\n\t\t\t\t// If we encounter an escaped % then don't increase the array index\n\t\t\t\tif (match === '%%') {\n\t\t\t\t\treturn '%';\n\t\t\t\t}\n\t\t\t\tindex++;\n\t\t\t\tconst formatter = createDebug.formatters[format];\n\t\t\t\tif (typeof formatter === 'function') {\n\t\t\t\t\tconst val = args[index];\n\t\t\t\t\tmatch = formatter.call(self, val);\n\n\t\t\t\t\t// Now we need to remove `args[index]` since it's inlined in the `format`\n\t\t\t\t\targs.splice(index, 1);\n\t\t\t\t\tindex--;\n\t\t\t\t}\n\t\t\t\treturn match;\n\t\t\t});\n\n\t\t\t// Apply env-specific formatting (colors, etc.)\n\t\t\tcreateDebug.formatArgs.call(self, args);\n\n\t\t\tconst logFn = self.log || createDebug.log;\n\t\t\tlogFn.apply(self, args);\n\t\t}\n\n\t\tdebug.namespace = namespace;\n\t\tdebug.useColors = createDebug.useColors();\n\t\tdebug.color = createDebug.selectColor(namespace);\n\t\tdebug.extend = extend;\n\t\tdebug.destroy = createDebug.destroy; // XXX Temporary. Will be removed in the next major release.\n\n\t\tObject.defineProperty(debug, 'enabled', {\n\t\t\tenumerable: true,\n\t\t\tconfigurable: false,\n\t\t\tget: () => {\n\t\t\t\tif (enableOverride !== null) {\n\t\t\t\t\treturn enableOverride;\n\t\t\t\t}\n\t\t\t\tif (namespacesCache !== createDebug.namespaces) {\n\t\t\t\t\tnamespacesCache = createDebug.namespaces;\n\t\t\t\t\tenabledCache = createDebug.enabled(namespace);\n\t\t\t\t}\n\n\t\t\t\treturn enabledCache;\n\t\t\t},\n\t\t\tset: v => {\n\t\t\t\tenableOverride = v;\n\t\t\t}\n\t\t});\n\n\t\t// Env-specific initialization logic for debug instances\n\t\tif (typeof createDebug.init === 'function') {\n\t\t\tcreateDebug.init(debug);\n\t\t}\n\n\t\treturn debug;\n\t}\n\n\tfunction extend(namespace, delimiter) {\n\t\tconst newDebug = createDebug(this.namespace + (typeof delimiter === 'undefined' ? ':' : delimiter) + namespace);\n\t\tnewDebug.log = this.log;\n\t\treturn newDebug;\n\t}\n\n\t/**\n\t* Enables a debug mode by namespaces. This can include modes\n\t* separated by a colon and wildcards.\n\t*\n\t* @param {String} namespaces\n\t* @api public\n\t*/\n\tfunction enable(namespaces) {\n\t\tcreateDebug.save(namespaces);\n\t\tcreateDebug.namespaces = namespaces;\n\n\t\tcreateDebug.names = [];\n\t\tcreateDebug.skips = [];\n\n\t\tlet i;\n\t\tconst split = (typeof namespaces === 'string' ? namespaces : '').split(/[\\s,]+/);\n\t\tconst len = split.length;\n\n\t\tfor (i = 0; i < len; i++) {\n\t\t\tif (!split[i]) {\n\t\t\t\t// ignore empty strings\n\t\t\t\tcontinue;\n\t\t\t}\n\n\t\t\tnamespaces = split[i].replace(/\\*/g, '.*?');\n\n\t\t\tif (namespaces[0] === '-') {\n\t\t\t\tcreateDebug.skips.push(new RegExp('^' + namespaces.slice(1) + '$'));\n\t\t\t} else {\n\t\t\t\tcreateDebug.names.push(new RegExp('^' + namespaces + '$'));\n\t\t\t}\n\t\t}\n\t}\n\n\t/**\n\t* Disable debug output.\n\t*\n\t* @return {String} namespaces\n\t* @api public\n\t*/\n\tfunction disable() {\n\t\tconst namespaces = [\n\t\t\t...createDebug.names.map(toNamespace),\n\t\t\t...createDebug.skips.map(toNamespace).map(namespace => '-' + namespace)\n\t\t].join(',');\n\t\tcreateDebug.enable('');\n\t\treturn namespaces;\n\t}\n\n\t/**\n\t* Returns true if the given mode name is enabled, false otherwise.\n\t*\n\t* @param {String} name\n\t* @return {Boolean}\n\t* @api public\n\t*/\n\tfunction enabled(name) {\n\t\tif (name[name.length - 1] === '*') {\n\t\t\treturn true;\n\t\t}\n\n\t\tlet i;\n\t\tlet len;\n\n\t\tfor (i = 0, len = createDebug.skips.length; i < len; i++) {\n\t\t\tif (createDebug.skips[i].test(name)) {\n\t\t\t\treturn false;\n\t\t\t}\n\t\t}\n\n\t\tfor (i = 0, len = createDebug.names.length; i < len; i++) {\n\t\t\tif (createDebug.names[i].test(name)) {\n\t\t\t\treturn true;\n\t\t\t}\n\t\t}\n\n\t\treturn false;\n\t}\n\n\t/**\n\t* Convert regexp to namespace\n\t*\n\t* @param {RegExp} regxep\n\t* @return {String} namespace\n\t* @api private\n\t*/\n\tfunction toNamespace(regexp) {\n\t\treturn regexp.toString()\n\t\t\t.substring(2, regexp.toString().length - 2)\n\t\t\t.replace(/\\.\\*\\?$/, '*');\n\t}\n\n\t/**\n\t* Coerce `val`.\n\t*\n\t* @param {Mixed} val\n\t* @return {Mixed}\n\t* @api private\n\t*/\n\tfunction coerce(val) {\n\t\tif (val instanceof Error) {\n\t\t\treturn val.stack || val.message;\n\t\t}\n\t\treturn val;\n\t}\n\n\t/**\n\t* XXX DO NOT USE. This is a temporary stub function.\n\t* XXX It WILL be removed in the next major release.\n\t*/\n\tfunction destroy() {\n\t\tconsole.warn('Instance method `debug.destroy()` is deprecated and no longer does anything. It will be removed in the next major version of `debug`.');\n\t}\n\n\tcreateDebug.enable(createDebug.load());\n\n\treturn createDebug;\n}\n\nmodule.exports = setup;\n\n\n//# sourceURL=webpack://frontend-boilerplate/./node_modules/debug/src/common.js?");
  
  /***/ }),
  
  /***/ "./node_modules/gsap/CSSPlugin.js":
  /*!****************************************!*\
    !*** ./node_modules/gsap/CSSPlugin.js ***!
    \****************************************/
  /***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {
  
  "use strict";
  eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   CSSPlugin: () => (/* binding */ CSSPlugin),\n/* harmony export */   _createElement: () => (/* binding */ _createElement),\n/* harmony export */   _getBBox: () => (/* binding */ _getBBox),\n/* harmony export */   checkPrefix: () => (/* binding */ _checkPropPrefix),\n/* harmony export */   \"default\": () => (/* binding */ CSSPlugin)\n/* harmony export */ });\n/* harmony import */ var _gsap_core_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./gsap-core.js */ \"./node_modules/gsap/gsap-core.js\");\n/*!\n * CSSPlugin 3.12.5\n * https://gsap.com\n *\n * Copyright 2008-2024, GreenSock. All rights reserved.\n * Subject to the terms at https://gsap.com/standard-license or for\n * Club GSAP members, the agreement issued with that membership.\n * @author: Jack Doyle, jack@greensock.com\n*/\n\n/* eslint-disable */\n\n\nvar _win,\n    _doc,\n    _docElement,\n    _pluginInitted,\n    _tempDiv,\n    _tempDivStyler,\n    _recentSetterPlugin,\n    _reverting,\n    _windowExists = function _windowExists() {\n  return typeof window !== \"undefined\";\n},\n    _transformProps = {},\n    _RAD2DEG = 180 / Math.PI,\n    _DEG2RAD = Math.PI / 180,\n    _atan2 = Math.atan2,\n    _bigNum = 1e8,\n    _capsExp = /([A-Z])/g,\n    _horizontalExp = /(left|right|width|margin|padding|x)/i,\n    _complexExp = /[\\s,\\(]\\S/,\n    _propertyAliases = {\n  autoAlpha: \"opacity,visibility\",\n  scale: \"scaleX,scaleY\",\n  alpha: \"opacity\"\n},\n    _renderCSSProp = function _renderCSSProp(ratio, data) {\n  return data.set(data.t, data.p, Math.round((data.s + data.c * ratio) * 10000) / 10000 + data.u, data);\n},\n    _renderPropWithEnd = function _renderPropWithEnd(ratio, data) {\n  return data.set(data.t, data.p, ratio === 1 ? data.e : Math.round((data.s + data.c * ratio) * 10000) / 10000 + data.u, data);\n},\n    _renderCSSPropWithBeginning = function _renderCSSPropWithBeginning(ratio, data) {\n  return data.set(data.t, data.p, ratio ? Math.round((data.s + data.c * ratio) * 10000) / 10000 + data.u : data.b, data);\n},\n    //if units change, we need a way to render the original unit/value when the tween goes all the way back to the beginning (ratio:0)\n_renderRoundedCSSProp = function _renderRoundedCSSProp(ratio, data) {\n  var value = data.s + data.c * ratio;\n  data.set(data.t, data.p, ~~(value + (value < 0 ? -.5 : .5)) + data.u, data);\n},\n    _renderNonTweeningValue = function _renderNonTweeningValue(ratio, data) {\n  return data.set(data.t, data.p, ratio ? data.e : data.b, data);\n},\n    _renderNonTweeningValueOnlyAtEnd = function _renderNonTweeningValueOnlyAtEnd(ratio, data) {\n  return data.set(data.t, data.p, ratio !== 1 ? data.b : data.e, data);\n},\n    _setterCSSStyle = function _setterCSSStyle(target, property, value) {\n  return target.style[property] = value;\n},\n    _setterCSSProp = function _setterCSSProp(target, property, value) {\n  return target.style.setProperty(property, value);\n},\n    _setterTransform = function _setterTransform(target, property, value) {\n  return target._gsap[property] = value;\n},\n    _setterScale = function _setterScale(target, property, value) {\n  return target._gsap.scaleX = target._gsap.scaleY = value;\n},\n    _setterScaleWithRender = function _setterScaleWithRender(target, property, value, data, ratio) {\n  var cache = target._gsap;\n  cache.scaleX = cache.scaleY = value;\n  cache.renderTransform(ratio, cache);\n},\n    _setterTransformWithRender = function _setterTransformWithRender(target, property, value, data, ratio) {\n  var cache = target._gsap;\n  cache[property] = value;\n  cache.renderTransform(ratio, cache);\n},\n    _transformProp = \"transform\",\n    _transformOriginProp = _transformProp + \"Origin\",\n    _saveStyle = function _saveStyle(property, isNotCSS) {\n  var _this = this;\n\n  var target = this.target,\n      style = target.style,\n      cache = target._gsap;\n\n  if (property in _transformProps && style) {\n    this.tfm = this.tfm || {};\n\n    if (property !== \"transform\") {\n      property = _propertyAliases[property] || property;\n      ~property.indexOf(\",\") ? property.split(\",\").forEach(function (a) {\n        return _this.tfm[a] = _get(target, a);\n      }) : this.tfm[property] = cache.x ? cache[property] : _get(target, property); // note: scale would map to \"scaleX,scaleY\", thus we loop and apply them both.\n\n      property === _transformOriginProp && (this.tfm.zOrigin = cache.zOrigin);\n    } else {\n      return _propertyAliases.transform.split(\",\").forEach(function (p) {\n        return _saveStyle.call(_this, p, isNotCSS);\n      });\n    }\n\n    if (this.props.indexOf(_transformProp) >= 0) {\n      return;\n    }\n\n    if (cache.svg) {\n      this.svgo = target.getAttribute(\"data-svg-origin\");\n      this.props.push(_transformOriginProp, isNotCSS, \"\");\n    }\n\n    property = _transformProp;\n  }\n\n  (style || isNotCSS) && this.props.push(property, isNotCSS, style[property]);\n},\n    _removeIndependentTransforms = function _removeIndependentTransforms(style) {\n  if (style.translate) {\n    style.removeProperty(\"translate\");\n    style.removeProperty(\"scale\");\n    style.removeProperty(\"rotate\");\n  }\n},\n    _revertStyle = function _revertStyle() {\n  var props = this.props,\n      target = this.target,\n      style = target.style,\n      cache = target._gsap,\n      i,\n      p;\n\n  for (i = 0; i < props.length; i += 3) {\n    // stored like this: property, isNotCSS, value\n    props[i + 1] ? target[props[i]] = props[i + 2] : props[i + 2] ? style[props[i]] = props[i + 2] : style.removeProperty(props[i].substr(0, 2) === \"--\" ? props[i] : props[i].replace(_capsExp, \"-$1\").toLowerCase());\n  }\n\n  if (this.tfm) {\n    for (p in this.tfm) {\n      cache[p] = this.tfm[p];\n    }\n\n    if (cache.svg) {\n      cache.renderTransform();\n      target.setAttribute(\"data-svg-origin\", this.svgo || \"\");\n    }\n\n    i = _reverting();\n\n    if ((!i || !i.isStart) && !style[_transformProp]) {\n      _removeIndependentTransforms(style);\n\n      if (cache.zOrigin && style[_transformOriginProp]) {\n        style[_transformOriginProp] += \" \" + cache.zOrigin + \"px\"; // since we're uncaching, we must put the zOrigin back into the transformOrigin so that we can pull it out accurately when we parse again. Otherwise, we'd lose the z portion of the origin since we extract it to protect from Safari bugs.\n\n        cache.zOrigin = 0;\n        cache.renderTransform();\n      }\n\n      cache.uncache = 1; // if it's a startAt that's being reverted in the _initTween() of the core, we don't need to uncache transforms. This is purely a performance optimization.\n    }\n  }\n},\n    _getStyleSaver = function _getStyleSaver(target, properties) {\n  var saver = {\n    target: target,\n    props: [],\n    revert: _revertStyle,\n    save: _saveStyle\n  };\n  target._gsap || _gsap_core_js__WEBPACK_IMPORTED_MODULE_0__.gsap.core.getCache(target); // just make sure there's a _gsap cache defined because we read from it in _saveStyle() and it's more efficient to just check it here once.\n\n  properties && properties.split(\",\").forEach(function (p) {\n    return saver.save(p);\n  });\n  return saver;\n},\n    _supports3D,\n    _createElement = function _createElement(type, ns) {\n  var e = _doc.createElementNS ? _doc.createElementNS((ns || \"http://www.w3.org/1999/xhtml\").replace(/^https/, \"http\"), type) : _doc.createElement(type); //some servers swap in https for http in the namespace which can break things, making \"style\" inaccessible.\n\n  return e && e.style ? e : _doc.createElement(type); //some environments won't allow access to the element's style when created with a namespace in which case we default to the standard createElement() to work around the issue. Also note that when GSAP is embedded directly inside an SVG file, createElement() won't allow access to the style object in Firefox (see https://gsap.com/forums/topic/20215-problem-using-tweenmax-in-standalone-self-containing-svg-file-err-cannot-set-property-csstext-of-undefined/).\n},\n    _getComputedProperty = function _getComputedProperty(target, property, skipPrefixFallback) {\n  var cs = getComputedStyle(target);\n  return cs[property] || cs.getPropertyValue(property.replace(_capsExp, \"-$1\").toLowerCase()) || cs.getPropertyValue(property) || !skipPrefixFallback && _getComputedProperty(target, _checkPropPrefix(property) || property, 1) || \"\"; //css variables may not need caps swapped out for dashes and lowercase.\n},\n    _prefixes = \"O,Moz,ms,Ms,Webkit\".split(\",\"),\n    _checkPropPrefix = function _checkPropPrefix(property, element, preferPrefix) {\n  var e = element || _tempDiv,\n      s = e.style,\n      i = 5;\n\n  if (property in s && !preferPrefix) {\n    return property;\n  }\n\n  property = property.charAt(0).toUpperCase() + property.substr(1);\n\n  while (i-- && !(_prefixes[i] + property in s)) {}\n\n  return i < 0 ? null : (i === 3 ? \"ms\" : i >= 0 ? _prefixes[i] : \"\") + property;\n},\n    _initCore = function _initCore() {\n  if (_windowExists() && window.document) {\n    _win = window;\n    _doc = _win.document;\n    _docElement = _doc.documentElement;\n    _tempDiv = _createElement(\"div\") || {\n      style: {}\n    };\n    _tempDivStyler = _createElement(\"div\");\n    _transformProp = _checkPropPrefix(_transformProp);\n    _transformOriginProp = _transformProp + \"Origin\";\n    _tempDiv.style.cssText = \"border-width:0;line-height:0;position:absolute;padding:0\"; //make sure to override certain properties that may contaminate measurements, in case the user has overreaching style sheets.\n\n    _supports3D = !!_checkPropPrefix(\"perspective\");\n    _reverting = _gsap_core_js__WEBPACK_IMPORTED_MODULE_0__.gsap.core.reverting;\n    _pluginInitted = 1;\n  }\n},\n    _getBBoxHack = function _getBBoxHack(swapIfPossible) {\n  //works around issues in some browsers (like Firefox) that don't correctly report getBBox() on SVG elements inside a <defs> element and/or <mask>. We try creating an SVG, adding it to the documentElement and toss the element in there so that it's definitely part of the rendering tree, then grab the bbox and if it works, we actually swap out the original getBBox() method for our own that does these extra steps whenever getBBox is needed. This helps ensure that performance is optimal (only do all these extra steps when absolutely necessary...most elements don't need it).\n  var svg = _createElement(\"svg\", this.ownerSVGElement && this.ownerSVGElement.getAttribute(\"xmlns\") || \"http://www.w3.org/2000/svg\"),\n      oldParent = this.parentNode,\n      oldSibling = this.nextSibling,\n      oldCSS = this.style.cssText,\n      bbox;\n\n  _docElement.appendChild(svg);\n\n  svg.appendChild(this);\n  this.style.display = \"block\";\n\n  if (swapIfPossible) {\n    try {\n      bbox = this.getBBox();\n      this._gsapBBox = this.getBBox; //store the original\n\n      this.getBBox = _getBBoxHack;\n    } catch (e) {}\n  } else if (this._gsapBBox) {\n    bbox = this._gsapBBox();\n  }\n\n  if (oldParent) {\n    if (oldSibling) {\n      oldParent.insertBefore(this, oldSibling);\n    } else {\n      oldParent.appendChild(this);\n    }\n  }\n\n  _docElement.removeChild(svg);\n\n  this.style.cssText = oldCSS;\n  return bbox;\n},\n    _getAttributeFallbacks = function _getAttributeFallbacks(target, attributesArray) {\n  var i = attributesArray.length;\n\n  while (i--) {\n    if (target.hasAttribute(attributesArray[i])) {\n      return target.getAttribute(attributesArray[i]);\n    }\n  }\n},\n    _getBBox = function _getBBox(target) {\n  var bounds;\n\n  try {\n    bounds = target.getBBox(); //Firefox throws errors if you try calling getBBox() on an SVG element that's not rendered (like in a <symbol> or <defs>). https://bugzilla.mozilla.org/show_bug.cgi?id=612118\n  } catch (error) {\n    bounds = _getBBoxHack.call(target, true);\n  }\n\n  bounds && (bounds.width || bounds.height) || target.getBBox === _getBBoxHack || (bounds = _getBBoxHack.call(target, true)); //some browsers (like Firefox) misreport the bounds if the element has zero width and height (it just assumes it's at x:0, y:0), thus we need to manually grab the position in that case.\n\n  return bounds && !bounds.width && !bounds.x && !bounds.y ? {\n    x: +_getAttributeFallbacks(target, [\"x\", \"cx\", \"x1\"]) || 0,\n    y: +_getAttributeFallbacks(target, [\"y\", \"cy\", \"y1\"]) || 0,\n    width: 0,\n    height: 0\n  } : bounds;\n},\n    _isSVG = function _isSVG(e) {\n  return !!(e.getCTM && (!e.parentNode || e.ownerSVGElement) && _getBBox(e));\n},\n    //reports if the element is an SVG on which getBBox() actually works\n_removeProperty = function _removeProperty(target, property) {\n  if (property) {\n    var style = target.style,\n        first2Chars;\n\n    if (property in _transformProps && property !== _transformOriginProp) {\n      property = _transformProp;\n    }\n\n    if (style.removeProperty) {\n      first2Chars = property.substr(0, 2);\n\n      if (first2Chars === \"ms\" || property.substr(0, 6) === \"webkit\") {\n        //Microsoft and some Webkit browsers don't conform to the standard of capitalizing the first prefix character, so we adjust so that when we prefix the caps with a dash, it's correct (otherwise it'd be \"ms-transform\" instead of \"-ms-transform\" for IE9, for example)\n        property = \"-\" + property;\n      }\n\n      style.removeProperty(first2Chars === \"--\" ? property : property.replace(_capsExp, \"-$1\").toLowerCase());\n    } else {\n      //note: old versions of IE use \"removeAttribute()\" instead of \"removeProperty()\"\n      style.removeAttribute(property);\n    }\n  }\n},\n    _addNonTweeningPT = function _addNonTweeningPT(plugin, target, property, beginning, end, onlySetAtEnd) {\n  var pt = new _gsap_core_js__WEBPACK_IMPORTED_MODULE_0__.PropTween(plugin._pt, target, property, 0, 1, onlySetAtEnd ? _renderNonTweeningValueOnlyAtEnd : _renderNonTweeningValue);\n  plugin._pt = pt;\n  pt.b = beginning;\n  pt.e = end;\n\n  plugin._props.push(property);\n\n  return pt;\n},\n    _nonConvertibleUnits = {\n  deg: 1,\n  rad: 1,\n  turn: 1\n},\n    _nonStandardLayouts = {\n  grid: 1,\n  flex: 1\n},\n    //takes a single value like 20px and converts it to the unit specified, like \"%\", returning only the numeric amount.\n_convertToUnit = function _convertToUnit(target, property, value, unit) {\n  var curValue = parseFloat(value) || 0,\n      curUnit = (value + \"\").trim().substr((curValue + \"\").length) || \"px\",\n      // some browsers leave extra whitespace at the beginning of CSS variables, hence the need to trim()\n  style = _tempDiv.style,\n      horizontal = _horizontalExp.test(property),\n      isRootSVG = target.tagName.toLowerCase() === \"svg\",\n      measureProperty = (isRootSVG ? \"client\" : \"offset\") + (horizontal ? \"Width\" : \"Height\"),\n      amount = 100,\n      toPixels = unit === \"px\",\n      toPercent = unit === \"%\",\n      px,\n      parent,\n      cache,\n      isSVG;\n\n  if (unit === curUnit || !curValue || _nonConvertibleUnits[unit] || _nonConvertibleUnits[curUnit]) {\n    return curValue;\n  }\n\n  curUnit !== \"px\" && !toPixels && (curValue = _convertToUnit(target, property, value, \"px\"));\n  isSVG = target.getCTM && _isSVG(target);\n\n  if ((toPercent || curUnit === \"%\") && (_transformProps[property] || ~property.indexOf(\"adius\"))) {\n    px = isSVG ? target.getBBox()[horizontal ? \"width\" : \"height\"] : target[measureProperty];\n    return (0,_gsap_core_js__WEBPACK_IMPORTED_MODULE_0__._round)(toPercent ? curValue / px * amount : curValue / 100 * px);\n  }\n\n  style[horizontal ? \"width\" : \"height\"] = amount + (toPixels ? curUnit : unit);\n  parent = ~property.indexOf(\"adius\") || unit === \"em\" && target.appendChild && !isRootSVG ? target : target.parentNode;\n\n  if (isSVG) {\n    parent = (target.ownerSVGElement || {}).parentNode;\n  }\n\n  if (!parent || parent === _doc || !parent.appendChild) {\n    parent = _doc.body;\n  }\n\n  cache = parent._gsap;\n\n  if (cache && toPercent && cache.width && horizontal && cache.time === _gsap_core_js__WEBPACK_IMPORTED_MODULE_0__._ticker.time && !cache.uncache) {\n    return (0,_gsap_core_js__WEBPACK_IMPORTED_MODULE_0__._round)(curValue / cache.width * amount);\n  } else {\n    if (toPercent && (property === \"height\" || property === \"width\")) {\n      // if we're dealing with width/height that's inside a container with padding and/or it's a flexbox/grid container, we must apply it to the target itself rather than the _tempDiv in order to ensure complete accuracy, factoring in the parent's padding.\n      var v = target.style[property];\n      target.style[property] = amount + unit;\n      px = target[measureProperty];\n      v ? target.style[property] = v : _removeProperty(target, property);\n    } else {\n      (toPercent || curUnit === \"%\") && !_nonStandardLayouts[_getComputedProperty(parent, \"display\")] && (style.position = _getComputedProperty(target, \"position\"));\n      parent === target && (style.position = \"static\"); // like for borderRadius, if it's a % we must have it relative to the target itself but that may not have position: relative or position: absolute in which case it'd go up the chain until it finds its offsetParent (bad). position: static protects against that.\n\n      parent.appendChild(_tempDiv);\n      px = _tempDiv[measureProperty];\n      parent.removeChild(_tempDiv);\n      style.position = \"absolute\";\n    }\n\n    if (horizontal && toPercent) {\n      cache = (0,_gsap_core_js__WEBPACK_IMPORTED_MODULE_0__._getCache)(parent);\n      cache.time = _gsap_core_js__WEBPACK_IMPORTED_MODULE_0__._ticker.time;\n      cache.width = parent[measureProperty];\n    }\n  }\n\n  return (0,_gsap_core_js__WEBPACK_IMPORTED_MODULE_0__._round)(toPixels ? px * curValue / amount : px && curValue ? amount / px * curValue : 0);\n},\n    _get = function _get(target, property, unit, uncache) {\n  var value;\n  _pluginInitted || _initCore();\n\n  if (property in _propertyAliases && property !== \"transform\") {\n    property = _propertyAliases[property];\n\n    if (~property.indexOf(\",\")) {\n      property = property.split(\",\")[0];\n    }\n  }\n\n  if (_transformProps[property] && property !== \"transform\") {\n    value = _parseTransform(target, uncache);\n    value = property !== \"transformOrigin\" ? value[property] : value.svg ? value.origin : _firstTwoOnly(_getComputedProperty(target, _transformOriginProp)) + \" \" + value.zOrigin + \"px\";\n  } else {\n    value = target.style[property];\n\n    if (!value || value === \"auto\" || uncache || ~(value + \"\").indexOf(\"calc(\")) {\n      value = _specialProps[property] && _specialProps[property](target, property, unit) || _getComputedProperty(target, property) || (0,_gsap_core_js__WEBPACK_IMPORTED_MODULE_0__._getProperty)(target, property) || (property === \"opacity\" ? 1 : 0); // note: some browsers, like Firefox, don't report borderRadius correctly! Instead, it only reports every corner like  borderTopLeftRadius\n    }\n  }\n\n  return unit && !~(value + \"\").trim().indexOf(\" \") ? _convertToUnit(target, property, value, unit) + unit : value;\n},\n    _tweenComplexCSSString = function _tweenComplexCSSString(target, prop, start, end) {\n  // note: we call _tweenComplexCSSString.call(pluginInstance...) to ensure that it's scoped properly. We may call it from within a plugin too, thus \"this\" would refer to the plugin.\n  if (!start || start === \"none\") {\n    // some browsers like Safari actually PREFER the prefixed property and mis-report the unprefixed value like clipPath (BUG). In other words, even though clipPath exists in the style (\"clipPath\" in target.style) and it's set in the CSS properly (along with -webkit-clip-path), Safari reports clipPath as \"none\" whereas WebkitClipPath reports accurately like \"ellipse(100% 0% at 50% 0%)\", so in this case we must SWITCH to using the prefixed property instead. See https://gsap.com/forums/topic/18310-clippath-doesnt-work-on-ios/\n    var p = _checkPropPrefix(prop, target, 1),\n        s = p && _getComputedProperty(target, p, 1);\n\n    if (s && s !== start) {\n      prop = p;\n      start = s;\n    } else if (prop === \"borderColor\") {\n      start = _getComputedProperty(target, \"borderTopColor\"); // Firefox bug: always reports \"borderColor\" as \"\", so we must fall back to borderTopColor. See https://gsap.com/forums/topic/24583-how-to-return-colors-that-i-had-after-reverse/\n    }\n  }\n\n  var pt = new _gsap_core_js__WEBPACK_IMPORTED_MODULE_0__.PropTween(this._pt, target.style, prop, 0, 1, _gsap_core_js__WEBPACK_IMPORTED_MODULE_0__._renderComplexString),\n      index = 0,\n      matchIndex = 0,\n      a,\n      result,\n      startValues,\n      startNum,\n      color,\n      startValue,\n      endValue,\n      endNum,\n      chunk,\n      endUnit,\n      startUnit,\n      endValues;\n  pt.b = start;\n  pt.e = end;\n  start += \"\"; // ensure values are strings\n\n  end += \"\";\n\n  if (end === \"auto\") {\n    startValue = target.style[prop];\n    target.style[prop] = end;\n    end = _getComputedProperty(target, prop) || end;\n    startValue ? target.style[prop] = startValue : _removeProperty(target, prop);\n  }\n\n  a = [start, end];\n\n  (0,_gsap_core_js__WEBPACK_IMPORTED_MODULE_0__._colorStringFilter)(a); // pass an array with the starting and ending values and let the filter do whatever it needs to the values. If colors are found, it returns true and then we must match where the color shows up order-wise because for things like boxShadow, sometimes the browser provides the computed values with the color FIRST, but the user provides it with the color LAST, so flip them if necessary. Same for drop-shadow().\n\n\n  start = a[0];\n  end = a[1];\n  startValues = start.match(_gsap_core_js__WEBPACK_IMPORTED_MODULE_0__._numWithUnitExp) || [];\n  endValues = end.match(_gsap_core_js__WEBPACK_IMPORTED_MODULE_0__._numWithUnitExp) || [];\n\n  if (endValues.length) {\n    while (result = _gsap_core_js__WEBPACK_IMPORTED_MODULE_0__._numWithUnitExp.exec(end)) {\n      endValue = result[0];\n      chunk = end.substring(index, result.index);\n\n      if (color) {\n        color = (color + 1) % 5;\n      } else if (chunk.substr(-5) === \"rgba(\" || chunk.substr(-5) === \"hsla(\") {\n        color = 1;\n      }\n\n      if (endValue !== (startValue = startValues[matchIndex++] || \"\")) {\n        startNum = parseFloat(startValue) || 0;\n        startUnit = startValue.substr((startNum + \"\").length);\n        endValue.charAt(1) === \"=\" && (endValue = (0,_gsap_core_js__WEBPACK_IMPORTED_MODULE_0__._parseRelative)(startNum, endValue) + startUnit);\n        endNum = parseFloat(endValue);\n        endUnit = endValue.substr((endNum + \"\").length);\n        index = _gsap_core_js__WEBPACK_IMPORTED_MODULE_0__._numWithUnitExp.lastIndex - endUnit.length;\n\n        if (!endUnit) {\n          //if something like \"perspective:300\" is passed in and we must add a unit to the end\n          endUnit = endUnit || _gsap_core_js__WEBPACK_IMPORTED_MODULE_0__._config.units[prop] || startUnit;\n\n          if (index === end.length) {\n            end += endUnit;\n            pt.e += endUnit;\n          }\n        }\n\n        if (startUnit !== endUnit) {\n          startNum = _convertToUnit(target, prop, startValue, endUnit) || 0;\n        } // these nested PropTweens are handled in a special way - we'll never actually call a render or setter method on them. We'll just loop through them in the parent complex string PropTween's render method.\n\n\n        pt._pt = {\n          _next: pt._pt,\n          p: chunk || matchIndex === 1 ? chunk : \",\",\n          //note: SVG spec allows omission of comma/space when a negative sign is wedged between two numbers, like 2.5-5.3 instead of 2.5,-5.3 but when tweening, the negative value may switch to positive, so we insert the comma just in case.\n          s: startNum,\n          c: endNum - startNum,\n          m: color && color < 4 || prop === \"zIndex\" ? Math.round : 0\n        };\n      }\n    }\n\n    pt.c = index < end.length ? end.substring(index, end.length) : \"\"; //we use the \"c\" of the PropTween to store the final part of the string (after the last number)\n  } else {\n    pt.r = prop === \"display\" && end === \"none\" ? _renderNonTweeningValueOnlyAtEnd : _renderNonTweeningValue;\n  }\n\n  _gsap_core_js__WEBPACK_IMPORTED_MODULE_0__._relExp.test(end) && (pt.e = 0); //if the end string contains relative values or dynamic random(...) values, delete the end it so that on the final render we don't actually set it to the string with += or -= characters (forces it to use the calculated value).\n\n  this._pt = pt; //start the linked list with this new PropTween. Remember, we call _tweenComplexCSSString.call(pluginInstance...) to ensure that it's scoped properly. We may call it from within another plugin too, thus \"this\" would refer to the plugin.\n\n  return pt;\n},\n    _keywordToPercent = {\n  top: \"0%\",\n  bottom: \"100%\",\n  left: \"0%\",\n  right: \"100%\",\n  center: \"50%\"\n},\n    _convertKeywordsToPercentages = function _convertKeywordsToPercentages(value) {\n  var split = value.split(\" \"),\n      x = split[0],\n      y = split[1] || \"50%\";\n\n  if (x === \"top\" || x === \"bottom\" || y === \"left\" || y === \"right\") {\n    //the user provided them in the wrong order, so flip them\n    value = x;\n    x = y;\n    y = value;\n  }\n\n  split[0] = _keywordToPercent[x] || x;\n  split[1] = _keywordToPercent[y] || y;\n  return split.join(\" \");\n},\n    _renderClearProps = function _renderClearProps(ratio, data) {\n  if (data.tween && data.tween._time === data.tween._dur) {\n    var target = data.t,\n        style = target.style,\n        props = data.u,\n        cache = target._gsap,\n        prop,\n        clearTransforms,\n        i;\n\n    if (props === \"all\" || props === true) {\n      style.cssText = \"\";\n      clearTransforms = 1;\n    } else {\n      props = props.split(\",\");\n      i = props.length;\n\n      while (--i > -1) {\n        prop = props[i];\n\n        if (_transformProps[prop]) {\n          clearTransforms = 1;\n          prop = prop === \"transformOrigin\" ? _transformOriginProp : _transformProp;\n        }\n\n        _removeProperty(target, prop);\n      }\n    }\n\n    if (clearTransforms) {\n      _removeProperty(target, _transformProp);\n\n      if (cache) {\n        cache.svg && target.removeAttribute(\"transform\");\n\n        _parseTransform(target, 1); // force all the cached values back to \"normal\"/identity, otherwise if there's another tween that's already set to render transforms on this element, it could display the wrong values.\n\n\n        cache.uncache = 1;\n\n        _removeIndependentTransforms(style);\n      }\n    }\n  }\n},\n    // note: specialProps should return 1 if (and only if) they have a non-zero priority. It indicates we need to sort the linked list.\n_specialProps = {\n  clearProps: function clearProps(plugin, target, property, endValue, tween) {\n    if (tween.data !== \"isFromStart\") {\n      var pt = plugin._pt = new _gsap_core_js__WEBPACK_IMPORTED_MODULE_0__.PropTween(plugin._pt, target, property, 0, 0, _renderClearProps);\n      pt.u = endValue;\n      pt.pr = -10;\n      pt.tween = tween;\n\n      plugin._props.push(property);\n\n      return 1;\n    }\n  }\n  /* className feature (about 0.4kb gzipped).\n  , className(plugin, target, property, endValue, tween) {\n  \tlet _renderClassName = (ratio, data) => {\n  \t\t\tdata.css.render(ratio, data.css);\n  \t\t\tif (!ratio || ratio === 1) {\n  \t\t\t\tlet inline = data.rmv,\n  \t\t\t\t\ttarget = data.t,\n  \t\t\t\t\tp;\n  \t\t\t\ttarget.setAttribute(\"class\", ratio ? data.e : data.b);\n  \t\t\t\tfor (p in inline) {\n  \t\t\t\t\t_removeProperty(target, p);\n  \t\t\t\t}\n  \t\t\t}\n  \t\t},\n  \t\t_getAllStyles = (target) => {\n  \t\t\tlet styles = {},\n  \t\t\t\tcomputed = getComputedStyle(target),\n  \t\t\t\tp;\n  \t\t\tfor (p in computed) {\n  \t\t\t\tif (isNaN(p) && p !== \"cssText\" && p !== \"length\") {\n  \t\t\t\t\tstyles[p] = computed[p];\n  \t\t\t\t}\n  \t\t\t}\n  \t\t\t_setDefaults(styles, _parseTransform(target, 1));\n  \t\t\treturn styles;\n  \t\t},\n  \t\tstartClassList = target.getAttribute(\"class\"),\n  \t\tstyle = target.style,\n  \t\tcssText = style.cssText,\n  \t\tcache = target._gsap,\n  \t\tclassPT = cache.classPT,\n  \t\tinlineToRemoveAtEnd = {},\n  \t\tdata = {t:target, plugin:plugin, rmv:inlineToRemoveAtEnd, b:startClassList, e:(endValue.charAt(1) !== \"=\") ? endValue : startClassList.replace(new RegExp(\"(?:\\\\s|^)\" + endValue.substr(2) + \"(?![\\\\w-])\"), \"\") + ((endValue.charAt(0) === \"+\") ? \" \" + endValue.substr(2) : \"\")},\n  \t\tchangingVars = {},\n  \t\tstartVars = _getAllStyles(target),\n  \t\ttransformRelated = /(transform|perspective)/i,\n  \t\tendVars, p;\n  \tif (classPT) {\n  \t\tclassPT.r(1, classPT.d);\n  \t\t_removeLinkedListItem(classPT.d.plugin, classPT, \"_pt\");\n  \t}\n  \ttarget.setAttribute(\"class\", data.e);\n  \tendVars = _getAllStyles(target, true);\n  \ttarget.setAttribute(\"class\", startClassList);\n  \tfor (p in endVars) {\n  \t\tif (endVars[p] !== startVars[p] && !transformRelated.test(p)) {\n  \t\t\tchangingVars[p] = endVars[p];\n  \t\t\tif (!style[p] && style[p] !== \"0\") {\n  \t\t\t\tinlineToRemoveAtEnd[p] = 1;\n  \t\t\t}\n  \t\t}\n  \t}\n  \tcache.classPT = plugin._pt = new PropTween(plugin._pt, target, \"className\", 0, 0, _renderClassName, data, 0, -11);\n  \tif (style.cssText !== cssText) { //only apply if things change. Otherwise, in cases like a background-image that's pulled dynamically, it could cause a refresh. See https://gsap.com/forums/topic/20368-possible-gsap-bug-switching-classnames-in-chrome/.\n  \t\tstyle.cssText = cssText; //we recorded cssText before we swapped classes and ran _getAllStyles() because in cases when a className tween is overwritten, we remove all the related tweening properties from that class change (otherwise class-specific stuff can't override properties we've directly set on the target's style object due to specificity).\n  \t}\n  \t_parseTransform(target, true); //to clear the caching of transforms\n  \tdata.css = new gsap.plugins.css();\n  \tdata.css.init(target, changingVars, tween);\n  \tplugin._props.push(...data.css._props);\n  \treturn 1;\n  }\n  */\n\n},\n\n/*\n * --------------------------------------------------------------------------------------\n * TRANSFORMS\n * --------------------------------------------------------------------------------------\n */\n_identity2DMatrix = [1, 0, 0, 1, 0, 0],\n    _rotationalProperties = {},\n    _isNullTransform = function _isNullTransform(value) {\n  return value === \"matrix(1, 0, 0, 1, 0, 0)\" || value === \"none\" || !value;\n},\n    _getComputedTransformMatrixAsArray = function _getComputedTransformMatrixAsArray(target) {\n  var matrixString = _getComputedProperty(target, _transformProp);\n\n  return _isNullTransform(matrixString) ? _identity2DMatrix : matrixString.substr(7).match(_gsap_core_js__WEBPACK_IMPORTED_MODULE_0__._numExp).map(_gsap_core_js__WEBPACK_IMPORTED_MODULE_0__._round);\n},\n    _getMatrix = function _getMatrix(target, force2D) {\n  var cache = target._gsap || (0,_gsap_core_js__WEBPACK_IMPORTED_MODULE_0__._getCache)(target),\n      style = target.style,\n      matrix = _getComputedTransformMatrixAsArray(target),\n      parent,\n      nextSibling,\n      temp,\n      addedToDOM;\n\n  if (cache.svg && target.getAttribute(\"transform\")) {\n    temp = target.transform.baseVal.consolidate().matrix; //ensures that even complex values like \"translate(50,60) rotate(135,0,0)\" are parsed because it mashes it into a matrix.\n\n    matrix = [temp.a, temp.b, temp.c, temp.d, temp.e, temp.f];\n    return matrix.join(\",\") === \"1,0,0,1,0,0\" ? _identity2DMatrix : matrix;\n  } else if (matrix === _identity2DMatrix && !target.offsetParent && target !== _docElement && !cache.svg) {\n    //note: if offsetParent is null, that means the element isn't in the normal document flow, like if it has display:none or one of its ancestors has display:none). Firefox returns null for getComputedStyle() if the element is in an iframe that has display:none. https://bugzilla.mozilla.org/show_bug.cgi?id=548397\n    //browsers don't report transforms accurately unless the element is in the DOM and has a display value that's not \"none\". Firefox and Microsoft browsers have a partial bug where they'll report transforms even if display:none BUT not any percentage-based values like translate(-50%, 8px) will be reported as if it's translate(0, 8px).\n    temp = style.display;\n    style.display = \"block\";\n    parent = target.parentNode;\n\n    if (!parent || !target.offsetParent) {\n      // note: in 3.3.0 we switched target.offsetParent to _doc.body.contains(target) to avoid [sometimes unnecessary] MutationObserver calls but that wasn't adequate because there are edge cases where nested position: fixed elements need to get reparented to accurately sense transforms. See https://github.com/greensock/GSAP/issues/388 and https://github.com/greensock/GSAP/issues/375\n      addedToDOM = 1; //flag\n\n      nextSibling = target.nextElementSibling;\n\n      _docElement.appendChild(target); //we must add it to the DOM in order to get values properly\n\n    }\n\n    matrix = _getComputedTransformMatrixAsArray(target);\n    temp ? style.display = temp : _removeProperty(target, \"display\");\n\n    if (addedToDOM) {\n      nextSibling ? parent.insertBefore(target, nextSibling) : parent ? parent.appendChild(target) : _docElement.removeChild(target);\n    }\n  }\n\n  return force2D && matrix.length > 6 ? [matrix[0], matrix[1], matrix[4], matrix[5], matrix[12], matrix[13]] : matrix;\n},\n    _applySVGOrigin = function _applySVGOrigin(target, origin, originIsAbsolute, smooth, matrixArray, pluginToAddPropTweensTo) {\n  var cache = target._gsap,\n      matrix = matrixArray || _getMatrix(target, true),\n      xOriginOld = cache.xOrigin || 0,\n      yOriginOld = cache.yOrigin || 0,\n      xOffsetOld = cache.xOffset || 0,\n      yOffsetOld = cache.yOffset || 0,\n      a = matrix[0],\n      b = matrix[1],\n      c = matrix[2],\n      d = matrix[3],\n      tx = matrix[4],\n      ty = matrix[5],\n      originSplit = origin.split(\" \"),\n      xOrigin = parseFloat(originSplit[0]) || 0,\n      yOrigin = parseFloat(originSplit[1]) || 0,\n      bounds,\n      determinant,\n      x,\n      y;\n\n  if (!originIsAbsolute) {\n    bounds = _getBBox(target);\n    xOrigin = bounds.x + (~originSplit[0].indexOf(\"%\") ? xOrigin / 100 * bounds.width : xOrigin);\n    yOrigin = bounds.y + (~(originSplit[1] || originSplit[0]).indexOf(\"%\") ? yOrigin / 100 * bounds.height : yOrigin); // if (!(\"xOrigin\" in cache) && (xOrigin || yOrigin)) { // added in 3.12.3, reverted in 3.12.4; requires more exploration\n    // \txOrigin -= bounds.x;\n    // \tyOrigin -= bounds.y;\n    // }\n  } else if (matrix !== _identity2DMatrix && (determinant = a * d - b * c)) {\n    //if it's zero (like if scaleX and scaleY are zero), skip it to avoid errors with dividing by zero.\n    x = xOrigin * (d / determinant) + yOrigin * (-c / determinant) + (c * ty - d * tx) / determinant;\n    y = xOrigin * (-b / determinant) + yOrigin * (a / determinant) - (a * ty - b * tx) / determinant;\n    xOrigin = x;\n    yOrigin = y; // theory: we only had to do this for smoothing and it assumes that the previous one was not originIsAbsolute.\n  }\n\n  if (smooth || smooth !== false && cache.smooth) {\n    tx = xOrigin - xOriginOld;\n    ty = yOrigin - yOriginOld;\n    cache.xOffset = xOffsetOld + (tx * a + ty * c) - tx;\n    cache.yOffset = yOffsetOld + (tx * b + ty * d) - ty;\n  } else {\n    cache.xOffset = cache.yOffset = 0;\n  }\n\n  cache.xOrigin = xOrigin;\n  cache.yOrigin = yOrigin;\n  cache.smooth = !!smooth;\n  cache.origin = origin;\n  cache.originIsAbsolute = !!originIsAbsolute;\n  target.style[_transformOriginProp] = \"0px 0px\"; //otherwise, if someone sets  an origin via CSS, it will likely interfere with the SVG transform attribute ones (because remember, we're baking the origin into the matrix() value).\n\n  if (pluginToAddPropTweensTo) {\n    _addNonTweeningPT(pluginToAddPropTweensTo, cache, \"xOrigin\", xOriginOld, xOrigin);\n\n    _addNonTweeningPT(pluginToAddPropTweensTo, cache, \"yOrigin\", yOriginOld, yOrigin);\n\n    _addNonTweeningPT(pluginToAddPropTweensTo, cache, \"xOffset\", xOffsetOld, cache.xOffset);\n\n    _addNonTweeningPT(pluginToAddPropTweensTo, cache, \"yOffset\", yOffsetOld, cache.yOffset);\n  }\n\n  target.setAttribute(\"data-svg-origin\", xOrigin + \" \" + yOrigin);\n},\n    _parseTransform = function _parseTransform(target, uncache) {\n  var cache = target._gsap || new _gsap_core_js__WEBPACK_IMPORTED_MODULE_0__.GSCache(target);\n\n  if (\"x\" in cache && !uncache && !cache.uncache) {\n    return cache;\n  }\n\n  var style = target.style,\n      invertedScaleX = cache.scaleX < 0,\n      px = \"px\",\n      deg = \"deg\",\n      cs = getComputedStyle(target),\n      origin = _getComputedProperty(target, _transformOriginProp) || \"0\",\n      x,\n      y,\n      z,\n      scaleX,\n      scaleY,\n      rotation,\n      rotationX,\n      rotationY,\n      skewX,\n      skewY,\n      perspective,\n      xOrigin,\n      yOrigin,\n      matrix,\n      angle,\n      cos,\n      sin,\n      a,\n      b,\n      c,\n      d,\n      a12,\n      a22,\n      t1,\n      t2,\n      t3,\n      a13,\n      a23,\n      a33,\n      a42,\n      a43,\n      a32;\n  x = y = z = rotation = rotationX = rotationY = skewX = skewY = perspective = 0;\n  scaleX = scaleY = 1;\n  cache.svg = !!(target.getCTM && _isSVG(target));\n\n  if (cs.translate) {\n    // accommodate independent transforms by combining them into normal ones.\n    if (cs.translate !== \"none\" || cs.scale !== \"none\" || cs.rotate !== \"none\") {\n      style[_transformProp] = (cs.translate !== \"none\" ? \"translate3d(\" + (cs.translate + \" 0 0\").split(\" \").slice(0, 3).join(\", \") + \") \" : \"\") + (cs.rotate !== \"none\" ? \"rotate(\" + cs.rotate + \") \" : \"\") + (cs.scale !== \"none\" ? \"scale(\" + cs.scale.split(\" \").join(\",\") + \") \" : \"\") + (cs[_transformProp] !== \"none\" ? cs[_transformProp] : \"\");\n    }\n\n    style.scale = style.rotate = style.translate = \"none\";\n  }\n\n  matrix = _getMatrix(target, cache.svg);\n\n  if (cache.svg) {\n    if (cache.uncache) {\n      // if cache.uncache is true (and maybe if origin is 0,0), we need to set element.style.transformOrigin = (cache.xOrigin - bbox.x) + \"px \" + (cache.yOrigin - bbox.y) + \"px\". Previously we let the data-svg-origin stay instead, but when introducing revert(), it complicated things.\n      t2 = target.getBBox();\n      origin = cache.xOrigin - t2.x + \"px \" + (cache.yOrigin - t2.y) + \"px\";\n      t1 = \"\";\n    } else {\n      t1 = !uncache && target.getAttribute(\"data-svg-origin\"); //  Remember, to work around browser inconsistencies we always force SVG elements' transformOrigin to 0,0 and offset the translation accordingly.\n    }\n\n    _applySVGOrigin(target, t1 || origin, !!t1 || cache.originIsAbsolute, cache.smooth !== false, matrix);\n  }\n\n  xOrigin = cache.xOrigin || 0;\n  yOrigin = cache.yOrigin || 0;\n\n  if (matrix !== _identity2DMatrix) {\n    a = matrix[0]; //a11\n\n    b = matrix[1]; //a21\n\n    c = matrix[2]; //a31\n\n    d = matrix[3]; //a41\n\n    x = a12 = matrix[4];\n    y = a22 = matrix[5]; //2D matrix\n\n    if (matrix.length === 6) {\n      scaleX = Math.sqrt(a * a + b * b);\n      scaleY = Math.sqrt(d * d + c * c);\n      rotation = a || b ? _atan2(b, a) * _RAD2DEG : 0; //note: if scaleX is 0, we cannot accurately measure rotation. Same for skewX with a scaleY of 0. Therefore, we default to the previously recorded value (or zero if that doesn't exist).\n\n      skewX = c || d ? _atan2(c, d) * _RAD2DEG + rotation : 0;\n      skewX && (scaleY *= Math.abs(Math.cos(skewX * _DEG2RAD)));\n\n      if (cache.svg) {\n        x -= xOrigin - (xOrigin * a + yOrigin * c);\n        y -= yOrigin - (xOrigin * b + yOrigin * d);\n      } //3D matrix\n\n    } else {\n      a32 = matrix[6];\n      a42 = matrix[7];\n      a13 = matrix[8];\n      a23 = matrix[9];\n      a33 = matrix[10];\n      a43 = matrix[11];\n      x = matrix[12];\n      y = matrix[13];\n      z = matrix[14];\n      angle = _atan2(a32, a33);\n      rotationX = angle * _RAD2DEG; //rotationX\n\n      if (angle) {\n        cos = Math.cos(-angle);\n        sin = Math.sin(-angle);\n        t1 = a12 * cos + a13 * sin;\n        t2 = a22 * cos + a23 * sin;\n        t3 = a32 * cos + a33 * sin;\n        a13 = a12 * -sin + a13 * cos;\n        a23 = a22 * -sin + a23 * cos;\n        a33 = a32 * -sin + a33 * cos;\n        a43 = a42 * -sin + a43 * cos;\n        a12 = t1;\n        a22 = t2;\n        a32 = t3;\n      } //rotationY\n\n\n      angle = _atan2(-c, a33);\n      rotationY = angle * _RAD2DEG;\n\n      if (angle) {\n        cos = Math.cos(-angle);\n        sin = Math.sin(-angle);\n        t1 = a * cos - a13 * sin;\n        t2 = b * cos - a23 * sin;\n        t3 = c * cos - a33 * sin;\n        a43 = d * sin + a43 * cos;\n        a = t1;\n        b = t2;\n        c = t3;\n      } //rotationZ\n\n\n      angle = _atan2(b, a);\n      rotation = angle * _RAD2DEG;\n\n      if (angle) {\n        cos = Math.cos(angle);\n        sin = Math.sin(angle);\n        t1 = a * cos + b * sin;\n        t2 = a12 * cos + a22 * sin;\n        b = b * cos - a * sin;\n        a22 = a22 * cos - a12 * sin;\n        a = t1;\n        a12 = t2;\n      }\n\n      if (rotationX && Math.abs(rotationX) + Math.abs(rotation) > 359.9) {\n        //when rotationY is set, it will often be parsed as 180 degrees different than it should be, and rotationX and rotation both being 180 (it looks the same), so we adjust for that here.\n        rotationX = rotation = 0;\n        rotationY = 180 - rotationY;\n      }\n\n      scaleX = (0,_gsap_core_js__WEBPACK_IMPORTED_MODULE_0__._round)(Math.sqrt(a * a + b * b + c * c));\n      scaleY = (0,_gsap_core_js__WEBPACK_IMPORTED_MODULE_0__._round)(Math.sqrt(a22 * a22 + a32 * a32));\n      angle = _atan2(a12, a22);\n      skewX = Math.abs(angle) > 0.0002 ? angle * _RAD2DEG : 0;\n      perspective = a43 ? 1 / (a43 < 0 ? -a43 : a43) : 0;\n    }\n\n    if (cache.svg) {\n      //sense if there are CSS transforms applied on an SVG element in which case we must overwrite them when rendering. The transform attribute is more reliable cross-browser, but we can't just remove the CSS ones because they may be applied in a CSS rule somewhere (not just inline).\n      t1 = target.getAttribute(\"transform\");\n      cache.forceCSS = target.setAttribute(\"transform\", \"\") || !_isNullTransform(_getComputedProperty(target, _transformProp));\n      t1 && target.setAttribute(\"transform\", t1);\n    }\n  }\n\n  if (Math.abs(skewX) > 90 && Math.abs(skewX) < 270) {\n    if (invertedScaleX) {\n      scaleX *= -1;\n      skewX += rotation <= 0 ? 180 : -180;\n      rotation += rotation <= 0 ? 180 : -180;\n    } else {\n      scaleY *= -1;\n      skewX += skewX <= 0 ? 180 : -180;\n    }\n  }\n\n  uncache = uncache || cache.uncache;\n  cache.x = x - ((cache.xPercent = x && (!uncache && cache.xPercent || (Math.round(target.offsetWidth / 2) === Math.round(-x) ? -50 : 0))) ? target.offsetWidth * cache.xPercent / 100 : 0) + px;\n  cache.y = y - ((cache.yPercent = y && (!uncache && cache.yPercent || (Math.round(target.offsetHeight / 2) === Math.round(-y) ? -50 : 0))) ? target.offsetHeight * cache.yPercent / 100 : 0) + px;\n  cache.z = z + px;\n  cache.scaleX = (0,_gsap_core_js__WEBPACK_IMPORTED_MODULE_0__._round)(scaleX);\n  cache.scaleY = (0,_gsap_core_js__WEBPACK_IMPORTED_MODULE_0__._round)(scaleY);\n  cache.rotation = (0,_gsap_core_js__WEBPACK_IMPORTED_MODULE_0__._round)(rotation) + deg;\n  cache.rotationX = (0,_gsap_core_js__WEBPACK_IMPORTED_MODULE_0__._round)(rotationX) + deg;\n  cache.rotationY = (0,_gsap_core_js__WEBPACK_IMPORTED_MODULE_0__._round)(rotationY) + deg;\n  cache.skewX = skewX + deg;\n  cache.skewY = skewY + deg;\n  cache.transformPerspective = perspective + px;\n\n  if (cache.zOrigin = parseFloat(origin.split(\" \")[2]) || !uncache && cache.zOrigin || 0) {\n    style[_transformOriginProp] = _firstTwoOnly(origin);\n  }\n\n  cache.xOffset = cache.yOffset = 0;\n  cache.force3D = _gsap_core_js__WEBPACK_IMPORTED_MODULE_0__._config.force3D;\n  cache.renderTransform = cache.svg ? _renderSVGTransforms : _supports3D ? _renderCSSTransforms : _renderNon3DTransforms;\n  cache.uncache = 0;\n  return cache;\n},\n    _firstTwoOnly = function _firstTwoOnly(value) {\n  return (value = value.split(\" \"))[0] + \" \" + value[1];\n},\n    //for handling transformOrigin values, stripping out the 3rd dimension\n_addPxTranslate = function _addPxTranslate(target, start, value) {\n  var unit = (0,_gsap_core_js__WEBPACK_IMPORTED_MODULE_0__.getUnit)(start);\n  return (0,_gsap_core_js__WEBPACK_IMPORTED_MODULE_0__._round)(parseFloat(start) + parseFloat(_convertToUnit(target, \"x\", value + \"px\", unit))) + unit;\n},\n    _renderNon3DTransforms = function _renderNon3DTransforms(ratio, cache) {\n  cache.z = \"0px\";\n  cache.rotationY = cache.rotationX = \"0deg\";\n  cache.force3D = 0;\n\n  _renderCSSTransforms(ratio, cache);\n},\n    _zeroDeg = \"0deg\",\n    _zeroPx = \"0px\",\n    _endParenthesis = \") \",\n    _renderCSSTransforms = function _renderCSSTransforms(ratio, cache) {\n  var _ref = cache || this,\n      xPercent = _ref.xPercent,\n      yPercent = _ref.yPercent,\n      x = _ref.x,\n      y = _ref.y,\n      z = _ref.z,\n      rotation = _ref.rotation,\n      rotationY = _ref.rotationY,\n      rotationX = _ref.rotationX,\n      skewX = _ref.skewX,\n      skewY = _ref.skewY,\n      scaleX = _ref.scaleX,\n      scaleY = _ref.scaleY,\n      transformPerspective = _ref.transformPerspective,\n      force3D = _ref.force3D,\n      target = _ref.target,\n      zOrigin = _ref.zOrigin,\n      transforms = \"\",\n      use3D = force3D === \"auto\" && ratio && ratio !== 1 || force3D === true; // Safari has a bug that causes it not to render 3D transform-origin values properly, so we force the z origin to 0, record it in the cache, and then do the math here to offset the translate values accordingly (basically do the 3D transform-origin part manually)\n\n\n  if (zOrigin && (rotationX !== _zeroDeg || rotationY !== _zeroDeg)) {\n    var angle = parseFloat(rotationY) * _DEG2RAD,\n        a13 = Math.sin(angle),\n        a33 = Math.cos(angle),\n        cos;\n\n    angle = parseFloat(rotationX) * _DEG2RAD;\n    cos = Math.cos(angle);\n    x = _addPxTranslate(target, x, a13 * cos * -zOrigin);\n    y = _addPxTranslate(target, y, -Math.sin(angle) * -zOrigin);\n    z = _addPxTranslate(target, z, a33 * cos * -zOrigin + zOrigin);\n  }\n\n  if (transformPerspective !== _zeroPx) {\n    transforms += \"perspective(\" + transformPerspective + _endParenthesis;\n  }\n\n  if (xPercent || yPercent) {\n    transforms += \"translate(\" + xPercent + \"%, \" + yPercent + \"%) \";\n  }\n\n  if (use3D || x !== _zeroPx || y !== _zeroPx || z !== _zeroPx) {\n    transforms += z !== _zeroPx || use3D ? \"translate3d(\" + x + \", \" + y + \", \" + z + \") \" : \"translate(\" + x + \", \" + y + _endParenthesis;\n  }\n\n  if (rotation !== _zeroDeg) {\n    transforms += \"rotate(\" + rotation + _endParenthesis;\n  }\n\n  if (rotationY !== _zeroDeg) {\n    transforms += \"rotateY(\" + rotationY + _endParenthesis;\n  }\n\n  if (rotationX !== _zeroDeg) {\n    transforms += \"rotateX(\" + rotationX + _endParenthesis;\n  }\n\n  if (skewX !== _zeroDeg || skewY !== _zeroDeg) {\n    transforms += \"skew(\" + skewX + \", \" + skewY + _endParenthesis;\n  }\n\n  if (scaleX !== 1 || scaleY !== 1) {\n    transforms += \"scale(\" + scaleX + \", \" + scaleY + _endParenthesis;\n  }\n\n  target.style[_transformProp] = transforms || \"translate(0, 0)\";\n},\n    _renderSVGTransforms = function _renderSVGTransforms(ratio, cache) {\n  var _ref2 = cache || this,\n      xPercent = _ref2.xPercent,\n      yPercent = _ref2.yPercent,\n      x = _ref2.x,\n      y = _ref2.y,\n      rotation = _ref2.rotation,\n      skewX = _ref2.skewX,\n      skewY = _ref2.skewY,\n      scaleX = _ref2.scaleX,\n      scaleY = _ref2.scaleY,\n      target = _ref2.target,\n      xOrigin = _ref2.xOrigin,\n      yOrigin = _ref2.yOrigin,\n      xOffset = _ref2.xOffset,\n      yOffset = _ref2.yOffset,\n      forceCSS = _ref2.forceCSS,\n      tx = parseFloat(x),\n      ty = parseFloat(y),\n      a11,\n      a21,\n      a12,\n      a22,\n      temp;\n\n  rotation = parseFloat(rotation);\n  skewX = parseFloat(skewX);\n  skewY = parseFloat(skewY);\n\n  if (skewY) {\n    //for performance reasons, we combine all skewing into the skewX and rotation values. Remember, a skewY of 10 degrees looks the same as a rotation of 10 degrees plus a skewX of 10 degrees.\n    skewY = parseFloat(skewY);\n    skewX += skewY;\n    rotation += skewY;\n  }\n\n  if (rotation || skewX) {\n    rotation *= _DEG2RAD;\n    skewX *= _DEG2RAD;\n    a11 = Math.cos(rotation) * scaleX;\n    a21 = Math.sin(rotation) * scaleX;\n    a12 = Math.sin(rotation - skewX) * -scaleY;\n    a22 = Math.cos(rotation - skewX) * scaleY;\n\n    if (skewX) {\n      skewY *= _DEG2RAD;\n      temp = Math.tan(skewX - skewY);\n      temp = Math.sqrt(1 + temp * temp);\n      a12 *= temp;\n      a22 *= temp;\n\n      if (skewY) {\n        temp = Math.tan(skewY);\n        temp = Math.sqrt(1 + temp * temp);\n        a11 *= temp;\n        a21 *= temp;\n      }\n    }\n\n    a11 = (0,_gsap_core_js__WEBPACK_IMPORTED_MODULE_0__._round)(a11);\n    a21 = (0,_gsap_core_js__WEBPACK_IMPORTED_MODULE_0__._round)(a21);\n    a12 = (0,_gsap_core_js__WEBPACK_IMPORTED_MODULE_0__._round)(a12);\n    a22 = (0,_gsap_core_js__WEBPACK_IMPORTED_MODULE_0__._round)(a22);\n  } else {\n    a11 = scaleX;\n    a22 = scaleY;\n    a21 = a12 = 0;\n  }\n\n  if (tx && !~(x + \"\").indexOf(\"px\") || ty && !~(y + \"\").indexOf(\"px\")) {\n    tx = _convertToUnit(target, \"x\", x, \"px\");\n    ty = _convertToUnit(target, \"y\", y, \"px\");\n  }\n\n  if (xOrigin || yOrigin || xOffset || yOffset) {\n    tx = (0,_gsap_core_js__WEBPACK_IMPORTED_MODULE_0__._round)(tx + xOrigin - (xOrigin * a11 + yOrigin * a12) + xOffset);\n    ty = (0,_gsap_core_js__WEBPACK_IMPORTED_MODULE_0__._round)(ty + yOrigin - (xOrigin * a21 + yOrigin * a22) + yOffset);\n  }\n\n  if (xPercent || yPercent) {\n    //The SVG spec doesn't support percentage-based translation in the \"transform\" attribute, so we merge it into the translation to simulate it.\n    temp = target.getBBox();\n    tx = (0,_gsap_core_js__WEBPACK_IMPORTED_MODULE_0__._round)(tx + xPercent / 100 * temp.width);\n    ty = (0,_gsap_core_js__WEBPACK_IMPORTED_MODULE_0__._round)(ty + yPercent / 100 * temp.height);\n  }\n\n  temp = \"matrix(\" + a11 + \",\" + a21 + \",\" + a12 + \",\" + a22 + \",\" + tx + \",\" + ty + \")\";\n  target.setAttribute(\"transform\", temp);\n  forceCSS && (target.style[_transformProp] = temp); //some browsers prioritize CSS transforms over the transform attribute. When we sense that the user has CSS transforms applied, we must overwrite them this way (otherwise some browser simply won't render the transform attribute changes!)\n},\n    _addRotationalPropTween = function _addRotationalPropTween(plugin, target, property, startNum, endValue) {\n  var cap = 360,\n      isString = (0,_gsap_core_js__WEBPACK_IMPORTED_MODULE_0__._isString)(endValue),\n      endNum = parseFloat(endValue) * (isString && ~endValue.indexOf(\"rad\") ? _RAD2DEG : 1),\n      change = endNum - startNum,\n      finalValue = startNum + change + \"deg\",\n      direction,\n      pt;\n\n  if (isString) {\n    direction = endValue.split(\"_\")[1];\n\n    if (direction === \"short\") {\n      change %= cap;\n\n      if (change !== change % (cap / 2)) {\n        change += change < 0 ? cap : -cap;\n      }\n    }\n\n    if (direction === \"cw\" && change < 0) {\n      change = (change + cap * _bigNum) % cap - ~~(change / cap) * cap;\n    } else if (direction === \"ccw\" && change > 0) {\n      change = (change - cap * _bigNum) % cap - ~~(change / cap) * cap;\n    }\n  }\n\n  plugin._pt = pt = new _gsap_core_js__WEBPACK_IMPORTED_MODULE_0__.PropTween(plugin._pt, target, property, startNum, change, _renderPropWithEnd);\n  pt.e = finalValue;\n  pt.u = \"deg\";\n\n  plugin._props.push(property);\n\n  return pt;\n},\n    _assign = function _assign(target, source) {\n  // Internet Explorer doesn't have Object.assign(), so we recreate it here.\n  for (var p in source) {\n    target[p] = source[p];\n  }\n\n  return target;\n},\n    _addRawTransformPTs = function _addRawTransformPTs(plugin, transforms, target) {\n  //for handling cases where someone passes in a whole transform string, like transform: \"scale(2, 3) rotate(20deg) translateY(30em)\"\n  var startCache = _assign({}, target._gsap),\n      exclude = \"perspective,force3D,transformOrigin,svgOrigin\",\n      style = target.style,\n      endCache,\n      p,\n      startValue,\n      endValue,\n      startNum,\n      endNum,\n      startUnit,\n      endUnit;\n\n  if (startCache.svg) {\n    startValue = target.getAttribute(\"transform\");\n    target.setAttribute(\"transform\", \"\");\n    style[_transformProp] = transforms;\n    endCache = _parseTransform(target, 1);\n\n    _removeProperty(target, _transformProp);\n\n    target.setAttribute(\"transform\", startValue);\n  } else {\n    startValue = getComputedStyle(target)[_transformProp];\n    style[_transformProp] = transforms;\n    endCache = _parseTransform(target, 1);\n    style[_transformProp] = startValue;\n  }\n\n  for (p in _transformProps) {\n    startValue = startCache[p];\n    endValue = endCache[p];\n\n    if (startValue !== endValue && exclude.indexOf(p) < 0) {\n      //tweening to no perspective gives very unintuitive results - just keep the same perspective in that case.\n      startUnit = (0,_gsap_core_js__WEBPACK_IMPORTED_MODULE_0__.getUnit)(startValue);\n      endUnit = (0,_gsap_core_js__WEBPACK_IMPORTED_MODULE_0__.getUnit)(endValue);\n      startNum = startUnit !== endUnit ? _convertToUnit(target, p, startValue, endUnit) : parseFloat(startValue);\n      endNum = parseFloat(endValue);\n      plugin._pt = new _gsap_core_js__WEBPACK_IMPORTED_MODULE_0__.PropTween(plugin._pt, endCache, p, startNum, endNum - startNum, _renderCSSProp);\n      plugin._pt.u = endUnit || 0;\n\n      plugin._props.push(p);\n    }\n  }\n\n  _assign(endCache, startCache);\n}; // handle splitting apart padding, margin, borderWidth, and borderRadius into their 4 components. Firefox, for example, won't report borderRadius correctly - it will only do borderTopLeftRadius and the other corners. We also want to handle paddingTop, marginLeft, borderRightWidth, etc.\n\n\n(0,_gsap_core_js__WEBPACK_IMPORTED_MODULE_0__._forEachName)(\"padding,margin,Width,Radius\", function (name, index) {\n  var t = \"Top\",\n      r = \"Right\",\n      b = \"Bottom\",\n      l = \"Left\",\n      props = (index < 3 ? [t, r, b, l] : [t + l, t + r, b + r, b + l]).map(function (side) {\n    return index < 2 ? name + side : \"border\" + side + name;\n  });\n\n  _specialProps[index > 1 ? \"border\" + name : name] = function (plugin, target, property, endValue, tween) {\n    var a, vars;\n\n    if (arguments.length < 4) {\n      // getter, passed target, property, and unit (from _get())\n      a = props.map(function (prop) {\n        return _get(plugin, prop, property);\n      });\n      vars = a.join(\" \");\n      return vars.split(a[0]).length === 5 ? a[0] : vars;\n    }\n\n    a = (endValue + \"\").split(\" \");\n    vars = {};\n    props.forEach(function (prop, i) {\n      return vars[prop] = a[i] = a[i] || a[(i - 1) / 2 | 0];\n    });\n    plugin.init(target, vars, tween);\n  };\n});\n\nvar CSSPlugin = {\n  name: \"css\",\n  register: _initCore,\n  targetTest: function targetTest(target) {\n    return target.style && target.nodeType;\n  },\n  init: function init(target, vars, tween, index, targets) {\n    var props = this._props,\n        style = target.style,\n        startAt = tween.vars.startAt,\n        startValue,\n        endValue,\n        endNum,\n        startNum,\n        type,\n        specialProp,\n        p,\n        startUnit,\n        endUnit,\n        relative,\n        isTransformRelated,\n        transformPropTween,\n        cache,\n        smooth,\n        hasPriority,\n        inlineProps;\n    _pluginInitted || _initCore(); // we may call init() multiple times on the same plugin instance, like when adding special properties, so make sure we don't overwrite the revert data or inlineProps\n\n    this.styles = this.styles || _getStyleSaver(target);\n    inlineProps = this.styles.props;\n    this.tween = tween;\n\n    for (p in vars) {\n      if (p === \"autoRound\") {\n        continue;\n      }\n\n      endValue = vars[p];\n\n      if (_gsap_core_js__WEBPACK_IMPORTED_MODULE_0__._plugins[p] && (0,_gsap_core_js__WEBPACK_IMPORTED_MODULE_0__._checkPlugin)(p, vars, tween, index, target, targets)) {\n        // plugins\n        continue;\n      }\n\n      type = typeof endValue;\n      specialProp = _specialProps[p];\n\n      if (type === \"function\") {\n        endValue = endValue.call(tween, index, target, targets);\n        type = typeof endValue;\n      }\n\n      if (type === \"string\" && ~endValue.indexOf(\"random(\")) {\n        endValue = (0,_gsap_core_js__WEBPACK_IMPORTED_MODULE_0__._replaceRandom)(endValue);\n      }\n\n      if (specialProp) {\n        specialProp(this, target, p, endValue, tween) && (hasPriority = 1);\n      } else if (p.substr(0, 2) === \"--\") {\n        //CSS variable\n        startValue = (getComputedStyle(target).getPropertyValue(p) + \"\").trim();\n        endValue += \"\";\n        _gsap_core_js__WEBPACK_IMPORTED_MODULE_0__._colorExp.lastIndex = 0;\n\n        if (!_gsap_core_js__WEBPACK_IMPORTED_MODULE_0__._colorExp.test(startValue)) {\n          // colors don't have units\n          startUnit = (0,_gsap_core_js__WEBPACK_IMPORTED_MODULE_0__.getUnit)(startValue);\n          endUnit = (0,_gsap_core_js__WEBPACK_IMPORTED_MODULE_0__.getUnit)(endValue);\n        }\n\n        endUnit ? startUnit !== endUnit && (startValue = _convertToUnit(target, p, startValue, endUnit) + endUnit) : startUnit && (endValue += startUnit);\n        this.add(style, \"setProperty\", startValue, endValue, index, targets, 0, 0, p);\n        props.push(p);\n        inlineProps.push(p, 0, style[p]);\n      } else if (type !== \"undefined\") {\n        if (startAt && p in startAt) {\n          // in case someone hard-codes a complex value as the start, like top: \"calc(2vh / 2)\". Without this, it'd use the computed value (always in px)\n          startValue = typeof startAt[p] === \"function\" ? startAt[p].call(tween, index, target, targets) : startAt[p];\n          (0,_gsap_core_js__WEBPACK_IMPORTED_MODULE_0__._isString)(startValue) && ~startValue.indexOf(\"random(\") && (startValue = (0,_gsap_core_js__WEBPACK_IMPORTED_MODULE_0__._replaceRandom)(startValue));\n          (0,_gsap_core_js__WEBPACK_IMPORTED_MODULE_0__.getUnit)(startValue + \"\") || startValue === \"auto\" || (startValue += _gsap_core_js__WEBPACK_IMPORTED_MODULE_0__._config.units[p] || (0,_gsap_core_js__WEBPACK_IMPORTED_MODULE_0__.getUnit)(_get(target, p)) || \"\"); // for cases when someone passes in a unitless value like {x: 100}; if we try setting translate(100, 0px) it won't work.\n\n          (startValue + \"\").charAt(1) === \"=\" && (startValue = _get(target, p)); // can't work with relative values\n        } else {\n          startValue = _get(target, p);\n        }\n\n        startNum = parseFloat(startValue);\n        relative = type === \"string\" && endValue.charAt(1) === \"=\" && endValue.substr(0, 2);\n        relative && (endValue = endValue.substr(2));\n        endNum = parseFloat(endValue);\n\n        if (p in _propertyAliases) {\n          if (p === \"autoAlpha\") {\n            //special case where we control the visibility along with opacity. We still allow the opacity value to pass through and get tweened.\n            if (startNum === 1 && _get(target, \"visibility\") === \"hidden\" && endNum) {\n              //if visibility is initially set to \"hidden\", we should interpret that as intent to make opacity 0 (a convenience)\n              startNum = 0;\n            }\n\n            inlineProps.push(\"visibility\", 0, style.visibility);\n\n            _addNonTweeningPT(this, style, \"visibility\", startNum ? \"inherit\" : \"hidden\", endNum ? \"inherit\" : \"hidden\", !endNum);\n          }\n\n          if (p !== \"scale\" && p !== \"transform\") {\n            p = _propertyAliases[p];\n            ~p.indexOf(\",\") && (p = p.split(\",\")[0]);\n          }\n        }\n\n        isTransformRelated = p in _transformProps; //--- TRANSFORM-RELATED ---\n\n        if (isTransformRelated) {\n          this.styles.save(p);\n\n          if (!transformPropTween) {\n            cache = target._gsap;\n            cache.renderTransform && !vars.parseTransform || _parseTransform(target, vars.parseTransform); // if, for example, gsap.set(... {transform:\"translateX(50vw)\"}), the _get() call doesn't parse the transform, thus cache.renderTransform won't be set yet so force the parsing of the transform here.\n\n            smooth = vars.smoothOrigin !== false && cache.smooth;\n            transformPropTween = this._pt = new _gsap_core_js__WEBPACK_IMPORTED_MODULE_0__.PropTween(this._pt, style, _transformProp, 0, 1, cache.renderTransform, cache, 0, -1); //the first time through, create the rendering PropTween so that it runs LAST (in the linked list, we keep adding to the beginning)\n\n            transformPropTween.dep = 1; //flag it as dependent so that if things get killed/overwritten and this is the only PropTween left, we can safely kill the whole tween.\n          }\n\n          if (p === \"scale\") {\n            this._pt = new _gsap_core_js__WEBPACK_IMPORTED_MODULE_0__.PropTween(this._pt, cache, \"scaleY\", cache.scaleY, (relative ? (0,_gsap_core_js__WEBPACK_IMPORTED_MODULE_0__._parseRelative)(cache.scaleY, relative + endNum) : endNum) - cache.scaleY || 0, _renderCSSProp);\n            this._pt.u = 0;\n            props.push(\"scaleY\", p);\n            p += \"X\";\n          } else if (p === \"transformOrigin\") {\n            inlineProps.push(_transformOriginProp, 0, style[_transformOriginProp]);\n            endValue = _convertKeywordsToPercentages(endValue); //in case something like \"left top\" or \"bottom right\" is passed in. Convert to percentages.\n\n            if (cache.svg) {\n              _applySVGOrigin(target, endValue, 0, smooth, 0, this);\n            } else {\n              endUnit = parseFloat(endValue.split(\" \")[2]) || 0; //handle the zOrigin separately!\n\n              endUnit !== cache.zOrigin && _addNonTweeningPT(this, cache, \"zOrigin\", cache.zOrigin, endUnit);\n\n              _addNonTweeningPT(this, style, p, _firstTwoOnly(startValue), _firstTwoOnly(endValue));\n            }\n\n            continue;\n          } else if (p === \"svgOrigin\") {\n            _applySVGOrigin(target, endValue, 1, smooth, 0, this);\n\n            continue;\n          } else if (p in _rotationalProperties) {\n            _addRotationalPropTween(this, cache, p, startNum, relative ? (0,_gsap_core_js__WEBPACK_IMPORTED_MODULE_0__._parseRelative)(startNum, relative + endValue) : endValue);\n\n            continue;\n          } else if (p === \"smoothOrigin\") {\n            _addNonTweeningPT(this, cache, \"smooth\", cache.smooth, endValue);\n\n            continue;\n          } else if (p === \"force3D\") {\n            cache[p] = endValue;\n            continue;\n          } else if (p === \"transform\") {\n            _addRawTransformPTs(this, endValue, target);\n\n            continue;\n          }\n        } else if (!(p in style)) {\n          p = _checkPropPrefix(p) || p;\n        }\n\n        if (isTransformRelated || (endNum || endNum === 0) && (startNum || startNum === 0) && !_complexExp.test(endValue) && p in style) {\n          startUnit = (startValue + \"\").substr((startNum + \"\").length);\n          endNum || (endNum = 0); // protect against NaN\n\n          endUnit = (0,_gsap_core_js__WEBPACK_IMPORTED_MODULE_0__.getUnit)(endValue) || (p in _gsap_core_js__WEBPACK_IMPORTED_MODULE_0__._config.units ? _gsap_core_js__WEBPACK_IMPORTED_MODULE_0__._config.units[p] : startUnit);\n          startUnit !== endUnit && (startNum = _convertToUnit(target, p, startValue, endUnit));\n          this._pt = new _gsap_core_js__WEBPACK_IMPORTED_MODULE_0__.PropTween(this._pt, isTransformRelated ? cache : style, p, startNum, (relative ? (0,_gsap_core_js__WEBPACK_IMPORTED_MODULE_0__._parseRelative)(startNum, relative + endNum) : endNum) - startNum, !isTransformRelated && (endUnit === \"px\" || p === \"zIndex\") && vars.autoRound !== false ? _renderRoundedCSSProp : _renderCSSProp);\n          this._pt.u = endUnit || 0;\n\n          if (startUnit !== endUnit && endUnit !== \"%\") {\n            //when the tween goes all the way back to the beginning, we need to revert it to the OLD/ORIGINAL value (with those units). We record that as a \"b\" (beginning) property and point to a render method that handles that. (performance optimization)\n            this._pt.b = startValue;\n            this._pt.r = _renderCSSPropWithBeginning;\n          }\n        } else if (!(p in style)) {\n          if (p in target) {\n            //maybe it's not a style - it could be a property added directly to an element in which case we'll try to animate that.\n            this.add(target, p, startValue || target[p], relative ? relative + endValue : endValue, index, targets);\n          } else if (p !== \"parseTransform\") {\n            (0,_gsap_core_js__WEBPACK_IMPORTED_MODULE_0__._missingPlugin)(p, endValue);\n\n            continue;\n          }\n        } else {\n          _tweenComplexCSSString.call(this, target, p, startValue, relative ? relative + endValue : endValue);\n        }\n\n        isTransformRelated || (p in style ? inlineProps.push(p, 0, style[p]) : inlineProps.push(p, 1, startValue || target[p]));\n        props.push(p);\n      }\n    }\n\n    hasPriority && (0,_gsap_core_js__WEBPACK_IMPORTED_MODULE_0__._sortPropTweensByPriority)(this);\n  },\n  render: function render(ratio, data) {\n    if (data.tween._time || !_reverting()) {\n      var pt = data._pt;\n\n      while (pt) {\n        pt.r(ratio, pt.d);\n        pt = pt._next;\n      }\n    } else {\n      data.styles.revert();\n    }\n  },\n  get: _get,\n  aliases: _propertyAliases,\n  getSetter: function getSetter(target, property, plugin) {\n    //returns a setter function that accepts target, property, value and applies it accordingly. Remember, properties like \"x\" aren't as simple as target.style.property = value because they've got to be applied to a proxy object and then merged into a transform string in a renderer.\n    var p = _propertyAliases[property];\n    p && p.indexOf(\",\") < 0 && (property = p);\n    return property in _transformProps && property !== _transformOriginProp && (target._gsap.x || _get(target, \"x\")) ? plugin && _recentSetterPlugin === plugin ? property === \"scale\" ? _setterScale : _setterTransform : (_recentSetterPlugin = plugin || {}) && (property === \"scale\" ? _setterScaleWithRender : _setterTransformWithRender) : target.style && !(0,_gsap_core_js__WEBPACK_IMPORTED_MODULE_0__._isUndefined)(target.style[property]) ? _setterCSSStyle : ~property.indexOf(\"-\") ? _setterCSSProp : (0,_gsap_core_js__WEBPACK_IMPORTED_MODULE_0__._getSetter)(target, property);\n  },\n  core: {\n    _removeProperty: _removeProperty,\n    _getMatrix: _getMatrix\n  }\n};\n_gsap_core_js__WEBPACK_IMPORTED_MODULE_0__.gsap.utils.checkPrefix = _checkPropPrefix;\n_gsap_core_js__WEBPACK_IMPORTED_MODULE_0__.gsap.core.getStyleSaver = _getStyleSaver;\n\n(function (positionAndScale, rotation, others, aliases) {\n  var all = (0,_gsap_core_js__WEBPACK_IMPORTED_MODULE_0__._forEachName)(positionAndScale + \",\" + rotation + \",\" + others, function (name) {\n    _transformProps[name] = 1;\n  });\n\n  (0,_gsap_core_js__WEBPACK_IMPORTED_MODULE_0__._forEachName)(rotation, function (name) {\n    _gsap_core_js__WEBPACK_IMPORTED_MODULE_0__._config.units[name] = \"deg\";\n    _rotationalProperties[name] = 1;\n  });\n\n  _propertyAliases[all[13]] = positionAndScale + \",\" + rotation;\n\n  (0,_gsap_core_js__WEBPACK_IMPORTED_MODULE_0__._forEachName)(aliases, function (name) {\n    var split = name.split(\":\");\n    _propertyAliases[split[1]] = all[split[0]];\n  });\n})(\"x,y,z,scale,scaleX,scaleY,xPercent,yPercent\", \"rotation,rotationX,rotationY,skewX,skewY\", \"transform,transformOrigin,svgOrigin,force3D,smoothOrigin,transformPerspective\", \"0:translateX,1:translateY,2:translateZ,8:rotate,8:rotationZ,8:rotateZ,9:rotateX,10:rotateY\");\n\n(0,_gsap_core_js__WEBPACK_IMPORTED_MODULE_0__._forEachName)(\"x,y,z,top,right,bottom,left,width,height,fontSize,padding,margin,perspective\", function (name) {\n  _gsap_core_js__WEBPACK_IMPORTED_MODULE_0__._config.units[name] = \"px\";\n});\n\n_gsap_core_js__WEBPACK_IMPORTED_MODULE_0__.gsap.registerPlugin(CSSPlugin);\n\n\n//# sourceURL=webpack://frontend-boilerplate/./node_modules/gsap/CSSPlugin.js?");
  
  /***/ }),
  
  /***/ "./node_modules/gsap/gsap-core.js":
  /*!****************************************!*\
    !*** ./node_modules/gsap/gsap-core.js ***!
    \****************************************/
  /***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {
  
  "use strict";
  eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   Animation: () => (/* binding */ Animation),\n/* harmony export */   Back: () => (/* binding */ Back),\n/* harmony export */   Bounce: () => (/* binding */ Bounce),\n/* harmony export */   Circ: () => (/* binding */ Circ),\n/* harmony export */   Cubic: () => (/* binding */ Cubic),\n/* harmony export */   Elastic: () => (/* binding */ Elastic),\n/* harmony export */   Expo: () => (/* binding */ Expo),\n/* harmony export */   GSCache: () => (/* binding */ GSCache),\n/* harmony export */   Linear: () => (/* binding */ Linear),\n/* harmony export */   Power0: () => (/* binding */ Power0),\n/* harmony export */   Power1: () => (/* binding */ Power1),\n/* harmony export */   Power2: () => (/* binding */ Power2),\n/* harmony export */   Power3: () => (/* binding */ Power3),\n/* harmony export */   Power4: () => (/* binding */ Power4),\n/* harmony export */   PropTween: () => (/* binding */ PropTween),\n/* harmony export */   Quad: () => (/* binding */ Quad),\n/* harmony export */   Quart: () => (/* binding */ Quart),\n/* harmony export */   Quint: () => (/* binding */ Quint),\n/* harmony export */   Sine: () => (/* binding */ Sine),\n/* harmony export */   SteppedEase: () => (/* binding */ SteppedEase),\n/* harmony export */   Strong: () => (/* binding */ Strong),\n/* harmony export */   Timeline: () => (/* binding */ Timeline),\n/* harmony export */   TimelineLite: () => (/* binding */ Timeline),\n/* harmony export */   TimelineMax: () => (/* binding */ Timeline),\n/* harmony export */   Tween: () => (/* binding */ Tween),\n/* harmony export */   TweenLite: () => (/* binding */ Tween),\n/* harmony export */   TweenMax: () => (/* binding */ Tween),\n/* harmony export */   _checkPlugin: () => (/* binding */ _checkPlugin),\n/* harmony export */   _colorExp: () => (/* binding */ _colorExp),\n/* harmony export */   _colorStringFilter: () => (/* binding */ _colorStringFilter),\n/* harmony export */   _config: () => (/* binding */ _config),\n/* harmony export */   _forEachName: () => (/* binding */ _forEachName),\n/* harmony export */   _getCache: () => (/* binding */ _getCache),\n/* harmony export */   _getProperty: () => (/* binding */ _getProperty),\n/* harmony export */   _getSetter: () => (/* binding */ _getSetter),\n/* harmony export */   _isString: () => (/* binding */ _isString),\n/* harmony export */   _isUndefined: () => (/* binding */ _isUndefined),\n/* harmony export */   _missingPlugin: () => (/* binding */ _missingPlugin),\n/* harmony export */   _numExp: () => (/* binding */ _numExp),\n/* harmony export */   _numWithUnitExp: () => (/* binding */ _numWithUnitExp),\n/* harmony export */   _parseRelative: () => (/* binding */ _parseRelative),\n/* harmony export */   _plugins: () => (/* binding */ _plugins),\n/* harmony export */   _relExp: () => (/* binding */ _relExp),\n/* harmony export */   _removeLinkedListItem: () => (/* binding */ _removeLinkedListItem),\n/* harmony export */   _renderComplexString: () => (/* binding */ _renderComplexString),\n/* harmony export */   _replaceRandom: () => (/* binding */ _replaceRandom),\n/* harmony export */   _round: () => (/* binding */ _round),\n/* harmony export */   _roundModifier: () => (/* binding */ _roundModifier),\n/* harmony export */   _setDefaults: () => (/* binding */ _setDefaults),\n/* harmony export */   _sortPropTweensByPriority: () => (/* binding */ _sortPropTweensByPriority),\n/* harmony export */   _ticker: () => (/* binding */ _ticker),\n/* harmony export */   clamp: () => (/* binding */ clamp),\n/* harmony export */   \"default\": () => (/* binding */ gsap),\n/* harmony export */   distribute: () => (/* binding */ distribute),\n/* harmony export */   getUnit: () => (/* binding */ getUnit),\n/* harmony export */   gsap: () => (/* binding */ gsap),\n/* harmony export */   interpolate: () => (/* binding */ interpolate),\n/* harmony export */   mapRange: () => (/* binding */ mapRange),\n/* harmony export */   normalize: () => (/* binding */ normalize),\n/* harmony export */   pipe: () => (/* binding */ pipe),\n/* harmony export */   random: () => (/* binding */ random),\n/* harmony export */   selector: () => (/* binding */ selector),\n/* harmony export */   shuffle: () => (/* binding */ shuffle),\n/* harmony export */   snap: () => (/* binding */ snap),\n/* harmony export */   splitColor: () => (/* binding */ splitColor),\n/* harmony export */   toArray: () => (/* binding */ toArray),\n/* harmony export */   unitize: () => (/* binding */ unitize),\n/* harmony export */   wrap: () => (/* binding */ wrap),\n/* harmony export */   wrapYoyo: () => (/* binding */ wrapYoyo)\n/* harmony export */ });\nfunction _assertThisInitialized(self) { if (self === void 0) { throw new ReferenceError(\"this hasn't been initialised - super() hasn't been called\"); } return self; }\n\nfunction _inheritsLoose(subClass, superClass) { subClass.prototype = Object.create(superClass.prototype); subClass.prototype.constructor = subClass; subClass.__proto__ = superClass; }\n\n/*!\n * GSAP 3.12.5\n * https://gsap.com\n *\n * @license Copyright 2008-2024, GreenSock. All rights reserved.\n * Subject to the terms at https://gsap.com/standard-license or for\n * Club GSAP members, the agreement issued with that membership.\n * @author: Jack Doyle, jack@greensock.com\n*/\n\n/* eslint-disable */\nvar _config = {\n  autoSleep: 120,\n  force3D: \"auto\",\n  nullTargetWarn: 1,\n  units: {\n    lineHeight: \"\"\n  }\n},\n    _defaults = {\n  duration: .5,\n  overwrite: false,\n  delay: 0\n},\n    _suppressOverwrites,\n    _reverting,\n    _context,\n    _bigNum = 1e8,\n    _tinyNum = 1 / _bigNum,\n    _2PI = Math.PI * 2,\n    _HALF_PI = _2PI / 4,\n    _gsID = 0,\n    _sqrt = Math.sqrt,\n    _cos = Math.cos,\n    _sin = Math.sin,\n    _isString = function _isString(value) {\n  return typeof value === \"string\";\n},\n    _isFunction = function _isFunction(value) {\n  return typeof value === \"function\";\n},\n    _isNumber = function _isNumber(value) {\n  return typeof value === \"number\";\n},\n    _isUndefined = function _isUndefined(value) {\n  return typeof value === \"undefined\";\n},\n    _isObject = function _isObject(value) {\n  return typeof value === \"object\";\n},\n    _isNotFalse = function _isNotFalse(value) {\n  return value !== false;\n},\n    _windowExists = function _windowExists() {\n  return typeof window !== \"undefined\";\n},\n    _isFuncOrString = function _isFuncOrString(value) {\n  return _isFunction(value) || _isString(value);\n},\n    _isTypedArray = typeof ArrayBuffer === \"function\" && ArrayBuffer.isView || function () {},\n    // note: IE10 has ArrayBuffer, but NOT ArrayBuffer.isView().\n_isArray = Array.isArray,\n    _strictNumExp = /(?:-?\\.?\\d|\\.)+/gi,\n    //only numbers (including negatives and decimals) but NOT relative values.\n_numExp = /[-+=.]*\\d+[.e\\-+]*\\d*[e\\-+]*\\d*/g,\n    //finds any numbers, including ones that start with += or -=, negative numbers, and ones in scientific notation like 1e-8.\n_numWithUnitExp = /[-+=.]*\\d+[.e-]*\\d*[a-z%]*/g,\n    _complexStringNumExp = /[-+=.]*\\d+\\.?\\d*(?:e-|e\\+)?\\d*/gi,\n    //duplicate so that while we're looping through matches from exec(), it doesn't contaminate the lastIndex of _numExp which we use to search for colors too.\n_relExp = /[+-]=-?[.\\d]+/,\n    _delimitedValueExp = /[^,'\"\\[\\]\\s]+/gi,\n    // previously /[#\\-+.]*\\b[a-z\\d\\-=+%.]+/gi but didn't catch special characters.\n_unitExp = /^[+\\-=e\\s\\d]*\\d+[.\\d]*([a-z]*|%)\\s*$/i,\n    _globalTimeline,\n    _win,\n    _coreInitted,\n    _doc,\n    _globals = {},\n    _installScope = {},\n    _coreReady,\n    _install = function _install(scope) {\n  return (_installScope = _merge(scope, _globals)) && gsap;\n},\n    _missingPlugin = function _missingPlugin(property, value) {\n  return console.warn(\"Invalid property\", property, \"set to\", value, \"Missing plugin? gsap.registerPlugin()\");\n},\n    _warn = function _warn(message, suppress) {\n  return !suppress && console.warn(message);\n},\n    _addGlobal = function _addGlobal(name, obj) {\n  return name && (_globals[name] = obj) && _installScope && (_installScope[name] = obj) || _globals;\n},\n    _emptyFunc = function _emptyFunc() {\n  return 0;\n},\n    _startAtRevertConfig = {\n  suppressEvents: true,\n  isStart: true,\n  kill: false\n},\n    _revertConfigNoKill = {\n  suppressEvents: true,\n  kill: false\n},\n    _revertConfig = {\n  suppressEvents: true\n},\n    _reservedProps = {},\n    _lazyTweens = [],\n    _lazyLookup = {},\n    _lastRenderedFrame,\n    _plugins = {},\n    _effects = {},\n    _nextGCFrame = 30,\n    _harnessPlugins = [],\n    _callbackNames = \"\",\n    _harness = function _harness(targets) {\n  var target = targets[0],\n      harnessPlugin,\n      i;\n  _isObject(target) || _isFunction(target) || (targets = [targets]);\n\n  if (!(harnessPlugin = (target._gsap || {}).harness)) {\n    // find the first target with a harness. We assume targets passed into an animation will be of similar type, meaning the same kind of harness can be used for them all (performance optimization)\n    i = _harnessPlugins.length;\n\n    while (i-- && !_harnessPlugins[i].targetTest(target)) {}\n\n    harnessPlugin = _harnessPlugins[i];\n  }\n\n  i = targets.length;\n\n  while (i--) {\n    targets[i] && (targets[i]._gsap || (targets[i]._gsap = new GSCache(targets[i], harnessPlugin))) || targets.splice(i, 1);\n  }\n\n  return targets;\n},\n    _getCache = function _getCache(target) {\n  return target._gsap || _harness(toArray(target))[0]._gsap;\n},\n    _getProperty = function _getProperty(target, property, v) {\n  return (v = target[property]) && _isFunction(v) ? target[property]() : _isUndefined(v) && target.getAttribute && target.getAttribute(property) || v;\n},\n    _forEachName = function _forEachName(names, func) {\n  return (names = names.split(\",\")).forEach(func) || names;\n},\n    //split a comma-delimited list of names into an array, then run a forEach() function and return the split array (this is just a way to consolidate/shorten some code).\n_round = function _round(value) {\n  return Math.round(value * 100000) / 100000 || 0;\n},\n    _roundPrecise = function _roundPrecise(value) {\n  return Math.round(value * 10000000) / 10000000 || 0;\n},\n    // increased precision mostly for timing values.\n_parseRelative = function _parseRelative(start, value) {\n  var operator = value.charAt(0),\n      end = parseFloat(value.substr(2));\n  start = parseFloat(start);\n  return operator === \"+\" ? start + end : operator === \"-\" ? start - end : operator === \"*\" ? start * end : start / end;\n},\n    _arrayContainsAny = function _arrayContainsAny(toSearch, toFind) {\n  //searches one array to find matches for any of the items in the toFind array. As soon as one is found, it returns true. It does NOT return all the matches; it's simply a boolean search.\n  var l = toFind.length,\n      i = 0;\n\n  for (; toSearch.indexOf(toFind[i]) < 0 && ++i < l;) {}\n\n  return i < l;\n},\n    _lazyRender = function _lazyRender() {\n  var l = _lazyTweens.length,\n      a = _lazyTweens.slice(0),\n      i,\n      tween;\n\n  _lazyLookup = {};\n  _lazyTweens.length = 0;\n\n  for (i = 0; i < l; i++) {\n    tween = a[i];\n    tween && tween._lazy && (tween.render(tween._lazy[0], tween._lazy[1], true)._lazy = 0);\n  }\n},\n    _lazySafeRender = function _lazySafeRender(animation, time, suppressEvents, force) {\n  _lazyTweens.length && !_reverting && _lazyRender();\n  animation.render(time, suppressEvents, force || _reverting && time < 0 && (animation._initted || animation._startAt));\n  _lazyTweens.length && !_reverting && _lazyRender(); //in case rendering caused any tweens to lazy-init, we should render them because typically when someone calls seek() or time() or progress(), they expect an immediate render.\n},\n    _numericIfPossible = function _numericIfPossible(value) {\n  var n = parseFloat(value);\n  return (n || n === 0) && (value + \"\").match(_delimitedValueExp).length < 2 ? n : _isString(value) ? value.trim() : value;\n},\n    _passThrough = function _passThrough(p) {\n  return p;\n},\n    _setDefaults = function _setDefaults(obj, defaults) {\n  for (var p in defaults) {\n    p in obj || (obj[p] = defaults[p]);\n  }\n\n  return obj;\n},\n    _setKeyframeDefaults = function _setKeyframeDefaults(excludeDuration) {\n  return function (obj, defaults) {\n    for (var p in defaults) {\n      p in obj || p === \"duration\" && excludeDuration || p === \"ease\" || (obj[p] = defaults[p]);\n    }\n  };\n},\n    _merge = function _merge(base, toMerge) {\n  for (var p in toMerge) {\n    base[p] = toMerge[p];\n  }\n\n  return base;\n},\n    _mergeDeep = function _mergeDeep(base, toMerge) {\n  for (var p in toMerge) {\n    p !== \"__proto__\" && p !== \"constructor\" && p !== \"prototype\" && (base[p] = _isObject(toMerge[p]) ? _mergeDeep(base[p] || (base[p] = {}), toMerge[p]) : toMerge[p]);\n  }\n\n  return base;\n},\n    _copyExcluding = function _copyExcluding(obj, excluding) {\n  var copy = {},\n      p;\n\n  for (p in obj) {\n    p in excluding || (copy[p] = obj[p]);\n  }\n\n  return copy;\n},\n    _inheritDefaults = function _inheritDefaults(vars) {\n  var parent = vars.parent || _globalTimeline,\n      func = vars.keyframes ? _setKeyframeDefaults(_isArray(vars.keyframes)) : _setDefaults;\n\n  if (_isNotFalse(vars.inherit)) {\n    while (parent) {\n      func(vars, parent.vars.defaults);\n      parent = parent.parent || parent._dp;\n    }\n  }\n\n  return vars;\n},\n    _arraysMatch = function _arraysMatch(a1, a2) {\n  var i = a1.length,\n      match = i === a2.length;\n\n  while (match && i-- && a1[i] === a2[i]) {}\n\n  return i < 0;\n},\n    _addLinkedListItem = function _addLinkedListItem(parent, child, firstProp, lastProp, sortBy) {\n  if (firstProp === void 0) {\n    firstProp = \"_first\";\n  }\n\n  if (lastProp === void 0) {\n    lastProp = \"_last\";\n  }\n\n  var prev = parent[lastProp],\n      t;\n\n  if (sortBy) {\n    t = child[sortBy];\n\n    while (prev && prev[sortBy] > t) {\n      prev = prev._prev;\n    }\n  }\n\n  if (prev) {\n    child._next = prev._next;\n    prev._next = child;\n  } else {\n    child._next = parent[firstProp];\n    parent[firstProp] = child;\n  }\n\n  if (child._next) {\n    child._next._prev = child;\n  } else {\n    parent[lastProp] = child;\n  }\n\n  child._prev = prev;\n  child.parent = child._dp = parent;\n  return child;\n},\n    _removeLinkedListItem = function _removeLinkedListItem(parent, child, firstProp, lastProp) {\n  if (firstProp === void 0) {\n    firstProp = \"_first\";\n  }\n\n  if (lastProp === void 0) {\n    lastProp = \"_last\";\n  }\n\n  var prev = child._prev,\n      next = child._next;\n\n  if (prev) {\n    prev._next = next;\n  } else if (parent[firstProp] === child) {\n    parent[firstProp] = next;\n  }\n\n  if (next) {\n    next._prev = prev;\n  } else if (parent[lastProp] === child) {\n    parent[lastProp] = prev;\n  }\n\n  child._next = child._prev = child.parent = null; // don't delete the _dp just so we can revert if necessary. But parent should be null to indicate the item isn't in a linked list.\n},\n    _removeFromParent = function _removeFromParent(child, onlyIfParentHasAutoRemove) {\n  child.parent && (!onlyIfParentHasAutoRemove || child.parent.autoRemoveChildren) && child.parent.remove && child.parent.remove(child);\n  child._act = 0;\n},\n    _uncache = function _uncache(animation, child) {\n  if (animation && (!child || child._end > animation._dur || child._start < 0)) {\n    // performance optimization: if a child animation is passed in we should only uncache if that child EXTENDS the animation (its end time is beyond the end)\n    var a = animation;\n\n    while (a) {\n      a._dirty = 1;\n      a = a.parent;\n    }\n  }\n\n  return animation;\n},\n    _recacheAncestors = function _recacheAncestors(animation) {\n  var parent = animation.parent;\n\n  while (parent && parent.parent) {\n    //sometimes we must force a re-sort of all children and update the duration/totalDuration of all ancestor timelines immediately in case, for example, in the middle of a render loop, one tween alters another tween's timeScale which shoves its startTime before 0, forcing the parent timeline to shift around and shiftChildren() which could affect that next tween's render (startTime). Doesn't matter for the root timeline though.\n    parent._dirty = 1;\n    parent.totalDuration();\n    parent = parent.parent;\n  }\n\n  return animation;\n},\n    _rewindStartAt = function _rewindStartAt(tween, totalTime, suppressEvents, force) {\n  return tween._startAt && (_reverting ? tween._startAt.revert(_revertConfigNoKill) : tween.vars.immediateRender && !tween.vars.autoRevert || tween._startAt.render(totalTime, true, force));\n},\n    _hasNoPausedAncestors = function _hasNoPausedAncestors(animation) {\n  return !animation || animation._ts && _hasNoPausedAncestors(animation.parent);\n},\n    _elapsedCycleDuration = function _elapsedCycleDuration(animation) {\n  return animation._repeat ? _animationCycle(animation._tTime, animation = animation.duration() + animation._rDelay) * animation : 0;\n},\n    // feed in the totalTime and cycleDuration and it'll return the cycle (iteration minus 1) and if the playhead is exactly at the very END, it will NOT bump up to the next cycle.\n_animationCycle = function _animationCycle(tTime, cycleDuration) {\n  var whole = Math.floor(tTime /= cycleDuration);\n  return tTime && whole === tTime ? whole - 1 : whole;\n},\n    _parentToChildTotalTime = function _parentToChildTotalTime(parentTime, child) {\n  return (parentTime - child._start) * child._ts + (child._ts >= 0 ? 0 : child._dirty ? child.totalDuration() : child._tDur);\n},\n    _setEnd = function _setEnd(animation) {\n  return animation._end = _roundPrecise(animation._start + (animation._tDur / Math.abs(animation._ts || animation._rts || _tinyNum) || 0));\n},\n    _alignPlayhead = function _alignPlayhead(animation, totalTime) {\n  // adjusts the animation's _start and _end according to the provided totalTime (only if the parent's smoothChildTiming is true and the animation isn't paused). It doesn't do any rendering or forcing things back into parent timelines, etc. - that's what totalTime() is for.\n  var parent = animation._dp;\n\n  if (parent && parent.smoothChildTiming && animation._ts) {\n    animation._start = _roundPrecise(parent._time - (animation._ts > 0 ? totalTime / animation._ts : ((animation._dirty ? animation.totalDuration() : animation._tDur) - totalTime) / -animation._ts));\n\n    _setEnd(animation);\n\n    parent._dirty || _uncache(parent, animation); //for performance improvement. If the parent's cache is already dirty, it already took care of marking the ancestors as dirty too, so skip the function call here.\n  }\n\n  return animation;\n},\n\n/*\n_totalTimeToTime = (clampedTotalTime, duration, repeat, repeatDelay, yoyo) => {\n\tlet cycleDuration = duration + repeatDelay,\n\t\ttime = _round(clampedTotalTime % cycleDuration);\n\tif (time > duration) {\n\t\ttime = duration;\n\t}\n\treturn (yoyo && (~~(clampedTotalTime / cycleDuration) & 1)) ? duration - time : time;\n},\n*/\n_postAddChecks = function _postAddChecks(timeline, child) {\n  var t;\n\n  if (child._time || !child._dur && child._initted || child._start < timeline._time && (child._dur || !child.add)) {\n    // in case, for example, the _start is moved on a tween that has already rendered, or if it's being inserted into a timeline BEFORE where the playhead is currently. Imagine it's at its end state, then the startTime is moved WAY later (after the end of this timeline), it should render at its beginning. Special case: if it's a timeline (has .add() method) and no duration, we can skip rendering because the user may be populating it AFTER adding it to a parent timeline (unconventional, but possible, and we wouldn't want it to get removed if the parent's autoRemoveChildren is true).\n    t = _parentToChildTotalTime(timeline.rawTime(), child);\n\n    if (!child._dur || _clamp(0, child.totalDuration(), t) - child._tTime > _tinyNum) {\n      child.render(t, true);\n    }\n  } //if the timeline has already ended but the inserted tween/timeline extends the duration, we should enable this timeline again so that it renders properly. We should also align the playhead with the parent timeline's when appropriate.\n\n\n  if (_uncache(timeline, child)._dp && timeline._initted && timeline._time >= timeline._dur && timeline._ts) {\n    //in case any of the ancestors had completed but should now be enabled...\n    if (timeline._dur < timeline.duration()) {\n      t = timeline;\n\n      while (t._dp) {\n        t.rawTime() >= 0 && t.totalTime(t._tTime); //moves the timeline (shifts its startTime) if necessary, and also enables it. If it's currently zero, though, it may not be scheduled to render until later so there's no need to force it to align with the current playhead position. Only move to catch up with the playhead.\n\n        t = t._dp;\n      }\n    }\n\n    timeline._zTime = -_tinyNum; // helps ensure that the next render() will be forced (crossingStart = true in render()), even if the duration hasn't changed (we're adding a child which would need to get rendered). Definitely an edge case. Note: we MUST do this AFTER the loop above where the totalTime() might trigger a render() because this _addToTimeline() method gets called from the Animation constructor, BEFORE tweens even record their targets, etc. so we wouldn't want things to get triggered in the wrong order.\n  }\n},\n    _addToTimeline = function _addToTimeline(timeline, child, position, skipChecks) {\n  child.parent && _removeFromParent(child);\n  child._start = _roundPrecise((_isNumber(position) ? position : position || timeline !== _globalTimeline ? _parsePosition(timeline, position, child) : timeline._time) + child._delay);\n  child._end = _roundPrecise(child._start + (child.totalDuration() / Math.abs(child.timeScale()) || 0));\n\n  _addLinkedListItem(timeline, child, \"_first\", \"_last\", timeline._sort ? \"_start\" : 0);\n\n  _isFromOrFromStart(child) || (timeline._recent = child);\n  skipChecks || _postAddChecks(timeline, child);\n  timeline._ts < 0 && _alignPlayhead(timeline, timeline._tTime); // if the timeline is reversed and the new child makes it longer, we may need to adjust the parent's _start (push it back)\n\n  return timeline;\n},\n    _scrollTrigger = function _scrollTrigger(animation, trigger) {\n  return (_globals.ScrollTrigger || _missingPlugin(\"scrollTrigger\", trigger)) && _globals.ScrollTrigger.create(trigger, animation);\n},\n    _attemptInitTween = function _attemptInitTween(tween, time, force, suppressEvents, tTime) {\n  _initTween(tween, time, tTime);\n\n  if (!tween._initted) {\n    return 1;\n  }\n\n  if (!force && tween._pt && !_reverting && (tween._dur && tween.vars.lazy !== false || !tween._dur && tween.vars.lazy) && _lastRenderedFrame !== _ticker.frame) {\n    _lazyTweens.push(tween);\n\n    tween._lazy = [tTime, suppressEvents];\n    return 1;\n  }\n},\n    _parentPlayheadIsBeforeStart = function _parentPlayheadIsBeforeStart(_ref) {\n  var parent = _ref.parent;\n  return parent && parent._ts && parent._initted && !parent._lock && (parent.rawTime() < 0 || _parentPlayheadIsBeforeStart(parent));\n},\n    // check parent's _lock because when a timeline repeats/yoyos and does its artificial wrapping, we shouldn't force the ratio back to 0\n_isFromOrFromStart = function _isFromOrFromStart(_ref2) {\n  var data = _ref2.data;\n  return data === \"isFromStart\" || data === \"isStart\";\n},\n    _renderZeroDurationTween = function _renderZeroDurationTween(tween, totalTime, suppressEvents, force) {\n  var prevRatio = tween.ratio,\n      ratio = totalTime < 0 || !totalTime && (!tween._start && _parentPlayheadIsBeforeStart(tween) && !(!tween._initted && _isFromOrFromStart(tween)) || (tween._ts < 0 || tween._dp._ts < 0) && !_isFromOrFromStart(tween)) ? 0 : 1,\n      // if the tween or its parent is reversed and the totalTime is 0, we should go to a ratio of 0. Edge case: if a from() or fromTo() stagger tween is placed later in a timeline, the \"startAt\" zero-duration tween could initially render at a time when the parent timeline's playhead is technically BEFORE where this tween is, so make sure that any \"from\" and \"fromTo\" startAt tweens are rendered the first time at a ratio of 1.\n  repeatDelay = tween._rDelay,\n      tTime = 0,\n      pt,\n      iteration,\n      prevIteration;\n\n  if (repeatDelay && tween._repeat) {\n    // in case there's a zero-duration tween that has a repeat with a repeatDelay\n    tTime = _clamp(0, tween._tDur, totalTime);\n    iteration = _animationCycle(tTime, repeatDelay);\n    tween._yoyo && iteration & 1 && (ratio = 1 - ratio);\n\n    if (iteration !== _animationCycle(tween._tTime, repeatDelay)) {\n      // if iteration changed\n      prevRatio = 1 - ratio;\n      tween.vars.repeatRefresh && tween._initted && tween.invalidate();\n    }\n  }\n\n  if (ratio !== prevRatio || _reverting || force || tween._zTime === _tinyNum || !totalTime && tween._zTime) {\n    if (!tween._initted && _attemptInitTween(tween, totalTime, force, suppressEvents, tTime)) {\n      // if we render the very beginning (time == 0) of a fromTo(), we must force the render (normal tweens wouldn't need to render at a time of 0 when the prevTime was also 0). This is also mandatory to make sure overwriting kicks in immediately.\n      return;\n    }\n\n    prevIteration = tween._zTime;\n    tween._zTime = totalTime || (suppressEvents ? _tinyNum : 0); // when the playhead arrives at EXACTLY time 0 (right on top) of a zero-duration tween, we need to discern if events are suppressed so that when the playhead moves again (next time), it'll trigger the callback. If events are NOT suppressed, obviously the callback would be triggered in this render. Basically, the callback should fire either when the playhead ARRIVES or LEAVES this exact spot, not both. Imagine doing a timeline.seek(0) and there's a callback that sits at 0. Since events are suppressed on that seek() by default, nothing will fire, but when the playhead moves off of that position, the callback should fire. This behavior is what people intuitively expect.\n\n    suppressEvents || (suppressEvents = totalTime && !prevIteration); // if it was rendered previously at exactly 0 (_zTime) and now the playhead is moving away, DON'T fire callbacks otherwise they'll seem like duplicates.\n\n    tween.ratio = ratio;\n    tween._from && (ratio = 1 - ratio);\n    tween._time = 0;\n    tween._tTime = tTime;\n    pt = tween._pt;\n\n    while (pt) {\n      pt.r(ratio, pt.d);\n      pt = pt._next;\n    }\n\n    totalTime < 0 && _rewindStartAt(tween, totalTime, suppressEvents, true);\n    tween._onUpdate && !suppressEvents && _callback(tween, \"onUpdate\");\n    tTime && tween._repeat && !suppressEvents && tween.parent && _callback(tween, \"onRepeat\");\n\n    if ((totalTime >= tween._tDur || totalTime < 0) && tween.ratio === ratio) {\n      ratio && _removeFromParent(tween, 1);\n\n      if (!suppressEvents && !_reverting) {\n        _callback(tween, ratio ? \"onComplete\" : \"onReverseComplete\", true);\n\n        tween._prom && tween._prom();\n      }\n    }\n  } else if (!tween._zTime) {\n    tween._zTime = totalTime;\n  }\n},\n    _findNextPauseTween = function _findNextPauseTween(animation, prevTime, time) {\n  var child;\n\n  if (time > prevTime) {\n    child = animation._first;\n\n    while (child && child._start <= time) {\n      if (child.data === \"isPause\" && child._start > prevTime) {\n        return child;\n      }\n\n      child = child._next;\n    }\n  } else {\n    child = animation._last;\n\n    while (child && child._start >= time) {\n      if (child.data === \"isPause\" && child._start < prevTime) {\n        return child;\n      }\n\n      child = child._prev;\n    }\n  }\n},\n    _setDuration = function _setDuration(animation, duration, skipUncache, leavePlayhead) {\n  var repeat = animation._repeat,\n      dur = _roundPrecise(duration) || 0,\n      totalProgress = animation._tTime / animation._tDur;\n  totalProgress && !leavePlayhead && (animation._time *= dur / animation._dur);\n  animation._dur = dur;\n  animation._tDur = !repeat ? dur : repeat < 0 ? 1e10 : _roundPrecise(dur * (repeat + 1) + animation._rDelay * repeat);\n  totalProgress > 0 && !leavePlayhead && _alignPlayhead(animation, animation._tTime = animation._tDur * totalProgress);\n  animation.parent && _setEnd(animation);\n  skipUncache || _uncache(animation.parent, animation);\n  return animation;\n},\n    _onUpdateTotalDuration = function _onUpdateTotalDuration(animation) {\n  return animation instanceof Timeline ? _uncache(animation) : _setDuration(animation, animation._dur);\n},\n    _zeroPosition = {\n  _start: 0,\n  endTime: _emptyFunc,\n  totalDuration: _emptyFunc\n},\n    _parsePosition = function _parsePosition(animation, position, percentAnimation) {\n  var labels = animation.labels,\n      recent = animation._recent || _zeroPosition,\n      clippedDuration = animation.duration() >= _bigNum ? recent.endTime(false) : animation._dur,\n      //in case there's a child that infinitely repeats, users almost never intend for the insertion point of a new child to be based on a SUPER long value like that so we clip it and assume the most recently-added child's endTime should be used instead.\n  i,\n      offset,\n      isPercent;\n\n  if (_isString(position) && (isNaN(position) || position in labels)) {\n    //if the string is a number like \"1\", check to see if there's a label with that name, otherwise interpret it as a number (absolute value).\n    offset = position.charAt(0);\n    isPercent = position.substr(-1) === \"%\";\n    i = position.indexOf(\"=\");\n\n    if (offset === \"<\" || offset === \">\") {\n      i >= 0 && (position = position.replace(/=/, \"\"));\n      return (offset === \"<\" ? recent._start : recent.endTime(recent._repeat >= 0)) + (parseFloat(position.substr(1)) || 0) * (isPercent ? (i < 0 ? recent : percentAnimation).totalDuration() / 100 : 1);\n    }\n\n    if (i < 0) {\n      position in labels || (labels[position] = clippedDuration);\n      return labels[position];\n    }\n\n    offset = parseFloat(position.charAt(i - 1) + position.substr(i + 1));\n\n    if (isPercent && percentAnimation) {\n      offset = offset / 100 * (_isArray(percentAnimation) ? percentAnimation[0] : percentAnimation).totalDuration();\n    }\n\n    return i > 1 ? _parsePosition(animation, position.substr(0, i - 1), percentAnimation) + offset : clippedDuration + offset;\n  }\n\n  return position == null ? clippedDuration : +position;\n},\n    _createTweenType = function _createTweenType(type, params, timeline) {\n  var isLegacy = _isNumber(params[1]),\n      varsIndex = (isLegacy ? 2 : 1) + (type < 2 ? 0 : 1),\n      vars = params[varsIndex],\n      irVars,\n      parent;\n\n  isLegacy && (vars.duration = params[1]);\n  vars.parent = timeline;\n\n  if (type) {\n    irVars = vars;\n    parent = timeline;\n\n    while (parent && !(\"immediateRender\" in irVars)) {\n      // inheritance hasn't happened yet, but someone may have set a default in an ancestor timeline. We could do vars.immediateRender = _isNotFalse(_inheritDefaults(vars).immediateRender) but that'd exact a slight performance penalty because _inheritDefaults() also runs in the Tween constructor. We're paying a small kb price here to gain speed.\n      irVars = parent.vars.defaults || {};\n      parent = _isNotFalse(parent.vars.inherit) && parent.parent;\n    }\n\n    vars.immediateRender = _isNotFalse(irVars.immediateRender);\n    type < 2 ? vars.runBackwards = 1 : vars.startAt = params[varsIndex - 1]; // \"from\" vars\n  }\n\n  return new Tween(params[0], vars, params[varsIndex + 1]);\n},\n    _conditionalReturn = function _conditionalReturn(value, func) {\n  return value || value === 0 ? func(value) : func;\n},\n    _clamp = function _clamp(min, max, value) {\n  return value < min ? min : value > max ? max : value;\n},\n    getUnit = function getUnit(value, v) {\n  return !_isString(value) || !(v = _unitExp.exec(value)) ? \"\" : v[1];\n},\n    // note: protect against padded numbers as strings, like \"100.100\". That shouldn't return \"00\" as the unit. If it's numeric, return no unit.\nclamp = function clamp(min, max, value) {\n  return _conditionalReturn(value, function (v) {\n    return _clamp(min, max, v);\n  });\n},\n    _slice = [].slice,\n    _isArrayLike = function _isArrayLike(value, nonEmpty) {\n  return value && _isObject(value) && \"length\" in value && (!nonEmpty && !value.length || value.length - 1 in value && _isObject(value[0])) && !value.nodeType && value !== _win;\n},\n    _flatten = function _flatten(ar, leaveStrings, accumulator) {\n  if (accumulator === void 0) {\n    accumulator = [];\n  }\n\n  return ar.forEach(function (value) {\n    var _accumulator;\n\n    return _isString(value) && !leaveStrings || _isArrayLike(value, 1) ? (_accumulator = accumulator).push.apply(_accumulator, toArray(value)) : accumulator.push(value);\n  }) || accumulator;\n},\n    //takes any value and returns an array. If it's a string (and leaveStrings isn't true), it'll use document.querySelectorAll() and convert that to an array. It'll also accept iterables like jQuery objects.\ntoArray = function toArray(value, scope, leaveStrings) {\n  return _context && !scope && _context.selector ? _context.selector(value) : _isString(value) && !leaveStrings && (_coreInitted || !_wake()) ? _slice.call((scope || _doc).querySelectorAll(value), 0) : _isArray(value) ? _flatten(value, leaveStrings) : _isArrayLike(value) ? _slice.call(value, 0) : value ? [value] : [];\n},\n    selector = function selector(value) {\n  value = toArray(value)[0] || _warn(\"Invalid scope\") || {};\n  return function (v) {\n    var el = value.current || value.nativeElement || value;\n    return toArray(v, el.querySelectorAll ? el : el === value ? _warn(\"Invalid scope\") || _doc.createElement(\"div\") : value);\n  };\n},\n    shuffle = function shuffle(a) {\n  return a.sort(function () {\n    return .5 - Math.random();\n  });\n},\n    // alternative that's a bit faster and more reliably diverse but bigger:   for (let j, v, i = a.length; i; j = Math.floor(Math.random() * i), v = a[--i], a[i] = a[j], a[j] = v); return a;\n//for distributing values across an array. Can accept a number, a function or (most commonly) a function which can contain the following properties: {base, amount, from, ease, grid, axis, length, each}. Returns a function that expects the following parameters: index, target, array. Recognizes the following\ndistribute = function distribute(v) {\n  if (_isFunction(v)) {\n    return v;\n  }\n\n  var vars = _isObject(v) ? v : {\n    each: v\n  },\n      //n:1 is just to indicate v was a number; we leverage that later to set v according to the length we get. If a number is passed in, we treat it like the old stagger value where 0.1, for example, would mean that things would be distributed with 0.1 between each element in the array rather than a total \"amount\" that's chunked out among them all.\n  ease = _parseEase(vars.ease),\n      from = vars.from || 0,\n      base = parseFloat(vars.base) || 0,\n      cache = {},\n      isDecimal = from > 0 && from < 1,\n      ratios = isNaN(from) || isDecimal,\n      axis = vars.axis,\n      ratioX = from,\n      ratioY = from;\n\n  if (_isString(from)) {\n    ratioX = ratioY = {\n      center: .5,\n      edges: .5,\n      end: 1\n    }[from] || 0;\n  } else if (!isDecimal && ratios) {\n    ratioX = from[0];\n    ratioY = from[1];\n  }\n\n  return function (i, target, a) {\n    var l = (a || vars).length,\n        distances = cache[l],\n        originX,\n        originY,\n        x,\n        y,\n        d,\n        j,\n        max,\n        min,\n        wrapAt;\n\n    if (!distances) {\n      wrapAt = vars.grid === \"auto\" ? 0 : (vars.grid || [1, _bigNum])[1];\n\n      if (!wrapAt) {\n        max = -_bigNum;\n\n        while (max < (max = a[wrapAt++].getBoundingClientRect().left) && wrapAt < l) {}\n\n        wrapAt < l && wrapAt--;\n      }\n\n      distances = cache[l] = [];\n      originX = ratios ? Math.min(wrapAt, l) * ratioX - .5 : from % wrapAt;\n      originY = wrapAt === _bigNum ? 0 : ratios ? l * ratioY / wrapAt - .5 : from / wrapAt | 0;\n      max = 0;\n      min = _bigNum;\n\n      for (j = 0; j < l; j++) {\n        x = j % wrapAt - originX;\n        y = originY - (j / wrapAt | 0);\n        distances[j] = d = !axis ? _sqrt(x * x + y * y) : Math.abs(axis === \"y\" ? y : x);\n        d > max && (max = d);\n        d < min && (min = d);\n      }\n\n      from === \"random\" && shuffle(distances);\n      distances.max = max - min;\n      distances.min = min;\n      distances.v = l = (parseFloat(vars.amount) || parseFloat(vars.each) * (wrapAt > l ? l - 1 : !axis ? Math.max(wrapAt, l / wrapAt) : axis === \"y\" ? l / wrapAt : wrapAt) || 0) * (from === \"edges\" ? -1 : 1);\n      distances.b = l < 0 ? base - l : base;\n      distances.u = getUnit(vars.amount || vars.each) || 0; //unit\n\n      ease = ease && l < 0 ? _invertEase(ease) : ease;\n    }\n\n    l = (distances[i] - distances.min) / distances.max || 0;\n    return _roundPrecise(distances.b + (ease ? ease(l) : l) * distances.v) + distances.u; //round in order to work around floating point errors\n  };\n},\n    _roundModifier = function _roundModifier(v) {\n  //pass in 0.1 get a function that'll round to the nearest tenth, or 5 to round to the closest 5, or 0.001 to the closest 1000th, etc.\n  var p = Math.pow(10, ((v + \"\").split(\".\")[1] || \"\").length); //to avoid floating point math errors (like 24 * 0.1 == 2.4000000000000004), we chop off at a specific number of decimal places (much faster than toFixed())\n\n  return function (raw) {\n    var n = _roundPrecise(Math.round(parseFloat(raw) / v) * v * p);\n\n    return (n - n % 1) / p + (_isNumber(raw) ? 0 : getUnit(raw)); // n - n % 1 replaces Math.floor() in order to handle negative values properly. For example, Math.floor(-150.00000000000003) is 151!\n  };\n},\n    snap = function snap(snapTo, value) {\n  var isArray = _isArray(snapTo),\n      radius,\n      is2D;\n\n  if (!isArray && _isObject(snapTo)) {\n    radius = isArray = snapTo.radius || _bigNum;\n\n    if (snapTo.values) {\n      snapTo = toArray(snapTo.values);\n\n      if (is2D = !_isNumber(snapTo[0])) {\n        radius *= radius; //performance optimization so we don't have to Math.sqrt() in the loop.\n      }\n    } else {\n      snapTo = _roundModifier(snapTo.increment);\n    }\n  }\n\n  return _conditionalReturn(value, !isArray ? _roundModifier(snapTo) : _isFunction(snapTo) ? function (raw) {\n    is2D = snapTo(raw);\n    return Math.abs(is2D - raw) <= radius ? is2D : raw;\n  } : function (raw) {\n    var x = parseFloat(is2D ? raw.x : raw),\n        y = parseFloat(is2D ? raw.y : 0),\n        min = _bigNum,\n        closest = 0,\n        i = snapTo.length,\n        dx,\n        dy;\n\n    while (i--) {\n      if (is2D) {\n        dx = snapTo[i].x - x;\n        dy = snapTo[i].y - y;\n        dx = dx * dx + dy * dy;\n      } else {\n        dx = Math.abs(snapTo[i] - x);\n      }\n\n      if (dx < min) {\n        min = dx;\n        closest = i;\n      }\n    }\n\n    closest = !radius || min <= radius ? snapTo[closest] : raw;\n    return is2D || closest === raw || _isNumber(raw) ? closest : closest + getUnit(raw);\n  });\n},\n    random = function random(min, max, roundingIncrement, returnFunction) {\n  return _conditionalReturn(_isArray(min) ? !max : roundingIncrement === true ? !!(roundingIncrement = 0) : !returnFunction, function () {\n    return _isArray(min) ? min[~~(Math.random() * min.length)] : (roundingIncrement = roundingIncrement || 1e-5) && (returnFunction = roundingIncrement < 1 ? Math.pow(10, (roundingIncrement + \"\").length - 2) : 1) && Math.floor(Math.round((min - roundingIncrement / 2 + Math.random() * (max - min + roundingIncrement * .99)) / roundingIncrement) * roundingIncrement * returnFunction) / returnFunction;\n  });\n},\n    pipe = function pipe() {\n  for (var _len = arguments.length, functions = new Array(_len), _key = 0; _key < _len; _key++) {\n    functions[_key] = arguments[_key];\n  }\n\n  return function (value) {\n    return functions.reduce(function (v, f) {\n      return f(v);\n    }, value);\n  };\n},\n    unitize = function unitize(func, unit) {\n  return function (value) {\n    return func(parseFloat(value)) + (unit || getUnit(value));\n  };\n},\n    normalize = function normalize(min, max, value) {\n  return mapRange(min, max, 0, 1, value);\n},\n    _wrapArray = function _wrapArray(a, wrapper, value) {\n  return _conditionalReturn(value, function (index) {\n    return a[~~wrapper(index)];\n  });\n},\n    wrap = function wrap(min, max, value) {\n  // NOTE: wrap() CANNOT be an arrow function! A very odd compiling bug causes problems (unrelated to GSAP).\n  var range = max - min;\n  return _isArray(min) ? _wrapArray(min, wrap(0, min.length), max) : _conditionalReturn(value, function (value) {\n    return (range + (value - min) % range) % range + min;\n  });\n},\n    wrapYoyo = function wrapYoyo(min, max, value) {\n  var range = max - min,\n      total = range * 2;\n  return _isArray(min) ? _wrapArray(min, wrapYoyo(0, min.length - 1), max) : _conditionalReturn(value, function (value) {\n    value = (total + (value - min) % total) % total || 0;\n    return min + (value > range ? total - value : value);\n  });\n},\n    _replaceRandom = function _replaceRandom(value) {\n  //replaces all occurrences of random(...) in a string with the calculated random value. can be a range like random(-100, 100, 5) or an array like random([0, 100, 500])\n  var prev = 0,\n      s = \"\",\n      i,\n      nums,\n      end,\n      isArray;\n\n  while (~(i = value.indexOf(\"random(\", prev))) {\n    end = value.indexOf(\")\", i);\n    isArray = value.charAt(i + 7) === \"[\";\n    nums = value.substr(i + 7, end - i - 7).match(isArray ? _delimitedValueExp : _strictNumExp);\n    s += value.substr(prev, i - prev) + random(isArray ? nums : +nums[0], isArray ? 0 : +nums[1], +nums[2] || 1e-5);\n    prev = end + 1;\n  }\n\n  return s + value.substr(prev, value.length - prev);\n},\n    mapRange = function mapRange(inMin, inMax, outMin, outMax, value) {\n  var inRange = inMax - inMin,\n      outRange = outMax - outMin;\n  return _conditionalReturn(value, function (value) {\n    return outMin + ((value - inMin) / inRange * outRange || 0);\n  });\n},\n    interpolate = function interpolate(start, end, progress, mutate) {\n  var func = isNaN(start + end) ? 0 : function (p) {\n    return (1 - p) * start + p * end;\n  };\n\n  if (!func) {\n    var isString = _isString(start),\n        master = {},\n        p,\n        i,\n        interpolators,\n        l,\n        il;\n\n    progress === true && (mutate = 1) && (progress = null);\n\n    if (isString) {\n      start = {\n        p: start\n      };\n      end = {\n        p: end\n      };\n    } else if (_isArray(start) && !_isArray(end)) {\n      interpolators = [];\n      l = start.length;\n      il = l - 2;\n\n      for (i = 1; i < l; i++) {\n        interpolators.push(interpolate(start[i - 1], start[i])); //build the interpolators up front as a performance optimization so that when the function is called many times, it can just reuse them.\n      }\n\n      l--;\n\n      func = function func(p) {\n        p *= l;\n        var i = Math.min(il, ~~p);\n        return interpolators[i](p - i);\n      };\n\n      progress = end;\n    } else if (!mutate) {\n      start = _merge(_isArray(start) ? [] : {}, start);\n    }\n\n    if (!interpolators) {\n      for (p in end) {\n        _addPropTween.call(master, start, p, \"get\", end[p]);\n      }\n\n      func = function func(p) {\n        return _renderPropTweens(p, master) || (isString ? start.p : start);\n      };\n    }\n  }\n\n  return _conditionalReturn(progress, func);\n},\n    _getLabelInDirection = function _getLabelInDirection(timeline, fromTime, backward) {\n  //used for nextLabel() and previousLabel()\n  var labels = timeline.labels,\n      min = _bigNum,\n      p,\n      distance,\n      label;\n\n  for (p in labels) {\n    distance = labels[p] - fromTime;\n\n    if (distance < 0 === !!backward && distance && min > (distance = Math.abs(distance))) {\n      label = p;\n      min = distance;\n    }\n  }\n\n  return label;\n},\n    _callback = function _callback(animation, type, executeLazyFirst) {\n  var v = animation.vars,\n      callback = v[type],\n      prevContext = _context,\n      context = animation._ctx,\n      params,\n      scope,\n      result;\n\n  if (!callback) {\n    return;\n  }\n\n  params = v[type + \"Params\"];\n  scope = v.callbackScope || animation;\n  executeLazyFirst && _lazyTweens.length && _lazyRender(); //in case rendering caused any tweens to lazy-init, we should render them because typically when a timeline finishes, users expect things to have rendered fully. Imagine an onUpdate on a timeline that reports/checks tweened values.\n\n  context && (_context = context);\n  result = params ? callback.apply(scope, params) : callback.call(scope);\n  _context = prevContext;\n  return result;\n},\n    _interrupt = function _interrupt(animation) {\n  _removeFromParent(animation);\n\n  animation.scrollTrigger && animation.scrollTrigger.kill(!!_reverting);\n  animation.progress() < 1 && _callback(animation, \"onInterrupt\");\n  return animation;\n},\n    _quickTween,\n    _registerPluginQueue = [],\n    _createPlugin = function _createPlugin(config) {\n  if (!config) return;\n  config = !config.name && config[\"default\"] || config; // UMD packaging wraps things oddly, so for example MotionPathHelper becomes {MotionPathHelper:MotionPathHelper, default:MotionPathHelper}.\n\n  if (_windowExists() || config.headless) {\n    // edge case: some build tools may pass in a null/undefined value\n    var name = config.name,\n        isFunc = _isFunction(config),\n        Plugin = name && !isFunc && config.init ? function () {\n      this._props = [];\n    } : config,\n        //in case someone passes in an object that's not a plugin, like CustomEase\n    instanceDefaults = {\n      init: _emptyFunc,\n      render: _renderPropTweens,\n      add: _addPropTween,\n      kill: _killPropTweensOf,\n      modifier: _addPluginModifier,\n      rawVars: 0\n    },\n        statics = {\n      targetTest: 0,\n      get: 0,\n      getSetter: _getSetter,\n      aliases: {},\n      register: 0\n    };\n\n    _wake();\n\n    if (config !== Plugin) {\n      if (_plugins[name]) {\n        return;\n      }\n\n      _setDefaults(Plugin, _setDefaults(_copyExcluding(config, instanceDefaults), statics)); //static methods\n\n\n      _merge(Plugin.prototype, _merge(instanceDefaults, _copyExcluding(config, statics))); //instance methods\n\n\n      _plugins[Plugin.prop = name] = Plugin;\n\n      if (config.targetTest) {\n        _harnessPlugins.push(Plugin);\n\n        _reservedProps[name] = 1;\n      }\n\n      name = (name === \"css\" ? \"CSS\" : name.charAt(0).toUpperCase() + name.substr(1)) + \"Plugin\"; //for the global name. \"motionPath\" should become MotionPathPlugin\n    }\n\n    _addGlobal(name, Plugin);\n\n    config.register && config.register(gsap, Plugin, PropTween);\n  } else {\n    _registerPluginQueue.push(config);\n  }\n},\n\n/*\n * --------------------------------------------------------------------------------------\n * COLORS\n * --------------------------------------------------------------------------------------\n */\n_255 = 255,\n    _colorLookup = {\n  aqua: [0, _255, _255],\n  lime: [0, _255, 0],\n  silver: [192, 192, 192],\n  black: [0, 0, 0],\n  maroon: [128, 0, 0],\n  teal: [0, 128, 128],\n  blue: [0, 0, _255],\n  navy: [0, 0, 128],\n  white: [_255, _255, _255],\n  olive: [128, 128, 0],\n  yellow: [_255, _255, 0],\n  orange: [_255, 165, 0],\n  gray: [128, 128, 128],\n  purple: [128, 0, 128],\n  green: [0, 128, 0],\n  red: [_255, 0, 0],\n  pink: [_255, 192, 203],\n  cyan: [0, _255, _255],\n  transparent: [_255, _255, _255, 0]\n},\n    // possible future idea to replace the hard-coded color name values - put this in the ticker.wake() where we set the _doc:\n// let ctx = _doc.createElement(\"canvas\").getContext(\"2d\");\n// _forEachName(\"aqua,lime,silver,black,maroon,teal,blue,navy,white,olive,yellow,orange,gray,purple,green,red,pink,cyan\", color => {ctx.fillStyle = color; _colorLookup[color] = splitColor(ctx.fillStyle)});\n_hue = function _hue(h, m1, m2) {\n  h += h < 0 ? 1 : h > 1 ? -1 : 0;\n  return (h * 6 < 1 ? m1 + (m2 - m1) * h * 6 : h < .5 ? m2 : h * 3 < 2 ? m1 + (m2 - m1) * (2 / 3 - h) * 6 : m1) * _255 + .5 | 0;\n},\n    splitColor = function splitColor(v, toHSL, forceAlpha) {\n  var a = !v ? _colorLookup.black : _isNumber(v) ? [v >> 16, v >> 8 & _255, v & _255] : 0,\n      r,\n      g,\n      b,\n      h,\n      s,\n      l,\n      max,\n      min,\n      d,\n      wasHSL;\n\n  if (!a) {\n    if (v.substr(-1) === \",\") {\n      //sometimes a trailing comma is included and we should chop it off (typically from a comma-delimited list of values like a textShadow:\"2px 2px 2px blue, 5px 5px 5px rgb(255,0,0)\" - in this example \"blue,\" has a trailing comma. We could strip it out inside parseComplex() but we'd need to do it to the beginning and ending values plus it wouldn't provide protection from other potential scenarios like if the user passes in a similar value.\n      v = v.substr(0, v.length - 1);\n    }\n\n    if (_colorLookup[v]) {\n      a = _colorLookup[v];\n    } else if (v.charAt(0) === \"#\") {\n      if (v.length < 6) {\n        //for shorthand like #9F0 or #9F0F (could have alpha)\n        r = v.charAt(1);\n        g = v.charAt(2);\n        b = v.charAt(3);\n        v = \"#\" + r + r + g + g + b + b + (v.length === 5 ? v.charAt(4) + v.charAt(4) : \"\");\n      }\n\n      if (v.length === 9) {\n        // hex with alpha, like #fd5e53ff\n        a = parseInt(v.substr(1, 6), 16);\n        return [a >> 16, a >> 8 & _255, a & _255, parseInt(v.substr(7), 16) / 255];\n      }\n\n      v = parseInt(v.substr(1), 16);\n      a = [v >> 16, v >> 8 & _255, v & _255];\n    } else if (v.substr(0, 3) === \"hsl\") {\n      a = wasHSL = v.match(_strictNumExp);\n\n      if (!toHSL) {\n        h = +a[0] % 360 / 360;\n        s = +a[1] / 100;\n        l = +a[2] / 100;\n        g = l <= .5 ? l * (s + 1) : l + s - l * s;\n        r = l * 2 - g;\n        a.length > 3 && (a[3] *= 1); //cast as number\n\n        a[0] = _hue(h + 1 / 3, r, g);\n        a[1] = _hue(h, r, g);\n        a[2] = _hue(h - 1 / 3, r, g);\n      } else if (~v.indexOf(\"=\")) {\n        //if relative values are found, just return the raw strings with the relative prefixes in place.\n        a = v.match(_numExp);\n        forceAlpha && a.length < 4 && (a[3] = 1);\n        return a;\n      }\n    } else {\n      a = v.match(_strictNumExp) || _colorLookup.transparent;\n    }\n\n    a = a.map(Number);\n  }\n\n  if (toHSL && !wasHSL) {\n    r = a[0] / _255;\n    g = a[1] / _255;\n    b = a[2] / _255;\n    max = Math.max(r, g, b);\n    min = Math.min(r, g, b);\n    l = (max + min) / 2;\n\n    if (max === min) {\n      h = s = 0;\n    } else {\n      d = max - min;\n      s = l > 0.5 ? d / (2 - max - min) : d / (max + min);\n      h = max === r ? (g - b) / d + (g < b ? 6 : 0) : max === g ? (b - r) / d + 2 : (r - g) / d + 4;\n      h *= 60;\n    }\n\n    a[0] = ~~(h + .5);\n    a[1] = ~~(s * 100 + .5);\n    a[2] = ~~(l * 100 + .5);\n  }\n\n  forceAlpha && a.length < 4 && (a[3] = 1);\n  return a;\n},\n    _colorOrderData = function _colorOrderData(v) {\n  // strips out the colors from the string, finds all the numeric slots (with units) and returns an array of those. The Array also has a \"c\" property which is an Array of the index values where the colors belong. This is to help work around issues where there's a mis-matched order of color/numeric data like drop-shadow(#f00 0px 1px 2px) and drop-shadow(0x 1px 2px #f00). This is basically a helper function used in _formatColors()\n  var values = [],\n      c = [],\n      i = -1;\n  v.split(_colorExp).forEach(function (v) {\n    var a = v.match(_numWithUnitExp) || [];\n    values.push.apply(values, a);\n    c.push(i += a.length + 1);\n  });\n  values.c = c;\n  return values;\n},\n    _formatColors = function _formatColors(s, toHSL, orderMatchData) {\n  var result = \"\",\n      colors = (s + result).match(_colorExp),\n      type = toHSL ? \"hsla(\" : \"rgba(\",\n      i = 0,\n      c,\n      shell,\n      d,\n      l;\n\n  if (!colors) {\n    return s;\n  }\n\n  colors = colors.map(function (color) {\n    return (color = splitColor(color, toHSL, 1)) && type + (toHSL ? color[0] + \",\" + color[1] + \"%,\" + color[2] + \"%,\" + color[3] : color.join(\",\")) + \")\";\n  });\n\n  if (orderMatchData) {\n    d = _colorOrderData(s);\n    c = orderMatchData.c;\n\n    if (c.join(result) !== d.c.join(result)) {\n      shell = s.replace(_colorExp, \"1\").split(_numWithUnitExp);\n      l = shell.length - 1;\n\n      for (; i < l; i++) {\n        result += shell[i] + (~c.indexOf(i) ? colors.shift() || type + \"0,0,0,0)\" : (d.length ? d : colors.length ? colors : orderMatchData).shift());\n      }\n    }\n  }\n\n  if (!shell) {\n    shell = s.split(_colorExp);\n    l = shell.length - 1;\n\n    for (; i < l; i++) {\n      result += shell[i] + colors[i];\n    }\n  }\n\n  return result + shell[l];\n},\n    _colorExp = function () {\n  var s = \"(?:\\\\b(?:(?:rgb|rgba|hsl|hsla)\\\\(.+?\\\\))|\\\\B#(?:[0-9a-f]{3,4}){1,2}\\\\b\",\n      //we'll dynamically build this Regular Expression to conserve file size. After building it, it will be able to find rgb(), rgba(), # (hexadecimal), and named color values like red, blue, purple, etc.,\n  p;\n\n  for (p in _colorLookup) {\n    s += \"|\" + p + \"\\\\b\";\n  }\n\n  return new RegExp(s + \")\", \"gi\");\n}(),\n    _hslExp = /hsl[a]?\\(/,\n    _colorStringFilter = function _colorStringFilter(a) {\n  var combined = a.join(\" \"),\n      toHSL;\n  _colorExp.lastIndex = 0;\n\n  if (_colorExp.test(combined)) {\n    toHSL = _hslExp.test(combined);\n    a[1] = _formatColors(a[1], toHSL);\n    a[0] = _formatColors(a[0], toHSL, _colorOrderData(a[1])); // make sure the order of numbers/colors match with the END value.\n\n    return true;\n  }\n},\n\n/*\n * --------------------------------------------------------------------------------------\n * TICKER\n * --------------------------------------------------------------------------------------\n */\n_tickerActive,\n    _ticker = function () {\n  var _getTime = Date.now,\n      _lagThreshold = 500,\n      _adjustedLag = 33,\n      _startTime = _getTime(),\n      _lastUpdate = _startTime,\n      _gap = 1000 / 240,\n      _nextTime = _gap,\n      _listeners = [],\n      _id,\n      _req,\n      _raf,\n      _self,\n      _delta,\n      _i,\n      _tick = function _tick(v) {\n    var elapsed = _getTime() - _lastUpdate,\n        manual = v === true,\n        overlap,\n        dispatch,\n        time,\n        frame;\n\n    (elapsed > _lagThreshold || elapsed < 0) && (_startTime += elapsed - _adjustedLag);\n    _lastUpdate += elapsed;\n    time = _lastUpdate - _startTime;\n    overlap = time - _nextTime;\n\n    if (overlap > 0 || manual) {\n      frame = ++_self.frame;\n      _delta = time - _self.time * 1000;\n      _self.time = time = time / 1000;\n      _nextTime += overlap + (overlap >= _gap ? 4 : _gap - overlap);\n      dispatch = 1;\n    }\n\n    manual || (_id = _req(_tick)); //make sure the request is made before we dispatch the \"tick\" event so that timing is maintained. Otherwise, if processing the \"tick\" requires a bunch of time (like 15ms) and we're using a setTimeout() that's based on 16.7ms, it'd technically take 31.7ms between frames otherwise.\n\n    if (dispatch) {\n      for (_i = 0; _i < _listeners.length; _i++) {\n        // use _i and check _listeners.length instead of a variable because a listener could get removed during the loop, and if that happens to an element less than the current index, it'd throw things off in the loop.\n        _listeners[_i](time, _delta, frame, v);\n      }\n    }\n  };\n\n  _self = {\n    time: 0,\n    frame: 0,\n    tick: function tick() {\n      _tick(true);\n    },\n    deltaRatio: function deltaRatio(fps) {\n      return _delta / (1000 / (fps || 60));\n    },\n    wake: function wake() {\n      if (_coreReady) {\n        if (!_coreInitted && _windowExists()) {\n          _win = _coreInitted = window;\n          _doc = _win.document || {};\n          _globals.gsap = gsap;\n          (_win.gsapVersions || (_win.gsapVersions = [])).push(gsap.version);\n\n          _install(_installScope || _win.GreenSockGlobals || !_win.gsap && _win || {});\n\n          _registerPluginQueue.forEach(_createPlugin);\n        }\n\n        _raf = typeof requestAnimationFrame !== \"undefined\" && requestAnimationFrame;\n        _id && _self.sleep();\n\n        _req = _raf || function (f) {\n          return setTimeout(f, _nextTime - _self.time * 1000 + 1 | 0);\n        };\n\n        _tickerActive = 1;\n\n        _tick(2);\n      }\n    },\n    sleep: function sleep() {\n      (_raf ? cancelAnimationFrame : clearTimeout)(_id);\n      _tickerActive = 0;\n      _req = _emptyFunc;\n    },\n    lagSmoothing: function lagSmoothing(threshold, adjustedLag) {\n      _lagThreshold = threshold || Infinity; // zero should be interpreted as basically unlimited\n\n      _adjustedLag = Math.min(adjustedLag || 33, _lagThreshold);\n    },\n    fps: function fps(_fps) {\n      _gap = 1000 / (_fps || 240);\n      _nextTime = _self.time * 1000 + _gap;\n    },\n    add: function add(callback, once, prioritize) {\n      var func = once ? function (t, d, f, v) {\n        callback(t, d, f, v);\n\n        _self.remove(func);\n      } : callback;\n\n      _self.remove(callback);\n\n      _listeners[prioritize ? \"unshift\" : \"push\"](func);\n\n      _wake();\n\n      return func;\n    },\n    remove: function remove(callback, i) {\n      ~(i = _listeners.indexOf(callback)) && _listeners.splice(i, 1) && _i >= i && _i--;\n    },\n    _listeners: _listeners\n  };\n  return _self;\n}(),\n    _wake = function _wake() {\n  return !_tickerActive && _ticker.wake();\n},\n    //also ensures the core classes are initialized.\n\n/*\n* -------------------------------------------------\n* EASING\n* -------------------------------------------------\n*/\n_easeMap = {},\n    _customEaseExp = /^[\\d.\\-M][\\d.\\-,\\s]/,\n    _quotesExp = /[\"']/g,\n    _parseObjectInString = function _parseObjectInString(value) {\n  //takes a string like \"{wiggles:10, type:anticipate})\" and turns it into a real object. Notice it ends in \")\" and includes the {} wrappers. This is because we only use this function for parsing ease configs and prioritized optimization rather than reusability.\n  var obj = {},\n      split = value.substr(1, value.length - 3).split(\":\"),\n      key = split[0],\n      i = 1,\n      l = split.length,\n      index,\n      val,\n      parsedVal;\n\n  for (; i < l; i++) {\n    val = split[i];\n    index = i !== l - 1 ? val.lastIndexOf(\",\") : val.length;\n    parsedVal = val.substr(0, index);\n    obj[key] = isNaN(parsedVal) ? parsedVal.replace(_quotesExp, \"\").trim() : +parsedVal;\n    key = val.substr(index + 1).trim();\n  }\n\n  return obj;\n},\n    _valueInParentheses = function _valueInParentheses(value) {\n  var open = value.indexOf(\"(\") + 1,\n      close = value.indexOf(\")\"),\n      nested = value.indexOf(\"(\", open);\n  return value.substring(open, ~nested && nested < close ? value.indexOf(\")\", close + 1) : close);\n},\n    _configEaseFromString = function _configEaseFromString(name) {\n  //name can be a string like \"elastic.out(1,0.5)\", and pass in _easeMap as obj and it'll parse it out and call the actual function like _easeMap.Elastic.easeOut.config(1,0.5). It will also parse custom ease strings as long as CustomEase is loaded and registered (internally as _easeMap._CE).\n  var split = (name + \"\").split(\"(\"),\n      ease = _easeMap[split[0]];\n  return ease && split.length > 1 && ease.config ? ease.config.apply(null, ~name.indexOf(\"{\") ? [_parseObjectInString(split[1])] : _valueInParentheses(name).split(\",\").map(_numericIfPossible)) : _easeMap._CE && _customEaseExp.test(name) ? _easeMap._CE(\"\", name) : ease;\n},\n    _invertEase = function _invertEase(ease) {\n  return function (p) {\n    return 1 - ease(1 - p);\n  };\n},\n    // allow yoyoEase to be set in children and have those affected when the parent/ancestor timeline yoyos.\n_propagateYoyoEase = function _propagateYoyoEase(timeline, isYoyo) {\n  var child = timeline._first,\n      ease;\n\n  while (child) {\n    if (child instanceof Timeline) {\n      _propagateYoyoEase(child, isYoyo);\n    } else if (child.vars.yoyoEase && (!child._yoyo || !child._repeat) && child._yoyo !== isYoyo) {\n      if (child.timeline) {\n        _propagateYoyoEase(child.timeline, isYoyo);\n      } else {\n        ease = child._ease;\n        child._ease = child._yEase;\n        child._yEase = ease;\n        child._yoyo = isYoyo;\n      }\n    }\n\n    child = child._next;\n  }\n},\n    _parseEase = function _parseEase(ease, defaultEase) {\n  return !ease ? defaultEase : (_isFunction(ease) ? ease : _easeMap[ease] || _configEaseFromString(ease)) || defaultEase;\n},\n    _insertEase = function _insertEase(names, easeIn, easeOut, easeInOut) {\n  if (easeOut === void 0) {\n    easeOut = function easeOut(p) {\n      return 1 - easeIn(1 - p);\n    };\n  }\n\n  if (easeInOut === void 0) {\n    easeInOut = function easeInOut(p) {\n      return p < .5 ? easeIn(p * 2) / 2 : 1 - easeIn((1 - p) * 2) / 2;\n    };\n  }\n\n  var ease = {\n    easeIn: easeIn,\n    easeOut: easeOut,\n    easeInOut: easeInOut\n  },\n      lowercaseName;\n\n  _forEachName(names, function (name) {\n    _easeMap[name] = _globals[name] = ease;\n    _easeMap[lowercaseName = name.toLowerCase()] = easeOut;\n\n    for (var p in ease) {\n      _easeMap[lowercaseName + (p === \"easeIn\" ? \".in\" : p === \"easeOut\" ? \".out\" : \".inOut\")] = _easeMap[name + \".\" + p] = ease[p];\n    }\n  });\n\n  return ease;\n},\n    _easeInOutFromOut = function _easeInOutFromOut(easeOut) {\n  return function (p) {\n    return p < .5 ? (1 - easeOut(1 - p * 2)) / 2 : .5 + easeOut((p - .5) * 2) / 2;\n  };\n},\n    _configElastic = function _configElastic(type, amplitude, period) {\n  var p1 = amplitude >= 1 ? amplitude : 1,\n      //note: if amplitude is < 1, we simply adjust the period for a more natural feel. Otherwise the math doesn't work right and the curve starts at 1.\n  p2 = (period || (type ? .3 : .45)) / (amplitude < 1 ? amplitude : 1),\n      p3 = p2 / _2PI * (Math.asin(1 / p1) || 0),\n      easeOut = function easeOut(p) {\n    return p === 1 ? 1 : p1 * Math.pow(2, -10 * p) * _sin((p - p3) * p2) + 1;\n  },\n      ease = type === \"out\" ? easeOut : type === \"in\" ? function (p) {\n    return 1 - easeOut(1 - p);\n  } : _easeInOutFromOut(easeOut);\n\n  p2 = _2PI / p2; //precalculate to optimize\n\n  ease.config = function (amplitude, period) {\n    return _configElastic(type, amplitude, period);\n  };\n\n  return ease;\n},\n    _configBack = function _configBack(type, overshoot) {\n  if (overshoot === void 0) {\n    overshoot = 1.70158;\n  }\n\n  var easeOut = function easeOut(p) {\n    return p ? --p * p * ((overshoot + 1) * p + overshoot) + 1 : 0;\n  },\n      ease = type === \"out\" ? easeOut : type === \"in\" ? function (p) {\n    return 1 - easeOut(1 - p);\n  } : _easeInOutFromOut(easeOut);\n\n  ease.config = function (overshoot) {\n    return _configBack(type, overshoot);\n  };\n\n  return ease;\n}; // a cheaper (kb and cpu) but more mild way to get a parameterized weighted ease by feeding in a value between -1 (easeIn) and 1 (easeOut) where 0 is linear.\n// _weightedEase = ratio => {\n// \tlet y = 0.5 + ratio / 2;\n// \treturn p => (2 * (1 - p) * p * y + p * p);\n// },\n// a stronger (but more expensive kb/cpu) parameterized weighted ease that lets you feed in a value between -1 (easeIn) and 1 (easeOut) where 0 is linear.\n// _weightedEaseStrong = ratio => {\n// \tratio = .5 + ratio / 2;\n// \tlet o = 1 / 3 * (ratio < .5 ? ratio : 1 - ratio),\n// \t\tb = ratio - o,\n// \t\tc = ratio + o;\n// \treturn p => p === 1 ? p : 3 * b * (1 - p) * (1 - p) * p + 3 * c * (1 - p) * p * p + p * p * p;\n// };\n\n\n_forEachName(\"Linear,Quad,Cubic,Quart,Quint,Strong\", function (name, i) {\n  var power = i < 5 ? i + 1 : i;\n\n  _insertEase(name + \",Power\" + (power - 1), i ? function (p) {\n    return Math.pow(p, power);\n  } : function (p) {\n    return p;\n  }, function (p) {\n    return 1 - Math.pow(1 - p, power);\n  }, function (p) {\n    return p < .5 ? Math.pow(p * 2, power) / 2 : 1 - Math.pow((1 - p) * 2, power) / 2;\n  });\n});\n\n_easeMap.Linear.easeNone = _easeMap.none = _easeMap.Linear.easeIn;\n\n_insertEase(\"Elastic\", _configElastic(\"in\"), _configElastic(\"out\"), _configElastic());\n\n(function (n, c) {\n  var n1 = 1 / c,\n      n2 = 2 * n1,\n      n3 = 2.5 * n1,\n      easeOut = function easeOut(p) {\n    return p < n1 ? n * p * p : p < n2 ? n * Math.pow(p - 1.5 / c, 2) + .75 : p < n3 ? n * (p -= 2.25 / c) * p + .9375 : n * Math.pow(p - 2.625 / c, 2) + .984375;\n  };\n\n  _insertEase(\"Bounce\", function (p) {\n    return 1 - easeOut(1 - p);\n  }, easeOut);\n})(7.5625, 2.75);\n\n_insertEase(\"Expo\", function (p) {\n  return p ? Math.pow(2, 10 * (p - 1)) : 0;\n});\n\n_insertEase(\"Circ\", function (p) {\n  return -(_sqrt(1 - p * p) - 1);\n});\n\n_insertEase(\"Sine\", function (p) {\n  return p === 1 ? 1 : -_cos(p * _HALF_PI) + 1;\n});\n\n_insertEase(\"Back\", _configBack(\"in\"), _configBack(\"out\"), _configBack());\n\n_easeMap.SteppedEase = _easeMap.steps = _globals.SteppedEase = {\n  config: function config(steps, immediateStart) {\n    if (steps === void 0) {\n      steps = 1;\n    }\n\n    var p1 = 1 / steps,\n        p2 = steps + (immediateStart ? 0 : 1),\n        p3 = immediateStart ? 1 : 0,\n        max = 1 - _tinyNum;\n    return function (p) {\n      return ((p2 * _clamp(0, max, p) | 0) + p3) * p1;\n    };\n  }\n};\n_defaults.ease = _easeMap[\"quad.out\"];\n\n_forEachName(\"onComplete,onUpdate,onStart,onRepeat,onReverseComplete,onInterrupt\", function (name) {\n  return _callbackNames += name + \",\" + name + \"Params,\";\n});\n/*\n * --------------------------------------------------------------------------------------\n * CACHE\n * --------------------------------------------------------------------------------------\n */\n\n\nvar GSCache = function GSCache(target, harness) {\n  this.id = _gsID++;\n  target._gsap = this;\n  this.target = target;\n  this.harness = harness;\n  this.get = harness ? harness.get : _getProperty;\n  this.set = harness ? harness.getSetter : _getSetter;\n};\n/*\n * --------------------------------------------------------------------------------------\n * ANIMATION\n * --------------------------------------------------------------------------------------\n */\n\nvar Animation = /*#__PURE__*/function () {\n  function Animation(vars) {\n    this.vars = vars;\n    this._delay = +vars.delay || 0;\n\n    if (this._repeat = vars.repeat === Infinity ? -2 : vars.repeat || 0) {\n      // TODO: repeat: Infinity on a timeline's children must flag that timeline internally and affect its totalDuration, otherwise it'll stop in the negative direction when reaching the start.\n      this._rDelay = vars.repeatDelay || 0;\n      this._yoyo = !!vars.yoyo || !!vars.yoyoEase;\n    }\n\n    this._ts = 1;\n\n    _setDuration(this, +vars.duration, 1, 1);\n\n    this.data = vars.data;\n\n    if (_context) {\n      this._ctx = _context;\n\n      _context.data.push(this);\n    }\n\n    _tickerActive || _ticker.wake();\n  }\n\n  var _proto = Animation.prototype;\n\n  _proto.delay = function delay(value) {\n    if (value || value === 0) {\n      this.parent && this.parent.smoothChildTiming && this.startTime(this._start + value - this._delay);\n      this._delay = value;\n      return this;\n    }\n\n    return this._delay;\n  };\n\n  _proto.duration = function duration(value) {\n    return arguments.length ? this.totalDuration(this._repeat > 0 ? value + (value + this._rDelay) * this._repeat : value) : this.totalDuration() && this._dur;\n  };\n\n  _proto.totalDuration = function totalDuration(value) {\n    if (!arguments.length) {\n      return this._tDur;\n    }\n\n    this._dirty = 0;\n    return _setDuration(this, this._repeat < 0 ? value : (value - this._repeat * this._rDelay) / (this._repeat + 1));\n  };\n\n  _proto.totalTime = function totalTime(_totalTime, suppressEvents) {\n    _wake();\n\n    if (!arguments.length) {\n      return this._tTime;\n    }\n\n    var parent = this._dp;\n\n    if (parent && parent.smoothChildTiming && this._ts) {\n      _alignPlayhead(this, _totalTime);\n\n      !parent._dp || parent.parent || _postAddChecks(parent, this); // edge case: if this is a child of a timeline that already completed, for example, we must re-activate the parent.\n      //in case any of the ancestor timelines had completed but should now be enabled, we should reset their totalTime() which will also ensure that they're lined up properly and enabled. Skip for animations that are on the root (wasteful). Example: a TimelineLite.exportRoot() is performed when there's a paused tween on the root, the export will not complete until that tween is unpaused, but imagine a child gets restarted later, after all [unpaused] tweens have completed. The start of that child would get pushed out, but one of the ancestors may have completed.\n\n      while (parent && parent.parent) {\n        if (parent.parent._time !== parent._start + (parent._ts >= 0 ? parent._tTime / parent._ts : (parent.totalDuration() - parent._tTime) / -parent._ts)) {\n          parent.totalTime(parent._tTime, true);\n        }\n\n        parent = parent.parent;\n      }\n\n      if (!this.parent && this._dp.autoRemoveChildren && (this._ts > 0 && _totalTime < this._tDur || this._ts < 0 && _totalTime > 0 || !this._tDur && !_totalTime)) {\n        //if the animation doesn't have a parent, put it back into its last parent (recorded as _dp for exactly cases like this). Limit to parents with autoRemoveChildren (like globalTimeline) so that if the user manually removes an animation from a timeline and then alters its playhead, it doesn't get added back in.\n        _addToTimeline(this._dp, this, this._start - this._delay);\n      }\n    }\n\n    if (this._tTime !== _totalTime || !this._dur && !suppressEvents || this._initted && Math.abs(this._zTime) === _tinyNum || !_totalTime && !this._initted && (this.add || this._ptLookup)) {\n      // check for _ptLookup on a Tween instance to ensure it has actually finished being instantiated, otherwise if this.reverse() gets called in the Animation constructor, it could trigger a render() here even though the _targets weren't populated, thus when _init() is called there won't be any PropTweens (it'll act like the tween is non-functional)\n      this._ts || (this._pTime = _totalTime); // otherwise, if an animation is paused, then the playhead is moved back to zero, then resumed, it'd revert back to the original time at the pause\n      //if (!this._lock) { // avoid endless recursion (not sure we need this yet or if it's worth the performance hit)\n      //   this._lock = 1;\n\n      _lazySafeRender(this, _totalTime, suppressEvents); //   this._lock = 0;\n      //}\n\n    }\n\n    return this;\n  };\n\n  _proto.time = function time(value, suppressEvents) {\n    return arguments.length ? this.totalTime(Math.min(this.totalDuration(), value + _elapsedCycleDuration(this)) % (this._dur + this._rDelay) || (value ? this._dur : 0), suppressEvents) : this._time; // note: if the modulus results in 0, the playhead could be exactly at the end or the beginning, and we always defer to the END with a non-zero value, otherwise if you set the time() to the very end (duration()), it would render at the START!\n  };\n\n  _proto.totalProgress = function totalProgress(value, suppressEvents) {\n    return arguments.length ? this.totalTime(this.totalDuration() * value, suppressEvents) : this.totalDuration() ? Math.min(1, this._tTime / this._tDur) : this.rawTime() > 0 ? 1 : 0;\n  };\n\n  _proto.progress = function progress(value, suppressEvents) {\n    return arguments.length ? this.totalTime(this.duration() * (this._yoyo && !(this.iteration() & 1) ? 1 - value : value) + _elapsedCycleDuration(this), suppressEvents) : this.duration() ? Math.min(1, this._time / this._dur) : this.rawTime() > 0 ? 1 : 0;\n  };\n\n  _proto.iteration = function iteration(value, suppressEvents) {\n    var cycleDuration = this.duration() + this._rDelay;\n\n    return arguments.length ? this.totalTime(this._time + (value - 1) * cycleDuration, suppressEvents) : this._repeat ? _animationCycle(this._tTime, cycleDuration) + 1 : 1;\n  } // potential future addition:\n  // isPlayingBackwards() {\n  // \tlet animation = this,\n  // \t\torientation = 1; // 1 = forward, -1 = backward\n  // \twhile (animation) {\n  // \t\torientation *= animation.reversed() || (animation.repeat() && !(animation.iteration() & 1)) ? -1 : 1;\n  // \t\tanimation = animation.parent;\n  // \t}\n  // \treturn orientation < 0;\n  // }\n  ;\n\n  _proto.timeScale = function timeScale(value, suppressEvents) {\n    if (!arguments.length) {\n      return this._rts === -_tinyNum ? 0 : this._rts; // recorded timeScale. Special case: if someone calls reverse() on an animation with timeScale of 0, we assign it -_tinyNum to remember it's reversed.\n    }\n\n    if (this._rts === value) {\n      return this;\n    }\n\n    var tTime = this.parent && this._ts ? _parentToChildTotalTime(this.parent._time, this) : this._tTime; // make sure to do the parentToChildTotalTime() BEFORE setting the new _ts because the old one must be used in that calculation.\n    // future addition? Up side: fast and minimal file size. Down side: only works on this animation; if a timeline is reversed, for example, its childrens' onReverse wouldn't get called.\n    //(+value < 0 && this._rts >= 0) && _callback(this, \"onReverse\", true);\n    // prioritize rendering where the parent's playhead lines up instead of this._tTime because there could be a tween that's animating another tween's timeScale in the same rendering loop (same parent), thus if the timeScale tween renders first, it would alter _start BEFORE _tTime was set on that tick (in the rendering loop), effectively freezing it until the timeScale tween finishes.\n\n    this._rts = +value || 0;\n    this._ts = this._ps || value === -_tinyNum ? 0 : this._rts; // _ts is the functional timeScale which would be 0 if the animation is paused.\n\n    this.totalTime(_clamp(-Math.abs(this._delay), this._tDur, tTime), suppressEvents !== false);\n\n    _setEnd(this); // if parent.smoothChildTiming was false, the end time didn't get updated in the _alignPlayhead() method, so do it here.\n\n\n    return _recacheAncestors(this);\n  };\n\n  _proto.paused = function paused(value) {\n    if (!arguments.length) {\n      return this._ps;\n    }\n\n    if (this._ps !== value) {\n      this._ps = value;\n\n      if (value) {\n        this._pTime = this._tTime || Math.max(-this._delay, this.rawTime()); // if the pause occurs during the delay phase, make sure that's factored in when resuming.\n\n        this._ts = this._act = 0; // _ts is the functional timeScale, so a paused tween would effectively have a timeScale of 0. We record the \"real\" timeScale as _rts (recorded time scale)\n      } else {\n        _wake();\n\n        this._ts = this._rts; //only defer to _pTime (pauseTime) if tTime is zero. Remember, someone could pause() an animation, then scrub the playhead and resume(). If the parent doesn't have smoothChildTiming, we render at the rawTime() because the startTime won't get updated.\n\n        this.totalTime(this.parent && !this.parent.smoothChildTiming ? this.rawTime() : this._tTime || this._pTime, this.progress() === 1 && Math.abs(this._zTime) !== _tinyNum && (this._tTime -= _tinyNum)); // edge case: animation.progress(1).pause().play() wouldn't render again because the playhead is already at the end, but the call to totalTime() below will add it back to its parent...and not remove it again (since removing only happens upon rendering at a new time). Offsetting the _tTime slightly is done simply to cause the final render in totalTime() that'll pop it off its timeline (if autoRemoveChildren is true, of course). Check to make sure _zTime isn't -_tinyNum to avoid an edge case where the playhead is pushed to the end but INSIDE a tween/callback, the timeline itself is paused thus halting rendering and leaving a few unrendered. When resuming, it wouldn't render those otherwise.\n      }\n    }\n\n    return this;\n  };\n\n  _proto.startTime = function startTime(value) {\n    if (arguments.length) {\n      this._start = value;\n      var parent = this.parent || this._dp;\n      parent && (parent._sort || !this.parent) && _addToTimeline(parent, this, value - this._delay);\n      return this;\n    }\n\n    return this._start;\n  };\n\n  _proto.endTime = function endTime(includeRepeats) {\n    return this._start + (_isNotFalse(includeRepeats) ? this.totalDuration() : this.duration()) / Math.abs(this._ts || 1);\n  };\n\n  _proto.rawTime = function rawTime(wrapRepeats) {\n    var parent = this.parent || this._dp; // _dp = detached parent\n\n    return !parent ? this._tTime : wrapRepeats && (!this._ts || this._repeat && this._time && this.totalProgress() < 1) ? this._tTime % (this._dur + this._rDelay) : !this._ts ? this._tTime : _parentToChildTotalTime(parent.rawTime(wrapRepeats), this);\n  };\n\n  _proto.revert = function revert(config) {\n    if (config === void 0) {\n      config = _revertConfig;\n    }\n\n    var prevIsReverting = _reverting;\n    _reverting = config;\n\n    if (this._initted || this._startAt) {\n      this.timeline && this.timeline.revert(config);\n      this.totalTime(-0.01, config.suppressEvents);\n    }\n\n    this.data !== \"nested\" && config.kill !== false && this.kill();\n    _reverting = prevIsReverting;\n    return this;\n  };\n\n  _proto.globalTime = function globalTime(rawTime) {\n    var animation = this,\n        time = arguments.length ? rawTime : animation.rawTime();\n\n    while (animation) {\n      time = animation._start + time / (Math.abs(animation._ts) || 1);\n      animation = animation._dp;\n    }\n\n    return !this.parent && this._sat ? this._sat.globalTime(rawTime) : time; // the _startAt tweens for .fromTo() and .from() that have immediateRender should always be FIRST in the timeline (important for context.revert()). \"_sat\" stands for _startAtTween, referring to the parent tween that created the _startAt. We must discern if that tween had immediateRender so that we can know whether or not to prioritize it in revert().\n  };\n\n  _proto.repeat = function repeat(value) {\n    if (arguments.length) {\n      this._repeat = value === Infinity ? -2 : value;\n      return _onUpdateTotalDuration(this);\n    }\n\n    return this._repeat === -2 ? Infinity : this._repeat;\n  };\n\n  _proto.repeatDelay = function repeatDelay(value) {\n    if (arguments.length) {\n      var time = this._time;\n      this._rDelay = value;\n\n      _onUpdateTotalDuration(this);\n\n      return time ? this.time(time) : this;\n    }\n\n    return this._rDelay;\n  };\n\n  _proto.yoyo = function yoyo(value) {\n    if (arguments.length) {\n      this._yoyo = value;\n      return this;\n    }\n\n    return this._yoyo;\n  };\n\n  _proto.seek = function seek(position, suppressEvents) {\n    return this.totalTime(_parsePosition(this, position), _isNotFalse(suppressEvents));\n  };\n\n  _proto.restart = function restart(includeDelay, suppressEvents) {\n    return this.play().totalTime(includeDelay ? -this._delay : 0, _isNotFalse(suppressEvents));\n  };\n\n  _proto.play = function play(from, suppressEvents) {\n    from != null && this.seek(from, suppressEvents);\n    return this.reversed(false).paused(false);\n  };\n\n  _proto.reverse = function reverse(from, suppressEvents) {\n    from != null && this.seek(from || this.totalDuration(), suppressEvents);\n    return this.reversed(true).paused(false);\n  };\n\n  _proto.pause = function pause(atTime, suppressEvents) {\n    atTime != null && this.seek(atTime, suppressEvents);\n    return this.paused(true);\n  };\n\n  _proto.resume = function resume() {\n    return this.paused(false);\n  };\n\n  _proto.reversed = function reversed(value) {\n    if (arguments.length) {\n      !!value !== this.reversed() && this.timeScale(-this._rts || (value ? -_tinyNum : 0)); // in case timeScale is zero, reversing would have no effect so we use _tinyNum.\n\n      return this;\n    }\n\n    return this._rts < 0;\n  };\n\n  _proto.invalidate = function invalidate() {\n    this._initted = this._act = 0;\n    this._zTime = -_tinyNum;\n    return this;\n  };\n\n  _proto.isActive = function isActive() {\n    var parent = this.parent || this._dp,\n        start = this._start,\n        rawTime;\n    return !!(!parent || this._ts && this._initted && parent.isActive() && (rawTime = parent.rawTime(true)) >= start && rawTime < this.endTime(true) - _tinyNum);\n  };\n\n  _proto.eventCallback = function eventCallback(type, callback, params) {\n    var vars = this.vars;\n\n    if (arguments.length > 1) {\n      if (!callback) {\n        delete vars[type];\n      } else {\n        vars[type] = callback;\n        params && (vars[type + \"Params\"] = params);\n        type === \"onUpdate\" && (this._onUpdate = callback);\n      }\n\n      return this;\n    }\n\n    return vars[type];\n  };\n\n  _proto.then = function then(onFulfilled) {\n    var self = this;\n    return new Promise(function (resolve) {\n      var f = _isFunction(onFulfilled) ? onFulfilled : _passThrough,\n          _resolve = function _resolve() {\n        var _then = self.then;\n        self.then = null; // temporarily null the then() method to avoid an infinite loop (see https://github.com/greensock/GSAP/issues/322)\n\n        _isFunction(f) && (f = f(self)) && (f.then || f === self) && (self.then = _then);\n        resolve(f);\n        self.then = _then;\n      };\n\n      if (self._initted && self.totalProgress() === 1 && self._ts >= 0 || !self._tTime && self._ts < 0) {\n        _resolve();\n      } else {\n        self._prom = _resolve;\n      }\n    });\n  };\n\n  _proto.kill = function kill() {\n    _interrupt(this);\n  };\n\n  return Animation;\n}();\n\n_setDefaults(Animation.prototype, {\n  _time: 0,\n  _start: 0,\n  _end: 0,\n  _tTime: 0,\n  _tDur: 0,\n  _dirty: 0,\n  _repeat: 0,\n  _yoyo: false,\n  parent: null,\n  _initted: false,\n  _rDelay: 0,\n  _ts: 1,\n  _dp: 0,\n  ratio: 0,\n  _zTime: -_tinyNum,\n  _prom: 0,\n  _ps: false,\n  _rts: 1\n});\n/*\n * -------------------------------------------------\n * TIMELINE\n * -------------------------------------------------\n */\n\n\nvar Timeline = /*#__PURE__*/function (_Animation) {\n  _inheritsLoose(Timeline, _Animation);\n\n  function Timeline(vars, position) {\n    var _this;\n\n    if (vars === void 0) {\n      vars = {};\n    }\n\n    _this = _Animation.call(this, vars) || this;\n    _this.labels = {};\n    _this.smoothChildTiming = !!vars.smoothChildTiming;\n    _this.autoRemoveChildren = !!vars.autoRemoveChildren;\n    _this._sort = _isNotFalse(vars.sortChildren);\n    _globalTimeline && _addToTimeline(vars.parent || _globalTimeline, _assertThisInitialized(_this), position);\n    vars.reversed && _this.reverse();\n    vars.paused && _this.paused(true);\n    vars.scrollTrigger && _scrollTrigger(_assertThisInitialized(_this), vars.scrollTrigger);\n    return _this;\n  }\n\n  var _proto2 = Timeline.prototype;\n\n  _proto2.to = function to(targets, vars, position) {\n    _createTweenType(0, arguments, this);\n\n    return this;\n  };\n\n  _proto2.from = function from(targets, vars, position) {\n    _createTweenType(1, arguments, this);\n\n    return this;\n  };\n\n  _proto2.fromTo = function fromTo(targets, fromVars, toVars, position) {\n    _createTweenType(2, arguments, this);\n\n    return this;\n  };\n\n  _proto2.set = function set(targets, vars, position) {\n    vars.duration = 0;\n    vars.parent = this;\n    _inheritDefaults(vars).repeatDelay || (vars.repeat = 0);\n    vars.immediateRender = !!vars.immediateRender;\n    new Tween(targets, vars, _parsePosition(this, position), 1);\n    return this;\n  };\n\n  _proto2.call = function call(callback, params, position) {\n    return _addToTimeline(this, Tween.delayedCall(0, callback, params), position);\n  } //ONLY for backward compatibility! Maybe delete?\n  ;\n\n  _proto2.staggerTo = function staggerTo(targets, duration, vars, stagger, position, onCompleteAll, onCompleteAllParams) {\n    vars.duration = duration;\n    vars.stagger = vars.stagger || stagger;\n    vars.onComplete = onCompleteAll;\n    vars.onCompleteParams = onCompleteAllParams;\n    vars.parent = this;\n    new Tween(targets, vars, _parsePosition(this, position));\n    return this;\n  };\n\n  _proto2.staggerFrom = function staggerFrom(targets, duration, vars, stagger, position, onCompleteAll, onCompleteAllParams) {\n    vars.runBackwards = 1;\n    _inheritDefaults(vars).immediateRender = _isNotFalse(vars.immediateRender);\n    return this.staggerTo(targets, duration, vars, stagger, position, onCompleteAll, onCompleteAllParams);\n  };\n\n  _proto2.staggerFromTo = function staggerFromTo(targets, duration, fromVars, toVars, stagger, position, onCompleteAll, onCompleteAllParams) {\n    toVars.startAt = fromVars;\n    _inheritDefaults(toVars).immediateRender = _isNotFalse(toVars.immediateRender);\n    return this.staggerTo(targets, duration, toVars, stagger, position, onCompleteAll, onCompleteAllParams);\n  };\n\n  _proto2.render = function render(totalTime, suppressEvents, force) {\n    var prevTime = this._time,\n        tDur = this._dirty ? this.totalDuration() : this._tDur,\n        dur = this._dur,\n        tTime = totalTime <= 0 ? 0 : _roundPrecise(totalTime),\n        // if a paused timeline is resumed (or its _start is updated for another reason...which rounds it), that could result in the playhead shifting a **tiny** amount and a zero-duration child at that spot may get rendered at a different ratio, like its totalTime in render() may be 1e-17 instead of 0, for example.\n    crossingStart = this._zTime < 0 !== totalTime < 0 && (this._initted || !dur),\n        time,\n        child,\n        next,\n        iteration,\n        cycleDuration,\n        prevPaused,\n        pauseTween,\n        timeScale,\n        prevStart,\n        prevIteration,\n        yoyo,\n        isYoyo;\n    this !== _globalTimeline && tTime > tDur && totalTime >= 0 && (tTime = tDur);\n\n    if (tTime !== this._tTime || force || crossingStart) {\n      if (prevTime !== this._time && dur) {\n        //if totalDuration() finds a child with a negative startTime and smoothChildTiming is true, things get shifted around internally so we need to adjust the time accordingly. For example, if a tween starts at -30 we must shift EVERYTHING forward 30 seconds and move this timeline's startTime backward by 30 seconds so that things align with the playhead (no jump).\n        tTime += this._time - prevTime;\n        totalTime += this._time - prevTime;\n      }\n\n      time = tTime;\n      prevStart = this._start;\n      timeScale = this._ts;\n      prevPaused = !timeScale;\n\n      if (crossingStart) {\n        dur || (prevTime = this._zTime); //when the playhead arrives at EXACTLY time 0 (right on top) of a zero-duration timeline, we need to discern if events are suppressed so that when the playhead moves again (next time), it'll trigger the callback. If events are NOT suppressed, obviously the callback would be triggered in this render. Basically, the callback should fire either when the playhead ARRIVES or LEAVES this exact spot, not both. Imagine doing a timeline.seek(0) and there's a callback that sits at 0. Since events are suppressed on that seek() by default, nothing will fire, but when the playhead moves off of that position, the callback should fire. This behavior is what people intuitively expect.\n\n        (totalTime || !suppressEvents) && (this._zTime = totalTime);\n      }\n\n      if (this._repeat) {\n        //adjust the time for repeats and yoyos\n        yoyo = this._yoyo;\n        cycleDuration = dur + this._rDelay;\n\n        if (this._repeat < -1 && totalTime < 0) {\n          return this.totalTime(cycleDuration * 100 + totalTime, suppressEvents, force);\n        }\n\n        time = _roundPrecise(tTime % cycleDuration); //round to avoid floating point errors. (4 % 0.8 should be 0 but some browsers report it as 0.79999999!)\n\n        if (tTime === tDur) {\n          // the tDur === tTime is for edge cases where there's a lengthy decimal on the duration and it may reach the very end but the time is rendered as not-quite-there (remember, tDur is rounded to 4 decimals whereas dur isn't)\n          iteration = this._repeat;\n          time = dur;\n        } else {\n          iteration = ~~(tTime / cycleDuration);\n\n          if (iteration && iteration === tTime / cycleDuration) {\n            time = dur;\n            iteration--;\n          }\n\n          time > dur && (time = dur);\n        }\n\n        prevIteration = _animationCycle(this._tTime, cycleDuration);\n        !prevTime && this._tTime && prevIteration !== iteration && this._tTime - prevIteration * cycleDuration - this._dur <= 0 && (prevIteration = iteration); // edge case - if someone does addPause() at the very beginning of a repeating timeline, that pause is technically at the same spot as the end which causes this._time to get set to 0 when the totalTime would normally place the playhead at the end. See https://gsap.com/forums/topic/23823-closing-nav-animation-not-working-on-ie-and-iphone-6-maybe-other-older-browser/?tab=comments#comment-113005 also, this._tTime - prevIteration * cycleDuration - this._dur <= 0 just checks to make sure it wasn't previously in the \"repeatDelay\" portion\n\n        if (yoyo && iteration & 1) {\n          time = dur - time;\n          isYoyo = 1;\n        }\n        /*\n        make sure children at the end/beginning of the timeline are rendered properly. If, for example,\n        a 3-second long timeline rendered at 2.9 seconds previously, and now renders at 3.2 seconds (which\n        would get translated to 2.8 seconds if the timeline yoyos or 0.2 seconds if it just repeats), there\n        could be a callback or a short tween that's at 2.95 or 3 seconds in which wouldn't render. So\n        we need to push the timeline to the end (and/or beginning depending on its yoyo value). Also we must\n        ensure that zero-duration tweens at the very beginning or end of the Timeline work.\n        */\n\n\n        if (iteration !== prevIteration && !this._lock) {\n          var rewinding = yoyo && prevIteration & 1,\n              doesWrap = rewinding === (yoyo && iteration & 1);\n          iteration < prevIteration && (rewinding = !rewinding);\n          prevTime = rewinding ? 0 : tTime % dur ? dur : tTime; // if the playhead is landing exactly at the end of an iteration, use that totalTime rather than only the duration, otherwise it'll skip the 2nd render since it's effectively at the same time.\n\n          this._lock = 1;\n          this.render(prevTime || (isYoyo ? 0 : _roundPrecise(iteration * cycleDuration)), suppressEvents, !dur)._lock = 0;\n          this._tTime = tTime; // if a user gets the iteration() inside the onRepeat, for example, it should be accurate.\n\n          !suppressEvents && this.parent && _callback(this, \"onRepeat\");\n          this.vars.repeatRefresh && !isYoyo && (this.invalidate()._lock = 1);\n\n          if (prevTime && prevTime !== this._time || prevPaused !== !this._ts || this.vars.onRepeat && !this.parent && !this._act) {\n            // if prevTime is 0 and we render at the very end, _time will be the end, thus won't match. So in this edge case, prevTime won't match _time but that's okay. If it gets killed in the onRepeat, eject as well.\n            return this;\n          }\n\n          dur = this._dur; // in case the duration changed in the onRepeat\n\n          tDur = this._tDur;\n\n          if (doesWrap) {\n            this._lock = 2;\n            prevTime = rewinding ? dur : -0.0001;\n            this.render(prevTime, true);\n            this.vars.repeatRefresh && !isYoyo && this.invalidate();\n          }\n\n          this._lock = 0;\n\n          if (!this._ts && !prevPaused) {\n            return this;\n          } //in order for yoyoEase to work properly when there's a stagger, we must swap out the ease in each sub-tween.\n\n\n          _propagateYoyoEase(this, isYoyo);\n        }\n      }\n\n      if (this._hasPause && !this._forcing && this._lock < 2) {\n        pauseTween = _findNextPauseTween(this, _roundPrecise(prevTime), _roundPrecise(time));\n\n        if (pauseTween) {\n          tTime -= time - (time = pauseTween._start);\n        }\n      }\n\n      this._tTime = tTime;\n      this._time = time;\n      this._act = !timeScale; //as long as it's not paused, force it to be active so that if the user renders independent of the parent timeline, it'll be forced to re-render on the next tick.\n\n      if (!this._initted) {\n        this._onUpdate = this.vars.onUpdate;\n        this._initted = 1;\n        this._zTime = totalTime;\n        prevTime = 0; // upon init, the playhead should always go forward; someone could invalidate() a completed timeline and then if they restart(), that would make child tweens render in reverse order which could lock in the wrong starting values if they build on each other, like tl.to(obj, {x: 100}).to(obj, {x: 0}).\n      }\n\n      if (!prevTime && time && !suppressEvents && !iteration) {\n        _callback(this, \"onStart\");\n\n        if (this._tTime !== tTime) {\n          // in case the onStart triggered a render at a different spot, eject. Like if someone did animation.pause(0.5) or something inside the onStart.\n          return this;\n        }\n      }\n\n      if (time >= prevTime && totalTime >= 0) {\n        child = this._first;\n\n        while (child) {\n          next = child._next;\n\n          if ((child._act || time >= child._start) && child._ts && pauseTween !== child) {\n            if (child.parent !== this) {\n              // an extreme edge case - the child's render could do something like kill() the \"next\" one in the linked list, or reparent it. In that case we must re-initiate the whole render to be safe.\n              return this.render(totalTime, suppressEvents, force);\n            }\n\n            child.render(child._ts > 0 ? (time - child._start) * child._ts : (child._dirty ? child.totalDuration() : child._tDur) + (time - child._start) * child._ts, suppressEvents, force);\n\n            if (time !== this._time || !this._ts && !prevPaused) {\n              //in case a tween pauses or seeks the timeline when rendering, like inside of an onUpdate/onComplete\n              pauseTween = 0;\n              next && (tTime += this._zTime = -_tinyNum); // it didn't finish rendering, so flag zTime as negative so that so that the next time render() is called it'll be forced (to render any remaining children)\n\n              break;\n            }\n          }\n\n          child = next;\n        }\n      } else {\n        child = this._last;\n        var adjustedTime = totalTime < 0 ? totalTime : time; //when the playhead goes backward beyond the start of this timeline, we must pass that information down to the child animations so that zero-duration tweens know whether to render their starting or ending values.\n\n        while (child) {\n          next = child._prev;\n\n          if ((child._act || adjustedTime <= child._end) && child._ts && pauseTween !== child) {\n            if (child.parent !== this) {\n              // an extreme edge case - the child's render could do something like kill() the \"next\" one in the linked list, or reparent it. In that case we must re-initiate the whole render to be safe.\n              return this.render(totalTime, suppressEvents, force);\n            }\n\n            child.render(child._ts > 0 ? (adjustedTime - child._start) * child._ts : (child._dirty ? child.totalDuration() : child._tDur) + (adjustedTime - child._start) * child._ts, suppressEvents, force || _reverting && (child._initted || child._startAt)); // if reverting, we should always force renders of initted tweens (but remember that .fromTo() or .from() may have a _startAt but not _initted yet). If, for example, a .fromTo() tween with a stagger (which creates an internal timeline) gets reverted BEFORE some of its child tweens render for the first time, it may not properly trigger them to revert.\n\n            if (time !== this._time || !this._ts && !prevPaused) {\n              //in case a tween pauses or seeks the timeline when rendering, like inside of an onUpdate/onComplete\n              pauseTween = 0;\n              next && (tTime += this._zTime = adjustedTime ? -_tinyNum : _tinyNum); // it didn't finish rendering, so adjust zTime so that so that the next time render() is called it'll be forced (to render any remaining children)\n\n              break;\n            }\n          }\n\n          child = next;\n        }\n      }\n\n      if (pauseTween && !suppressEvents) {\n        this.pause();\n        pauseTween.render(time >= prevTime ? 0 : -_tinyNum)._zTime = time >= prevTime ? 1 : -1;\n\n        if (this._ts) {\n          //the callback resumed playback! So since we may have held back the playhead due to where the pause is positioned, go ahead and jump to where it's SUPPOSED to be (if no pause happened).\n          this._start = prevStart; //if the pause was at an earlier time and the user resumed in the callback, it could reposition the timeline (changing its startTime), throwing things off slightly, so we make sure the _start doesn't shift.\n\n          _setEnd(this);\n\n          return this.render(totalTime, suppressEvents, force);\n        }\n      }\n\n      this._onUpdate && !suppressEvents && _callback(this, \"onUpdate\", true);\n      if (tTime === tDur && this._tTime >= this.totalDuration() || !tTime && prevTime) if (prevStart === this._start || Math.abs(timeScale) !== Math.abs(this._ts)) if (!this._lock) {\n        // remember, a child's callback may alter this timeline's playhead or timeScale which is why we need to add some of these checks.\n        (totalTime || !dur) && (tTime === tDur && this._ts > 0 || !tTime && this._ts < 0) && _removeFromParent(this, 1); // don't remove if the timeline is reversed and the playhead isn't at 0, otherwise tl.progress(1).reverse() won't work. Only remove if the playhead is at the end and timeScale is positive, or if the playhead is at 0 and the timeScale is negative.\n\n        if (!suppressEvents && !(totalTime < 0 && !prevTime) && (tTime || prevTime || !tDur)) {\n          _callback(this, tTime === tDur && totalTime >= 0 ? \"onComplete\" : \"onReverseComplete\", true);\n\n          this._prom && !(tTime < tDur && this.timeScale() > 0) && this._prom();\n        }\n      }\n    }\n\n    return this;\n  };\n\n  _proto2.add = function add(child, position) {\n    var _this2 = this;\n\n    _isNumber(position) || (position = _parsePosition(this, position, child));\n\n    if (!(child instanceof Animation)) {\n      if (_isArray(child)) {\n        child.forEach(function (obj) {\n          return _this2.add(obj, position);\n        });\n        return this;\n      }\n\n      if (_isString(child)) {\n        return this.addLabel(child, position);\n      }\n\n      if (_isFunction(child)) {\n        child = Tween.delayedCall(0, child);\n      } else {\n        return this;\n      }\n    }\n\n    return this !== child ? _addToTimeline(this, child, position) : this; //don't allow a timeline to be added to itself as a child!\n  };\n\n  _proto2.getChildren = function getChildren(nested, tweens, timelines, ignoreBeforeTime) {\n    if (nested === void 0) {\n      nested = true;\n    }\n\n    if (tweens === void 0) {\n      tweens = true;\n    }\n\n    if (timelines === void 0) {\n      timelines = true;\n    }\n\n    if (ignoreBeforeTime === void 0) {\n      ignoreBeforeTime = -_bigNum;\n    }\n\n    var a = [],\n        child = this._first;\n\n    while (child) {\n      if (child._start >= ignoreBeforeTime) {\n        if (child instanceof Tween) {\n          tweens && a.push(child);\n        } else {\n          timelines && a.push(child);\n          nested && a.push.apply(a, child.getChildren(true, tweens, timelines));\n        }\n      }\n\n      child = child._next;\n    }\n\n    return a;\n  };\n\n  _proto2.getById = function getById(id) {\n    var animations = this.getChildren(1, 1, 1),\n        i = animations.length;\n\n    while (i--) {\n      if (animations[i].vars.id === id) {\n        return animations[i];\n      }\n    }\n  };\n\n  _proto2.remove = function remove(child) {\n    if (_isString(child)) {\n      return this.removeLabel(child);\n    }\n\n    if (_isFunction(child)) {\n      return this.killTweensOf(child);\n    }\n\n    _removeLinkedListItem(this, child);\n\n    if (child === this._recent) {\n      this._recent = this._last;\n    }\n\n    return _uncache(this);\n  };\n\n  _proto2.totalTime = function totalTime(_totalTime2, suppressEvents) {\n    if (!arguments.length) {\n      return this._tTime;\n    }\n\n    this._forcing = 1;\n\n    if (!this._dp && this._ts) {\n      //special case for the global timeline (or any other that has no parent or detached parent).\n      this._start = _roundPrecise(_ticker.time - (this._ts > 0 ? _totalTime2 / this._ts : (this.totalDuration() - _totalTime2) / -this._ts));\n    }\n\n    _Animation.prototype.totalTime.call(this, _totalTime2, suppressEvents);\n\n    this._forcing = 0;\n    return this;\n  };\n\n  _proto2.addLabel = function addLabel(label, position) {\n    this.labels[label] = _parsePosition(this, position);\n    return this;\n  };\n\n  _proto2.removeLabel = function removeLabel(label) {\n    delete this.labels[label];\n    return this;\n  };\n\n  _proto2.addPause = function addPause(position, callback, params) {\n    var t = Tween.delayedCall(0, callback || _emptyFunc, params);\n    t.data = \"isPause\";\n    this._hasPause = 1;\n    return _addToTimeline(this, t, _parsePosition(this, position));\n  };\n\n  _proto2.removePause = function removePause(position) {\n    var child = this._first;\n    position = _parsePosition(this, position);\n\n    while (child) {\n      if (child._start === position && child.data === \"isPause\") {\n        _removeFromParent(child);\n      }\n\n      child = child._next;\n    }\n  };\n\n  _proto2.killTweensOf = function killTweensOf(targets, props, onlyActive) {\n    var tweens = this.getTweensOf(targets, onlyActive),\n        i = tweens.length;\n\n    while (i--) {\n      _overwritingTween !== tweens[i] && tweens[i].kill(targets, props);\n    }\n\n    return this;\n  };\n\n  _proto2.getTweensOf = function getTweensOf(targets, onlyActive) {\n    var a = [],\n        parsedTargets = toArray(targets),\n        child = this._first,\n        isGlobalTime = _isNumber(onlyActive),\n        // a number is interpreted as a global time. If the animation spans\n    children;\n\n    while (child) {\n      if (child instanceof Tween) {\n        if (_arrayContainsAny(child._targets, parsedTargets) && (isGlobalTime ? (!_overwritingTween || child._initted && child._ts) && child.globalTime(0) <= onlyActive && child.globalTime(child.totalDuration()) > onlyActive : !onlyActive || child.isActive())) {\n          // note: if this is for overwriting, it should only be for tweens that aren't paused and are initted.\n          a.push(child);\n        }\n      } else if ((children = child.getTweensOf(parsedTargets, onlyActive)).length) {\n        a.push.apply(a, children);\n      }\n\n      child = child._next;\n    }\n\n    return a;\n  } // potential future feature - targets() on timelines\n  // targets() {\n  // \tlet result = [];\n  // \tthis.getChildren(true, true, false).forEach(t => result.push(...t.targets()));\n  // \treturn result.filter((v, i) => result.indexOf(v) === i);\n  // }\n  ;\n\n  _proto2.tweenTo = function tweenTo(position, vars) {\n    vars = vars || {};\n\n    var tl = this,\n        endTime = _parsePosition(tl, position),\n        _vars = vars,\n        startAt = _vars.startAt,\n        _onStart = _vars.onStart,\n        onStartParams = _vars.onStartParams,\n        immediateRender = _vars.immediateRender,\n        initted,\n        tween = Tween.to(tl, _setDefaults({\n      ease: vars.ease || \"none\",\n      lazy: false,\n      immediateRender: false,\n      time: endTime,\n      overwrite: \"auto\",\n      duration: vars.duration || Math.abs((endTime - (startAt && \"time\" in startAt ? startAt.time : tl._time)) / tl.timeScale()) || _tinyNum,\n      onStart: function onStart() {\n        tl.pause();\n\n        if (!initted) {\n          var duration = vars.duration || Math.abs((endTime - (startAt && \"time\" in startAt ? startAt.time : tl._time)) / tl.timeScale());\n          tween._dur !== duration && _setDuration(tween, duration, 0, 1).render(tween._time, true, true);\n          initted = 1;\n        }\n\n        _onStart && _onStart.apply(tween, onStartParams || []); //in case the user had an onStart in the vars - we don't want to overwrite it.\n      }\n    }, vars));\n\n    return immediateRender ? tween.render(0) : tween;\n  };\n\n  _proto2.tweenFromTo = function tweenFromTo(fromPosition, toPosition, vars) {\n    return this.tweenTo(toPosition, _setDefaults({\n      startAt: {\n        time: _parsePosition(this, fromPosition)\n      }\n    }, vars));\n  };\n\n  _proto2.recent = function recent() {\n    return this._recent;\n  };\n\n  _proto2.nextLabel = function nextLabel(afterTime) {\n    if (afterTime === void 0) {\n      afterTime = this._time;\n    }\n\n    return _getLabelInDirection(this, _parsePosition(this, afterTime));\n  };\n\n  _proto2.previousLabel = function previousLabel(beforeTime) {\n    if (beforeTime === void 0) {\n      beforeTime = this._time;\n    }\n\n    return _getLabelInDirection(this, _parsePosition(this, beforeTime), 1);\n  };\n\n  _proto2.currentLabel = function currentLabel(value) {\n    return arguments.length ? this.seek(value, true) : this.previousLabel(this._time + _tinyNum);\n  };\n\n  _proto2.shiftChildren = function shiftChildren(amount, adjustLabels, ignoreBeforeTime) {\n    if (ignoreBeforeTime === void 0) {\n      ignoreBeforeTime = 0;\n    }\n\n    var child = this._first,\n        labels = this.labels,\n        p;\n\n    while (child) {\n      if (child._start >= ignoreBeforeTime) {\n        child._start += amount;\n        child._end += amount;\n      }\n\n      child = child._next;\n    }\n\n    if (adjustLabels) {\n      for (p in labels) {\n        if (labels[p] >= ignoreBeforeTime) {\n          labels[p] += amount;\n        }\n      }\n    }\n\n    return _uncache(this);\n  };\n\n  _proto2.invalidate = function invalidate(soft) {\n    var child = this._first;\n    this._lock = 0;\n\n    while (child) {\n      child.invalidate(soft);\n      child = child._next;\n    }\n\n    return _Animation.prototype.invalidate.call(this, soft);\n  };\n\n  _proto2.clear = function clear(includeLabels) {\n    if (includeLabels === void 0) {\n      includeLabels = true;\n    }\n\n    var child = this._first,\n        next;\n\n    while (child) {\n      next = child._next;\n      this.remove(child);\n      child = next;\n    }\n\n    this._dp && (this._time = this._tTime = this._pTime = 0);\n    includeLabels && (this.labels = {});\n    return _uncache(this);\n  };\n\n  _proto2.totalDuration = function totalDuration(value) {\n    var max = 0,\n        self = this,\n        child = self._last,\n        prevStart = _bigNum,\n        prev,\n        start,\n        parent;\n\n    if (arguments.length) {\n      return self.timeScale((self._repeat < 0 ? self.duration() : self.totalDuration()) / (self.reversed() ? -value : value));\n    }\n\n    if (self._dirty) {\n      parent = self.parent;\n\n      while (child) {\n        prev = child._prev; //record it here in case the tween changes position in the sequence...\n\n        child._dirty && child.totalDuration(); //could change the tween._startTime, so make sure the animation's cache is clean before analyzing it.\n\n        start = child._start;\n\n        if (start > prevStart && self._sort && child._ts && !self._lock) {\n          //in case one of the tweens shifted out of order, it needs to be re-inserted into the correct position in the sequence\n          self._lock = 1; //prevent endless recursive calls - there are methods that get triggered that check duration/totalDuration when we add().\n\n          _addToTimeline(self, child, start - child._delay, 1)._lock = 0;\n        } else {\n          prevStart = start;\n        }\n\n        if (start < 0 && child._ts) {\n          //children aren't allowed to have negative startTimes unless smoothChildTiming is true, so adjust here if one is found.\n          max -= start;\n\n          if (!parent && !self._dp || parent && parent.smoothChildTiming) {\n            self._start += start / self._ts;\n            self._time -= start;\n            self._tTime -= start;\n          }\n\n          self.shiftChildren(-start, false, -1e999);\n          prevStart = 0;\n        }\n\n        child._end > max && child._ts && (max = child._end);\n        child = prev;\n      }\n\n      _setDuration(self, self === _globalTimeline && self._time > max ? self._time : max, 1, 1);\n\n      self._dirty = 0;\n    }\n\n    return self._tDur;\n  };\n\n  Timeline.updateRoot = function updateRoot(time) {\n    if (_globalTimeline._ts) {\n      _lazySafeRender(_globalTimeline, _parentToChildTotalTime(time, _globalTimeline));\n\n      _lastRenderedFrame = _ticker.frame;\n    }\n\n    if (_ticker.frame >= _nextGCFrame) {\n      _nextGCFrame += _config.autoSleep || 120;\n      var child = _globalTimeline._first;\n      if (!child || !child._ts) if (_config.autoSleep && _ticker._listeners.length < 2) {\n        while (child && !child._ts) {\n          child = child._next;\n        }\n\n        child || _ticker.sleep();\n      }\n    }\n  };\n\n  return Timeline;\n}(Animation);\n\n_setDefaults(Timeline.prototype, {\n  _lock: 0,\n  _hasPause: 0,\n  _forcing: 0\n});\n\nvar _addComplexStringPropTween = function _addComplexStringPropTween(target, prop, start, end, setter, stringFilter, funcParam) {\n  //note: we call _addComplexStringPropTween.call(tweenInstance...) to ensure that it's scoped properly. We may call it from within a plugin too, thus \"this\" would refer to the plugin.\n  var pt = new PropTween(this._pt, target, prop, 0, 1, _renderComplexString, null, setter),\n      index = 0,\n      matchIndex = 0,\n      result,\n      startNums,\n      color,\n      endNum,\n      chunk,\n      startNum,\n      hasRandom,\n      a;\n  pt.b = start;\n  pt.e = end;\n  start += \"\"; //ensure values are strings\n\n  end += \"\";\n\n  if (hasRandom = ~end.indexOf(\"random(\")) {\n    end = _replaceRandom(end);\n  }\n\n  if (stringFilter) {\n    a = [start, end];\n    stringFilter(a, target, prop); //pass an array with the starting and ending values and let the filter do whatever it needs to the values.\n\n    start = a[0];\n    end = a[1];\n  }\n\n  startNums = start.match(_complexStringNumExp) || [];\n\n  while (result = _complexStringNumExp.exec(end)) {\n    endNum = result[0];\n    chunk = end.substring(index, result.index);\n\n    if (color) {\n      color = (color + 1) % 5;\n    } else if (chunk.substr(-5) === \"rgba(\") {\n      color = 1;\n    }\n\n    if (endNum !== startNums[matchIndex++]) {\n      startNum = parseFloat(startNums[matchIndex - 1]) || 0; //these nested PropTweens are handled in a special way - we'll never actually call a render or setter method on them. We'll just loop through them in the parent complex string PropTween's render method.\n\n      pt._pt = {\n        _next: pt._pt,\n        p: chunk || matchIndex === 1 ? chunk : \",\",\n        //note: SVG spec allows omission of comma/space when a negative sign is wedged between two numbers, like 2.5-5.3 instead of 2.5,-5.3 but when tweening, the negative value may switch to positive, so we insert the comma just in case.\n        s: startNum,\n        c: endNum.charAt(1) === \"=\" ? _parseRelative(startNum, endNum) - startNum : parseFloat(endNum) - startNum,\n        m: color && color < 4 ? Math.round : 0\n      };\n      index = _complexStringNumExp.lastIndex;\n    }\n  }\n\n  pt.c = index < end.length ? end.substring(index, end.length) : \"\"; //we use the \"c\" of the PropTween to store the final part of the string (after the last number)\n\n  pt.fp = funcParam;\n\n  if (_relExp.test(end) || hasRandom) {\n    pt.e = 0; //if the end string contains relative values or dynamic random(...) values, delete the end it so that on the final render we don't actually set it to the string with += or -= characters (forces it to use the calculated value).\n  }\n\n  this._pt = pt; //start the linked list with this new PropTween. Remember, we call _addComplexStringPropTween.call(tweenInstance...) to ensure that it's scoped properly. We may call it from within a plugin too, thus \"this\" would refer to the plugin.\n\n  return pt;\n},\n    _addPropTween = function _addPropTween(target, prop, start, end, index, targets, modifier, stringFilter, funcParam, optional) {\n  _isFunction(end) && (end = end(index || 0, target, targets));\n  var currentValue = target[prop],\n      parsedStart = start !== \"get\" ? start : !_isFunction(currentValue) ? currentValue : funcParam ? target[prop.indexOf(\"set\") || !_isFunction(target[\"get\" + prop.substr(3)]) ? prop : \"get\" + prop.substr(3)](funcParam) : target[prop](),\n      setter = !_isFunction(currentValue) ? _setterPlain : funcParam ? _setterFuncWithParam : _setterFunc,\n      pt;\n\n  if (_isString(end)) {\n    if (~end.indexOf(\"random(\")) {\n      end = _replaceRandom(end);\n    }\n\n    if (end.charAt(1) === \"=\") {\n      pt = _parseRelative(parsedStart, end) + (getUnit(parsedStart) || 0);\n\n      if (pt || pt === 0) {\n        // to avoid isNaN, like if someone passes in a value like \"!= whatever\"\n        end = pt;\n      }\n    }\n  }\n\n  if (!optional || parsedStart !== end || _forceAllPropTweens) {\n    if (!isNaN(parsedStart * end) && end !== \"\") {\n      // fun fact: any number multiplied by \"\" is evaluated as the number 0!\n      pt = new PropTween(this._pt, target, prop, +parsedStart || 0, end - (parsedStart || 0), typeof currentValue === \"boolean\" ? _renderBoolean : _renderPlain, 0, setter);\n      funcParam && (pt.fp = funcParam);\n      modifier && pt.modifier(modifier, this, target);\n      return this._pt = pt;\n    }\n\n    !currentValue && !(prop in target) && _missingPlugin(prop, end);\n    return _addComplexStringPropTween.call(this, target, prop, parsedStart, end, setter, stringFilter || _config.stringFilter, funcParam);\n  }\n},\n    //creates a copy of the vars object and processes any function-based values (putting the resulting values directly into the copy) as well as strings with \"random()\" in them. It does NOT process relative values.\n_processVars = function _processVars(vars, index, target, targets, tween) {\n  _isFunction(vars) && (vars = _parseFuncOrString(vars, tween, index, target, targets));\n\n  if (!_isObject(vars) || vars.style && vars.nodeType || _isArray(vars) || _isTypedArray(vars)) {\n    return _isString(vars) ? _parseFuncOrString(vars, tween, index, target, targets) : vars;\n  }\n\n  var copy = {},\n      p;\n\n  for (p in vars) {\n    copy[p] = _parseFuncOrString(vars[p], tween, index, target, targets);\n  }\n\n  return copy;\n},\n    _checkPlugin = function _checkPlugin(property, vars, tween, index, target, targets) {\n  var plugin, pt, ptLookup, i;\n\n  if (_plugins[property] && (plugin = new _plugins[property]()).init(target, plugin.rawVars ? vars[property] : _processVars(vars[property], index, target, targets, tween), tween, index, targets) !== false) {\n    tween._pt = pt = new PropTween(tween._pt, target, property, 0, 1, plugin.render, plugin, 0, plugin.priority);\n\n    if (tween !== _quickTween) {\n      ptLookup = tween._ptLookup[tween._targets.indexOf(target)]; //note: we can't use tween._ptLookup[index] because for staggered tweens, the index from the fullTargets array won't match what it is in each individual tween that spawns from the stagger.\n\n      i = plugin._props.length;\n\n      while (i--) {\n        ptLookup[plugin._props[i]] = pt;\n      }\n    }\n  }\n\n  return plugin;\n},\n    _overwritingTween,\n    //store a reference temporarily so we can avoid overwriting itself.\n_forceAllPropTweens,\n    _initTween = function _initTween(tween, time, tTime) {\n  var vars = tween.vars,\n      ease = vars.ease,\n      startAt = vars.startAt,\n      immediateRender = vars.immediateRender,\n      lazy = vars.lazy,\n      onUpdate = vars.onUpdate,\n      runBackwards = vars.runBackwards,\n      yoyoEase = vars.yoyoEase,\n      keyframes = vars.keyframes,\n      autoRevert = vars.autoRevert,\n      dur = tween._dur,\n      prevStartAt = tween._startAt,\n      targets = tween._targets,\n      parent = tween.parent,\n      fullTargets = parent && parent.data === \"nested\" ? parent.vars.targets : targets,\n      autoOverwrite = tween._overwrite === \"auto\" && !_suppressOverwrites,\n      tl = tween.timeline,\n      cleanVars,\n      i,\n      p,\n      pt,\n      target,\n      hasPriority,\n      gsData,\n      harness,\n      plugin,\n      ptLookup,\n      index,\n      harnessVars,\n      overwritten;\n  tl && (!keyframes || !ease) && (ease = \"none\");\n  tween._ease = _parseEase(ease, _defaults.ease);\n  tween._yEase = yoyoEase ? _invertEase(_parseEase(yoyoEase === true ? ease : yoyoEase, _defaults.ease)) : 0;\n\n  if (yoyoEase && tween._yoyo && !tween._repeat) {\n    //there must have been a parent timeline with yoyo:true that is currently in its yoyo phase, so flip the eases.\n    yoyoEase = tween._yEase;\n    tween._yEase = tween._ease;\n    tween._ease = yoyoEase;\n  }\n\n  tween._from = !tl && !!vars.runBackwards; //nested timelines should never run backwards - the backwards-ness is in the child tweens.\n\n  if (!tl || keyframes && !vars.stagger) {\n    //if there's an internal timeline, skip all the parsing because we passed that task down the chain.\n    harness = targets[0] ? _getCache(targets[0]).harness : 0;\n    harnessVars = harness && vars[harness.prop]; //someone may need to specify CSS-specific values AND non-CSS values, like if the element has an \"x\" property plus it's a standard DOM element. We allow people to distinguish by wrapping plugin-specific stuff in a css:{} object for example.\n\n    cleanVars = _copyExcluding(vars, _reservedProps);\n\n    if (prevStartAt) {\n      prevStartAt._zTime < 0 && prevStartAt.progress(1); // in case it's a lazy startAt that hasn't rendered yet.\n\n      time < 0 && runBackwards && immediateRender && !autoRevert ? prevStartAt.render(-1, true) : prevStartAt.revert(runBackwards && dur ? _revertConfigNoKill : _startAtRevertConfig); // if it's a \"startAt\" (not \"from()\" or runBackwards: true), we only need to do a shallow revert (keep transforms cached in CSSPlugin)\n      // don't just _removeFromParent(prevStartAt.render(-1, true)) because that'll leave inline styles. We're creating a new _startAt for \"startAt\" tweens that re-capture things to ensure that if the pre-tween values changed since the tween was created, they're recorded.\n\n      prevStartAt._lazy = 0;\n    }\n\n    if (startAt) {\n      _removeFromParent(tween._startAt = Tween.set(targets, _setDefaults({\n        data: \"isStart\",\n        overwrite: false,\n        parent: parent,\n        immediateRender: true,\n        lazy: !prevStartAt && _isNotFalse(lazy),\n        startAt: null,\n        delay: 0,\n        onUpdate: onUpdate && function () {\n          return _callback(tween, \"onUpdate\");\n        },\n        stagger: 0\n      }, startAt))); //copy the properties/values into a new object to avoid collisions, like var to = {x:0}, from = {x:500}; timeline.fromTo(e, from, to).fromTo(e, to, from);\n\n\n      tween._startAt._dp = 0; // don't allow it to get put back into root timeline! Like when revert() is called and totalTime() gets set.\n\n      tween._startAt._sat = tween; // used in globalTime(). _sat stands for _startAtTween\n\n      time < 0 && (_reverting || !immediateRender && !autoRevert) && tween._startAt.revert(_revertConfigNoKill); // rare edge case, like if a render is forced in the negative direction of a non-initted tween.\n\n      if (immediateRender) {\n        if (dur && time <= 0 && tTime <= 0) {\n          // check tTime here because in the case of a yoyo tween whose playhead gets pushed to the end like tween.progress(1), we should allow it through so that the onComplete gets fired properly.\n          time && (tween._zTime = time);\n          return; //we skip initialization here so that overwriting doesn't occur until the tween actually begins. Otherwise, if you create several immediateRender:true tweens of the same target/properties to drop into a Timeline, the last one created would overwrite the first ones because they didn't get placed into the timeline yet before the first render occurs and kicks in overwriting.\n        }\n      }\n    } else if (runBackwards && dur) {\n      //from() tweens must be handled uniquely: their beginning values must be rendered but we don't want overwriting to occur yet (when time is still 0). Wait until the tween actually begins before doing all the routines like overwriting. At that time, we should render at the END of the tween to ensure that things initialize correctly (remember, from() tweens go backwards)\n      if (!prevStartAt) {\n        time && (immediateRender = false); //in rare cases (like if a from() tween runs and then is invalidate()-ed), immediateRender could be true but the initial forced-render gets skipped, so there's no need to force the render in this context when the _time is greater than 0\n\n        p = _setDefaults({\n          overwrite: false,\n          data: \"isFromStart\",\n          //we tag the tween with as \"isFromStart\" so that if [inside a plugin] we need to only do something at the very END of a tween, we have a way of identifying this tween as merely the one that's setting the beginning values for a \"from()\" tween. For example, clearProps in CSSPlugin should only get applied at the very END of a tween and without this tag, from(...{height:100, clearProps:\"height\", delay:1}) would wipe the height at the beginning of the tween and after 1 second, it'd kick back in.\n          lazy: immediateRender && !prevStartAt && _isNotFalse(lazy),\n          immediateRender: immediateRender,\n          //zero-duration tweens render immediately by default, but if we're not specifically instructed to render this tween immediately, we should skip this and merely _init() to record the starting values (rendering them immediately would push them to completion which is wasteful in that case - we'd have to render(-1) immediately after)\n          stagger: 0,\n          parent: parent //ensures that nested tweens that had a stagger are handled properly, like gsap.from(\".class\", {y: gsap.utils.wrap([-100,100]), stagger: 0.5})\n\n        }, cleanVars);\n        harnessVars && (p[harness.prop] = harnessVars); // in case someone does something like .from(..., {css:{}})\n\n        _removeFromParent(tween._startAt = Tween.set(targets, p));\n\n        tween._startAt._dp = 0; // don't allow it to get put back into root timeline!\n\n        tween._startAt._sat = tween; // used in globalTime()\n\n        time < 0 && (_reverting ? tween._startAt.revert(_revertConfigNoKill) : tween._startAt.render(-1, true));\n        tween._zTime = time;\n\n        if (!immediateRender) {\n          _initTween(tween._startAt, _tinyNum, _tinyNum); //ensures that the initial values are recorded\n\n        } else if (!time) {\n          return;\n        }\n      }\n    }\n\n    tween._pt = tween._ptCache = 0;\n    lazy = dur && _isNotFalse(lazy) || lazy && !dur;\n\n    for (i = 0; i < targets.length; i++) {\n      target = targets[i];\n      gsData = target._gsap || _harness(targets)[i]._gsap;\n      tween._ptLookup[i] = ptLookup = {};\n      _lazyLookup[gsData.id] && _lazyTweens.length && _lazyRender(); //if other tweens of the same target have recently initted but haven't rendered yet, we've got to force the render so that the starting values are correct (imagine populating a timeline with a bunch of sequential tweens and then jumping to the end)\n\n      index = fullTargets === targets ? i : fullTargets.indexOf(target);\n\n      if (harness && (plugin = new harness()).init(target, harnessVars || cleanVars, tween, index, fullTargets) !== false) {\n        tween._pt = pt = new PropTween(tween._pt, target, plugin.name, 0, 1, plugin.render, plugin, 0, plugin.priority);\n\n        plugin._props.forEach(function (name) {\n          ptLookup[name] = pt;\n        });\n\n        plugin.priority && (hasPriority = 1);\n      }\n\n      if (!harness || harnessVars) {\n        for (p in cleanVars) {\n          if (_plugins[p] && (plugin = _checkPlugin(p, cleanVars, tween, index, target, fullTargets))) {\n            plugin.priority && (hasPriority = 1);\n          } else {\n            ptLookup[p] = pt = _addPropTween.call(tween, target, p, \"get\", cleanVars[p], index, fullTargets, 0, vars.stringFilter);\n          }\n        }\n      }\n\n      tween._op && tween._op[i] && tween.kill(target, tween._op[i]);\n\n      if (autoOverwrite && tween._pt) {\n        _overwritingTween = tween;\n\n        _globalTimeline.killTweensOf(target, ptLookup, tween.globalTime(time)); // make sure the overwriting doesn't overwrite THIS tween!!!\n\n\n        overwritten = !tween.parent;\n        _overwritingTween = 0;\n      }\n\n      tween._pt && lazy && (_lazyLookup[gsData.id] = 1);\n    }\n\n    hasPriority && _sortPropTweensByPriority(tween);\n    tween._onInit && tween._onInit(tween); //plugins like RoundProps must wait until ALL of the PropTweens are instantiated. In the plugin's init() function, it sets the _onInit on the tween instance. May not be pretty/intuitive, but it's fast and keeps file size down.\n  }\n\n  tween._onUpdate = onUpdate;\n  tween._initted = (!tween._op || tween._pt) && !overwritten; // if overwrittenProps resulted in the entire tween being killed, do NOT flag it as initted or else it may render for one tick.\n\n  keyframes && time <= 0 && tl.render(_bigNum, true, true); // if there's a 0% keyframe, it'll render in the \"before\" state for any staggered/delayed animations thus when the following tween initializes, it'll use the \"before\" state instead of the \"after\" state as the initial values.\n},\n    _updatePropTweens = function _updatePropTweens(tween, property, value, start, startIsRelative, ratio, time, skipRecursion) {\n  var ptCache = (tween._pt && tween._ptCache || (tween._ptCache = {}))[property],\n      pt,\n      rootPT,\n      lookup,\n      i;\n\n  if (!ptCache) {\n    ptCache = tween._ptCache[property] = [];\n    lookup = tween._ptLookup;\n    i = tween._targets.length;\n\n    while (i--) {\n      pt = lookup[i][property];\n\n      if (pt && pt.d && pt.d._pt) {\n        // it's a plugin, so find the nested PropTween\n        pt = pt.d._pt;\n\n        while (pt && pt.p !== property && pt.fp !== property) {\n          // \"fp\" is functionParam for things like setting CSS variables which require .setProperty(\"--var-name\", value)\n          pt = pt._next;\n        }\n      }\n\n      if (!pt) {\n        // there is no PropTween associated with that property, so we must FORCE one to be created and ditch out of this\n        // if the tween has other properties that already rendered at new positions, we'd normally have to rewind to put them back like tween.render(0, true) before forcing an _initTween(), but that can create another edge case like tweening a timeline's progress would trigger onUpdates to fire which could move other things around. It's better to just inform users that .resetTo() should ONLY be used for tweens that already have that property. For example, you can't gsap.to(...{ y: 0 }) and then tween.restTo(\"x\", 200) for example.\n        _forceAllPropTweens = 1; // otherwise, when we _addPropTween() and it finds no change between the start and end values, it skips creating a PropTween (for efficiency...why tween when there's no difference?) but in this case we NEED that PropTween created so we can edit it.\n\n        tween.vars[property] = \"+=0\";\n\n        _initTween(tween, time);\n\n        _forceAllPropTweens = 0;\n        return skipRecursion ? _warn(property + \" not eligible for reset\") : 1; // if someone tries to do a quickTo() on a special property like borderRadius which must get split into 4 different properties, that's not eligible for .resetTo().\n      }\n\n      ptCache.push(pt);\n    }\n  }\n\n  i = ptCache.length;\n\n  while (i--) {\n    rootPT = ptCache[i];\n    pt = rootPT._pt || rootPT; // complex values may have nested PropTweens. We only accommodate the FIRST value.\n\n    pt.s = (start || start === 0) && !startIsRelative ? start : pt.s + (start || 0) + ratio * pt.c;\n    pt.c = value - pt.s;\n    rootPT.e && (rootPT.e = _round(value) + getUnit(rootPT.e)); // mainly for CSSPlugin (end value)\n\n    rootPT.b && (rootPT.b = pt.s + getUnit(rootPT.b)); // (beginning value)\n  }\n},\n    _addAliasesToVars = function _addAliasesToVars(targets, vars) {\n  var harness = targets[0] ? _getCache(targets[0]).harness : 0,\n      propertyAliases = harness && harness.aliases,\n      copy,\n      p,\n      i,\n      aliases;\n\n  if (!propertyAliases) {\n    return vars;\n  }\n\n  copy = _merge({}, vars);\n\n  for (p in propertyAliases) {\n    if (p in copy) {\n      aliases = propertyAliases[p].split(\",\");\n      i = aliases.length;\n\n      while (i--) {\n        copy[aliases[i]] = copy[p];\n      }\n    }\n  }\n\n  return copy;\n},\n    // parses multiple formats, like {\"0%\": {x: 100}, {\"50%\": {x: -20}} and { x: {\"0%\": 100, \"50%\": -20} }, and an \"ease\" can be set on any object. We populate an \"allProps\" object with an Array for each property, like {x: [{}, {}], y:[{}, {}]} with data for each property tween. The objects have a \"t\" (time), \"v\", (value), and \"e\" (ease) property. This allows us to piece together a timeline later.\n_parseKeyframe = function _parseKeyframe(prop, obj, allProps, easeEach) {\n  var ease = obj.ease || easeEach || \"power1.inOut\",\n      p,\n      a;\n\n  if (_isArray(obj)) {\n    a = allProps[prop] || (allProps[prop] = []); // t = time (out of 100), v = value, e = ease\n\n    obj.forEach(function (value, i) {\n      return a.push({\n        t: i / (obj.length - 1) * 100,\n        v: value,\n        e: ease\n      });\n    });\n  } else {\n    for (p in obj) {\n      a = allProps[p] || (allProps[p] = []);\n      p === \"ease\" || a.push({\n        t: parseFloat(prop),\n        v: obj[p],\n        e: ease\n      });\n    }\n  }\n},\n    _parseFuncOrString = function _parseFuncOrString(value, tween, i, target, targets) {\n  return _isFunction(value) ? value.call(tween, i, target, targets) : _isString(value) && ~value.indexOf(\"random(\") ? _replaceRandom(value) : value;\n},\n    _staggerTweenProps = _callbackNames + \"repeat,repeatDelay,yoyo,repeatRefresh,yoyoEase,autoRevert\",\n    _staggerPropsToSkip = {};\n\n_forEachName(_staggerTweenProps + \",id,stagger,delay,duration,paused,scrollTrigger\", function (name) {\n  return _staggerPropsToSkip[name] = 1;\n});\n/*\n * --------------------------------------------------------------------------------------\n * TWEEN\n * --------------------------------------------------------------------------------------\n */\n\n\nvar Tween = /*#__PURE__*/function (_Animation2) {\n  _inheritsLoose(Tween, _Animation2);\n\n  function Tween(targets, vars, position, skipInherit) {\n    var _this3;\n\n    if (typeof vars === \"number\") {\n      position.duration = vars;\n      vars = position;\n      position = null;\n    }\n\n    _this3 = _Animation2.call(this, skipInherit ? vars : _inheritDefaults(vars)) || this;\n    var _this3$vars = _this3.vars,\n        duration = _this3$vars.duration,\n        delay = _this3$vars.delay,\n        immediateRender = _this3$vars.immediateRender,\n        stagger = _this3$vars.stagger,\n        overwrite = _this3$vars.overwrite,\n        keyframes = _this3$vars.keyframes,\n        defaults = _this3$vars.defaults,\n        scrollTrigger = _this3$vars.scrollTrigger,\n        yoyoEase = _this3$vars.yoyoEase,\n        parent = vars.parent || _globalTimeline,\n        parsedTargets = (_isArray(targets) || _isTypedArray(targets) ? _isNumber(targets[0]) : \"length\" in vars) ? [targets] : toArray(targets),\n        tl,\n        i,\n        copy,\n        l,\n        p,\n        curTarget,\n        staggerFunc,\n        staggerVarsToMerge;\n    _this3._targets = parsedTargets.length ? _harness(parsedTargets) : _warn(\"GSAP target \" + targets + \" not found. https://gsap.com\", !_config.nullTargetWarn) || [];\n    _this3._ptLookup = []; //PropTween lookup. An array containing an object for each target, having keys for each tweening property\n\n    _this3._overwrite = overwrite;\n\n    if (keyframes || stagger || _isFuncOrString(duration) || _isFuncOrString(delay)) {\n      vars = _this3.vars;\n      tl = _this3.timeline = new Timeline({\n        data: \"nested\",\n        defaults: defaults || {},\n        targets: parent && parent.data === \"nested\" ? parent.vars.targets : parsedTargets\n      }); // we need to store the targets because for staggers and keyframes, we end up creating an individual tween for each but function-based values need to know the index and the whole Array of targets.\n\n      tl.kill();\n      tl.parent = tl._dp = _assertThisInitialized(_this3);\n      tl._start = 0;\n\n      if (stagger || _isFuncOrString(duration) || _isFuncOrString(delay)) {\n        l = parsedTargets.length;\n        staggerFunc = stagger && distribute(stagger);\n\n        if (_isObject(stagger)) {\n          //users can pass in callbacks like onStart/onComplete in the stagger object. These should fire with each individual tween.\n          for (p in stagger) {\n            if (~_staggerTweenProps.indexOf(p)) {\n              staggerVarsToMerge || (staggerVarsToMerge = {});\n              staggerVarsToMerge[p] = stagger[p];\n            }\n          }\n        }\n\n        for (i = 0; i < l; i++) {\n          copy = _copyExcluding(vars, _staggerPropsToSkip);\n          copy.stagger = 0;\n          yoyoEase && (copy.yoyoEase = yoyoEase);\n          staggerVarsToMerge && _merge(copy, staggerVarsToMerge);\n          curTarget = parsedTargets[i]; //don't just copy duration or delay because if they're a string or function, we'd end up in an infinite loop because _isFuncOrString() would evaluate as true in the child tweens, entering this loop, etc. So we parse the value straight from vars and default to 0.\n\n          copy.duration = +_parseFuncOrString(duration, _assertThisInitialized(_this3), i, curTarget, parsedTargets);\n          copy.delay = (+_parseFuncOrString(delay, _assertThisInitialized(_this3), i, curTarget, parsedTargets) || 0) - _this3._delay;\n\n          if (!stagger && l === 1 && copy.delay) {\n            // if someone does delay:\"random(1, 5)\", repeat:-1, for example, the delay shouldn't be inside the repeat.\n            _this3._delay = delay = copy.delay;\n            _this3._start += delay;\n            copy.delay = 0;\n          }\n\n          tl.to(curTarget, copy, staggerFunc ? staggerFunc(i, curTarget, parsedTargets) : 0);\n          tl._ease = _easeMap.none;\n        }\n\n        tl.duration() ? duration = delay = 0 : _this3.timeline = 0; // if the timeline's duration is 0, we don't need a timeline internally!\n      } else if (keyframes) {\n        _inheritDefaults(_setDefaults(tl.vars.defaults, {\n          ease: \"none\"\n        }));\n\n        tl._ease = _parseEase(keyframes.ease || vars.ease || \"none\");\n        var time = 0,\n            a,\n            kf,\n            v;\n\n        if (_isArray(keyframes)) {\n          keyframes.forEach(function (frame) {\n            return tl.to(parsedTargets, frame, \">\");\n          });\n          tl.duration(); // to ensure tl._dur is cached because we tap into it for performance purposes in the render() method.\n        } else {\n          copy = {};\n\n          for (p in keyframes) {\n            p === \"ease\" || p === \"easeEach\" || _parseKeyframe(p, keyframes[p], copy, keyframes.easeEach);\n          }\n\n          for (p in copy) {\n            a = copy[p].sort(function (a, b) {\n              return a.t - b.t;\n            });\n            time = 0;\n\n            for (i = 0; i < a.length; i++) {\n              kf = a[i];\n              v = {\n                ease: kf.e,\n                duration: (kf.t - (i ? a[i - 1].t : 0)) / 100 * duration\n              };\n              v[p] = kf.v;\n              tl.to(parsedTargets, v, time);\n              time += v.duration;\n            }\n          }\n\n          tl.duration() < duration && tl.to({}, {\n            duration: duration - tl.duration()\n          }); // in case keyframes didn't go to 100%\n        }\n      }\n\n      duration || _this3.duration(duration = tl.duration());\n    } else {\n      _this3.timeline = 0; //speed optimization, faster lookups (no going up the prototype chain)\n    }\n\n    if (overwrite === true && !_suppressOverwrites) {\n      _overwritingTween = _assertThisInitialized(_this3);\n\n      _globalTimeline.killTweensOf(parsedTargets);\n\n      _overwritingTween = 0;\n    }\n\n    _addToTimeline(parent, _assertThisInitialized(_this3), position);\n\n    vars.reversed && _this3.reverse();\n    vars.paused && _this3.paused(true);\n\n    if (immediateRender || !duration && !keyframes && _this3._start === _roundPrecise(parent._time) && _isNotFalse(immediateRender) && _hasNoPausedAncestors(_assertThisInitialized(_this3)) && parent.data !== \"nested\") {\n      _this3._tTime = -_tinyNum; //forces a render without having to set the render() \"force\" parameter to true because we want to allow lazying by default (using the \"force\" parameter always forces an immediate full render)\n\n      _this3.render(Math.max(0, -delay) || 0); //in case delay is negative\n\n    }\n\n    scrollTrigger && _scrollTrigger(_assertThisInitialized(_this3), scrollTrigger);\n    return _this3;\n  }\n\n  var _proto3 = Tween.prototype;\n\n  _proto3.render = function render(totalTime, suppressEvents, force) {\n    var prevTime = this._time,\n        tDur = this._tDur,\n        dur = this._dur,\n        isNegative = totalTime < 0,\n        tTime = totalTime > tDur - _tinyNum && !isNegative ? tDur : totalTime < _tinyNum ? 0 : totalTime,\n        time,\n        pt,\n        iteration,\n        cycleDuration,\n        prevIteration,\n        isYoyo,\n        ratio,\n        timeline,\n        yoyoEase;\n\n    if (!dur) {\n      _renderZeroDurationTween(this, totalTime, suppressEvents, force);\n    } else if (tTime !== this._tTime || !totalTime || force || !this._initted && this._tTime || this._startAt && this._zTime < 0 !== isNegative) {\n      //this senses if we're crossing over the start time, in which case we must record _zTime and force the render, but we do it in this lengthy conditional way for performance reasons (usually we can skip the calculations): this._initted && (this._zTime < 0) !== (totalTime < 0)\n      time = tTime;\n      timeline = this.timeline;\n\n      if (this._repeat) {\n        //adjust the time for repeats and yoyos\n        cycleDuration = dur + this._rDelay;\n\n        if (this._repeat < -1 && isNegative) {\n          return this.totalTime(cycleDuration * 100 + totalTime, suppressEvents, force);\n        }\n\n        time = _roundPrecise(tTime % cycleDuration); //round to avoid floating point errors. (4 % 0.8 should be 0 but some browsers report it as 0.79999999!)\n\n        if (tTime === tDur) {\n          // the tDur === tTime is for edge cases where there's a lengthy decimal on the duration and it may reach the very end but the time is rendered as not-quite-there (remember, tDur is rounded to 4 decimals whereas dur isn't)\n          iteration = this._repeat;\n          time = dur;\n        } else {\n          iteration = ~~(tTime / cycleDuration);\n\n          if (iteration && iteration === _roundPrecise(tTime / cycleDuration)) {\n            time = dur;\n            iteration--;\n          }\n\n          time > dur && (time = dur);\n        }\n\n        isYoyo = this._yoyo && iteration & 1;\n\n        if (isYoyo) {\n          yoyoEase = this._yEase;\n          time = dur - time;\n        }\n\n        prevIteration = _animationCycle(this._tTime, cycleDuration);\n\n        if (time === prevTime && !force && this._initted && iteration === prevIteration) {\n          //could be during the repeatDelay part. No need to render and fire callbacks.\n          this._tTime = tTime;\n          return this;\n        }\n\n        if (iteration !== prevIteration) {\n          timeline && this._yEase && _propagateYoyoEase(timeline, isYoyo); //repeatRefresh functionality\n\n          if (this.vars.repeatRefresh && !isYoyo && !this._lock && this._time !== cycleDuration && this._initted) {\n            // this._time will === cycleDuration when we render at EXACTLY the end of an iteration. Without this condition, it'd often do the repeatRefresh render TWICE (again on the very next tick).\n            this._lock = force = 1; //force, otherwise if lazy is true, the _attemptInitTween() will return and we'll jump out and get caught bouncing on each tick.\n\n            this.render(_roundPrecise(cycleDuration * iteration), true).invalidate()._lock = 0;\n          }\n        }\n      }\n\n      if (!this._initted) {\n        if (_attemptInitTween(this, isNegative ? totalTime : time, force, suppressEvents, tTime)) {\n          this._tTime = 0; // in constructor if immediateRender is true, we set _tTime to -_tinyNum to have the playhead cross the starting point but we can't leave _tTime as a negative number.\n\n          return this;\n        }\n\n        if (prevTime !== this._time && !(force && this.vars.repeatRefresh && iteration !== prevIteration)) {\n          // rare edge case - during initialization, an onUpdate in the _startAt (.fromTo()) might force this tween to render at a different spot in which case we should ditch this render() call so that it doesn't revert the values. But we also don't want to dump if we're doing a repeatRefresh render!\n          return this;\n        }\n\n        if (dur !== this._dur) {\n          // while initting, a plugin like InertiaPlugin might alter the duration, so rerun from the start to ensure everything renders as it should.\n          return this.render(totalTime, suppressEvents, force);\n        }\n      }\n\n      this._tTime = tTime;\n      this._time = time;\n\n      if (!this._act && this._ts) {\n        this._act = 1; //as long as it's not paused, force it to be active so that if the user renders independent of the parent timeline, it'll be forced to re-render on the next tick.\n\n        this._lazy = 0;\n      }\n\n      this.ratio = ratio = (yoyoEase || this._ease)(time / dur);\n\n      if (this._from) {\n        this.ratio = ratio = 1 - ratio;\n      }\n\n      if (time && !prevTime && !suppressEvents && !iteration) {\n        _callback(this, \"onStart\");\n\n        if (this._tTime !== tTime) {\n          // in case the onStart triggered a render at a different spot, eject. Like if someone did animation.pause(0.5) or something inside the onStart.\n          return this;\n        }\n      }\n\n      pt = this._pt;\n\n      while (pt) {\n        pt.r(ratio, pt.d);\n        pt = pt._next;\n      }\n\n      timeline && timeline.render(totalTime < 0 ? totalTime : timeline._dur * timeline._ease(time / this._dur), suppressEvents, force) || this._startAt && (this._zTime = totalTime);\n\n      if (this._onUpdate && !suppressEvents) {\n        isNegative && _rewindStartAt(this, totalTime, suppressEvents, force); //note: for performance reasons, we tuck this conditional logic inside less traveled areas (most tweens don't have an onUpdate). We'd just have it at the end before the onComplete, but the values should be updated before any onUpdate is called, so we ALSO put it here and then if it's not called, we do so later near the onComplete.\n\n        _callback(this, \"onUpdate\");\n      }\n\n      this._repeat && iteration !== prevIteration && this.vars.onRepeat && !suppressEvents && this.parent && _callback(this, \"onRepeat\");\n\n      if ((tTime === this._tDur || !tTime) && this._tTime === tTime) {\n        isNegative && !this._onUpdate && _rewindStartAt(this, totalTime, true, true);\n        (totalTime || !dur) && (tTime === this._tDur && this._ts > 0 || !tTime && this._ts < 0) && _removeFromParent(this, 1); // don't remove if we're rendering at exactly a time of 0, as there could be autoRevert values that should get set on the next tick (if the playhead goes backward beyond the startTime, negative totalTime). Don't remove if the timeline is reversed and the playhead isn't at 0, otherwise tl.progress(1).reverse() won't work. Only remove if the playhead is at the end and timeScale is positive, or if the playhead is at 0 and the timeScale is negative.\n\n        if (!suppressEvents && !(isNegative && !prevTime) && (tTime || prevTime || isYoyo)) {\n          // if prevTime and tTime are zero, we shouldn't fire the onReverseComplete. This could happen if you gsap.to(... {paused:true}).play();\n          _callback(this, tTime === tDur ? \"onComplete\" : \"onReverseComplete\", true);\n\n          this._prom && !(tTime < tDur && this.timeScale() > 0) && this._prom();\n        }\n      }\n    }\n\n    return this;\n  };\n\n  _proto3.targets = function targets() {\n    return this._targets;\n  };\n\n  _proto3.invalidate = function invalidate(soft) {\n    // \"soft\" gives us a way to clear out everything EXCEPT the recorded pre-\"from\" portion of from() tweens. Otherwise, for example, if you tween.progress(1).render(0, true true).invalidate(), the \"from\" values would persist and then on the next render, the from() tweens would initialize and the current value would match the \"from\" values, thus animate from the same value to the same value (no animation). We tap into this in ScrollTrigger's refresh() where we must push a tween to completion and then back again but honor its init state in case the tween is dependent on another tween further up on the page.\n    (!soft || !this.vars.runBackwards) && (this._startAt = 0);\n    this._pt = this._op = this._onUpdate = this._lazy = this.ratio = 0;\n    this._ptLookup = [];\n    this.timeline && this.timeline.invalidate(soft);\n    return _Animation2.prototype.invalidate.call(this, soft);\n  };\n\n  _proto3.resetTo = function resetTo(property, value, start, startIsRelative, skipRecursion) {\n    _tickerActive || _ticker.wake();\n    this._ts || this.play();\n    var time = Math.min(this._dur, (this._dp._time - this._start) * this._ts),\n        ratio;\n    this._initted || _initTween(this, time);\n    ratio = this._ease(time / this._dur); // don't just get tween.ratio because it may not have rendered yet.\n    // possible future addition to allow an object with multiple values to update, like tween.resetTo({x: 100, y: 200}); At this point, it doesn't seem worth the added kb given the fact that most users will likely opt for the convenient gsap.quickTo() way of interacting with this method.\n    // if (_isObject(property)) { // performance optimization\n    // \tfor (p in property) {\n    // \t\tif (_updatePropTweens(this, p, property[p], value ? value[p] : null, start, ratio, time)) {\n    // \t\t\treturn this.resetTo(property, value, start, startIsRelative); // if a PropTween wasn't found for the property, it'll get forced with a re-initialization so we need to jump out and start over again.\n    // \t\t}\n    // \t}\n    // } else {\n\n    if (_updatePropTweens(this, property, value, start, startIsRelative, ratio, time, skipRecursion)) {\n      return this.resetTo(property, value, start, startIsRelative, 1); // if a PropTween wasn't found for the property, it'll get forced with a re-initialization so we need to jump out and start over again.\n    } //}\n\n\n    _alignPlayhead(this, 0);\n\n    this.parent || _addLinkedListItem(this._dp, this, \"_first\", \"_last\", this._dp._sort ? \"_start\" : 0);\n    return this.render(0);\n  };\n\n  _proto3.kill = function kill(targets, vars) {\n    if (vars === void 0) {\n      vars = \"all\";\n    }\n\n    if (!targets && (!vars || vars === \"all\")) {\n      this._lazy = this._pt = 0;\n      return this.parent ? _interrupt(this) : this;\n    }\n\n    if (this.timeline) {\n      var tDur = this.timeline.totalDuration();\n      this.timeline.killTweensOf(targets, vars, _overwritingTween && _overwritingTween.vars.overwrite !== true)._first || _interrupt(this); // if nothing is left tweening, interrupt.\n\n      this.parent && tDur !== this.timeline.totalDuration() && _setDuration(this, this._dur * this.timeline._tDur / tDur, 0, 1); // if a nested tween is killed that changes the duration, it should affect this tween's duration. We must use the ratio, though, because sometimes the internal timeline is stretched like for keyframes where they don't all add up to whatever the parent tween's duration was set to.\n\n      return this;\n    }\n\n    var parsedTargets = this._targets,\n        killingTargets = targets ? toArray(targets) : parsedTargets,\n        propTweenLookup = this._ptLookup,\n        firstPT = this._pt,\n        overwrittenProps,\n        curLookup,\n        curOverwriteProps,\n        props,\n        p,\n        pt,\n        i;\n\n    if ((!vars || vars === \"all\") && _arraysMatch(parsedTargets, killingTargets)) {\n      vars === \"all\" && (this._pt = 0);\n      return _interrupt(this);\n    }\n\n    overwrittenProps = this._op = this._op || [];\n\n    if (vars !== \"all\") {\n      //so people can pass in a comma-delimited list of property names\n      if (_isString(vars)) {\n        p = {};\n\n        _forEachName(vars, function (name) {\n          return p[name] = 1;\n        });\n\n        vars = p;\n      }\n\n      vars = _addAliasesToVars(parsedTargets, vars);\n    }\n\n    i = parsedTargets.length;\n\n    while (i--) {\n      if (~killingTargets.indexOf(parsedTargets[i])) {\n        curLookup = propTweenLookup[i];\n\n        if (vars === \"all\") {\n          overwrittenProps[i] = vars;\n          props = curLookup;\n          curOverwriteProps = {};\n        } else {\n          curOverwriteProps = overwrittenProps[i] = overwrittenProps[i] || {};\n          props = vars;\n        }\n\n        for (p in props) {\n          pt = curLookup && curLookup[p];\n\n          if (pt) {\n            if (!(\"kill\" in pt.d) || pt.d.kill(p) === true) {\n              _removeLinkedListItem(this, pt, \"_pt\");\n            }\n\n            delete curLookup[p];\n          }\n\n          if (curOverwriteProps !== \"all\") {\n            curOverwriteProps[p] = 1;\n          }\n        }\n      }\n    }\n\n    this._initted && !this._pt && firstPT && _interrupt(this); //if all tweening properties are killed, kill the tween. Without this line, if there's a tween with multiple targets and then you killTweensOf() each target individually, the tween would technically still remain active and fire its onComplete even though there aren't any more properties tweening.\n\n    return this;\n  };\n\n  Tween.to = function to(targets, vars) {\n    return new Tween(targets, vars, arguments[2]);\n  };\n\n  Tween.from = function from(targets, vars) {\n    return _createTweenType(1, arguments);\n  };\n\n  Tween.delayedCall = function delayedCall(delay, callback, params, scope) {\n    return new Tween(callback, 0, {\n      immediateRender: false,\n      lazy: false,\n      overwrite: false,\n      delay: delay,\n      onComplete: callback,\n      onReverseComplete: callback,\n      onCompleteParams: params,\n      onReverseCompleteParams: params,\n      callbackScope: scope\n    }); // we must use onReverseComplete too for things like timeline.add(() => {...}) which should be triggered in BOTH directions (forward and reverse)\n  };\n\n  Tween.fromTo = function fromTo(targets, fromVars, toVars) {\n    return _createTweenType(2, arguments);\n  };\n\n  Tween.set = function set(targets, vars) {\n    vars.duration = 0;\n    vars.repeatDelay || (vars.repeat = 0);\n    return new Tween(targets, vars);\n  };\n\n  Tween.killTweensOf = function killTweensOf(targets, props, onlyActive) {\n    return _globalTimeline.killTweensOf(targets, props, onlyActive);\n  };\n\n  return Tween;\n}(Animation);\n\n_setDefaults(Tween.prototype, {\n  _targets: [],\n  _lazy: 0,\n  _startAt: 0,\n  _op: 0,\n  _onInit: 0\n}); //add the pertinent timeline methods to Tween instances so that users can chain conveniently and create a timeline automatically. (removed due to concerns that it'd ultimately add to more confusion especially for beginners)\n// _forEachName(\"to,from,fromTo,set,call,add,addLabel,addPause\", name => {\n// \tTween.prototype[name] = function() {\n// \t\tlet tl = new Timeline();\n// \t\treturn _addToTimeline(tl, this)[name].apply(tl, toArray(arguments));\n// \t}\n// });\n//for backward compatibility. Leverage the timeline calls.\n\n\n_forEachName(\"staggerTo,staggerFrom,staggerFromTo\", function (name) {\n  Tween[name] = function () {\n    var tl = new Timeline(),\n        params = _slice.call(arguments, 0);\n\n    params.splice(name === \"staggerFromTo\" ? 5 : 4, 0, 0);\n    return tl[name].apply(tl, params);\n  };\n});\n/*\n * --------------------------------------------------------------------------------------\n * PROPTWEEN\n * --------------------------------------------------------------------------------------\n */\n\n\nvar _setterPlain = function _setterPlain(target, property, value) {\n  return target[property] = value;\n},\n    _setterFunc = function _setterFunc(target, property, value) {\n  return target[property](value);\n},\n    _setterFuncWithParam = function _setterFuncWithParam(target, property, value, data) {\n  return target[property](data.fp, value);\n},\n    _setterAttribute = function _setterAttribute(target, property, value) {\n  return target.setAttribute(property, value);\n},\n    _getSetter = function _getSetter(target, property) {\n  return _isFunction(target[property]) ? _setterFunc : _isUndefined(target[property]) && target.setAttribute ? _setterAttribute : _setterPlain;\n},\n    _renderPlain = function _renderPlain(ratio, data) {\n  return data.set(data.t, data.p, Math.round((data.s + data.c * ratio) * 1000000) / 1000000, data);\n},\n    _renderBoolean = function _renderBoolean(ratio, data) {\n  return data.set(data.t, data.p, !!(data.s + data.c * ratio), data);\n},\n    _renderComplexString = function _renderComplexString(ratio, data) {\n  var pt = data._pt,\n      s = \"\";\n\n  if (!ratio && data.b) {\n    //b = beginning string\n    s = data.b;\n  } else if (ratio === 1 && data.e) {\n    //e = ending string\n    s = data.e;\n  } else {\n    while (pt) {\n      s = pt.p + (pt.m ? pt.m(pt.s + pt.c * ratio) : Math.round((pt.s + pt.c * ratio) * 10000) / 10000) + s; //we use the \"p\" property for the text inbetween (like a suffix). And in the context of a complex string, the modifier (m) is typically just Math.round(), like for RGB colors.\n\n      pt = pt._next;\n    }\n\n    s += data.c; //we use the \"c\" of the PropTween to store the final chunk of non-numeric text.\n  }\n\n  data.set(data.t, data.p, s, data);\n},\n    _renderPropTweens = function _renderPropTweens(ratio, data) {\n  var pt = data._pt;\n\n  while (pt) {\n    pt.r(ratio, pt.d);\n    pt = pt._next;\n  }\n},\n    _addPluginModifier = function _addPluginModifier(modifier, tween, target, property) {\n  var pt = this._pt,\n      next;\n\n  while (pt) {\n    next = pt._next;\n    pt.p === property && pt.modifier(modifier, tween, target);\n    pt = next;\n  }\n},\n    _killPropTweensOf = function _killPropTweensOf(property) {\n  var pt = this._pt,\n      hasNonDependentRemaining,\n      next;\n\n  while (pt) {\n    next = pt._next;\n\n    if (pt.p === property && !pt.op || pt.op === property) {\n      _removeLinkedListItem(this, pt, \"_pt\");\n    } else if (!pt.dep) {\n      hasNonDependentRemaining = 1;\n    }\n\n    pt = next;\n  }\n\n  return !hasNonDependentRemaining;\n},\n    _setterWithModifier = function _setterWithModifier(target, property, value, data) {\n  data.mSet(target, property, data.m.call(data.tween, value, data.mt), data);\n},\n    _sortPropTweensByPriority = function _sortPropTweensByPriority(parent) {\n  var pt = parent._pt,\n      next,\n      pt2,\n      first,\n      last; //sorts the PropTween linked list in order of priority because some plugins need to do their work after ALL of the PropTweens were created (like RoundPropsPlugin and ModifiersPlugin)\n\n  while (pt) {\n    next = pt._next;\n    pt2 = first;\n\n    while (pt2 && pt2.pr > pt.pr) {\n      pt2 = pt2._next;\n    }\n\n    if (pt._prev = pt2 ? pt2._prev : last) {\n      pt._prev._next = pt;\n    } else {\n      first = pt;\n    }\n\n    if (pt._next = pt2) {\n      pt2._prev = pt;\n    } else {\n      last = pt;\n    }\n\n    pt = next;\n  }\n\n  parent._pt = first;\n}; //PropTween key: t = target, p = prop, r = renderer, d = data, s = start, c = change, op = overwriteProperty (ONLY populated when it's different than p), pr = priority, _next/_prev for the linked list siblings, set = setter, m = modifier, mSet = modifierSetter (the original setter, before a modifier was added)\n\n\nvar PropTween = /*#__PURE__*/function () {\n  function PropTween(next, target, prop, start, change, renderer, data, setter, priority) {\n    this.t = target;\n    this.s = start;\n    this.c = change;\n    this.p = prop;\n    this.r = renderer || _renderPlain;\n    this.d = data || this;\n    this.set = setter || _setterPlain;\n    this.pr = priority || 0;\n    this._next = next;\n\n    if (next) {\n      next._prev = this;\n    }\n  }\n\n  var _proto4 = PropTween.prototype;\n\n  _proto4.modifier = function modifier(func, tween, target) {\n    this.mSet = this.mSet || this.set; //in case it was already set (a PropTween can only have one modifier)\n\n    this.set = _setterWithModifier;\n    this.m = func;\n    this.mt = target; //modifier target\n\n    this.tween = tween;\n  };\n\n  return PropTween;\n}(); //Initialization tasks\n\n_forEachName(_callbackNames + \"parent,duration,ease,delay,overwrite,runBackwards,startAt,yoyo,immediateRender,repeat,repeatDelay,data,paused,reversed,lazy,callbackScope,stringFilter,id,yoyoEase,stagger,inherit,repeatRefresh,keyframes,autoRevert,scrollTrigger\", function (name) {\n  return _reservedProps[name] = 1;\n});\n\n_globals.TweenMax = _globals.TweenLite = Tween;\n_globals.TimelineLite = _globals.TimelineMax = Timeline;\n_globalTimeline = new Timeline({\n  sortChildren: false,\n  defaults: _defaults,\n  autoRemoveChildren: true,\n  id: \"root\",\n  smoothChildTiming: true\n});\n_config.stringFilter = _colorStringFilter;\n\nvar _media = [],\n    _listeners = {},\n    _emptyArray = [],\n    _lastMediaTime = 0,\n    _contextID = 0,\n    _dispatch = function _dispatch(type) {\n  return (_listeners[type] || _emptyArray).map(function (f) {\n    return f();\n  });\n},\n    _onMediaChange = function _onMediaChange() {\n  var time = Date.now(),\n      matches = [];\n\n  if (time - _lastMediaTime > 2) {\n    _dispatch(\"matchMediaInit\");\n\n    _media.forEach(function (c) {\n      var queries = c.queries,\n          conditions = c.conditions,\n          match,\n          p,\n          anyMatch,\n          toggled;\n\n      for (p in queries) {\n        match = _win.matchMedia(queries[p]).matches; // Firefox doesn't update the \"matches\" property of the MediaQueryList object correctly - it only does so as it calls its change handler - so we must re-create a media query here to ensure it's accurate.\n\n        match && (anyMatch = 1);\n\n        if (match !== conditions[p]) {\n          conditions[p] = match;\n          toggled = 1;\n        }\n      }\n\n      if (toggled) {\n        c.revert();\n        anyMatch && matches.push(c);\n      }\n    });\n\n    _dispatch(\"matchMediaRevert\");\n\n    matches.forEach(function (c) {\n      return c.onMatch(c, function (func) {\n        return c.add(null, func);\n      });\n    });\n    _lastMediaTime = time;\n\n    _dispatch(\"matchMedia\");\n  }\n};\n\nvar Context = /*#__PURE__*/function () {\n  function Context(func, scope) {\n    this.selector = scope && selector(scope);\n    this.data = [];\n    this._r = []; // returned/cleanup functions\n\n    this.isReverted = false;\n    this.id = _contextID++; // to work around issues that frameworks like Vue cause by making things into Proxies which make it impossible to do something like _media.indexOf(this) because \"this\" would no longer refer to the Context instance itself - it'd refer to a Proxy! We needed a way to identify the context uniquely\n\n    func && this.add(func);\n  }\n\n  var _proto5 = Context.prototype;\n\n  _proto5.add = function add(name, func, scope) {\n    // possible future addition if we need the ability to add() an animation to a context and for whatever reason cannot create that animation inside of a context.add(() => {...}) function.\n    // if (name && _isFunction(name.revert)) {\n    // \tthis.data.push(name);\n    // \treturn (name._ctx = this);\n    // }\n    if (_isFunction(name)) {\n      scope = func;\n      func = name;\n      name = _isFunction;\n    }\n\n    var self = this,\n        f = function f() {\n      var prev = _context,\n          prevSelector = self.selector,\n          result;\n      prev && prev !== self && prev.data.push(self);\n      scope && (self.selector = selector(scope));\n      _context = self;\n      result = func.apply(self, arguments);\n      _isFunction(result) && self._r.push(result);\n      _context = prev;\n      self.selector = prevSelector;\n      self.isReverted = false;\n      return result;\n    };\n\n    self.last = f;\n    return name === _isFunction ? f(self, function (func) {\n      return self.add(null, func);\n    }) : name ? self[name] = f : f;\n  };\n\n  _proto5.ignore = function ignore(func) {\n    var prev = _context;\n    _context = null;\n    func(this);\n    _context = prev;\n  };\n\n  _proto5.getTweens = function getTweens() {\n    var a = [];\n    this.data.forEach(function (e) {\n      return e instanceof Context ? a.push.apply(a, e.getTweens()) : e instanceof Tween && !(e.parent && e.parent.data === \"nested\") && a.push(e);\n    });\n    return a;\n  };\n\n  _proto5.clear = function clear() {\n    this._r.length = this.data.length = 0;\n  };\n\n  _proto5.kill = function kill(revert, matchMedia) {\n    var _this4 = this;\n\n    if (revert) {\n      (function () {\n        var tweens = _this4.getTweens(),\n            i = _this4.data.length,\n            t;\n\n        while (i--) {\n          // Flip plugin tweens are very different in that they should actually be pushed to their end. The plugin replaces the timeline's .revert() method to do exactly that. But we also need to remove any of those nested tweens inside the flip timeline so that they don't get individually reverted.\n          t = _this4.data[i];\n\n          if (t.data === \"isFlip\") {\n            t.revert();\n            t.getChildren(true, true, false).forEach(function (tween) {\n              return tweens.splice(tweens.indexOf(tween), 1);\n            });\n          }\n        } // save as an object so that we can cache the globalTime for each tween to optimize performance during the sort\n\n\n        tweens.map(function (t) {\n          return {\n            g: t._dur || t._delay || t._sat && !t._sat.vars.immediateRender ? t.globalTime(0) : -Infinity,\n            t: t\n          };\n        }).sort(function (a, b) {\n          return b.g - a.g || -Infinity;\n        }).forEach(function (o) {\n          return o.t.revert(revert);\n        }); // note: all of the _startAt tweens should be reverted in reverse order that they were created, and they'll all have the same globalTime (-1) so the \" || -1\" in the sort keeps the order properly.\n\n        i = _this4.data.length;\n\n        while (i--) {\n          // make sure we loop backwards so that, for example, SplitTexts that were created later on the same element get reverted first\n          t = _this4.data[i];\n\n          if (t instanceof Timeline) {\n            if (t.data !== \"nested\") {\n              t.scrollTrigger && t.scrollTrigger.revert();\n              t.kill(); // don't revert() the timeline because that's duplicating efforts since we already reverted all the tweens\n            }\n          } else {\n            !(t instanceof Tween) && t.revert && t.revert(revert);\n          }\n        }\n\n        _this4._r.forEach(function (f) {\n          return f(revert, _this4);\n        });\n\n        _this4.isReverted = true;\n      })();\n    } else {\n      this.data.forEach(function (e) {\n        return e.kill && e.kill();\n      });\n    }\n\n    this.clear();\n\n    if (matchMedia) {\n      var i = _media.length;\n\n      while (i--) {\n        // previously, we checked _media.indexOf(this), but some frameworks like Vue enforce Proxy objects that make it impossible to get the proper result that way, so we must use a unique ID number instead.\n        _media[i].id === this.id && _media.splice(i, 1);\n      }\n    }\n  };\n\n  _proto5.revert = function revert(config) {\n    this.kill(config || {});\n  };\n\n  return Context;\n}();\n\nvar MatchMedia = /*#__PURE__*/function () {\n  function MatchMedia(scope) {\n    this.contexts = [];\n    this.scope = scope;\n    _context && _context.data.push(this);\n  }\n\n  var _proto6 = MatchMedia.prototype;\n\n  _proto6.add = function add(conditions, func, scope) {\n    _isObject(conditions) || (conditions = {\n      matches: conditions\n    });\n    var context = new Context(0, scope || this.scope),\n        cond = context.conditions = {},\n        mq,\n        p,\n        active;\n    _context && !context.selector && (context.selector = _context.selector); // in case a context is created inside a context. Like a gsap.matchMedia() that's inside a scoped gsap.context()\n\n    this.contexts.push(context);\n    func = context.add(\"onMatch\", func);\n    context.queries = conditions;\n\n    for (p in conditions) {\n      if (p === \"all\") {\n        active = 1;\n      } else {\n        mq = _win.matchMedia(conditions[p]);\n\n        if (mq) {\n          _media.indexOf(context) < 0 && _media.push(context);\n          (cond[p] = mq.matches) && (active = 1);\n          mq.addListener ? mq.addListener(_onMediaChange) : mq.addEventListener(\"change\", _onMediaChange);\n        }\n      }\n    }\n\n    active && func(context, function (f) {\n      return context.add(null, f);\n    });\n    return this;\n  } // refresh() {\n  // \tlet time = _lastMediaTime,\n  // \t\tmedia = _media;\n  // \t_lastMediaTime = -1;\n  // \t_media = this.contexts;\n  // \t_onMediaChange();\n  // \t_lastMediaTime = time;\n  // \t_media = media;\n  // }\n  ;\n\n  _proto6.revert = function revert(config) {\n    this.kill(config || {});\n  };\n\n  _proto6.kill = function kill(revert) {\n    this.contexts.forEach(function (c) {\n      return c.kill(revert, true);\n    });\n  };\n\n  return MatchMedia;\n}();\n/*\n * --------------------------------------------------------------------------------------\n * GSAP\n * --------------------------------------------------------------------------------------\n */\n\n\nvar _gsap = {\n  registerPlugin: function registerPlugin() {\n    for (var _len2 = arguments.length, args = new Array(_len2), _key2 = 0; _key2 < _len2; _key2++) {\n      args[_key2] = arguments[_key2];\n    }\n\n    args.forEach(function (config) {\n      return _createPlugin(config);\n    });\n  },\n  timeline: function timeline(vars) {\n    return new Timeline(vars);\n  },\n  getTweensOf: function getTweensOf(targets, onlyActive) {\n    return _globalTimeline.getTweensOf(targets, onlyActive);\n  },\n  getProperty: function getProperty(target, property, unit, uncache) {\n    _isString(target) && (target = toArray(target)[0]); //in case selector text or an array is passed in\n\n    var getter = _getCache(target || {}).get,\n        format = unit ? _passThrough : _numericIfPossible;\n\n    unit === \"native\" && (unit = \"\");\n    return !target ? target : !property ? function (property, unit, uncache) {\n      return format((_plugins[property] && _plugins[property].get || getter)(target, property, unit, uncache));\n    } : format((_plugins[property] && _plugins[property].get || getter)(target, property, unit, uncache));\n  },\n  quickSetter: function quickSetter(target, property, unit) {\n    target = toArray(target);\n\n    if (target.length > 1) {\n      var setters = target.map(function (t) {\n        return gsap.quickSetter(t, property, unit);\n      }),\n          l = setters.length;\n      return function (value) {\n        var i = l;\n\n        while (i--) {\n          setters[i](value);\n        }\n      };\n    }\n\n    target = target[0] || {};\n\n    var Plugin = _plugins[property],\n        cache = _getCache(target),\n        p = cache.harness && (cache.harness.aliases || {})[property] || property,\n        // in case it's an alias, like \"rotate\" for \"rotation\".\n    setter = Plugin ? function (value) {\n      var p = new Plugin();\n      _quickTween._pt = 0;\n      p.init(target, unit ? value + unit : value, _quickTween, 0, [target]);\n      p.render(1, p);\n      _quickTween._pt && _renderPropTweens(1, _quickTween);\n    } : cache.set(target, p);\n\n    return Plugin ? setter : function (value) {\n      return setter(target, p, unit ? value + unit : value, cache, 1);\n    };\n  },\n  quickTo: function quickTo(target, property, vars) {\n    var _merge2;\n\n    var tween = gsap.to(target, _merge((_merge2 = {}, _merge2[property] = \"+=0.1\", _merge2.paused = true, _merge2), vars || {})),\n        func = function func(value, start, startIsRelative) {\n      return tween.resetTo(property, value, start, startIsRelative);\n    };\n\n    func.tween = tween;\n    return func;\n  },\n  isTweening: function isTweening(targets) {\n    return _globalTimeline.getTweensOf(targets, true).length > 0;\n  },\n  defaults: function defaults(value) {\n    value && value.ease && (value.ease = _parseEase(value.ease, _defaults.ease));\n    return _mergeDeep(_defaults, value || {});\n  },\n  config: function config(value) {\n    return _mergeDeep(_config, value || {});\n  },\n  registerEffect: function registerEffect(_ref3) {\n    var name = _ref3.name,\n        effect = _ref3.effect,\n        plugins = _ref3.plugins,\n        defaults = _ref3.defaults,\n        extendTimeline = _ref3.extendTimeline;\n    (plugins || \"\").split(\",\").forEach(function (pluginName) {\n      return pluginName && !_plugins[pluginName] && !_globals[pluginName] && _warn(name + \" effect requires \" + pluginName + \" plugin.\");\n    });\n\n    _effects[name] = function (targets, vars, tl) {\n      return effect(toArray(targets), _setDefaults(vars || {}, defaults), tl);\n    };\n\n    if (extendTimeline) {\n      Timeline.prototype[name] = function (targets, vars, position) {\n        return this.add(_effects[name](targets, _isObject(vars) ? vars : (position = vars) && {}, this), position);\n      };\n    }\n  },\n  registerEase: function registerEase(name, ease) {\n    _easeMap[name] = _parseEase(ease);\n  },\n  parseEase: function parseEase(ease, defaultEase) {\n    return arguments.length ? _parseEase(ease, defaultEase) : _easeMap;\n  },\n  getById: function getById(id) {\n    return _globalTimeline.getById(id);\n  },\n  exportRoot: function exportRoot(vars, includeDelayedCalls) {\n    if (vars === void 0) {\n      vars = {};\n    }\n\n    var tl = new Timeline(vars),\n        child,\n        next;\n    tl.smoothChildTiming = _isNotFalse(vars.smoothChildTiming);\n\n    _globalTimeline.remove(tl);\n\n    tl._dp = 0; //otherwise it'll get re-activated when adding children and be re-introduced into _globalTimeline's linked list (then added to itself).\n\n    tl._time = tl._tTime = _globalTimeline._time;\n    child = _globalTimeline._first;\n\n    while (child) {\n      next = child._next;\n\n      if (includeDelayedCalls || !(!child._dur && child instanceof Tween && child.vars.onComplete === child._targets[0])) {\n        _addToTimeline(tl, child, child._start - child._delay);\n      }\n\n      child = next;\n    }\n\n    _addToTimeline(_globalTimeline, tl, 0);\n\n    return tl;\n  },\n  context: function context(func, scope) {\n    return func ? new Context(func, scope) : _context;\n  },\n  matchMedia: function matchMedia(scope) {\n    return new MatchMedia(scope);\n  },\n  matchMediaRefresh: function matchMediaRefresh() {\n    return _media.forEach(function (c) {\n      var cond = c.conditions,\n          found,\n          p;\n\n      for (p in cond) {\n        if (cond[p]) {\n          cond[p] = false;\n          found = 1;\n        }\n      }\n\n      found && c.revert();\n    }) || _onMediaChange();\n  },\n  addEventListener: function addEventListener(type, callback) {\n    var a = _listeners[type] || (_listeners[type] = []);\n    ~a.indexOf(callback) || a.push(callback);\n  },\n  removeEventListener: function removeEventListener(type, callback) {\n    var a = _listeners[type],\n        i = a && a.indexOf(callback);\n    i >= 0 && a.splice(i, 1);\n  },\n  utils: {\n    wrap: wrap,\n    wrapYoyo: wrapYoyo,\n    distribute: distribute,\n    random: random,\n    snap: snap,\n    normalize: normalize,\n    getUnit: getUnit,\n    clamp: clamp,\n    splitColor: splitColor,\n    toArray: toArray,\n    selector: selector,\n    mapRange: mapRange,\n    pipe: pipe,\n    unitize: unitize,\n    interpolate: interpolate,\n    shuffle: shuffle\n  },\n  install: _install,\n  effects: _effects,\n  ticker: _ticker,\n  updateRoot: Timeline.updateRoot,\n  plugins: _plugins,\n  globalTimeline: _globalTimeline,\n  core: {\n    PropTween: PropTween,\n    globals: _addGlobal,\n    Tween: Tween,\n    Timeline: Timeline,\n    Animation: Animation,\n    getCache: _getCache,\n    _removeLinkedListItem: _removeLinkedListItem,\n    reverting: function reverting() {\n      return _reverting;\n    },\n    context: function context(toAdd) {\n      if (toAdd && _context) {\n        _context.data.push(toAdd);\n\n        toAdd._ctx = _context;\n      }\n\n      return _context;\n    },\n    suppressOverwrites: function suppressOverwrites(value) {\n      return _suppressOverwrites = value;\n    }\n  }\n};\n\n_forEachName(\"to,from,fromTo,delayedCall,set,killTweensOf\", function (name) {\n  return _gsap[name] = Tween[name];\n});\n\n_ticker.add(Timeline.updateRoot);\n\n_quickTween = _gsap.to({}, {\n  duration: 0\n}); // ---- EXTRA PLUGINS --------------------------------------------------------\n\nvar _getPluginPropTween = function _getPluginPropTween(plugin, prop) {\n  var pt = plugin._pt;\n\n  while (pt && pt.p !== prop && pt.op !== prop && pt.fp !== prop) {\n    pt = pt._next;\n  }\n\n  return pt;\n},\n    _addModifiers = function _addModifiers(tween, modifiers) {\n  var targets = tween._targets,\n      p,\n      i,\n      pt;\n\n  for (p in modifiers) {\n    i = targets.length;\n\n    while (i--) {\n      pt = tween._ptLookup[i][p];\n\n      if (pt && (pt = pt.d)) {\n        if (pt._pt) {\n          // is a plugin\n          pt = _getPluginPropTween(pt, p);\n        }\n\n        pt && pt.modifier && pt.modifier(modifiers[p], tween, targets[i], p);\n      }\n    }\n  }\n},\n    _buildModifierPlugin = function _buildModifierPlugin(name, modifier) {\n  return {\n    name: name,\n    rawVars: 1,\n    //don't pre-process function-based values or \"random()\" strings.\n    init: function init(target, vars, tween) {\n      tween._onInit = function (tween) {\n        var temp, p;\n\n        if (_isString(vars)) {\n          temp = {};\n\n          _forEachName(vars, function (name) {\n            return temp[name] = 1;\n          }); //if the user passes in a comma-delimited list of property names to roundProps, like \"x,y\", we round to whole numbers.\n\n\n          vars = temp;\n        }\n\n        if (modifier) {\n          temp = {};\n\n          for (p in vars) {\n            temp[p] = modifier(vars[p]);\n          }\n\n          vars = temp;\n        }\n\n        _addModifiers(tween, vars);\n      };\n    }\n  };\n}; //register core plugins\n\n\nvar gsap = _gsap.registerPlugin({\n  name: \"attr\",\n  init: function init(target, vars, tween, index, targets) {\n    var p, pt, v;\n    this.tween = tween;\n\n    for (p in vars) {\n      v = target.getAttribute(p) || \"\";\n      pt = this.add(target, \"setAttribute\", (v || 0) + \"\", vars[p], index, targets, 0, 0, p);\n      pt.op = p;\n      pt.b = v; // record the beginning value so we can revert()\n\n      this._props.push(p);\n    }\n  },\n  render: function render(ratio, data) {\n    var pt = data._pt;\n\n    while (pt) {\n      _reverting ? pt.set(pt.t, pt.p, pt.b, pt) : pt.r(ratio, pt.d); // if reverting, go back to the original (pt.b)\n\n      pt = pt._next;\n    }\n  }\n}, {\n  name: \"endArray\",\n  init: function init(target, value) {\n    var i = value.length;\n\n    while (i--) {\n      this.add(target, i, target[i] || 0, value[i], 0, 0, 0, 0, 0, 1);\n    }\n  }\n}, _buildModifierPlugin(\"roundProps\", _roundModifier), _buildModifierPlugin(\"modifiers\"), _buildModifierPlugin(\"snap\", snap)) || _gsap; //to prevent the core plugins from being dropped via aggressive tree shaking, we must include them in the variable declaration in this way.\n\nTween.version = Timeline.version = gsap.version = \"3.12.5\";\n_coreReady = 1;\n_windowExists() && _wake();\nvar Power0 = _easeMap.Power0,\n    Power1 = _easeMap.Power1,\n    Power2 = _easeMap.Power2,\n    Power3 = _easeMap.Power3,\n    Power4 = _easeMap.Power4,\n    Linear = _easeMap.Linear,\n    Quad = _easeMap.Quad,\n    Cubic = _easeMap.Cubic,\n    Quart = _easeMap.Quart,\n    Quint = _easeMap.Quint,\n    Strong = _easeMap.Strong,\n    Elastic = _easeMap.Elastic,\n    Back = _easeMap.Back,\n    SteppedEase = _easeMap.SteppedEase,\n    Bounce = _easeMap.Bounce,\n    Sine = _easeMap.Sine,\n    Expo = _easeMap.Expo,\n    Circ = _easeMap.Circ;\n\n //export some internal methods/orojects for use in CSSPlugin so that we can externalize that file and allow custom builds that exclude it.\n\n\n\n//# sourceURL=webpack://frontend-boilerplate/./node_modules/gsap/gsap-core.js?");
  
  /***/ }),
  
  /***/ "./node_modules/gsap/index.js":
  /*!************************************!*\
    !*** ./node_modules/gsap/index.js ***!
    \************************************/
  /***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {
  
  "use strict";
  eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   Back: () => (/* reexport safe */ _gsap_core_js__WEBPACK_IMPORTED_MODULE_0__.Back),\n/* harmony export */   Bounce: () => (/* reexport safe */ _gsap_core_js__WEBPACK_IMPORTED_MODULE_0__.Bounce),\n/* harmony export */   CSSPlugin: () => (/* reexport safe */ _CSSPlugin_js__WEBPACK_IMPORTED_MODULE_1__.CSSPlugin),\n/* harmony export */   Circ: () => (/* reexport safe */ _gsap_core_js__WEBPACK_IMPORTED_MODULE_0__.Circ),\n/* harmony export */   Cubic: () => (/* reexport safe */ _gsap_core_js__WEBPACK_IMPORTED_MODULE_0__.Cubic),\n/* harmony export */   Elastic: () => (/* reexport safe */ _gsap_core_js__WEBPACK_IMPORTED_MODULE_0__.Elastic),\n/* harmony export */   Expo: () => (/* reexport safe */ _gsap_core_js__WEBPACK_IMPORTED_MODULE_0__.Expo),\n/* harmony export */   Linear: () => (/* reexport safe */ _gsap_core_js__WEBPACK_IMPORTED_MODULE_0__.Linear),\n/* harmony export */   Power0: () => (/* reexport safe */ _gsap_core_js__WEBPACK_IMPORTED_MODULE_0__.Power0),\n/* harmony export */   Power1: () => (/* reexport safe */ _gsap_core_js__WEBPACK_IMPORTED_MODULE_0__.Power1),\n/* harmony export */   Power2: () => (/* reexport safe */ _gsap_core_js__WEBPACK_IMPORTED_MODULE_0__.Power2),\n/* harmony export */   Power3: () => (/* reexport safe */ _gsap_core_js__WEBPACK_IMPORTED_MODULE_0__.Power3),\n/* harmony export */   Power4: () => (/* reexport safe */ _gsap_core_js__WEBPACK_IMPORTED_MODULE_0__.Power4),\n/* harmony export */   Quad: () => (/* reexport safe */ _gsap_core_js__WEBPACK_IMPORTED_MODULE_0__.Quad),\n/* harmony export */   Quart: () => (/* reexport safe */ _gsap_core_js__WEBPACK_IMPORTED_MODULE_0__.Quart),\n/* harmony export */   Quint: () => (/* reexport safe */ _gsap_core_js__WEBPACK_IMPORTED_MODULE_0__.Quint),\n/* harmony export */   Sine: () => (/* reexport safe */ _gsap_core_js__WEBPACK_IMPORTED_MODULE_0__.Sine),\n/* harmony export */   SteppedEase: () => (/* reexport safe */ _gsap_core_js__WEBPACK_IMPORTED_MODULE_0__.SteppedEase),\n/* harmony export */   Strong: () => (/* reexport safe */ _gsap_core_js__WEBPACK_IMPORTED_MODULE_0__.Strong),\n/* harmony export */   TimelineLite: () => (/* reexport safe */ _gsap_core_js__WEBPACK_IMPORTED_MODULE_0__.TimelineLite),\n/* harmony export */   TimelineMax: () => (/* reexport safe */ _gsap_core_js__WEBPACK_IMPORTED_MODULE_0__.TimelineMax),\n/* harmony export */   TweenLite: () => (/* reexport safe */ _gsap_core_js__WEBPACK_IMPORTED_MODULE_0__.TweenLite),\n/* harmony export */   TweenMax: () => (/* binding */ TweenMaxWithCSS),\n/* harmony export */   \"default\": () => (/* binding */ gsapWithCSS),\n/* harmony export */   gsap: () => (/* binding */ gsapWithCSS)\n/* harmony export */ });\n/* harmony import */ var _gsap_core_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./gsap-core.js */ \"./node_modules/gsap/gsap-core.js\");\n/* harmony import */ var _CSSPlugin_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./CSSPlugin.js */ \"./node_modules/gsap/CSSPlugin.js\");\n\n\nvar gsapWithCSS = _gsap_core_js__WEBPACK_IMPORTED_MODULE_0__.gsap.registerPlugin(_CSSPlugin_js__WEBPACK_IMPORTED_MODULE_1__.CSSPlugin) || _gsap_core_js__WEBPACK_IMPORTED_MODULE_0__.gsap,\n    // to protect from tree shaking\nTweenMaxWithCSS = gsapWithCSS.core.Tween;\n\n\n//# sourceURL=webpack://frontend-boilerplate/./node_modules/gsap/index.js?");
  
  /***/ }),
  
  /***/ "./node_modules/load-script/index.js":
  /*!*******************************************!*\
    !*** ./node_modules/load-script/index.js ***!
    \*******************************************/
  /***/ ((module) => {
  
  eval("\nmodule.exports = function load (src, opts, cb) {\n  var head = document.head || document.getElementsByTagName('head')[0]\n  var script = document.createElement('script')\n\n  if (typeof opts === 'function') {\n    cb = opts\n    opts = {}\n  }\n\n  opts = opts || {}\n  cb = cb || function() {}\n\n  script.type = opts.type || 'text/javascript'\n  script.charset = opts.charset || 'utf8';\n  script.async = 'async' in opts ? !!opts.async : true\n  script.src = src\n\n  if (opts.attrs) {\n    setAttributes(script, opts.attrs)\n  }\n\n  if (opts.text) {\n    script.text = '' + opts.text\n  }\n\n  var onend = 'onload' in script ? stdOnEnd : ieOnEnd\n  onend(script, cb)\n\n  // some good legacy browsers (firefox) fail the 'in' detection above\n  // so as a fallback we always set onload\n  // old IE will ignore this and new IE will set onload\n  if (!script.onload) {\n    stdOnEnd(script, cb);\n  }\n\n  head.appendChild(script)\n}\n\nfunction setAttributes(script, attrs) {\n  for (var attr in attrs) {\n    script.setAttribute(attr, attrs[attr]);\n  }\n}\n\nfunction stdOnEnd (script, cb) {\n  script.onload = function () {\n    this.onerror = this.onload = null\n    cb(null, script)\n  }\n  script.onerror = function () {\n    // this.onload = null here is necessary\n    // because even IE9 works not like others\n    this.onerror = this.onload = null\n    cb(new Error('Failed to load ' + this.src), script)\n  }\n}\n\nfunction ieOnEnd (script, cb) {\n  script.onreadystatechange = function () {\n    if (this.readyState != 'complete' && this.readyState != 'loaded') return\n    this.onreadystatechange = null\n    cb(null, script) // there is no way to catch loading errors in IE8\n  }\n}\n\n\n//# sourceURL=webpack://frontend-boilerplate/./node_modules/load-script/index.js?");
  
  /***/ }),
  
  /***/ "./node_modules/ms/index.js":
  /*!**********************************!*\
    !*** ./node_modules/ms/index.js ***!
    \**********************************/
  /***/ ((module) => {
  
  eval("/**\n * Helpers.\n */\n\nvar s = 1000;\nvar m = s * 60;\nvar h = m * 60;\nvar d = h * 24;\nvar w = d * 7;\nvar y = d * 365.25;\n\n/**\n * Parse or format the given `val`.\n *\n * Options:\n *\n *  - `long` verbose formatting [false]\n *\n * @param {String|Number} val\n * @param {Object} [options]\n * @throws {Error} throw an error if val is not a non-empty string or a number\n * @return {String|Number}\n * @api public\n */\n\nmodule.exports = function (val, options) {\n  options = options || {};\n  var type = typeof val;\n  if (type === 'string' && val.length > 0) {\n    return parse(val);\n  } else if (type === 'number' && isFinite(val)) {\n    return options.long ? fmtLong(val) : fmtShort(val);\n  }\n  throw new Error(\n    'val is not a non-empty string or a valid number. val=' +\n      JSON.stringify(val)\n  );\n};\n\n/**\n * Parse the given `str` and return milliseconds.\n *\n * @param {String} str\n * @return {Number}\n * @api private\n */\n\nfunction parse(str) {\n  str = String(str);\n  if (str.length > 100) {\n    return;\n  }\n  var match = /^(-?(?:\\d+)?\\.?\\d+) *(milliseconds?|msecs?|ms|seconds?|secs?|s|minutes?|mins?|m|hours?|hrs?|h|days?|d|weeks?|w|years?|yrs?|y)?$/i.exec(\n    str\n  );\n  if (!match) {\n    return;\n  }\n  var n = parseFloat(match[1]);\n  var type = (match[2] || 'ms').toLowerCase();\n  switch (type) {\n    case 'years':\n    case 'year':\n    case 'yrs':\n    case 'yr':\n    case 'y':\n      return n * y;\n    case 'weeks':\n    case 'week':\n    case 'w':\n      return n * w;\n    case 'days':\n    case 'day':\n    case 'd':\n      return n * d;\n    case 'hours':\n    case 'hour':\n    case 'hrs':\n    case 'hr':\n    case 'h':\n      return n * h;\n    case 'minutes':\n    case 'minute':\n    case 'mins':\n    case 'min':\n    case 'm':\n      return n * m;\n    case 'seconds':\n    case 'second':\n    case 'secs':\n    case 'sec':\n    case 's':\n      return n * s;\n    case 'milliseconds':\n    case 'millisecond':\n    case 'msecs':\n    case 'msec':\n    case 'ms':\n      return n;\n    default:\n      return undefined;\n  }\n}\n\n/**\n * Short format for `ms`.\n *\n * @param {Number} ms\n * @return {String}\n * @api private\n */\n\nfunction fmtShort(ms) {\n  var msAbs = Math.abs(ms);\n  if (msAbs >= d) {\n    return Math.round(ms / d) + 'd';\n  }\n  if (msAbs >= h) {\n    return Math.round(ms / h) + 'h';\n  }\n  if (msAbs >= m) {\n    return Math.round(ms / m) + 'm';\n  }\n  if (msAbs >= s) {\n    return Math.round(ms / s) + 's';\n  }\n  return ms + 'ms';\n}\n\n/**\n * Long format for `ms`.\n *\n * @param {Number} ms\n * @return {String}\n * @api private\n */\n\nfunction fmtLong(ms) {\n  var msAbs = Math.abs(ms);\n  if (msAbs >= d) {\n    return plural(ms, msAbs, d, 'day');\n  }\n  if (msAbs >= h) {\n    return plural(ms, msAbs, h, 'hour');\n  }\n  if (msAbs >= m) {\n    return plural(ms, msAbs, m, 'minute');\n  }\n  if (msAbs >= s) {\n    return plural(ms, msAbs, s, 'second');\n  }\n  return ms + ' ms';\n}\n\n/**\n * Pluralization helper.\n */\n\nfunction plural(ms, msAbs, n, name) {\n  var isPlural = msAbs >= n * 1.5;\n  return Math.round(ms / n) + ' ' + name + (isPlural ? 's' : '');\n}\n\n\n//# sourceURL=webpack://frontend-boilerplate/./node_modules/ms/index.js?");
  
  /***/ }),
  
  /***/ "./node_modules/scroll-out/lib/index.js":
  /*!**********************************************!*\
    !*** ./node_modules/scroll-out/lib/index.js ***!
    \**********************************************/
  /***/ ((module) => {
  
  "use strict";
  eval("\n\nfunction clamp(v, min, max) {\r\n    return min > v ? min : max < v ? max : v;\r\n}\r\nfunction sign(x) {\r\n    return (+(x > 0) - +(x < 0));\r\n}\r\nfunction round(n) {\r\n    return Math.round(n * 10000) / 10000;\r\n}\n\nvar cache = {};\r\nfunction replacer(match) {\r\n    return '-' + match[0].toLowerCase();\r\n}\r\nfunction hyphenate(value) {\r\n    return cache[value] || (cache[value] = value.replace(/([A-Z])/g, replacer));\r\n}\n\n/** find elements */\r\nfunction $(e, parent) {\r\n    return !e || e.length === 0\r\n        ? // null or empty string returns empty array\r\n            []\r\n        : e.nodeName\r\n            ? // a single element is wrapped in an array\r\n                [e]\r\n            : // selector and NodeList are converted to Element[]\r\n                [].slice.call(e[0].nodeName ? e : (parent || document.documentElement).querySelectorAll(e));\r\n}\r\nfunction setAttrs(el, attrs) {\r\n    // tslint:disable-next-line:forin\r\n    for (var key in attrs) {\r\n        if (key.indexOf('_')) {\r\n            el.setAttribute('data-' + hyphenate(key), attrs[key]);\r\n        }\r\n    }\r\n}\r\nfunction setProps(cssProps) {\r\n    return function (el, props) {\r\n        for (var key in props) {\r\n            if (key.indexOf('_') && (cssProps === true || cssProps[key])) {\r\n                el.style.setProperty('--' + hyphenate(key), round(props[key]));\r\n            }\r\n        }\r\n    };\r\n}\n\nvar clearTask;\r\nvar subscribers = [];\r\nfunction loop() {\r\n    clearTask = 0;\r\n    subscribers.slice().forEach(function (s2) { return s2(); });\r\n    enqueue();\r\n}\r\nfunction enqueue() {\r\n    if (!clearTask && subscribers.length) {\r\n        clearTask = requestAnimationFrame(loop);\r\n    }\r\n}\r\nfunction subscribe(fn) {\r\n    subscribers.push(fn);\r\n    enqueue();\r\n    return function () {\r\n        subscribers = subscribers.filter(function (s) { return s !== fn; });\r\n        if (!subscribers.length && clearTask) {\r\n            cancelAnimationFrame(clearTask);\r\n            clearTask = 0;\r\n        }\r\n    };\r\n}\n\nfunction unwrap(value, el, ctx, doc) {\r\n    return typeof value === 'function' ? value(el, ctx, doc) : value;\r\n}\r\nfunction noop() { }\n\n/**\r\n * Creates a new instance of ScrollOut that marks elements in the viewport with\r\n * an \"in\" class and marks elements outside of the viewport with an \"out\"\r\n */\r\n// tslint:disable-next-line:no-default-export\r\nfunction main (opts) {\r\n    // Apply default options.\r\n    opts = opts || {};\r\n    // Debounce onChange/onHidden/onShown.\r\n    var onChange = opts.onChange || noop;\r\n    var onHidden = opts.onHidden || noop;\r\n    var onShown = opts.onShown || noop;\r\n    var onScroll = opts.onScroll || noop;\r\n    var props = opts.cssProps ? setProps(opts.cssProps) : noop;\r\n    var se = opts.scrollingElement;\r\n    var container = se ? $(se)[0] : window;\r\n    var doc = se ? $(se)[0] : document.documentElement;\r\n    var rootChanged = false;\r\n    var scrollingElementContext = {};\r\n    var elementContextList = [];\r\n    var clientOffsetX, clientOffsety;\r\n    var sub;\r\n    function index() {\r\n        elementContextList = $(opts.targets || '[data-scroll]', $(opts.scope || doc)[0]).map(function (el) { return ({ element: el }); });\r\n    }\r\n    function update() {\r\n        // Calculate position, direction and ratio.\r\n        var clientWidth = doc.clientWidth;\r\n        var clientHeight = doc.clientHeight;\r\n        var scrollDirX = sign(-clientOffsetX + (clientOffsetX = doc.scrollLeft || window.pageXOffset));\r\n        var scrollDirY = sign(-clientOffsety + (clientOffsety = doc.scrollTop || window.pageYOffset));\r\n        var scrollPercentX = doc.scrollLeft / (doc.scrollWidth - clientWidth || 1);\r\n        var scrollPercentY = doc.scrollTop / (doc.scrollHeight - clientHeight || 1);\r\n        // Detect if the root context has changed.\r\n        rootChanged =\r\n            rootChanged ||\r\n                scrollingElementContext.scrollDirX !== scrollDirX ||\r\n                scrollingElementContext.scrollDirY !== scrollDirY ||\r\n                scrollingElementContext.scrollPercentX !== scrollPercentX ||\r\n                scrollingElementContext.scrollPercentY !== scrollPercentY;\r\n        scrollingElementContext.scrollDirX = scrollDirX;\r\n        scrollingElementContext.scrollDirY = scrollDirY;\r\n        scrollingElementContext.scrollPercentX = scrollPercentX;\r\n        scrollingElementContext.scrollPercentY = scrollPercentY;\r\n        var childChanged = false;\r\n        for (var index_1 = 0; index_1 < elementContextList.length; index_1++) {\r\n            var ctx = elementContextList[index_1];\r\n            var element = ctx.element;\r\n            // find the distance from the element to the scrolling container\r\n            var target = element;\r\n            var offsetX = 0;\r\n            var offsetY = 0;\r\n            do {\r\n                offsetX += target.offsetLeft;\r\n                offsetY += target.offsetTop;\r\n                target = target.offsetParent;\r\n            } while (target && target !== container);\r\n            // Get element dimensions.\r\n            var elementHeight = element.clientHeight || element.offsetHeight || 0;\r\n            var elementWidth = element.clientWidth || element.offsetWidth || 0;\r\n            // Find visible ratios for each element.\r\n            var visibleX = (clamp(offsetX + elementWidth, clientOffsetX, clientOffsetX + clientWidth) -\r\n                clamp(offsetX, clientOffsetX, clientOffsetX + clientWidth)) /\r\n                elementWidth;\r\n            var visibleY = (clamp(offsetY + elementHeight, clientOffsety, clientOffsety + clientHeight) -\r\n                clamp(offsetY, clientOffsety, clientOffsety + clientHeight)) /\r\n                elementHeight;\r\n            var intersectX = visibleX === 1 ? 0 : sign(offsetX - clientOffsetX);\r\n            var intersectY = visibleY === 1 ? 0 : sign(offsetY - clientOffsety);\r\n            var viewportX = clamp((clientOffsetX - (elementWidth / 2 + offsetX - clientWidth / 2)) / (clientWidth / 2), -1, 1);\r\n            var viewportY = clamp((clientOffsety - (elementHeight / 2 + offsetY - clientHeight / 2)) / (clientHeight / 2), -1, 1);\r\n            var visible = void 0;\r\n            if (opts.offset) {\r\n                visible = unwrap(opts.offset, element, ctx, doc) <= clientOffsety ? 1 : 0;\r\n            }\r\n            else if ((unwrap(opts.threshold, element, ctx, doc) || 0) < visibleX * visibleY) {\r\n                visible = 1;\r\n            }\r\n            else {\r\n                visible = 0;\r\n            }\r\n            var changedVisible = ctx.visible !== visible;\r\n            var changed = ctx._changed ||\r\n                changedVisible ||\r\n                ctx.visibleX !== visibleX ||\r\n                ctx.visibleY !== visibleY ||\r\n                ctx.index !== index_1 ||\r\n                ctx.elementHeight !== elementHeight ||\r\n                ctx.elementWidth !== elementWidth ||\r\n                ctx.offsetX !== offsetX ||\r\n                ctx.offsetY !== offsetY ||\r\n                ctx.intersectX !== ctx.intersectX ||\r\n                ctx.intersectY !== ctx.intersectY ||\r\n                ctx.viewportX !== viewportX ||\r\n                ctx.viewportY !== viewportY;\r\n            if (changed) {\r\n                childChanged = true;\r\n                ctx._changed = true;\r\n                ctx._visibleChanged = changedVisible;\r\n                ctx.visible = visible;\r\n                ctx.elementHeight = elementHeight;\r\n                ctx.elementWidth = elementWidth;\r\n                ctx.index = index_1;\r\n                ctx.offsetX = offsetX;\r\n                ctx.offsetY = offsetY;\r\n                ctx.visibleX = visibleX;\r\n                ctx.visibleY = visibleY;\r\n                ctx.intersectX = intersectX;\r\n                ctx.intersectY = intersectY;\r\n                ctx.viewportX = viewportX;\r\n                ctx.viewportY = viewportY;\r\n                ctx.visible = visible;\r\n            }\r\n        }\r\n        if (!sub && (rootChanged || childChanged)) {\r\n            sub = subscribe(render);\r\n        }\r\n    }\r\n    function render() {\r\n        maybeUnsubscribe();\r\n        // Update root attributes if they have changed.\r\n        if (rootChanged) {\r\n            rootChanged = false;\r\n            setAttrs(doc, {\r\n                scrollDirX: scrollingElementContext.scrollDirX,\r\n                scrollDirY: scrollingElementContext.scrollDirY\r\n            });\r\n            props(doc, scrollingElementContext);\r\n            onScroll(doc, scrollingElementContext, elementContextList);\r\n        }\r\n        var len = elementContextList.length;\r\n        for (var x = len - 1; x > -1; x--) {\r\n            var ctx = elementContextList[x];\r\n            var el = ctx.element;\r\n            var visible = ctx.visible;\r\n            var justOnce = el.hasAttribute('scrollout-once') || false; // Once\r\n            if (ctx._changed) {\r\n                ctx._changed = false;\r\n                props(el, ctx);\r\n            }\r\n            if (ctx._visibleChanged) {\r\n                setAttrs(el, { scroll: visible ? 'in' : 'out' });\r\n                onChange(el, ctx, doc);\r\n                (visible ? onShown : onHidden)(el, ctx, doc);\r\n            }\r\n            // if this is shown multiple times, keep it in the list\r\n            if (visible && (opts.once || justOnce)) { // or if this element just display it once\r\n                elementContextList.splice(x, 1);\r\n            }\r\n        }\r\n    }\r\n    function maybeUnsubscribe() {\r\n        if (sub) {\r\n            sub();\r\n            sub = undefined;\r\n        }\r\n    }\r\n    // Run initialize index.\r\n    index();\r\n    update();\r\n    render();\r\n    // Collapses sequential updates into a single update.\r\n    var updateTaskId = 0;\r\n    var onUpdate = function () {\r\n        updateTaskId = updateTaskId || setTimeout(function () {\r\n            updateTaskId = 0;\r\n            update();\r\n        }, 0);\r\n    };\r\n    // Hook up document listeners to automatically detect changes.\r\n    window.addEventListener('resize', onUpdate);\r\n    container.addEventListener('scroll', onUpdate);\r\n    return {\r\n        index: index,\r\n        update: update,\r\n        teardown: function () {\r\n            maybeUnsubscribe();\r\n            window.removeEventListener('resize', onUpdate);\r\n            container.removeEventListener('scroll', onUpdate);\r\n        }\r\n    };\r\n}\n\nmodule.exports = main;\n\n\n//# sourceURL=webpack://frontend-boilerplate/./node_modules/scroll-out/lib/index.js?");
  
  /***/ }),
  
  /***/ "./node_modules/sister/src/sister.js":
  /*!*******************************************!*\
    !*** ./node_modules/sister/src/sister.js ***!
    \*******************************************/
  /***/ ((module) => {
  
  "use strict";
  eval("\n\nvar Sister;\n\n/**\n* @link https://github.com/gajus/sister for the canonical source repository\n* @license https://github.com/gajus/sister/blob/master/LICENSE BSD 3-Clause\n*/\nSister = function () {\n    var sister = {},\n        events = {};\n\n    /**\n     * @name handler\n     * @function\n     * @param {Object} data Event data.\n     */\n\n    /**\n     * @param {String} name Event name.\n     * @param {handler} handler\n     * @return {listener}\n     */\n    sister.on = function (name, handler) {\n        var listener = {name: name, handler: handler};\n        events[name] = events[name] || [];\n        events[name].unshift(listener);\n        return listener;\n    };\n\n    /**\n     * @param {listener}\n     */\n    sister.off = function (listener) {\n        var index = events[listener.name].indexOf(listener);\n\n        if (index !== -1) {\n            events[listener.name].splice(index, 1);\n        }\n    };\n\n    /**\n     * @param {String} name Event name.\n     * @param {Object} data Event data.\n     */\n    sister.trigger = function (name, data) {\n        var listeners = events[name],\n            i;\n\n        if (listeners) {\n            i = listeners.length;\n            while (i--) {\n                listeners[i].handler(data);\n            }\n        }\n    };\n\n    return sister;\n};\n\nmodule.exports = Sister;\n\n\n//# sourceURL=webpack://frontend-boilerplate/./node_modules/sister/src/sister.js?");
  
  /***/ }),
  
  /***/ "./node_modules/youtube-player/dist/FunctionStateMap.js":
  /*!**************************************************************!*\
    !*** ./node_modules/youtube-player/dist/FunctionStateMap.js ***!
    \**************************************************************/
  /***/ ((module, exports, __webpack_require__) => {
  
  "use strict";
  eval("\n\nObject.defineProperty(exports, \"__esModule\", ({\n  value: true\n}));\n\nvar _PlayerStates = __webpack_require__(/*! ./constants/PlayerStates */ \"./node_modules/youtube-player/dist/constants/PlayerStates.js\");\n\nvar _PlayerStates2 = _interopRequireDefault(_PlayerStates);\n\nfunction _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }\n\nexports[\"default\"] = {\n  pauseVideo: {\n    acceptableStates: [_PlayerStates2.default.ENDED, _PlayerStates2.default.PAUSED],\n    stateChangeRequired: false\n  },\n  playVideo: {\n    acceptableStates: [_PlayerStates2.default.ENDED, _PlayerStates2.default.PLAYING],\n    stateChangeRequired: false\n  },\n  seekTo: {\n    acceptableStates: [_PlayerStates2.default.ENDED, _PlayerStates2.default.PLAYING, _PlayerStates2.default.PAUSED],\n    stateChangeRequired: true,\n\n    // TRICKY: `seekTo` may not cause a state change if no buffering is\n    // required.\n    // eslint-disable-next-line unicorn/numeric-separators-style\n    timeout: 3000\n  }\n};\nmodule.exports = exports['default'];\n\n//# sourceURL=webpack://frontend-boilerplate/./node_modules/youtube-player/dist/FunctionStateMap.js?");
  
  /***/ }),
  
  /***/ "./node_modules/youtube-player/dist/YouTubePlayer.js":
  /*!***********************************************************!*\
    !*** ./node_modules/youtube-player/dist/YouTubePlayer.js ***!
    \***********************************************************/
  /***/ ((module, exports, __webpack_require__) => {
  
  "use strict";
  eval("\n\nObject.defineProperty(exports, \"__esModule\", ({\n  value: true\n}));\n\nvar _debug = __webpack_require__(/*! debug */ \"./node_modules/debug/src/browser.js\");\n\nvar _debug2 = _interopRequireDefault(_debug);\n\nvar _FunctionStateMap = __webpack_require__(/*! ./FunctionStateMap */ \"./node_modules/youtube-player/dist/FunctionStateMap.js\");\n\nvar _FunctionStateMap2 = _interopRequireDefault(_FunctionStateMap);\n\nvar _eventNames = __webpack_require__(/*! ./eventNames */ \"./node_modules/youtube-player/dist/eventNames.js\");\n\nvar _eventNames2 = _interopRequireDefault(_eventNames);\n\nvar _functionNames = __webpack_require__(/*! ./functionNames */ \"./node_modules/youtube-player/dist/functionNames.js\");\n\nvar _functionNames2 = _interopRequireDefault(_functionNames);\n\nfunction _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }\n\n/* eslint-disable promise/prefer-await-to-then */\n\nconst debug = (0, _debug2.default)('youtube-player');\n\nconst YouTubePlayer = {};\n\n/**\n * Construct an object that defines an event handler for all of the YouTube\n * player events. Proxy captured events through an event emitter.\n *\n * @todo Capture event parameters.\n * @see https://developers.google.com/youtube/iframe_api_reference#Events\n */\nYouTubePlayer.proxyEvents = emitter => {\n  const events = {};\n\n  for (const eventName of _eventNames2.default) {\n    const onEventName = 'on' + eventName.slice(0, 1).toUpperCase() + eventName.slice(1);\n\n    events[onEventName] = event => {\n      debug('event \"%s\"', onEventName, event);\n\n      emitter.trigger(eventName, event);\n    };\n  }\n\n  return events;\n};\n\n/**\n * Delays player API method execution until player state is ready.\n *\n * @todo Proxy all of the methods using Object.keys.\n * @todo See TRICKY below.\n * @param playerAPIReady Promise that resolves when player is ready.\n * @param strictState A flag designating whether or not to wait for\n * an acceptable state when calling supported functions.\n * @returns {object}\n */\nYouTubePlayer.promisifyPlayer = (playerAPIReady, strictState = false) => {\n  const functions = {};\n\n  for (const functionName of _functionNames2.default) {\n    if (strictState && _FunctionStateMap2.default[functionName]) {\n      functions[functionName] = (...args) => {\n        return playerAPIReady.then(player => {\n          const stateInfo = _FunctionStateMap2.default[functionName];\n          const playerState = player.getPlayerState();\n\n          // eslint-disable-next-line no-warning-comments\n          // TODO: Just spread the args into the function once Babel is fixed:\n          // https://github.com/babel/babel/issues/4270\n          //\n          // eslint-disable-next-line prefer-spread\n          const value = player[functionName].apply(player, args);\n\n          // TRICKY: For functions like `seekTo`, a change in state must be\n          // triggered given that the resulting state could match the initial\n          // state.\n          if (stateInfo.stateChangeRequired ||\n\n          // eslint-disable-next-line no-extra-parens\n          Array.isArray(stateInfo.acceptableStates) && !stateInfo.acceptableStates.includes(playerState)) {\n            return new Promise(resolve => {\n              const onPlayerStateChange = () => {\n                const playerStateAfterChange = player.getPlayerState();\n\n                let timeout;\n\n                if (typeof stateInfo.timeout === 'number') {\n                  timeout = setTimeout(() => {\n                    player.removeEventListener('onStateChange', onPlayerStateChange);\n\n                    resolve();\n                  }, stateInfo.timeout);\n                }\n\n                if (Array.isArray(stateInfo.acceptableStates) && stateInfo.acceptableStates.includes(playerStateAfterChange)) {\n                  player.removeEventListener('onStateChange', onPlayerStateChange);\n\n                  clearTimeout(timeout);\n\n                  resolve();\n                }\n              };\n\n              player.addEventListener('onStateChange', onPlayerStateChange);\n            }).then(() => {\n              return value;\n            });\n          }\n\n          return value;\n        });\n      };\n    } else {\n      functions[functionName] = (...args) => {\n        return playerAPIReady.then(player => {\n          // eslint-disable-next-line no-warning-comments\n          // TODO: Just spread the args into the function once Babel is fixed:\n          // https://github.com/babel/babel/issues/4270\n          //\n          // eslint-disable-next-line prefer-spread\n          return player[functionName].apply(player, args);\n        });\n      };\n    }\n  }\n\n  return functions;\n};\n\nexports[\"default\"] = YouTubePlayer;\nmodule.exports = exports['default'];\n\n//# sourceURL=webpack://frontend-boilerplate/./node_modules/youtube-player/dist/YouTubePlayer.js?");
  
  /***/ }),
  
  /***/ "./node_modules/youtube-player/dist/constants/PlayerStates.js":
  /*!********************************************************************!*\
    !*** ./node_modules/youtube-player/dist/constants/PlayerStates.js ***!
    \********************************************************************/
  /***/ ((module, exports) => {
  
  "use strict";
  eval("\n\nObject.defineProperty(exports, \"__esModule\", ({\n  value: true\n}));\nexports[\"default\"] = {\n  BUFFERING: 3,\n  ENDED: 0,\n  PAUSED: 2,\n  PLAYING: 1,\n  UNSTARTED: -1,\n  VIDEO_CUED: 5\n};\nmodule.exports = exports[\"default\"];\n\n//# sourceURL=webpack://frontend-boilerplate/./node_modules/youtube-player/dist/constants/PlayerStates.js?");
  
  /***/ }),
  
  /***/ "./node_modules/youtube-player/dist/eventNames.js":
  /*!********************************************************!*\
    !*** ./node_modules/youtube-player/dist/eventNames.js ***!
    \********************************************************/
  /***/ ((module, exports) => {
  
  "use strict";
  eval("\n\nObject.defineProperty(exports, \"__esModule\", ({\n  value: true\n}));\n\n\n/**\n * @see https://developers.google.com/youtube/iframe_api_reference#Events\n * `volumeChange` is not officially supported but seems to work\n * it emits an object: `{volume: 82.6923076923077, muted: false}`\n */\nexports[\"default\"] = ['ready', 'stateChange', 'playbackQualityChange', 'playbackRateChange', 'error', 'apiChange', 'volumeChange'];\nmodule.exports = exports['default'];\n\n//# sourceURL=webpack://frontend-boilerplate/./node_modules/youtube-player/dist/eventNames.js?");
  
  /***/ }),
  
  /***/ "./node_modules/youtube-player/dist/functionNames.js":
  /*!***********************************************************!*\
    !*** ./node_modules/youtube-player/dist/functionNames.js ***!
    \***********************************************************/
  /***/ ((module, exports) => {
  
  "use strict";
  eval("\n\nObject.defineProperty(exports, \"__esModule\", ({\n  value: true\n}));\n\n\n/**\n * @see https://developers.google.com/youtube/iframe_api_reference#Functions\n */\nexports[\"default\"] = ['cueVideoById', 'loadVideoById', 'cueVideoByUrl', 'loadVideoByUrl', 'playVideo', 'pauseVideo', 'stopVideo', 'getVideoLoadedFraction', 'cuePlaylist', 'loadPlaylist', 'nextVideo', 'previousVideo', 'playVideoAt', 'setShuffle', 'setLoop', 'getPlaylist', 'getPlaylistIndex', 'setOption', 'mute', 'unMute', 'isMuted', 'setVolume', 'getVolume', 'seekTo', 'getPlayerState', 'getPlaybackRate', 'setPlaybackRate', 'getAvailablePlaybackRates', 'getPlaybackQuality', 'setPlaybackQuality', 'getAvailableQualityLevels', 'getCurrentTime', 'getDuration', 'removeEventListener', 'getVideoUrl', 'getVideoEmbedCode', 'getOptions', 'getOption', 'addEventListener', 'destroy', 'setSize', 'getIframe', 'getSphericalProperties', 'setSphericalProperties'];\nmodule.exports = exports['default'];\n\n//# sourceURL=webpack://frontend-boilerplate/./node_modules/youtube-player/dist/functionNames.js?");
  
  /***/ }),
  
  /***/ "./node_modules/youtube-player/dist/index.js":
  /*!***************************************************!*\
    !*** ./node_modules/youtube-player/dist/index.js ***!
    \***************************************************/
  /***/ ((module, exports, __webpack_require__) => {
  
  "use strict";
  eval("\n\nObject.defineProperty(exports, \"__esModule\", ({\n  value: true\n}));\n\nvar _sister = __webpack_require__(/*! sister */ \"./node_modules/sister/src/sister.js\");\n\nvar _sister2 = _interopRequireDefault(_sister);\n\nvar _YouTubePlayer = __webpack_require__(/*! ./YouTubePlayer */ \"./node_modules/youtube-player/dist/YouTubePlayer.js\");\n\nvar _YouTubePlayer2 = _interopRequireDefault(_YouTubePlayer);\n\nvar _loadYouTubeIframeApi = __webpack_require__(/*! ./loadYouTubeIframeApi */ \"./node_modules/youtube-player/dist/loadYouTubeIframeApi.js\");\n\nvar _loadYouTubeIframeApi2 = _interopRequireDefault(_loadYouTubeIframeApi);\n\nfunction _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }\n\n/**\n * @typedef YT.Player\n * @see https://developers.google.com/youtube/iframe_api_reference\n */\n\n/**\n * @see https://developers.google.com/youtube/iframe_api_reference#Loading_a_Video_Player\n */\nlet youtubeIframeAPI;\n\n/**\n * A factory function used to produce an instance of YT.Player and queue function calls and proxy events of the resulting object.\n *\n * @param maybeElementId Either An existing YT.Player instance,\n * the DOM element or the id of the HTML element where the API will insert an <iframe>.\n * @param options See `options` (Ignored when using an existing YT.Player instance).\n * @param strictState A flag designating whether or not to wait for\n * an acceptable state when calling supported functions. Default: `false`.\n * See `FunctionStateMap.js` for supported functions and acceptable states.\n */\n\n\nexports[\"default\"] = (maybeElementId, options = {}, strictState = false) => {\n  const emitter = (0, _sister2.default)();\n\n  if (!youtubeIframeAPI) {\n    youtubeIframeAPI = (0, _loadYouTubeIframeApi2.default)(emitter);\n  }\n\n  if (options.events) {\n    throw new Error('Event handlers cannot be overwritten.');\n  }\n\n  // eslint-disable-next-line unicorn/prefer-query-selector\n  if (typeof maybeElementId === 'string' && !document.getElementById(maybeElementId)) {\n    throw new Error('Element \"' + maybeElementId + '\" does not exist.');\n  }\n\n  options.events = _YouTubePlayer2.default.proxyEvents(emitter);\n\n  const playerAPIReady = new Promise(resolve => {\n    if (typeof maybeElementId === 'object' && maybeElementId.playVideo instanceof Function) {\n      const player = maybeElementId;\n\n      resolve(player);\n    } else {\n      // asume maybeElementId can be rendered inside\n      // eslint-disable-next-line promise/catch-or-return\n      youtubeIframeAPI.then(YT => {\n        // eslint-disable-line promise/prefer-await-to-then\n        const player = new YT.Player(maybeElementId, options);\n\n        emitter.on('ready', () => {\n          resolve(player);\n        });\n\n        return null;\n      });\n    }\n  });\n\n  const playerApi = _YouTubePlayer2.default.promisifyPlayer(playerAPIReady, strictState);\n\n  playerApi.on = emitter.on;\n  playerApi.off = emitter.off;\n\n  return playerApi;\n};\n\nmodule.exports = exports['default'];\n\n//# sourceURL=webpack://frontend-boilerplate/./node_modules/youtube-player/dist/index.js?");
  
  /***/ }),
  
  /***/ "./node_modules/youtube-player/dist/loadYouTubeIframeApi.js":
  /*!******************************************************************!*\
    !*** ./node_modules/youtube-player/dist/loadYouTubeIframeApi.js ***!
    \******************************************************************/
  /***/ ((module, exports, __webpack_require__) => {
  
  "use strict";
  eval("\n\nObject.defineProperty(exports, \"__esModule\", ({\n  value: true\n}));\n\nvar _loadScript = __webpack_require__(/*! load-script */ \"./node_modules/load-script/index.js\");\n\nvar _loadScript2 = _interopRequireDefault(_loadScript);\n\nfunction _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }\n\nexports[\"default\"] = emitter => {\n  /**\n   * A promise that is resolved when window.onYouTubeIframeAPIReady is called.\n   * The promise is resolved with a reference to window.YT object.\n   */\n  const iframeAPIReady = new Promise(resolve => {\n    if (window.YT && window.YT.Player && window.YT.Player instanceof Function) {\n      resolve(window.YT);\n\n      return;\n    } else {\n      const protocol = window.location.protocol === 'http:' ? 'http:' : 'https:';\n\n      (0, _loadScript2.default)(protocol + '//www.youtube.com/iframe_api', error => {\n        if (error) {\n          emitter.trigger('error', error);\n        }\n      });\n    }\n\n    const previous = window.onYouTubeIframeAPIReady;\n\n    // The API will call this function when page has finished downloading\n    // the JavaScript for the player API.\n    window.onYouTubeIframeAPIReady = () => {\n      if (previous) {\n        previous();\n      }\n\n      resolve(window.YT);\n    };\n  });\n\n  return iframeAPIReady;\n};\n\nmodule.exports = exports['default'];\n\n//# sourceURL=webpack://frontend-boilerplate/./node_modules/youtube-player/dist/loadYouTubeIframeApi.js?");
  
  /***/ })
  
  /******/ 	});
  /************************************************************************/
  /******/ 	// The module cache
  /******/ 	var __webpack_module_cache__ = {};
  /******/ 	
  /******/ 	// The require function
  /******/ 	function __webpack_require__(moduleId) {
  /******/ 		// Check if module is in cache
  /******/ 		var cachedModule = __webpack_module_cache__[moduleId];
  /******/ 		if (cachedModule !== undefined) {
  /******/ 			return cachedModule.exports;
  /******/ 		}
  /******/ 		// Create a new module (and put it into the cache)
  /******/ 		var module = __webpack_module_cache__[moduleId] = {
  /******/ 			// no module.id needed
  /******/ 			// no module.loaded needed
  /******/ 			exports: {}
  /******/ 		};
  /******/ 	
  /******/ 		// Execute the module function
  /******/ 		__webpack_modules__[moduleId](module, module.exports, __webpack_require__);
  /******/ 	
  /******/ 		// Return the exports of the module
  /******/ 		return module.exports;
  /******/ 	}
  /******/ 	
  /************************************************************************/
  /******/ 	/* webpack/runtime/compat get default export */
  /******/ 	(() => {
  /******/ 		// getDefaultExport function for compatibility with non-harmony modules
  /******/ 		__webpack_require__.n = (module) => {
  /******/ 			var getter = module && module.__esModule ?
  /******/ 				() => (module['default']) :
  /******/ 				() => (module);
  /******/ 			__webpack_require__.d(getter, { a: getter });
  /******/ 			return getter;
  /******/ 		};
  /******/ 	})();
  /******/ 	
  /******/ 	/* webpack/runtime/define property getters */
  /******/ 	(() => {
  /******/ 		// define getter functions for harmony exports
  /******/ 		__webpack_require__.d = (exports, definition) => {
  /******/ 			for(var key in definition) {
  /******/ 				if(__webpack_require__.o(definition, key) && !__webpack_require__.o(exports, key)) {
  /******/ 					Object.defineProperty(exports, key, { enumerable: true, get: definition[key] });
  /******/ 				}
  /******/ 			}
  /******/ 		};
  /******/ 	})();
  /******/ 	
  /******/ 	/* webpack/runtime/hasOwnProperty shorthand */
  /******/ 	(() => {
  /******/ 		__webpack_require__.o = (obj, prop) => (Object.prototype.hasOwnProperty.call(obj, prop))
  /******/ 	})();
  /******/ 	
  /******/ 	/* webpack/runtime/make namespace object */
  /******/ 	(() => {
  /******/ 		// define __esModule on exports
  /******/ 		__webpack_require__.r = (exports) => {
  /******/ 			if(typeof Symbol !== 'undefined' && Symbol.toStringTag) {
  /******/ 				Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
  /******/ 			}
  /******/ 			Object.defineProperty(exports, '__esModule', { value: true });
  /******/ 		};
  /******/ 	})();
  /******/ 	
  /************************************************************************/
  /******/ 	
  /******/ 	// startup
  /******/ 	// Load entry module and return exports
  /******/ 	// This entry module can't be inlined because the eval devtool is used.
  /******/ 	var __webpack_exports__ = __webpack_require__("./app/static/js-m/scripts.js");
  /******/ 	
  /******/ })()
  ;
  